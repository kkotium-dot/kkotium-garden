# 작업 인계 — UX 수정 8건 후속 (Desktop 교차검증 결과)

> **담당 레인**: Claude Code
> **작성**: Desktop, 2026-08-10
> **BASELINE**: main 최신(`666ac87`, `git pull` 후 확인)
> **의존성**: 없음 — 아래 항목들은 이전 8건 수정을 다듬는 후속 작업
> **성격**: Desktop이 8건을 독립 재검증한 결과, 대부분 정확했으나 3가지 후속 필요

---

## Desktop 교차검증 결과 요약 (참고)

| # | 항목 | 검증 방법 | 결과 |
|---|---|---|---|
| 1 | 카테고리 오분류 | 프로덕션 API, 신선한 상품명 3개 | ✅ 대량 쏠림 해소, ⚠️ 잔여 오분류 발견(아래 A) |
| 2 | 가져오기 실패 무시 | 브라우저 UI 실제 재현(999999999999) | ✅ 완벽 — "0개 연동 완료 · 1개 실패 (...)" 정확 표시 |
| 3 | 페이지네이션 숫자 | 브라우저 스크린샷 | ✅ 완벽 — "1 2 3 ... 10" 정확 렌더 |
| 4 | 마진 할인가 미반영 | 코드 리뷰 | ✅ 정확(PERCENT/WON 처리·경계값 클램핑 확인) |
| 7 | 마진율 "0" 버그 | 코드 리뷰(1줄 diff) | ✅ 정확, 기존 컨벤션 일치 |
| 5+6 | 씨앗심기 리셋 방지 | 코드 리뷰만, **end-to-end 미검증** | 🔄 아래 C 참조 |

---

## A. #1 카테고리 오분류 — 강제 폴백은 해소, 잔여 오분류 존재

Desktop이 신선한(캐시 미적중) 상품명으로 재현:
```
"반려동물 자동급식기 스마트" → 가구/인테리어 > 주방가구 > 그릇장/컵보드  (여전히 무관)
"캠핑용 접이식 랜턴 후레쉬"   → NONE (빈 배열, 정직한 실패)
"USB 미니 선풍기 손선풍기"   → NONE (빈 배열, 정직한 실패)
```

**진단**: 강제 폴백 제거(8541d04)로 대량 쏠림은 해소됐다(이전엔 100% 가구/인테리어, 지금은 다양). 하지만 AI(Groq)가 "반려동물 자동급식기"를 여전히 "가구/인테리어"로 잘못 답하는 개별 사례가 남아있다 — 이건 강제 폴백과 무관한, AI 모델 자체의 오분류(주방가구 쪽 오답)로 보인다.

**요청**: `validateSuggestion()`의 fuzzy 매칭 단계(특히 3번 케이스 — d1 exact, d2 char-overlap 50%만으로 매칭)가 이런 개별 오답을 통과시키고 있는지 점검. 필요시 신뢰도 임계값을 높이거나(현재 스코어 기준 재검토), 반려동물 관련 상품이 "반려동물" d1으로 가야 한다는 도메인 힌트를 프롬프트에 추가하는 것도 검토(단, #1 원래 버그처럼 과도한 하드코딩 지시는 피할 것 — 화이트리스트가 아니라 "이런 키워드는 이 d1일 가능성이 높다"는 참고용 힌트 수준으로).

## B. dome_category_cache 오염 캐시 정리

Code가 결과문서에 남긴 미결 사항. `getCachedMapping`이 프롬프트 수정 이전에 저장된 "가구/인테리어" 오답을 계속 서빙할 수 있음.

**요청**: 
1. `dome_category_cache` 테이블에서 `d1='가구/인테리어'`인 레코드 중 `confidence`가 낮거나(60 이하, fallback 소스) 의심스러운 것들의 개수·샘플을 먼저 조회해 규모 파악.
2. 규모가 크면 **전체 삭제보다 "재검증 후 폐기" 전략** 검토(예: 캐시에 `createdAt`이 있다면 프롬프트 수정 커밋 시각 이전 것만 선별 삭제).
3. DB 변경은 신중히 — 삭제 범위를 정확히 확인 후 운영자 확인 없이 진행 가능한 규모(예: 명백히 소수)면 진행, 애매하면 Desktop/운영자에게 규모를 보고하고 확인 요청.

## C. #5+6 씨앗심기 프리필 — end-to-end 미검증 (Code가 검증 필요)

Desktop이 실제로 새 임포트 로직(d600760)으로 임포트된 상품이 DB에 있는지 확인했으나 **없었다**(`naverProductId IS NOT NULL AND description IS NOT NULL` 조회 결과 0건). 즉 이번 수정 이후 실제로 상품을 가져와서 씨앗심기 화면을 열어본 end-to-end 검증이 아직 없다.

**요청**: 
1. 실제(또는 테스트용) 네이버 상품 하나를 `/api/products/import`로 실제 가져오기.
2. 그 상품을 `?edit={id}`로 씨앗심기 화면에 열어서 description/naverCategoryCode/images/tags가 실제로 폼에 프리필되는지 브라우저로 직접 확인.
3. 검증 후 테스트로 만든 상품은 정리(방치 금지 — 프로덕션 DB에 테스트 데이터 남기지 말 것).
4. 안 채워지는 필드가 있다면 씨앗심기 폼의 초기값 매핑 로직(`src/app/products/new/page.tsx`)도 함께 점검 — import 라우트는 정확해도 폼이 그 필드를 안 읽고 있을 수 있음.

---

## 완료 후
- 결과를 `docs/handoff/CODE_UX_FIXES_RESULT_2026-08-10.md`에 이어서 기록(신규 파일 대신 기존 파일에 추가).
- 커밋 분리 권장(A/B/C 각각 또는 관련 있는 것끼리).
- 채팅으로 결과 요약 → Desktop이 재검증.

## 체크리스트
- [ ] A: 잔여 카테고리 오분류 점검·개선
- [ ] B: dome_category_cache 오염 규모 파악 및 정리(신중히)
- [ ] C: #5+6 end-to-end 브라우저 실측 검증 + 테스트 데이터 정리
- [ ] tsc 0 · build 0 (코드 변경 있을 경우)
- [ ] 결과 문서 갱신 + 커밋·push + 채팅 인계
