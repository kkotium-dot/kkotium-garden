# 현재 인계 (CURRENT)

> 이 파일 1개만 활성 인계. 갱신 규칙: 매 세션 종료 시 이 파일을 덮어쓴다(누적 아님) — 실시간 상태는 `docs/plan/TASK_BRIDGE.md` §3 ACTIVE가 정본(단, §3-A 작업큐 보드는 2026-07-22 이후 갱신되지 않아 낡음).

- **status**: 발행전검수 화면 개선 + 꼬띠 페르소나 표면축(#318) + 문서갱신 시스템(#319) + 브랜치·머지 리듬(#320) + **팀 구조 정정 완료**(CLAUDE.md·TASK_BRIDGE.md·PRINCIPLES_LEARNED.md 전부). 브랜치 push+build+preview 검증 완료. **merge는 대표님 저녁 검토 시간에 진행 예정**.
- **branch**: `feature/preview-copy-then-redesign` (HEAD `aa8fc2b`, origin push 완료, 이번 TASK_BRIDGE/PRINCIPLES 정정은 아직 미커밋)
- **goal**: 이 세션 기준 코드/문서 작업 완료. 남은 액션은 저녁 세션 merge뿐.
- **next-action**: 대표님이 저녁 시간에 브랜치 전체 검토 → merge.

---

## 1. 팀 구조 정정 — 최종 완료 (전 활성 문서 전수 확인)

**배경**: 운영자가 "1인 개발이 아니다 — 대표님+Claude 협업, 에이전트가 늘어나면 역할이 더 나뉠 것" 확정. "1인 개발이라는 단어 이외에 문제되는 내용이 없다면 그렇게 처리"라고 명시적으로 범위를 한정 — **"1인 개발" 단어만 제거, 그 외 문장 의미는 최대한 보존**하는 방식으로 처리.

**정정 대상 (전수 확인, 활성 문서만)**:
| 파일 | 위치 | 처리 |
|---|---|---|
| `CLAUDE.md` | 상단 각주, WHY 섹션, Git 규칙 | 팀 구조 각주 신설 + 3곳 정정 (이전 세션에서 완료) |
| `docs/plan/TASK_BRIDGE.md` | §4 SD-04 (STANDING DECISIONS) | "main 직접 push(1인 개발, 브랜치 없음)" → "main 직접 push이 기본, feature 브랜치는 예외적 사용, 최종 merge 승인은 항상 운영자"로 정정. Python 안전삽입(#29b) 사용, 정확히 1건 치환 확인 |
| `docs/plan/PRINCIPLES_LEARNED.md` | #320 본문 2곳 | "1인 개발 + AI 페어 프로그래밍" → "운영자+Claude 협업 환경" / "1인 개발이므로 PR 리뷰어 없이" → "최종 PR 검토·merge 승인은 항상 운영자 본인"으로 정정. 정확히 2건 치환 확인 |

**의도적으로 유지한 것 (혼동 방지 기록)**:
- `PRINCIPLES_LEARNED.md`의 "1인 셀러 워크플로우"(AEM MCP 관련), "1인 운영자가 구버전 화면을 보고"(#308 관련) — 이건 **스토어를 대표님 혼자 운영하신다는 사실**을 가리키는 정당한 표현이라 정정 대상 아님. "개발이 1인"이라는 프레이밍만 문제였음.
- `CLAUDE.md` WHY 섹션의 "운영자 본인이 스토어를 직접 운영" — 스토어 운영 형태는 사실 그대로 유지, 개발 체계만 분리해서 서술.
- **과거 archive 문서**(`SESSION_LOG_2026-05-13-PM.md`, `SECURITY_KEY_ROTATION_2026_05_21.md`, `CLAUDE_MD_FULL_2026-07-24.md` 등)의 "1인 개발" 표현 — 그 시점의 사실 기록이므로 손대지 않음(#29 문서 보존 원칙과 동일 정신).

**검증**: 모든 치환은 Python 스크립트로 "정확히 N건 매치" 확인 후 실행(우발적 다중치환 방지). sentinel grep 0건, 손상문자 0건, tsc 0 errors. `wc -l` 순증 없음(정확히 해당 문장만 교체).

---

## 2. 브랜치 상태

**커밋 8개 push 완료**(`3532d78`~`aa8fc2b`), 이번 TASK_BRIDGE/PRINCIPLES 정정 2개 파일은 아직 미커밋 — 다음 작업으로 커밋 예정. 문서 임계 경고 유지: `PRINCIPLES_LEARNED.md` 1483줄(다음 원칙 전 분할 필수).

## 3. 다음 Desktop READY

1. 이번 정정(TASK_BRIDGE.md, PRINCIPLES_LEARNED.md)을 커밋 → 저녁 세션 merge 검토에 포함
2. 저녁 세션에서 브랜치 전체 검토 → merge(#320)
3. `PRINCIPLES_LEARNED.md` 분할(#31) — 다음 원칙 추가 전 필수

## 4. 절대 금지 (매 세션 확인)

- 네이버 스토어 PUT/POST → 운영자 "GO" 없이 절대 금지
- 디스코드 실발송(크론 수동 실행) → 실제 알림 발송됨
- `KKOTTI_PERSONA_VOICE_GUIDE.md` §1~5 수정 금지(추가만)
- 과거 archive 문서(시점 기록)는 실태와 달라도 손대지 않음 — 역사 보존
- "1인 셀러/1인 운영자"(스토어 운영 형태) 표현은 정당 — "1인 개발"(개발 체계)만 정정 대상이었음, 혼동 금지
- 테스트 데이터 방치 → 주입했으면 같은 세션에 원복
- 허위 완료 보고 → 실측 못 한 것은 "미검증"으로 명시(#310)
- 세션 종료 시 문서 3종 점검 없이 "완료" 보고 금지(#319)
- 착수 전 인계 문서의 "미완료" 표기를 그대로 믿지 말고 실측 우선(#318/#319)
- 브랜치를 며칠~몇 주 방치하지 않는다 — 저녁 세션 우선 검토(#320)
