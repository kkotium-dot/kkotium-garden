# 현재 인계 (CURRENT)

> 이 파일 1개만 활성 인계. 갱신 규칙: 매 세션 종료 시 이 파일을 덮어쓴다(누적 아님) — 실시간 상태는 `docs/plan/TASK_BRIDGE.md` §3 ACTIVE가 정본(단, §3-A 작업큐 보드는 2026-07-22 이후 갱신되지 않아 낡음).

- **status**: 발행전검수 화면 개선 + 꼬띠 페르소나 표면축(#318) + 문서갱신 시스템(#319) + 브랜치·머지 리듬(#320) 전부 완료, 브랜치 push+build+preview 검증 완료. **merge는 대표님 저녁 검토 시간에 진행 예정**(오늘 착수 안 함, #320 규칙에 따라 대기 — 방치 아님, 명시적 스케줄).
- **branch**: `feature/preview-copy-then-redesign` (HEAD `35bedf0`, origin push 완료, PR 미생성, main과 diff 7커밋)
- **goal**: 이 세션 기준 코드/문서 작업 완료. 남은 액션은 저녁 세션에서 merge 검토뿐.
- **next-action**: 대표님이 저녁 시간에 함께 화면 확인 → 문제없으면 merge. 그 전까지 이 브랜치 상태 그대로 안전 대기(프로덕션 무영향 확인됨).

---

## 1. 이번 세션 완료 작업 전체 요약

**커밋 8개, `feature/preview-copy-then-redesign`에 순차 적재**:

| 커밋 | 내용 |
|---|---|
| `3532d78` | 발행전검수 문구 근본수정 |
| `994fb91` | 발행전검수 마스터-디테일 레이아웃 재설계 |
| `e16dcde` | 꼬띠 페르소나 §6 신설(원칙 #318) |
| `bf72731` | 판단표면 8곳 사투리 제거 |
| `373cca9` | `persona-audit.py` 반전판정 추가 |
| `34d8e74` | 세션 인계 문서 갱신(rev93) |
| `35bedf0` | 문서 갱신 시스템(#319) |
| (커밋예정) | 브랜치·머지 리듬(#320) — 이 파일과 함께 커밋 |

**전체 검증**: `npx tsc --noEmit` 0 errors(5회 재확인), `npm run build` 0 errors(전체 라우트 정상), Vercel preview 배포 READY + SHA 교차확인, 콘솔 에러 0(앱 관련), 접근성 트리로 실제 DOM 텍스트 재확인. **프로덕션(main) 무영향** — target이 preview로 명시, main 배포 이력 불변.

**미실행(의도적, 범위 밖 판단)**: "대표로 적용" 버튼(크롭 후보를 실제 DB 대표이미지로 쓰는 기능) — 실제 데이터 변경이라 이번 세션 지시 범위(문구·레이아웃·페르소나) 밖으로 판단해 클릭하지 않음. 필요 시 별도 검증 세션.

---

## 2. ★ 브랜치·머지 리듬 확정 (원칙 #320)

대표님 확정: "저녁 시간에 우선 검토, 그 외에도 필요하면 알려달라". 업계 조사 근거 포함 `CLAUDE.md`·`PRINCIPLES_LEARNED.md` #320에 정식 등재. **핵심**: 브랜치를 오래 묵히는 게 "신중함"이 아니라, merge *전* 검증을 철저히 하는 게 신중함. 브랜치 수명은 짧게 유지하는 게 업계 표준(짧을수록 충돌 위험↓).

**이번 브랜치는 오늘 생성**됐으므로, **다음 저녁 세션이 첫 검토 기회**. 그때 화면을 같이 보고 문제없으면 바로 merge 권장(#320).

---

## 3. ★ 문서 임계 경고 — 다음 원칙 추가 전 분할 필수

`docs/plan/PRINCIPLES_LEARNED.md` **1483줄 — 1500줄 임계까지 여유 17줄뿐**. 다음에 새 원칙(#321+)을 추가하기 전에 **반드시 먼저 분할**(#31)해야 한다. 방법은 기존 사례 참고: `docs/plan/archive/PRINCIPLES_LEARNED_archived-2026-07-22.md`(오래된 구간 이관, 삭제 0, 대조검증 필수).

---

## 4. 다음 Desktop READY

1. **저녁 세션에서 브랜치 검토 → merge**(#320) — 우선순위 1
2. `PRINCIPLES_LEARNED.md` 분할(#31) — merge 전이든 후든, 다음 원칙 추가 전에 필수
3. `TASK_BRIDGE.md` §3-A 보드 갱신(우선순위 낮음)
4. merge 후 프로덕션 재검증(SHA 교차확인 #309)

## 5. 절대 금지 (매 세션 확인)

- 네이버 스토어 PUT/POST → 운영자 "GO" 없이 절대 금지
- 디스코드 실발송(크론 수동 실행) → 실제 알림 발송됨
- `KKOTTI_PERSONA_VOICE_GUIDE.md` §1~5 수정 금지(추가만)
- 테스트 데이터 방치 → 주입했으면 같은 세션에 원복
- 허위 완료 보고 → 실측 못 한 것은 "미검증"으로 명시(#310)
- 세션 종료 시 문서 3종 점검 없이 "완료" 보고 금지(#319)
- 착수 전 인계 문서의 "미완료" 표기를 그대로 믿지 말고 실측 우선(#318/#319)
- 브랜치를 며칠~몇 주 방치하지 않는다 — 저녁 세션 우선 검토(#320)
