# 현재 인계 (CURRENT) — 2026-08-04 세션 종료 (실발송 완료 + 모바일 UX 1차 개선)

> 이 파일 1개만 활성 인계. 다음 세션은 이 파일 → `docs/design/SOURCING_DISCORD_MOBILE_LAYOUT_2026-08-04.md` → `WORK_SCHEDULE_BOARD.md` → `PRINCIPLES_LEARNED.md` #295~#326 순으로 읽고 시작.

- **status**: 소싱 추천 시스템 **실제 Discord 실발송 완료·확인 완료**. 발송 직전 발견한 이종상품 오염(캐리어 90원)을 하이브리드(키워드필터+가격경고)로 근본수정·프로덕션 배포·재검증. 운영자 모바일 스크린샷 피드백으로 AI코멘트 모순 근본수정 완료. **`SOURCING_RECOMMEND_LIVE` 환경변수는 아직 미설정**(Vercel 대시보드 수동 설정 필요 — Claude 도구로 직접 불가).
- **branch**: `main` (HEAD `9734338`, 프로덕션 배포 대기 확인 필요)
- **next-action(최우선)**: ①`9734338` 프로덕션 배포 확인 ②운영자가 Vercel 대시보드에서 `SOURCING_RECOMMEND_LIVE=true` 설정 ③다음 세션 소싱봇 레이아웃 재설계 옵션1 착수(`docs/design/SOURCING_DISCORD_MOBILE_LAYOUT_2026-08-04.md`)

---

## 1. ★★★ 소싱 추천 실발송 완료 (2026-08-04 19:47 KST)

**발송 전 검증**: 프로덕션 curl로 embed 전체를 미리 확인 → 캐리어 90원 이종상품 오염 발견(운영자 승인 후 즉시 수정, 아래 §2) → 수정 완료·재검증 후 발송.

**실발송 실행**: `POST /api/sourcing-recommend {dryRun:false}` → `discordSent: true` 확인. 운영자가 실제 Discord 모바일 클라이언트로 확인(스크린샷 2건 제공) — 5개 후보 정상 표시, ⚠️ 가격경고 정상 표시.

**남은 절차**: `SOURCING_RECOMMEND_LIVE=true`를 Vercel Production 환경변수에 설정해야 `daily` 크론(08:00 KST 자동실행)이 매일 자동 실발송한다. **이 설정은 Claude의 Vercel MCP 도구로 직접 할 수 없음** — 운영자가 Vercel 대시보드 → Settings → Environment Variables에서 직접 추가해야 함.

---

## 2. ★★ 이종상품 오염 방지 하이브리드 수정 (커밋 `0470e47`, 배포·검증 완료)

**발견**: 발송 전 최종점검 중 "캐리어" 검색결과에 90원짜리 "바퀴 커버 실리콘"(캐리어 부속품)이 최저가로 걸려 그대로 노출됨. 실측 원인: "캐리어" 전문검색에 여행가방과 무관한 부속품·동음이의 브랜드(에어컨 제조사 Carrier) 등 이종상품 다수 혼재.

**운영자 결정**: 하이브리드(A+B) 채택.
- A. 상품명 키워드 필터: 도매처 상품명에 검색 키워드가 전혀 없는 완전 무관 상품 배제(공백 정규화 후 비교 — 1차 구현에서 "캠핑테이블"↔"캠핑 테이블" 띄어쓰기 차이로 정상 매칭 전량 소실되는 부작용 발견·즉시 재수정)
- B. 가격 이상치 경고(`priceOutlier` 필드): 필터를 통과해도 남는 연관상품은 배제하지 않고 중앙값 대비 5배 이상 벗어나면 경고 표시만 추가 — 최종 판단은 운영자가 링크 확인 후 결정

**검증**: 로컬 dry-run 2회(수정 전 재현 → 1차 수정 부작용 발견 → 2차 수정 검증) + 프로덕션 curl 재검증(캐리어 90원·캠핑테이블 1,090원 둘 다 정확히 outlier 플래그, 5/5 정상 매칭 유지) + 실제 Discord 발송으로 최종 확인.

---

## 3. ★ AI코멘트 모순 근본수정 + 모바일 UX 1차 개선 (커밋 `9734338`)

**발견**(운영자 모바일 스크린샷 피드백): AI 인사이트가 "캐리어는 경쟁이 높으니 여행가방·배낭을 대안으로 찾으라"고 조언하는데, 바로 아래 필드에 정작 캐리어 도매처 링크가 나와 논리적으로 모순돼 보임.

**근본원인**: `generateAiInsight()` 프롬프트에 "도매처 링크가 이미 붙는다"는 컨텍스트가 없어 AI가 일반적인 "다른 곳을 찾으라"류 조언을 생성.

**수정**: 프롬프트에 IMPORTANT CONTEXT 블록 추가 — 도매처 링크 존재를 명시하고 "대안을 찾으라" 조언을 금지, 키워드별 구체적 실무 팁(최소주문량·품질확인·가격협상)을 요청하도록 지시. 가격 경고 문구에도 "(다른 상품일 수 있어요)" 이유 추가.

**검증**: 로컬 dry-run으로 5개 후보 전부 AI 코멘트가 구체적 실무조언으로 전환됨을 확인(예: "최소 주문량 100개 이상 확인", "제품 크기·무게·용량 정보 확인"), "대안을 찾으라"류 모순 문구 0건.

**미완료(다음 세션)**: 전체 레이아웃 재설계(순위·가격·경고를 한눈에 보이게 구조 변경). 설계 초안을 `docs/design/SOURCING_DISCORD_MOBILE_LAYOUT_2026-08-04.md`에 저장 — 옵션 1(필드 압축, 안전·빠름)부터 착수 권장.

---

## 4. 병렬작업 전체 최종 상태 (누락 0)

| 작업 | 상태 | 검증 수준 |
|---|---|---|
| 도매꾹 API 404 수정 | ✅ 완료·merge·프로덕션 배포·검증 완료 | tsc0·build0·dry-run 라이브·프로덕션 브라우저 |
| 음수마진 근본수정 | ✅ 완료·merge·프로덕션 배포·엔드투엔드 검증 완료 | tsc0·build0·로컬+프로덕션 curl 재검증 |
| 발행검수 UI 재설계 | ✅ 완료·merge·프로덕션 배포·브라우저 검증 완료 | 실제 상품 2건 스크린샷·DOM·API 3중 검증 |
| 페르소나 판단표면 분리 | ✅ 완료·merge·프로덕션 배포·검증 완료 | 97개 텍스트 노드 사투리 grep 0건 |
| 이미지경고 배지 오표시(신규) | ✅ 발견·근본수정·프로덕션 배포·재검증 완료 | 2개 상품 API=DOM 일치 |
| 4-Mode 추천시스템 SE05 정리 | ✅ 조사 완결(미마운트 확정·표식 등재) | grep 전수·vercel.json 대조 |
| **이종상품 오염 방지(신규)** | ✅ 완료·배포·검증 완료 | 로컬 2회+프로덕션 curl 재검증 |
| **소싱 실발송(신규)** | ✅ **완료** — 실제 Discord 발송·운영자 확인 | discordSent:true·스크린샷 확인 |
| **AI코멘트 모순 수정(신규)** | ✅ 완료·배포 대기 | 로컬 dry-run 검증 |
| **소싱봇 모바일 레이아웃 재설계** | ⛔ 설계만 완료, 구현 대기 | `docs/design/SOURCING_DISCORD_MOBILE_LAYOUT_2026-08-04.md` |

---

## 5. 다음 세션 시작 순서

```
[확인] 커밋 9734338 Vercel 프로덕션 배포 READY 확인
[운영자 액션 확인] SOURCING_RECOMMEND_LIVE=true 설정 여부 확인
   └─▶ 미설정 시 daily 크론은 여전히 dryRun으로만 동작(안전, 발송 안 됨)
[착수] 소싱봇 모바일 레이아웃 재설계 — 옵션 1(필드 압축)부터
   └─▶ 설계 문서: docs/design/SOURCING_DISCORD_MOBILE_LAYOUT_2026-08-04.md
   └─▶ 수정 파일: sourcing-recommender.ts의 buildSourcingRecommendEmbed() 단일 함수
```

## 6. 절대 금지 (매 세션 확인)
- 네이버 PUT/POST → 운영자 GO 없이 금지
- 디스코드 실발송 → **이제 SOURCING_RECOMMEND_LIVE가 켜지면 매일 자동 발송된다** — 레이아웃 변경 시 발송 문구가 깨지지 않는지 dry-run으로 반드시 먼저 확인
- 자동 발행 → 영구 금지(#307)
- 테스트 데이터 방치 → 이번 세션 임시파일(.tmp_verify*.json, .tmp_ai_check.json, /tmp/*.json) 전량 삭제 확인됨
- 외부 API 실패는 설정 의심 전 공급자 공지 실측(#324)
- 미실측 단정 금지(#310) · 부재증명 전수검색(#323) · 무음실패 금지(#270)
- 정적 라벨 상수에 실제값 미반영 패턴 의심(#325) · Chrome 확장 통신잡음과 앱결함 구분(#326)
- **신규**: 도매매칭 등 전문검색 기반 매칭은 이종상품 혼입 가능성을 항상 의심 — 배제(필터)와 경고(표시)를 병행하는 하이브리드 패턴을 기본으로 고려
- **신규**: AI 생성 문구(Groq 등)는 프롬프트에 "이미 확정된 사실"(예: 도매처 링크 존재)을 명시하지 않으면 그 사실과 모순되는 문구를 만들 수 있음 — AI 코멘트가 다른 필드와 같은 메시지에 들어갈 때는 프롬프트에 컨텍스트를 반드시 포함
- dev 서버: 이번 세션 PID 43578·43653·44317 전부 kill 완료. 다음 세션 시작 시 `lsof -ti:3000` 확인
