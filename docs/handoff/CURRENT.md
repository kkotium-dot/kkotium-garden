# 현재 인계 (CURRENT)

> 이 파일 1개만 활성 인계. 갱신 규칙: 매 세션 종료 시 이 파일을 덮어쓴다(누적 아님) — 실시간 상태는 `docs/plan/TASK_BRIDGE.md` §3 ACTIVE가 정본(단, §3-A 작업큐 보드는 2026-07-22 이후 갱신되지 않아 낡음).

- **status**: 발행전검수 화면 개선(#318) + 문서갱신 시스템(#319) + 브랜치·머지 리듬(#320) + 팀 구조 정정 + **`PRINCIPLES_LEARNED.md` 분할(#31) 완료**, 전부 검증 완료. 브랜치 push+build+preview 검증 완료. **merge는 대표님 저녁 검토 시간에 진행 예정**(방치 아님, #320 명시적 스케줄).
- **branch**: `feature/preview-copy-then-redesign` (HEAD `f6f4414`, 이번 분할 작업은 아직 미커밋)
- **goal**: 이 세션 기준 코드/문서 작업 완료. 남은 액션은 저녁 세션 merge뿐.
- **next-action**: 대표님이 저녁 시간에 브랜치 전체 검토(총 12커밋 예정) → merge.

---

## 1. 이번 세션 추가 작업 — 문서 임계 해소 (병렬 트랙)

지난 세션에서 발견한 "PRINCIPLES_LEARNED.md 1483줄, 임계 근접" 경고를 이번 세션에서 해소. 이 작업은 다른 진행 중 트랙(발행전검수 화면 등)과 파일이 전혀 겹치지 않는 독립 트랙이라 병렬 처리.

**PRINCIPLES_LEARNED.md 분할(#31)**:
- 원칙 #46~#227 구간(131건, 718줄)을 `docs/plan/archive/PRINCIPLES_LEARNED_archived-2026-07-30.md`로 이동
- 현재 파일: 1483줄 → 763줄(#254부터 시작, 임계까지 여유 737줄 확보)
- 상단 인덱스 표(실태와 어긋나 있던 것)도 함께 정정

**손실 0 검증(코드로 증명, 최고 수준 확실성)**:
```python
# git show HEAD:PRINCIPLES_LEARNED.md로 원본 복원
# (헤더 + archive본문 + 현재파일본문) 파이썬으로 재조합
# 원본과 라인단위 완전일치: Match: True
```
이건 "grep으로 대조 확인" 수준이 아니라 **원본을 코드로 정확히 재구성해 바이트 단위로 검증**한 것 — 이번 분할에 한해 이례적으로 엄격하게 검증(원칙 문서라는 중요도 감안).

**검증**: tsc 0 · sentinel 0건(양쪽 파일) · 손상문자 0건 · 재조합 완전일치.

---

## 2. 브랜치 최종 상태

**커밋 10개 push 완료**(`3532d78`~`f6f4414`, 전부 Vercel `READY` 확인), 이번 PRINCIPLES 분할 2개 파일(`PRINCIPLES_LEARNED.md` 수정 + archive 신규)은 아직 미커밋 — 다음 작업으로 커밋 예정.

## 3. 다음 Desktop READY

1. 이번 분할 작업 커밋(문서만, 저위험) → 저녁 세션 merge 검토에 포함
2. 저녁 세션에서 브랜치 전체(12커밋) 검토 → merge(#320)
3. 새 작업 지시 있을 시 원칙 #318/#319대로 착수 전 실측 우선

## 4. 절대 금지 (매 세션 확인)

- 네이버 스토어 PUT/POST → 운영자 "GO" 없이 절대 금지
- 디스코드 실발송(크론 수동 실행) → 실제 알림 발송됨
- `KKOTTI_PERSONA_VOICE_GUIDE.md` §1~5 수정 금지(추가만)
- 과거 archive 문서(시점 기록)는 실태와 달라도 손대지 않음 — 역사 보존
- "1인 셀러/1인 운영자"(스토어 운영 형태) 표현은 정당 — "1인 개발"(개발 체계)만 정정 대상이었음
- 테스트 데이터 방치 → 주입했으면 같은 세션에 원복
- 허위 완료 보고 → 실측 못 한 것은 "미검증"으로 명시(#310)
- 세션 종료 시 문서 3종 점검 없이 "완료" 보고 금지(#319)
- 착수 전 인계 문서의 "미완료" 표기를 그대로 믿지 말고 실측 우선(#318/#319)
- 브랜치를 며칠~몇 주 방치하지 않는다 — 저녁 세션 우선 검토(#320)
- MD 분할 시 "grep 대조"가 아니라 가능하면 재조합 완전일치 검증까지(오늘 사례 참고)
