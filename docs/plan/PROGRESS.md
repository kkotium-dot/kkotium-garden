# KKOTIUM GARDEN — 프로젝트 진행 현황
> 2026-07-08 (세션·Code) **존3 3탭 리팩터 (프리미엄 B2B SaaS) + PanelTabs 공통 컴포넌트 + 원칙 #212 박제** (Code turn·#55/#56/#3-1·레이아웃만·기능 무변경·비게이트·네이버 무접촉·비가역0). 권위 ZONE3_TAB_REFACTOR_SPEC_2026-07-08. 운영자 요청: 존3(우측 diff 슬라이드 패널) 과밀(동기화 diff+반영 push+품절 대체 3중 세로 적층)→시선 흐름 통제. verify-first(#181): DiffPanel 3중 적층 구조 실측(동기화 281-310·반영 312-358·품절대체 360-363). (1) PanelTabs 공통 컴포넌트(src/components/ui·재사용·#55·언더라인 탭·브랜드레드 active·배지 text/dot/tone·layout-only). (2) DiffPanel을 [동기화](기본·diff/drift)[반영](가격/재고 push)[품절대체](SubstituteEditor+체크리스트) 3탭 분절·활성 탭만 렌더(progressive disclosure)·탭 상태 로컬. (3) 배지 배선(동기화=app-SoR drift N필드 amber·반영=drift 시 점 blue·품절대체=미등록 시 '!' red·LinkedRow.hasSubstitute/driftFields). (4) 프리미엄 헤더(썸네일 40px+상품명 1줄 말줄임+상태 배지+X)·콘텐츠 20px 패딩·브랜드레드는 active/primary만(§2). ★기능 무변경·기존 내용 이동만(회귀 최소·loss0). 라이브 검증(로컬·명화): 3탭 렌더·기본 동기화 active·배지 '동기화 2필드'+반영 점·탭 전환 시 해당 관심사만 표시(반영→푸시만·품절대체→SubstituteEditor만·동기화표/품절 숨김)·헤더 썸네일+상태·과밀 해소(스크린샷). tsc0/build0·JSON valid·이모지0. **원칙 #212**(한 패널에 3개+ 관심사 적층 시 탭/아코디언 분절·1화면1작업·시선 흐름 통제·프리미엄 SaaS=정보밀도 높되 여백·primary 1개·브랜드색은 강조에만·공통 PanelTabs 재사용). 스펙 sync 커밋 1058200(#188). ★대기: Desktop prod 검증(3탭 전환·기본 동기화·배지·과밀 해소). PanelTabs 타 과밀 화면(관제탑 상세·스튜디오) 확장=후속.
> 2026-07-08 (세션·Code) **SUBSTITUTE 실전 강화 (리서치 적용) + 원칙 #211 박제** (Code turn·#56/#211/#3-1·문구·체크리스트·기본값·비게이트·네이버 무접촉·비가역0). 권위 SUBSTITUTE_PL3_RESEARCH_ENHANCE + STOCKOUT_SUBSTITUTE_RESEARCH_2026-07-08. 이미 빌드된 SUBSTITUTE(#210)에 리서치 실전로직 4종 얹기(실무 사고 방지=페널티·순위리셋·불일치 클레임). (1) 전환 전 확인 체크리스트(SubstituteEditor·4항목: 같은 카테고리/핵심키워드 유지/대표이미지 급변경 안함/동급·상위호환)→미체크 시 '순위 리셋·상품 불일치 신고 위험' 경고·전체 체크 시 완료 표시(클라 가이드). (2) substitute_ready 카드 단계별 액션 가이드(랭킹 영향 적은 순: ①옵션 일부 품절 ②재입고·재소싱 ③7~10일이면 예약구매 전환 ④대체상품 전환) + 페널티 안내(주문 취소 2점·선물 3점) + '최신 기준 스토어센터 확인' 링크. (3) 재고 임박 임계 기본값 5→10(신규 항목·리서치 기본값 '재고 10개 or 5일치')·툴팁·사용자 조정 가능. (4) 수치(임계·페널티)는 i18n 문구+링크로(하드코딩 금지·#211). 라이브 검증(로컬·명화): 존3 체크리스트 4체크박스·경고↔'전환 준비 확인 완료' 토글·툴팁('재고 10개 또는 5일치') 확인·명화는 저장된 임계 5 유지(기본 10은 신규 적용). 카드 단계/페널티=strings 배선+tsc(OOS 상품 부재로 카드 라이브 미표시·엔진 카드생성은 #210 세션 단위검증 완료). tsc0/build0·JSON valid·이모지0. **원칙 #211**(품절·재고 UX 수치=리서치 근거 기본값으로 시작·하드코딩 금지·안내문구+최신확인 링크·정책 변동성·대체전환은 '같은 카테고리+핵심키워드 유지' 보수 설계). 스펙 sync 커밋 946f13b(#188). ★대기: Desktop prod 검증(전환 체크리스트·임계 기본값·카드 단계/페널티). ★후속: §2 PL-3 강화(자동반영 경고·5단계 체크·역마진)=PL-3 빌드 시 얹기 · §3 product_alternatives 정리(substitute_info 일원화 권고)=운영자 결정 · PL3_PRICE_STOCK_PUSH_SPEC untracked=PL-3 태스크 대기.
> 2026-07-08 (세션·Code) **SUBSTITUTE — 품절 대체상품 관리 + OUTOFSTOCK 자동표출 + 원칙 #210 박제** (Code turn·#55/#56/#181/#190/#209/#3-1·읽기전용+앱측입력·네이버 쓰기0·비게이트·비가역0). 권위 SUBSTITUTE_STOCKOUT_SPEC_2026-07-08. ★verify-first(#181) 발견: 스펙 전제('SEED 테이블 없음 greenfield'·'기존 대체UI 저장 안 됨')를 실측→product_alternatives **테이블이 prod에 부재**(기존 AlternativeProductPanel이 dead table에 write=무효 저장)→스펙 '저장 안 됨' 정확·substitute_info가 유효 저장소. Supabase 프로브로 substitute_info jsonb 실존 확인 후 schema 동기화(substituteInfo @map). (1) product-link.ts SubstituteInfo 타입 + read/write guarded 헬퍼 + normalize/hasSubstitutePlan. (2) GET/PUT /api/products/{id}/substitute(read/persist). (3) SubstituteEditor 공통 컴포넌트(product-agnostic·#55·대체명/메모/재소싱링크/도매매번호/재고임계·sibling ko.json·Lucide·이모지0) → /products/link 존3 + SEED(products/new·savedProductId 가드) 양쪽. (4) C-9 substitute_ready intervention(#190): control-tower-engine ComputeContext.substitute + idle 브랜치(OUTOFSTOCK 트리거·sync_drift 미러·품절=매출정지라 idle 최우선)·hasSubstitute=false면 등록 넛지·asset-jobs-matrix naver_status_type=OUTOFSTOCK 감지 주입(PL-5a pull 재사용·신규폴링0). 재고임계 경보=엔진 준비(naver stock 미영속→dormant). (5) linked API substituteInfo+hasSubstitute 반환. 라이브 검증(prod DB·명화): substitute round-trip(GET null→PUT→persist 확인)·존3 SubstituteEditor 렌더+저장값 로드('명화 클립 디퓨저(블랙)')·linked hasSubstitute=true·엔진 단위검증 4케이스 PASS(OOS+대체→카드/OOS+미등록→넛지/재고정상→카드없음/OOS+drift→substitute_ready 우선). tsc0/build0(라우트 ƒ)·JSON valid·이모지0. **원칙 #210**(대체상품=품절 감지 시 자동표출 개입점·단순메모 금지·#56·OUTOFSTOCK은 PL-5a pull 재사용·신규폴링 금지). 스펙 sync 커밋 99ca2c4(#188). ★대기: Desktop prod 검증(명화 대체정보 입력·저장·품절카드 로직). ★플래그: (a)product_alternatives 테이블 prod 부재=기존 AlternativeProductPanel 저장 무효(별건 정리 후보) (b)PL3_PRICE_STOCK_PUSH_SPEC untracked=자체 빌드 태스크 대기.
> 2026-07-08 (세션·Code) **JOURNAL J2-2/J2-3 — 활동기록 kind 필터 + 더보기 페이지네이션** (Code turn·#55/#181/#191/#3-1·읽기전용·추가 fetch 0·비게이트·비가역0). 권위 JOURNAL_J2_FILTER_PAGINATION_SPEC_2026-07-08. 전제 JOURNAL 2축(J2-1 활동기록) DONE. 실측: 명화 44건 → 필터·페이지네이션 부재로 실사용 불편. verify-first(#181): ActivityTimeline + activity API(?limit) 실측 — kind=sourcing/job/asset/status·4소스 집계(#191 유지). J2-2 kind 필터칩(전체/크롤=sourcing/이미지=job/자산=asset/상태=status·건수 표시·0건 kind 미표시)·클라사이드 필터(추가 fetch 0). J2-3 더보기(기본 20건→+20 증분·필터와 조합·잔여 0 시 버튼 소멸)·단일 fetch limit=200(전 이력 확보·추가 fetch 0). i18n filter/more 블록(studio-strings·#3-1)·Lucide(ChevronDown)·이모지0. 라이브 검증(prod DB·명화): 활동 44건 → 칩 전체44/크롤1/이미지23/자산19/상태1(API 분해 정확) · 이미지 필터→job 23건만·20 표시·더보기(3) · 더보기 클릭→23 전체·버튼 소멸(잔여0). tsc0/build0·JSON valid·이모지0. ★대기: Desktop prod 검증(명화 일지 탭 필터 전환·더보기·건수). 스펙 sync 커밋 2b16aa7(#188).
> 2026-07-08 (세션·Code) **PL-5a 연동 drift 자동 감지·표면화 + 원칙 #209 박제** (Code turn·#55/#56/#190/#197/#181/#3-1·읽기전용·네이버 GET/상품·쓰기0·비게이트·비가역0). 권위 PL5A_DRIFT_DETECTION_SPEC_2026-07-08. 전제 PL-1 DONE. 실측 확인: linked API syncState가 정적 'SYNCED' 기본값(명화 실제 drift 있음에도 무의미) → 실제 비교로 전환. verify-first(#181): diffNaverProduct 시그니처 + C-9 detail_assembly idle-gated 패턴 실측 + Supabase information_schema로 drift_fields jsonb 컬럼 실존 확인(#68 역배포 가드) 후 schema.prisma driftFields @map 동기화. (1) GET /api/products/linked/drift-scan — LINKED 전상품 diffNaverProduct→앱SoR(name/salePrice/representativeImageUrl) drift만 카운트·재고 제외(#197: stockQuantity 미전달)·statusType=SHARED 마커·순차 Queue+에러격리(#82)·syncState/drift_fields/lastSyncedAt persist·60초 쿨다운(?force=1). (2) product-link.ts driftFields 배선(read/write guarded SQL·jsonb) + resolveLinkDisplay #209 정직화(미스캔=UNKNOWN·스캔됨=실제상태·정적 SYNCED 제거). linked API driftFields+counts.drift 반환. (3) /products/link UI: 행별 sync 배지(동기화됨/미확인/동기화 필요 N필드/상태확인)·상단 '동기화 필요 N건' 요약+[동기화 상태 새로고침](Radar)·drift 필터·행클릭→기존 diff 패널. sibling ko.json(#3-1)·Lucide·이모지0. (4) C-9 sync_drift intervention(#190·idle-gated·detail_assembly 미러·product-agnostic): control-tower-engine ComputeContext.syncDrift + idle 브랜치 카드(신규 큐 0)·asset-jobs-matrix readLinkFields 주입·widget 라벨/상세/필드 humanize. 라이브 검증(prod DB·실 네이버 GET): 명화 스캔전 UNKNOWN(#209 정직) → drift-scan → DRIFT·driftFields=[name,representativeImageUrl]·salePrice in-sync(방금 29000 정합)·statusMismatch0 = '동기화 필요 2필드' 배지+요약+필터(스펙 §4 정확 재현). C-9 sync_drift는 명화가 resolve_suspension+registry_drift 보유(비idle)라 미표면(설계대로·긴급 작업 미마스킹 #56·idle 전환 시 표면화). tsc0/build0(라우트 ƒ)·JSON valid·이모지0. **원칙 #209**(연동 상태 표시는 정적 기본값 금지·실측 비교 반영·재고=네이버 SoR 제외·미스캔은 UNKNOWN으로 정직). 스펙 sync 커밋 da86bac(#188). ★대기: Desktop prod 검증(명화 drift-scan=name·image·price in-sync·배지·persist·새로고침). PROGRESS.md 1500줄(#31) 분할=운영자 OK 대기.
> 2026-07-08 (세션·Code·Desktop결정 반영) **명화 발행가 29,000 확정·정합 완료 + 원칙 #208 박제 — 발행 준비도 100%(GO만 남음)** (트래커 갱신·코드변경0·비가역0·앱 DB 가격 정합=Desktop·가역). Desktop 가격 결정·정합 완료(권고방향·#205·권위 docs/design/MYEONGHWA_PRICING_DECISION_2026-07-08.md): 네이버쇼핑 경쟁분석(search_shop·중앙값 29,800·최밀집 27,900~30,900)→**29,000 확정**. 앱 salePrice 35,000→29,000 DB 정합(가역)·PL-1 diff로 네이버 일치 검증(salePrice drift 해소·app==naver). 3중 교차: 경쟁실가 29,800 중앙값 · 원기획 마진 2.03배=14,300×2.03≈29,000 · 원가구조(실원가 17,300→순이익 8,800·순마진 30%). → **명화 발행 준비도 100%(가격까지 완결)**: 원산지·옵션재고·고시규격·이미지·가격 정합 → 남은 것 = 운영자 발행 GO(#46 비가역·SALE 전환 PUT)뿐. **원칙 #208**(가격결정 = 경쟁실가 + 원가마진 + 셀러단계 3중 교차·search_shop으로 실경쟁가 근거화·추측 금지). **Code 착수 항목 0** — 발행 GO 시 명화 salePrice=29,000 기준. 대기: 명화 발행 GO(운영자) · PL-2 실 push(운영자) · SEC-2 · PL-3~5·J2-2/J2-3·SF-4b. 다음=운영자 GO/방향 대기(#87).
> 2026-07-08 (세션·Code·Desktop분석 반영) **명화 가격 drift 원가분석 첨부 (발행 전 확인 항목 보강)** (트래커 갱신·코드변경0·비가역0·네이버 무접촉). Desktop 가격 분석 완료 — 명화 가격 drift(네이버 29,000 vs 앱 35,000)에 원가/수익 근거 첨부. 실원가 = 공급가 14,300 + 배송 3,000 = **17,300**. · 29,000: 순이익 8,800 · 마진 30% · **원기획가**(margin 필드 2.03 = 29,000 대응 단서). · 35,000: 순이익 13,900 · 마진 40%. 정합 방향은 운영자 결정(29,000=원기획가로 앱 하향 / 또는 35,000 유지하고 네이버 push=PL-2 실 push·#196/#46). 발행·실 push 전 확정. **Code 착수 항목 0** — 운영자 GO/방향 대기 유지: PL-2 실 push(명화 가격 정합 포함) · 명화 발행(#46) · SEC-2 · PL-3~5·J2-2/J2-3·SF-4b. 다음=운영자 GO/방향 대기(#87).
> 2026-07-08 (세션·Code·Desktop검증 반영) **PL-1 naver-sync diff 검증 완료 등재 + 명화 가격 drift 발행 전 확인 항목 등재** (트래커 갱신·코드변경0·비가역0·네이버 무접촉). Desktop prod 검증 완료: PL-1 naver-sync diff 라이브 동작(3필드 diff·drift 감지 정상·재고=네이버 SoR #197). → PL-1(선택 임포트+읽기전용 pull+diff) DONE·prod검증. ★**발행 전 확인 항목 등재**: 명화 가격 drift — 네이버 29,000 vs 앱 35,000(diff 감지됨). 정합 방향은 운영자 결정(앱→네이버 push=PL-2 실 push 경로·GET-merge full PUT·#196/#46 / 또는 앱 가격 하향). 발행/실 push 전 반드시 확인. **Code 착수 항목 없음** — 운영자 GO/방향 대기 유지: PL-2 실 push(운영자 GO·명화 가격 drift 정합 포함) · 명화 발행(운영자 GO·#46) · SEC-2(운영자) · PL-3~5·J2-2/J2-3·SF-4b(대표 방향). 다음=운영자 GO/방향 신호 대기(관제탑 #87).
> 2026-07-08 (세션·Code·Desktop검증 반영) **PROXY-HEALTH DONE·prod검증 완료 — 시스템 전 영역 정상 상태 등재** (트래커 갱신·코드변경0·비가역0·네이버 무접촉). Desktop prod 검증 완료: healthy=true·배너 미표시(정상 케이스)·503 계층분류 실증(FUNNEL_DOWN). → PROXY-HEALTH 워크스트림 DONE·prod검증(#204 헬스체크+관제탑 경보 · #205/#206/#207 인시던트 런북·§3 계층강화 FUNNEL_DOWN/PROXY_AUTH). ★**시스템 전 영역 정상 상태**: 네이버 연동 healthy(프록시 복구·SPOF 가시화 배선 완료) · 주문 동기화(ORDER 워크스트림 DONE·prod검증) · 상품 링크(PL-1 DONE·PL-2 dry-run 검증) · 자산/합성 엔진 · SEO 정상. **Code 착수 가능 항목 없음** — 남은 건 전부 운영자 GO/방향 대기: PL-2 실 push(운영자 GO·GET-merge full PUT 보존검증) · 명화 발행(운영자 GO·#46 비가역) · SEC-2(운영자) · PL-3~5·J2-2/J2-3·SF-4b(대표 방향). 다음=운영자 GO/방향 신호 대기(관제탑 #87·단일 보드).
> 2026-07-08 (세션·Code) **PROXY-HEALTH §3 계층 강화 + 인시던트 런북 등재 + 원칙 #205/#206/#207 박제** (Code turn·#204/#206/#181/#3-1·읽기전용·네이버 GET 1회·비가역0). 권위 PROXY_INCIDENT_RUNBOOK_2026-07-08(Desktop 직접 진단·해결). 인시던트=Funnel serve 컴포넌트 멈춰 503(공개 URL은 열림·프록시 err log의 secret mismatch는 봇 노이즈·실제 앱 요청은 도달 못함)→`Tailscale serve reset`+`funnel --bg 3001` 2줄로 즉시 복구. 런북 §3 반영: 헬스체크가 **응답코드로 계층 구분**. 신규 클래스 2종 추가(FUNNEL_DOWN·PROXY_AUTH)로 5→7클래스. 매핑 계층 우선순위(mapToHealthClass): Naver 바디검출(IP_NOT_ALLOWED·AUTH_SIGN_INVALID·RATE_LIMIT)이 raw status보다 우선(네이버 secret-sign 401을 프록시 시크릿 오인 방지)→전송계층 status(503=FUNNEL_DOWN serve멈춤·401=PROXY_AUTH 시크릿불일치)→네트워크(ECONNRESET/timeout/DNS=PROXY_DOWN)→나머지 NAVER_ERROR. 배너=클래스별 구체 조치 문구(FUNNEL_DOWN=serve reset·PROXY_AUTH=양쪽 시크릿 일치·PROXY_DOWN=홈 Mac 확인)·Lucide(Unplug/Lock 추가)·이모지0·#3-1 ko.json. 라이브 4상태 검증(로컬 목 프록시): 정상 복구된 프록시→healthy:true·배너 미표시(alert 0) / 목 503→FUNNEL_DOWN·serve reset 문구 / 목 401→PROXY_AUTH·시크릿 문구 / (ECONNRESET→PROXY_DOWN=직전세션). tsc0/build0(라우트 ƒ). **원칙 #205**(직접 해결 가능 인프라/환경은 Desktop이 모든 커넥터·스킬로 직접 해결·운영자 승인·단 비가역/네이버 쓰기/발행은 GO 게이트 #46 유지) · **#206**(프록시 진단=계층 분리 Mac→Tailscale→Funnel→로컬프록시→외부도달성·err log secret mismatch는 공개 URL 봇 노이즈 가능→'앱 요청이 실제 프록시에 닿는지' 로그 증가로 확정·Funnel 503=serve reset) · **#207**(외부 도달성은 반드시 외부에서 검증·로컬 curl 정상≠외부 정상·#200 연장). 런북 커밋 eeb213d(#188). PL-2 dry-run Desktop 검증 완료(200·optionStockZeroed 3·stockQuantity 미변경)·실 push=운영자 GO 대기. ★대기: Desktop prod 검증 — 프록시 정상 복구됨→prod health=healthy:true·배너 소멸 확인(현재 정상 케이스 검증 가능).
> 2026-07-08 (세션·Code) **PROXY-HEALTH — 네이버 연동 헬스체크 + 관제탑 경보 배너 + 원칙 #204 박제** (Code turn·#62/#181/#56/#195/#3-1·읽기전용·네이버 GET 1회·비가역0). 권위 PROXY_HEALTH_SPEC_2026-07-07(운영자 승인). 배경=네이버 프록시(홈 Mac Tailscale Funnel)=모든 네이버 연동 SPOF·세션 중 조용히 다운(ECONNRESET) 시 동기화 멈춰도 앱 무표시. verify-first(#181): 기존 NaverApiError classifier(NaverFailKind) 재사용 확인 후 얹음. (1) GET /api/naver/health — 경량 네이버 호출 1회(GET /v1/seller/channels)→{healthy,errorClass?,message?,checkedAt}·60초 메모 캐시·?fresh=1 강제. errorClass 매핑(classifier→5클래스): NETWORK_RESET/TIMEOUT/DNS→PROXY_DOWN·IP_NOT_ALLOWED·AUTH_SIGN_INVALID·RATE_LIMIT·나머지→NAVER_ERROR. checkNaverHealth 무throw. (2) NaverHealthBanner(대시보드 Section 2 인박스 최상단·OrderProcessingNudge 위)·healthy=true·로딩·미분류 시 render null(노이즈0)·errorClass별 구체 가이드 문구·SPOF 노트·checkedAt 상대시각·#3-1 sibling ko.json·Lucide·이모지0·비강제#56. 엔티티=시스템 전역→대시보드 서피스(상품 C-9 매트릭스 아님·#195 연장). 라이브 2상태 검증: 정상(로컬 프록시 up)→healthy:true·배너 미표시(alert 0) / 불통 프록시 URL 주입 인스턴스(3009)→healthy:false·errorClass:PROXY_DOWN·한글 가이드 반환·배너 노출(role=alert·인박스 최상단·스크린샷 확인). tsc0/build0(라우트 ƒ 등록). **원칙 #204**(외부 SPOF는 헬스체크+가시적 경보 필수·조용한 실패 금지): 프록시/외부 API 등 단일 장애점은 주기·on-demand 헬스체크로 감지→관제탑 경보로 표면화→errorClass별 구체 가이드(무엇을 고칠지)까지 3단(감지→표면화→가이드). 스펙 sync 커밋(#188). ★대기: Desktop prod 검증 — 현재 프록시 다운 상태가 테스트 케이스(prod health=PROXY_DOWN·배너 노출 확인·복구 후 healthy·배너 소멸).
> 2026-07-07 (세션·Code) **PRODUCT-LINK PL-2 상태 push 빌드 (write-0·GO 게이트·실 push 대기)** (Code turn·#196/#197/#181/#46·쓰기0·실 PUT 미발생). 권위 PL2_STATUS_PUSH_SPEC. 첫 네이버 쓰기 경로를 쓰기0 경계까지 빌드. verify-first(#181): 명화 옵션 shape 실측(optionCombinations x3). updateProductStatus=full GET-merge(#196·11필드 보존)·재고수량 push 안함(#197: 품절=옵션 재고 0 또는 top-level 0·재판매=statusType만). POST naver-status(dryRun 기본 true·실 PUT=dryRun:false+confirm:true GO게이트#46). UI 존3 [품절 처리]/[재판매]→dry-run 미리보기(상태만 변경·GO 노트). 라이브 dry-run: OUTOFSTOCK→옵션3 재고0·stockChanged=false·changed=[detailAttribute] / SALE→[statusType] / GO게이트 확인(confirm 없음→applied=false). tsc0/build0·콘솔0. 992ef5f. ★대기: 운영자 'PL-2 push GO'→Desktop 실 PUT 보존검증. PL-3(가격)~PL-4(상세/옵션) 후속.
> 2026-07-07 (세션·Code) **ORDER 워크스트림 DONE·prod검증 + 원칙 #203 박제 (스코프 확정)** (docs·코드변경0·비가역0). ORDER-UI-1 Desktop prod 검증 완료(24행·펼침 배송지/우편번호/수령인/연락처/복사) → ORDER 워크스트림(SYNC-2+UI-1) DONE·prod검증. ★운영자 스코프 확정: 앱 주문 기능=정보 동기화+조회 전용, 발송/운영은 네이버 스토어. ORDER-QUEUE-2(앱 내 인라인 발송)=스코프아웃 제거. **원칙 #203**(주문 기능 스코프=정보 동기화+조회 전용·발송/운영=네이버·앱 목표=오류없는 정확 동기화). ORDER Code 착수 항목 없음. 다음=PL-2(운영자 방향 시) 또는 대기.
> 2026-07-07 (세션·Code) **ORDER-UI-1 주문 배송지 UI 표출 + 원칙 #202 박제** (Code turn·#181/#73/#3-1·UI only·네이버 무접촉·비가역0). 문제=ORDER-SYNC-2로 배송지 DB 24/24 복구됐으나 /orders 미표시(행 주소0·드러워 우편번호 없음)→발송 실무 불가. verify-first(#181): /api/orders shippingAddress+shippingZipcode 반환 확인·24행 렌더 확인. 수정=행별 펼치기 토글→인라인 배송 패널(배송지+우편번호+수령인+연락처+배송메모+복사)·기본 접힘·드러워 우편번호 합성·Order 타입 shippingZipcode 추가. 검증: 오현석(SHIPPING) 펼침→전체 주소+07365·복사·콘솔0·payment_date DESC·tsc0/build0. 5215277. **원칙 #202**(동기화 수정=DB+UI 표출 완결·데이터만 고치고 화면에 없으면 실무 미해결). Desktop prod 검증 완료(24행·펼침 배송지/우편번호/수령인/연락처/복사)·DONE.
> 2026-07-07 (세션·Code) **ORDER-SYNC-2 배송지 근본수정 + PL-1 스키마 동기화 + 원칙 #200/#201 박제** (Code turn·#62/#82/#181/#46·읽기전용·주문 변이=백필 upsert만). (1) ORDER-SYNC-2(권위 ORDER_SYNC2_ADDRESS_FIX_SPEC): 근본원인=평면 query 응답 배송지는 productOrder.shippingAddress 중첩인데 코드가 el.shippingAddress(부재)/el.delivery에서 조회→전 24건 배송지 빈값(customerName=order.ordererName 정상=한 필드 정상≠전 필드). verify-first(#181): throwaway 프로브로 경로 실측 확정. 수정=배송지 base+detailed 합성+shippingZipcode·backfill=1(전 로컬주문 재조회)·/orders 정렬 paymentDate DESC. 검증: backfill 24건 전건 addr_filled·addr_empty0(전상태). tsc0/build0. 7ccfa72. (2) PL-1 스키마 동기화: Desktop ALTER 적용완(실측 확인)→schema.prisma 8필드 타입화(naverProductId @unique+링크6+default)·prisma generate·guarded helper 유지. 93cd2a6. **원칙 #200**(외부 API 매핑=필드별 라이브 실측·한 필드 정상≠전 필드·중첩 경로 각각 #181) · **#201**(동기화 검증=건수+필드 완전성·핵심필드 없으면 실무 불가). 대기: ORDER-SYNC-2 Desktop prod 재감사 · PL-1 Desktop prod 검증.
> 2026-07-07 (세션·Code) **PRODUCT-LINK PL-1 구현 — 선택 임포트+읽기전용 pull (reverse-deploy-safe·네이버 쓰기0)** (Code turn·#181/#82/#196~#199/#3-1·네이버 GET/search만·비가역0). 권위 PL1_PRODUCT_IMPORT_SPEC. ★안전판: Supabase 8필드 ALTER 미적용(실측 부재)→schema.prisma에 타입필드 추가 시 전 Product 쿼리 P2022 앱파괴 위험 → 신규 7컬럼은 schema 무변경·guarded raw SQL(product-link.ts·42703/P2022 가드·기본값 degrade)로만 접근(기존 쿼리 무영향·배포안전·ALTER 후 자동활성). 백엔드(쓰기0): searchProducts/getChannelProduct api-client · GET naver/products/search(정규화+alreadyLinked) · POST products/import(channel→origin·중복차단·부분실패#82) · GET products/[id]/naver-sync(diff·재고=네이버 SoR#197) · GET products/linked. 발행 register 자동편입 훅(source=NATIVE·LINKED·guarded). UI /products/link 3존(진입/SaaS리스트 IMPORTED·NATIVE 배지+SYNCED/CONFLICT 칩·필터/diff패널 SoR)·Sidebar link2·i18n격리#3-1·이모지0. verify-first(#181): search shape 라이브 실측(Spring Page·382 SALE·size100 OK). 로컬 라이브 검증: 정규화·중복차단·linked(pre-ALTER 기본값)·diff(명화 35000 vs 29000)·modal 8페이지·콘솔0·tsc0/build0. a0d95ff. ★대기: (1)Desktop Supabase ALTER (2)ALTER 후 schema.prisma 타입 동기화 (3)Desktop prod 검증(임포트 라운드트립·배지·diff·중복차단). PL-2(push)~PL-5(cron) 후속.
> 2026-07-07 (세션·Code) **PRODUCT-LINK 워크스트림 등재 — 선택 임포트+양방향 동기화 허브 리서치 sync + 원칙 #196~#199 박제** (Code turn·docs only·코드변경0·비가역0·네이버 무접촉·#181/#46/#62). 권위 PRODUCT_LINK_BIDIRECTIONAL_SYNC_RESEARCH_2026-07-06(Desktop 심층 리서치). 방향: 선택 소수 상품 임포트(목록 체크박스+번호 직접입력)+필드별 SoR 양방향(pull 기본·push=GET-merge full PUT). ★verify-first(#181) 선행 실측(PL-1 스펙 대기 중): getProduct/updateStock/diffNaverProduct 3종 시그니처 확인·재사용 가능(리서치 §6 정합) · Product.naverProductId=String?(@unique 아님) → PL-1 스키마 확장 greenfield(channelProductNo·source·linkStatus·naverModifiedAt·lastSyncedAt·syncHash·syncState 미존재). **원칙 #196**(full-replace·모든 push GET-merge 필수·부분 PUT 금지·detailContent 생략=유지·seoInfo 항상 명시) · **#197**(필드별 SoR 고정·재고=네이버·상세/가격/옵션=앱·push 전 pull·syncHash 에코방지·CONFLICT 큐) · **#198**(origin/channel 유형 함께 저장·매핑키=originProductNo) · **#199**(선택 소수 상품 전제·full-replace ~2초/건·대량 금지). 로드맵 PL-1(read-only pull MVP)~PL-5. PRODUCT-LINK 스케줄 등재. 대기: Desktop PL-1 상세 스펙 전달 후 착수.
> 2026-07-07 (세션·Code) **ORDER-QUEUE-1 — 주문 처리 넛지 대시보드 서피스 + 원칙 #195 박제** (Code turn·#55/#56/#181/#46/#3-1·읽기전용·주문 변이 0). 권위 ORDER_QUEUE_SPEC_2026-07-06. 전제 ORDER-SYNC(DONE·prod검증)로 Order.status 정확 → 처리 필요 주문을 대시보드 액션 서피스에 능동 표면화(주문 관리 루프 완결). verify-first(#181): 대시보드 "오늘 처리 액션" 서피스=Section 2 받은편지함 실측 후 확장(신규 서피스 0). 트리거(읽기): PAID/PAYED=발송 필요·CANCEL_REQUESTED+RETURN_REQUESTED=클레임 응대. 신규 위젯 OrderProcessingNudge(#3-1 sibling ko.json·Lucide·이모지0·0건 self-hide)를 인박스 최상단 배선. 카운트=/api/dashboard/stats 읽기 확장(prisma.order.groupBy status·신규컬럼0)+DashboardStatsApiData 타입. 딥링크 /orders?status=PAID→기존 dispatch/confirm/claim 라우트(신규 배관0·#46 쓰기=운영자 클릭). 라이브(로컬 실DB PAID 1건): "발송 필요 1건" 노출·딥링크 필터 활성·클레임 0 미표시·콘솔0. tsc0/build0. **원칙 #195**(주문 개입 서피스 분리): 주문(Order) 개입은 상품(Product) C-9 매트릭스가 아니라 대시보드 액션 서피스 — 엔티티가 다르면 개입 서피스도 분리(#56 개입점을 엔티티에 맞게). 34c6c96. 스펙 sync 커밋(#188). ORDER-QUEUE 스케줄 등재. 대기: Desktop prod 검증(#45/#88). ORDER-QUEUE-2(인라인 dispatch)=스코프아웃(운영자 확정·#203).
> 2026-07-06 (세션·Code) **ORDER-SYNC 근본수정(#62) — 상태전이 캡처 last-changed 3-endpoint flow + daily cron(플랜 상한) — DONE·prod 검증 완료 + 원칙 #192/#193/#194 박제** (Code turn·#62/#82/#181/#46·읽기전용·주문 변이 0). 권위 ORDER_SYNC_FIX_SPEC. ★verify-first(#181)가 스펙 전제를 반증: 스펙/naver-doctor 권고 'from→lastChangedFrom 리네임'을 라이브 실측한 결과 product-orders 엔드포인트가 400 '`from 필드는 필수값 입니다`' 반환 — 즉 그 엔드포인트는 `from` 필수·생성일 기준이라 상태전이 캡처 불가. 정답=last-changed 3-endpoint flow(스펙이 대안으로 명시한 경로·라이브 검증): (1) GET product-orders/last-changed-statuses?lastChangedFrom&lastChangedTo(≤24h·초과 시 104140) → 변경 productOrderId (2) POST product-orders/query {productOrderIds} → 상세{order,productOrder,delivery,currentClaim?}(평면·content 래퍼 없음) (3) dedup+upsert(기존 STATUS_MAP/claim 매핑·평면 shape 매퍼). api-client: getOrders는 from/to로 복원(product-orders=생성일·대시보드 today-summary 정상 유지)+잘못된 주석 정정, 신규 getChangedOrderIds/getOrderDetails. route: 3-endpoint flow·honest total-failure gate(#82) 유지(전 윈도우 실패 OR 전 detail배치 실패 커버). cron: 신규 /api/cron/order-sync 일일 11:00 UTC(hours=48 lookback)·vercel.json crons 등재 — ★6h/hourly는 플랜 cron daily 상한 초과로 배포 거부(라이브 확정)→daily로 정정(#194)·cron/daily도 이미 주문 sync(hours=24) 호출 중이었으나 param 버그로 고장→본 수정으로 정상화(#193). 라이브 검증(proxy): manual=1&hours=720→success·32윈도우·changed/synced 캡처·NAVER_DIAG 에러0 / cron 라우트 인가200·미인가401. tsc0/build0. **원칙 #192**(주문 동기화 근본): 상태전이 캡처는 생성일(from/to) 아닌 변경분(last-changed-statuses→query) 3-endpoint flow. ★진단 도구(naver-doctor) 권고도 라이브 검증 필수—본 건 진단 권고가 라이브로 반증됨(#181·추측/미검증 진단 적용 금지). **원칙 #193**(동기화류 수동+자동 병행): 수동 전용은 stale—cron 병행·주문은 주기 짧게(플랜 daily 상한 내 최대—staggered ~2회/일). 스펙 v2 갱신 완료(Desktop·LIVE-CORRECTED): ORDER_SYNC_FIX_SPEC의 param-rename 전제는 라이브 반증됨→3-endpoint flow가 정답으로 스펙 전면 정정·sync 커밋(#188). **Desktop prod 검증 완료(2026-07-06·#45/#88)**: prod manual=1&hours=720→success·changed:13·synced:13·skip0·에러0(30일 상태전이 전부 캡처·수정 전 '24h 1건' 대비 정상화)·cron 배포 확인. **원칙 #194**(vercel.json cron 빈도=플랜 상한 준수): sub-daily(6h/hourly)는 배포 자체 실패(로컬 build 통과와 무관)→daily 상한 준수·cron 스케줄 변경은 #36 배포 verify 필수. ORDER-SYNC 워크스트림 DONE.
> 2026-07-06 (세션·Code) **명화 발행 진단 (Desktop) — 네이버 등록+SUSPENSION · 발행 잔여=SALE 전환 GO-게이트 PUT(#46)** (진단 기록·docs only·코드변경0·네이버 무접촉). 명화 상태: 네이버 등록 완료이나 statusType=SUSPENSION(비활성). 콘텐츠 준비 완료(판매가 35,000·원산지 중국·상세 set). 남은 것=네이버 SALE 전환 = 전체 payload PUT(update route·confirm:true·비가역 #46·전체교체 #3-7)로 statusType='SALE' emit. 엔진 근거: control-tower nextAction=resolve_suspension(registered+statusType!=SALE·control-tower-engine.ts:58·publishDrift). ★GO-게이트: 운영자 명시 승인 후에만 실 PUT(#46 비가역·Naver 접촉). Code 착수 불가(운영자 GO 대기).
> 2026-07-06 (세션·Code) **★ JOURNAL 워크스트림 완료 (마일스톤) — 일지 탭 2축(생명주기+활동기록) 완성** (Code turn·docs only·코드변경0·비가역0·네이버 무접촉). JOURNAL-1(JobLifecyclePanel 생명주기) + J2-1(활동 기록 타임라인)로 일지 탭 2축 구조 완성. J2-1 Desktop prod 검증 완료: activity API 44건·시간 역순·4소스(crawl_logs/asset_jobs/asset_registry/Product) + 일지 탭 활동기록 섹션 렌더(상대시각·이벤트 라벨·2축 공존·폴백 0). Code 추가액션 불요. 후속: J2-2(필터/더보기)·J2-3(#56 detail_assembly 연동)=백로그 · SF-4b(프리미엄 합성)=옵션.
> 2026-07-06 (세션·Code) **J2-1 DONE(활동 기록 타임라인·일지 탭 2번째 섹션) — prod 검증 완료(Desktop 2026-07-06·44건·시간역순·4소스·2축 공존·폴백0) + 원칙 #191 박제** (Code turn·#55/#181/#3-1). 권위 JOURNAL2_ACTIVITY_LOG_SPEC. 신규 GET /api/products/{id}/activity?limit=50 — 기존 이벤트 소스 집계(신규 로깅 테이블 0·#191): crawl_logs(소싱·crawledAt/sourcingStatus)·asset_jobs(잡·updatedAt/jobType/status)·asset_registry(자산생성·createdAt/stage)·Product(상태·updatedAt/status)→통일 shape {ts,kind,label,target} 시간 역순. 각 소스 guarded(P2021/P2022→[]·500 방지). ActivityTimeline 컴포넌트(SWR·Lucide·상대시각·읽기전용)를 일지 탭 journalSlot JobLifecyclePanel 아래 2번째 섹션에 배선(JOURNAL-1 flex-column 구조 재사용·신규 레이아웃 0)·한글 i18n 격리(#3-1 studio-strings.atelier.journal.activity). verify-first(#181): 실 스키마 필드 매핑 후 배선. ★라이브 API 스모크(prod DB): 실상품 activity 유효 shape·시간 역순(job/status 이벤트) 확인. 게이트 tsc0/build0·sentinel0·JSON valid·PrismaClient 0·이모지0. **원칙 #191**(활동 기록=기존 이벤트 소스 집계): 활동 기록은 asset_jobs/crawl_logs/Product/storage 기존 소스 집계로—신규 로깅 인프라 신설 금지(#181/#62)·통일 shape 병합·시간 역순. SF-5 DONE 반영 완료(직전 세션·엔진 게이팅 prod검증·노출=스모크 의존 honest). J2-2(필터/더보기)·J2-3(#56 연동)=후속. prod 검증 완료(Desktop 2026-07-06·44건·4소스). J2-2/J2-3 후속 · SF-4b 옵션.
> 2026-07-06 (세션·Code) **★ 조립엔진 #56 편입 완료 (마일스톤) — SF-1~SF-5 종료** (Code turn·docs only·코드변경0·비가역0·네이버 무접촉). 상세 조립 엔진(SF-1 읽기 보드·SF-2 이미지 배정·SF-3a 카피 제안·SF-3b 카피 영속·SF-4a 860px 미리보기)에 이어 SF-5(detail_assembly 개입)로 Operator Action Queue(#56)에 완전 편입 — 조립 엔진 + 운영 큐 통합 완성. SF-5 Desktop 검증: 엔진 게이팅 prod 확정(3상품 non-idle→detail_assembly 0·정상 억제), 양성 넛지 노출은 prod에 idle+미완 상품 부재로 관찰 불가→PURE 4케이스 스모크 의존(honest #45). Code 추가액션 불요. 잔여 옵션: SF-4b(프리미엄 이미지 합성)=후속 · JOURNAL-2(운영자 범위 Q).
> 2026-07-03 (세션·Code) **SF-5 DONE(상세 조립→Operator Action Queue 통합·detail_assembly 개입) — prod 검증 완료(엔진 게이팅·Desktop 2026-07-06) + 원칙 #190 박제** (Code turn·C-9 패턴 정확 미러·#55/#56/#35/#181/#46). 권위 SF5_ASSEMBLY_QUEUE_SPEC. 기존 C-9 Operator Action Queue에 개입 타입 1종 추가(신규 큐/카드/컬럼 0): intervention.ts INTERVENTION_DETAIL_ASSEMBLY+union+buildDetailAssemblyPayload · control-tower-engine ctx.assemblyNudge+idle-block 분기(category_dna_unseeded 미러·!nextAction일 때만=긴급작업 비마스킹 #56·detail_assembly가 dnaUnseeded보다 우선) · 딥링크 /studio?product={id}&tab=image(배양실 탭 #182) · asset-jobs-matrix 라우트에서 DB필드로 판정(assemblable=mainImage/detail_image_url/additionalImages/images · incomplete=detail_images 빈 OR description 빈). ★판정=DB 저비용·고재현 프록시(매트릭스에서 상품별 storage list 과비용·asset_registry 드리프트 회피—스펙의 composite/cutout/detail stage 의도를 DB 신호로 근사). 읽기 전용·신규 컬럼 0·발행 무관(#46). 한글 라벨 control-tower-strings.ko.json(#35): '상세 조립 미완'/'상세 캔버스 열기'+미완 분기(이미지/카피/둘 다). 위젯 IV 타입+interventionLabel+InterventionDetail 배선. 게이트 tsc0/build0·sentinel0·JSON valid·PrismaClient 0·PURE 4케이스 스모크(idle 넛지/완료 음성/긴급 비마스킹/조립 우선). **원칙 #190**(개입점 추가=기존 C-9 확장): 개입점 추가는 intervention.ts+computeActionQueueItem에 타입 1종 확장으로—별도 큐/카드 UI 신설 금지(단일 관제탑 #87). SF-5로 조립 엔진(SF-1~4a)이 #56 개입 흐름에 완전 편입. prod 검증 완료(Desktop 2026-07-06): 엔진 게이팅 확정(3상품 non-idle→detail_assembly 0·정상 억제). ★양성 넛지 노출은 prod에 idle+미완 상품 부재로 관찰 불가→PURE 4케이스 스모크에 의존(honest #45). 잔여 옵션: SF-4b(프리미엄 합성)·JOURNAL-2.
> 2026-07-03 (세션·Code) **★ SF 코어 조립엔진 완료 (마일스톤) — SF-1~SF-4a 전부 prod 검증(Desktop)** (Code turn·docs only·코드변경0·비가역0·네이버 무접촉). 상세 조립 엔진 코어 5단계 완성: SF-1(읽기 보드)·SF-2(이미지 배정→detail_images 영속)·SF-3a(무료 카피 제안)·SF-3b(카피 편집→description 영속)·SF-4a(860px 미리보기·860/360 토글·규격 인디케이터). Desktop prod 검증: 875px 미리보기 패널(이미지+카피 조립)·860/360 토글·규격 인디케이터 렌더 / 편집 textarea 16개 + '상세 카피 저장'→description 영속(조립 카피·실데이터 원산지 중국·판매가 확인)→달항아리 description 원복(무결성). Code 추가액션 불요. 대기: SF-5(#56 Operator Action Queue 통합)=Desktop 스펙 대기 · SF-4b(프리미엄 canvas/Firefly 합성)=옵션 후속 · JOURNAL-2(운영자 범위 Q).
> 2026-07-03 (세션·Code) **SF-4a(860px 미리보기)+SF-3b(카피 영속) DONE — DONE·prod 검증 완료(Desktop 2026-07-03) + 원칙 #189 박제** (Code turn·경로A HTML 조립·#55/#181/#46/#184/#188). 권위 SF4_DETAIL_EXPORT_SPEC. DetailAssemblyBoard: detail_images 순서 + 섹션 카피(편집)를 네이버식 860px detailContent 미리보기로 조립(신규 PURE lib detail-assemble.ts — buildDetailPreviewHtml/buildDetailCopyText·register buildDetailContent 패턴 미러). 데스크톱/모바일(860/360) 토글 + 규격 인디케이터(가로·이미지 수·카피 자수). SF-3b: SF-3a 제안→섹션별 편집→'상세 카피 저장'(명시적)으로 Product.description에 평탄화 텍스트 영속(신규 필드 0·#184 detail_images 평면 URL 유지·description은 발행 시 detailContent 본문). verify-first(#181): register/route buildDetailContent emit 구조 + PUT /api/products(sanitizeProductWrite allowlist) 실측 후 배선. detailContent HTML은 발행 시 계산(발행경로 무변경·#46 GO 게이트). 게이트 tsc0/build0·sentinel0·PURE 스모크(카피텍스트 빈섹션 생략 #82·860px 인터리브·빈→''). **원칙 #189**(네이버 상세=HTML 조립 우선): 상세페이지는 detailContent HTML 방식 — 이미지 합성(SF-4b)보다 HTML 조립이 텍스트 SEO 인덱싱·유지보수 유리. 프리미엄 이미지 합성은 옵션 분리. SF-4b(canvas/Firefly 합성)=후속. 검증 완료(Desktop 2026-07-03·875px 패널·860/360 토글·규격 인디케이터·description 영속·달항아리 원복). SF-4b(프리미엄 합성)=옵션 후속 · JOURNAL-2(운영자 범위 Q).
> 2026-07-03 (세션·Code) **SF-3a DONE(섹션별 카피 제안·읽기전용·무료 템플릿) — DONE·prod 검증 완료(Desktop 2026-07-03)** (Code turn·#55/#155/#181/#82). DetailAssemblyBoard 7섹션 각각에 카피 제안(헤드라인+본문) 표시 + 섹션별 복사 버튼. 신규 PURE lib section-copy.ts(buildSectionCopies·buildTemplateCopy 재사용): hook=골든키워드 훅(hookPhrase/seo_hook_text) · value=키워드 베네핏 · spec=상품데이터(카테고리/원산지/판매가) · usage=카테고리 템플릿 · trust=store_settings(A/S·문의·반품/교환비) · cta=구매유도 템플릿(무료배송/선물포장) · notice=공통안내 핀. 무료 우선(#155·AI 0)·소스 없으면 available=false→'생성 필요'(무날조 #82)·영속 0(SF-1 패턴·클립보드 복사만). verify-first(#181): 실 route(/api/products/{id}·/api/store-settings) + Product/StoreSettings 필드 shape 실측 후 매핑. PURE 스모크(풀데이터=7섹션 제안·희소데이터=value/spec/trust/notice 생성필요·hook/usage/cta 템플릿 초안). tsc0/build0·이모지0·sentinel0. SF-3b(편집+영속)=SF-4 내보내기와 함께 후속. 검증 완료(SF-3b/SF-4a 검증에 포함·Desktop 2026-07-03).
> 2026-07-03 (세션·Code) **SF-2 prod 검증 완료(Desktop) 반영 + 원칙 #186/#187 박제** (Code turn·docs only·코드변경0·비가역0·네이버 무접촉). Desktop production 라이브 e2e: 슬롯 배정→autosave→GET detail_images=[backdrop](평면) DB 확인→재오픈 '현재 조립됨' 스트립 복원→달항아리 원복. write 라운드트립 포함 전 경로 검증 완료(로컬 e2e→prod 격상·#88). SF-2 DONE·prod-verified 확정(Code 추가액션 불요). **원칙 #186**(스튜디오 보드 섹션 활성화 트리거): DetailAssemblyBoard 섹션 활성화는 섹션 카드 컨테이너 클릭으로 발생(헤더 클릭 아님)—상호작용 검증/자동화는 컨테이너 타깃. **원칙 #187**(prod 빌드 fiber introspection 신뢰 금지): 프로덕션 빌드는 __reactProps$ 등 React fiber 키가 minify로 탐지 false-negative 가능→UI 상호작용 검증은 fiber introspection 대신 관찰가능 상태변화(프롬프트/DOM/저장 칩)로 판정(#45/#88 연장). **원칙 #188**(Desktop 설계문서 sync-commit 의무): write_file로 생성된 Desktop 저작 설계문서(docs/design/*)는 sync 커밋에 항상 포함—미커밋 방치=유실 리스크(#62 프로세스 수정). 대기 게이트: SF-3(섹션 카피)=Desktop 스펙 대기 · JOURNAL-2=운영자 범위 Q. tsc 무관(docs only).
> [아카이브] 2026-06-30 이하 이전 기록은 archive/PROGRESS_archived-2026-07-08.md 참조 (#31 분할·동결·grep 검색용). 상세 세션 기록은 SESSION_LOG.md, hand-off는 TASK_BRIDGE.md.
