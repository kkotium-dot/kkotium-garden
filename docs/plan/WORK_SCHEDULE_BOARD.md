# 작업 스케줄 보드 (WORK SCHEDULE BOARD)

> **이 파일의 역할**: "지금 뭘 해야 하고, 뭘 동시에 해도 되고, 뭐가 막혀 있는지"를 한 화면에 보여주는 **단일 우선순위 권위 문서**.
> **갱신 의무**: 작업 착수·완료 시마다 즉시 갱신(#319). 세션 시작 시 이 파일을 먼저 본다.
> **최종 갱신**: 2026-07-30 (Desktop)
>
> **왜 만들었나**: `TASK_BRIDGE.md` §3-A 보드가 2026-07-22 이후 방치돼 이미 끝난 작업을 "대기중"으로 표시하는 사고가 있었다(#318). 그 재발 방지 + 운영자가 "이거 해도 되나요?"를 다시 묻지 않아도 되게 하기 위함.

---

## §0 이 보드 읽는 법

| 표기 | 뜻 |
|---|---|
| 🟢 **READY** | 지금 바로 착수 가능. 선행 작업 없음. |
| 🔵 **PARALLEL-OK** | 다른 작업과 **동시 진행 안전**(write set 겹침 없음, #322). |
| 🟡 **BLOCKED** | 선행 작업이 끝나야 착수 가능. 무엇에 막혔는지 명시. |
| ⏸️ **WAITING-OPERATOR** | 코드 문제 아님. 운영자 결정/확인만 필요. |
| ✅ **DONE** | 완료 + 검증 완료. |

**write set** = 그 작업이 *수정하는* 파일 목록. 두 작업의 write set이 겹치지 않으면 병렬 안전(wikidocs 098 원칙).

---

## §1 지금 당장 (이번 주)

| 순위 | 작업 | 상태 | write set | 담당 레인 | 비고 |
|---|---|---|---|---|---|
| 1 | **브랜치 merge**: `feature/preview-copy-then-redesign` (11커밋) | ⏸️ WAITING-OPERATOR | — | 운영자 | 저녁 검토 시간에 진행(#320). 배포검증·tsc·build 전부 통과 완료 |
| 2 | **P1 소싱추천 크론 연결 + dry-run** | 🟢 READY | `vercel.json`<br>`api/cron/daily/route.ts`<br>`api/sourcing-recommend/route.ts` | Code | PRD §11. 효과 최대·위험 최소. **1번과 무관하게 착수 가능** |
| 3 | **P2 시즌 캘린더 확장**(6개→20개+) | 🔵 PARALLEL-OK | `src/lib/season/season-calendar.ts`(신규)<br>`src/lib/discord.ts`(getSeasonContext) | Desktop(리서치)→Code(구현) | **2번과 write set 안 겹침 → 동시 진행 안전**. Desktop이 MCP로 시즌 데이터 리서치 선행 |

---

## §2 그다음 (P1·P2 완료 후)

| 순위 | 작업 | 상태 | 막힌 이유 | write set |
|---|---|---|---|---|
| 4 | **P3 검수관 신설**(필터+배제사유) | 🟡 BLOCKED | P1 완료 필요 — 필터할 후보가 먼저 생겨야 함 | `src/lib/sourcing/inspector.ts`(신규) |
| 5 | **P4 앱「꼬띠 브리핑」화면** | 🟡 BLOCKED | P3 완료 필요 — 배제사유까지 보여줘야 함 | `src/app/(dashboard)/...`(신규) |
| 6 | **P5 피드백 루프**(채택 이력 학습) | 🟡 BLOCKED | P4 완료 필요 — 채택 버튼이 있어야 데이터가 쌓임 | `prisma/schema.prisma`<br>`src/lib/sourcing/feedback.ts` |

---

## §3 대기 중 (우선순위 낮음, 급하지 않음)

| 작업 | 상태 | 비고 |
|---|---|---|
| `TASK_BRIDGE.md` §3-A 낡은 보드 정리 | 🟢 READY | 이 스케줄 보드가 그 역할을 대체 → §3-A에 "이 파일 참조" 포인터만 남기고 정리 권장 |
| Claude Code 서브에이전트 도입(`docs-guardian`·`principle-auditor`) | 🟢 READY | PRD §12. **P1~P5 완료 후 검토** — 지금 하면 본 기능이 늦어짐 |
| 「대표로 적용」 등 쓰기 동작 실사용 검증 | ⏸️ WAITING-OPERATOR | 실제 DB 변경이라 운영자 승인 필요 |

---

## §4 운영자 확인 대기 (답변 주시면 바로 반영)

PRD §10의 열린 질문 — **이거 4개만 정해주시면 P1 구현이 정확해집니다**:

| # | 질문 | 제안값 | 답변 |
|---|---|---|---|
| Q1 | 하루 추천 개수 | 3~5건(많으면 피로) | _대기_ |
| Q2 | 최소 순마진율 컷오프 | 25~30% | _대기_ |
| Q3 | 발송 시각 | 현행 08:00 유지 | _대기_ |
| Q4 | 제외할 카테고리 | 식품·의약외품 등 인증부담 | _대기_ |

**답변 없이도 P1 착수는 가능**하다(제안값을 기본으로 구현하고 나중에 설정으로 조정). 다만 확정되면 재작업이 줄어든다.

---

## §5 절대 어기면 안 되는 것 (매 작업 확인)

- 네이버 스토어 PUT/POST → 운영자 명시 "GO" 없이 **금지**
- **디스코드 실발송 → 운영자 승인 없이 금지.** P1 구현 시 반드시 dry-run 먼저
- 자동 발행 → **영구 금지**. 발행은 항상 운영자 검수 후(#307)
- 테스트 데이터 방치 → 주입했으면 같은 세션에 원복
- 허위 완료 보고 → 실측 못 한 건 "미검증" 명시(#310)
- 착수 전 실측 우선 → 문서의 "미완료" 표기를 그대로 믿지 말 것(#318/#319)

---

## §6 레인 배분 원칙 (Desktop ↔ Code ↔ Cowork)

wikidocs 098 「Agent Teams와 병렬 작업」 원칙 적용:

| 레인 | 담당 | 이유 |
|---|---|---|
| **Desktop** (설계·검증) | PRD 작성, MCP 리서치(시즌 데이터), 브라우저 실측, 배포 검증, 문서 갱신 | 코드 파일 생성/편집·git commit 불가 |
| **Code** (구현·배포) | 코드 작성, 커밋, 빌드, 테스트, 대용량 한글 MD 처리 | 실제 파일 쓰기 권한 |
| **Cowork** | 여러 파일에 걸친 광범위 리서치·문서 정리 | 파일 3개 이하 작업은 Code 단독이 효율적 |

**병렬 진행 시 필수 규칙**(wikidocs 097 「동시 작업 주의」):
> 다른 레인이 동시에 작업 중일 수 있다. **담당 write set 밖의 파일은 되돌리거나 정리하지 말 것.** 범위 밖 변경을 발견하면 수정하지 말고 요약만 남긴다.

---

## §7 갱신 이력

| 날짜 | 변경 |
|---|---|
| 2026-07-30 | 최초 작성(Desktop). #318 문서정합성 사고 재발방지 + 꼬띠 에이전트 PRD 반영 |
