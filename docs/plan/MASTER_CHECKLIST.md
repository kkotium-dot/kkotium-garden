# 원본메모+v.01+v.02+신규지적 마스터 체크리스트

원칙#376(완료표는 원본전체 재대조 필수) 실천 문서. 원본메모(2026-09-12),
지침서 v.01, v.02, 대표님 추가 지적사항 전체를 하나의 표로 관리한다.
**"완료" 표시는 반드시 검증방법(curl/DB조회/브라우저 클릭)을 명시해야
유효하다** — 검증방법이 비어있으면 미완료로 간주한다.

마지막 갱신: 2026-09-22 (rev205 완료 — #57 naver_certification 입력UI 완전근본수정: 법령(전기용품및생활용품안전관리법)+실데이터(가습기 6/36개 상품) 교차검증으로 필요성 확정, 씨앗심기 D3.5 섹션에 입력필드 신설+hydrate+저장연결, 브라우저 실측 완료. 총 60항목)

## 범례
- ✅완료 = 코드수정+배포+아래 명시된 방법으로 실측검증까지 끝남
- 🔶부분 = 일부만 구현되었거나 유사기능은 있으나 요구 UX와 차이 있음
- ❌미착수 = 전혀 손대지 않음
- ❓미확인 = 구현 여부 자체를 아직 조사 안 함

| # | 항목 | 출처 | 상태 | 검증방법 | 커밋/rev |
|---|---|---|---|---|---|
| 1 | 카테고리 오분류(개껌 등)·2단계추천 | 원본/v.01/v.02 | ✅완료(범위내) | 개껌오분류 rev180 완전해결(curl 3회). 액세서리류 오분류 rev181+183 최종근본수정 완료 — "헤어핀"·"목걸이"·"방향제" 3건 무관카테고리 오분류 완전해소 확인(curl 5종 재검증). 근본원인: headNoun 선정단계가 STOP_NOUNS(leaf매칭 사전)를 안 써서 "액세서리" 같은 후행동격어가 헤드노운으로 오선정 → HEAD_NOUN_EXCLUDE가 STOP_NOUNS_SET 흡수하도록 통합. 정직기록: 재검증 중 별건 2건 신규발견(#44/#45로 별도 등재, 이번 수정 범위 밖) | rev163-166, rev180-181, rev183(최종) |
| 2 | 카테고리 Groq+Gemini 하이브리드 | 원본/v.01/v.02 | ✅완료 | curl 실측(agreement 로직) | rev164 |
| 3 | 크롤→씨앗심기 데이터유실(prefill) | 원본/v.01/v.02 | ✅완료 | Code 병합+tsc+build | rev166-167 |
| 4 | 추가썸네일 콤마→줄바꿈 구분자 | 원본/v.01/v.02 | ✅완료 | curl+openpyxl 셀값 확인 | rev168 |
| 5 | 배송비유형 명칭(조건부 무료 등) | 원본/v.01/v.02 | ✅완료 | 실제 네이버파일 대조+curl | rev163 |
| 6 | 태그→판매자특이사항 오바인딩 | 원본/v.01 | ✅완료(정정) | 재조사 결과 현재 미재현 확인(정직보고) | rev168 |
| 7 | 제주/도서산간 배송비 | 원본/v.01/v.02 | ✅완료(정정) | 네이버 규격상 엑셀컬럼 자체 없음 확인, UI는 이미 존재 | rev172 |
| 8 | 조합형 옵션 엑셀 미반영 | 원본/v.01/v.02 | ✅완료 | curl+openpyxl 옵션컬럼 확인 | rev168 |
| 9 | 단독형 옵션 엑셀 미반영 | 신규(9/15) | ✅완료 | curl PUT→DB조회→엑셀다운 3단 실측 | rev175 |
| 10 | 품절안전망 앱상품 검색 먹통 | 원본/v.01/v.02 | ✅완료 | curl+브라우저(상태배지) 실측 | rev171 |
| 11 | 대체상품명 로직 분리 요청 | 원본/v.01/v.02 | ✅완료(설계확인) | 코드조사로 이미 분리설계 확인, 힌트텍스트 추가 | rev171 |
| 12 | 공급사 한글→영문코드 변환 | 원본/v.01/v.02 | ✅완료 | 브라우저 실측(오너클랜→ONKR) | rev170 |
| 13 | Gemini OCR 범위(썸네일+상세) | v.01/v.02 | ✅완료 | curl 다중이미지 3장 동시처리 확인 | rev174 |
| 14 | OCR 첫 이미지만 인식 | 신규(9/16) | ✅완료 | curl 3장+1장 결과 상이함 확인 | rev174 |
| 15 | SEO 카테고리 필드 바인딩 안됨 | v.01/v.02 | ✅완료 | #1과 동일 사안, API 실측 | rev163-166 |
| 16 | 오너클랜 GraphQL 크롤확장 | v.01/v.02 | ✅완료 | 원본 표현 "크롤확장"은 오해소지 — 실제 설계는 크롤링(신규상품 발견) 아니라 재고조회 API(ownerclan-adapter.ts, 읽기전용, 발주는 Sprint8 미구현). UI통합 실측: /settings/supplier-login "공급사 열쇠방"에 "오너클랜(OWC·GraphQL API) 연동됨" 카드 확인. DB실측: 오너클랜 상품 2건 재고(9999개·29997개) 당일 00:51 갱신 확인 — 자동크론 정상 작동 중 | rev158-159(구현) / 이번세션(재확인) |
| 17 | 꽃한아름담기 성공모달+수량정확도 | 원본/v.01/v.02 | ✅완료 | OPERATOR_FEEDBACK_TRIAGE.md B4(2026-09-09)에 이미 완료 기록됐으나 MASTER_CHECKLIST 미반영 상태였음. 코드 확인: crawl/page.tsx 담기성공모달("꿀통 꽃수레에 담았어요! 계속 담을까요, 이동할까요?")+"꽃수레로 이동"/"계속 담기" 버튼. bulk 경로 "N개 꿀통 꽃수레에 담겼습니다"의 N은 API(prisma.$executeRaw 실제 영향행수)가 반환한 정확한 개수(추정치 아님). 브라우저 실측: 실제 도매매 URL(https://domeme.domeggook.com/s/55884601) 크롤→"꽃수레에 담기" 클릭→모달 정확히 렌더 확인, "꽃수레로 이동" 클릭→목록에 정확히 반영 확인. 테스트 데이터 즉시 원복(삭제, 10개→9개 확인) | 원본구현(2026-09-09, B4)/이번세션(재확인) |
| 18 | 꽃수레 드래그정렬 | 원본/v.01/v.02 | ❌미착수 | - | - |
| 19 | 꽃수레 동종상품 그룹화 | 원본/v.01/v.02 | ❌미착수 | - | - |
| 20 | 품절대체상품 그룹 시각화(꽃수레/정원창고) | 원본/v.01/v.02 | ❌미착수 | - | - |
| 21 | Studio(꽃단장 작업실) 3-Domain 공간 구조(좌20% 도구함-중55% 메인도마-우25% SEO관제탑) | 제미나이_꽃틔움_시스템_고도화_2026-09-17.md 섹션5 | ✅완료 | rev162에서 이미 근본구현 완료된 상태였음(좌384px·중앙1709px·우420px, AtelierShell.tsx). 이번 세션 코드+브라우저 재확인 — 회귀 없음, 정상 유지 확인. **판정 정정(2026-09-22)**: 이전 기록이 항목명을 "3단 스플릿뷰 슬라이딩 씨앗심기"로 잘못 적어 섹션7(정원창고)의 별개 요구사항(#58)과 혼동을 일으켰음 — 원문 재대조로 이 항목은 정확히 섹션5(Studio 3-Domain 구조)에 해당함을 확정, 항목명 정정 | rev162(구현)/rev186(재확인)/이번세션(항목명 정정) |
| 22 | 꽃단장작업실 복귀버튼 상태보존 | 원본(이전세션)/v.02 | ✅완료 | "씨앗심기로 복귀" 클릭 시 /products/new?edit=<정확한productId>로 이동, 카테고리·태그·SEO점수(92점) 등 기존 데이터 전부 보존 확인(브라우저 실측) | rev186 |
| 23 | 상세캔버스 이미지 렌더링 | 원본(이전세션)/v.02 | ✅완료 | 이중 근본결함 확정·수정: ①layout.tsx/AtelierShell.tsx의 flex 높이상속 체인 minHeight:0·height:100% 누락으로 좌측 배양실 패널이 뷰포트 무시하고 2180px까지 확장(정상 1024px) — 2개 파일 수정. ②(진짜 최종원인) StepGroup이 컴포넌트 본문 내 인라인 함수라 매 리렌더마다 새 컴포넌트 타입으로 재인식되어 React가 계속 언마운트+재마운트 → CSS 진입애니메이션(opacity 0→1)이 매번 리셋되어 3초+ 대기해도 opacity:0 고정, DOM엔 정상 존재하지만 화면엔 안 보이던 상태(elementFromPoint로 실제 텍스트 확인해 DOM존재 자체는 확정한 뒤 발견). useCallback으로 함수참조 안정화해 해결. 클린배포 실측(스크롤없이 즉시 렌더링) 확인 | rev186 |
| 24 | 꽃틔움 자체 SEO튜닝지수+좀비부활소 | v.02 | ✅완료 | computeZombieVerdict(마진율·상품명SEO점수·카테고리트렌드·공급사신뢰도·이미지수·품절대체설정·공급처단절 7신호 종합) 이미 구현됨(src/lib/products/tuning-signals.ts). 브라우저 실측: /products/reactivation에서 긴급/방치됨/성장이탈/재고갈등/소싱이탈 5분류, 진행률 게이지·배지 정상 렌더링 확인 | rev186 |
| 25 | 상세페이지 이미지 업로드 실패 | 신규(9/15) | ✅완료 | 브라우저 fetch 100x100px 실측(detail통과/main거부) | rev169 |
| 26 | 에러메시지 외계어 | 신규(9/15) | ✅완료 | #25와 동일 수정, 코드 확인 | rev169 |
| 27 | 부가세 컬럼(과세상품 등) | 신규(9/15) | ✅완료 | curl+openpyxl+DB일괄정정 | rev172 |
| 28 | A/S전화번호 형식 위반 | 신규(9/16) | ✅완료 | curl+openpyxl+DB일괄정정(27건) | rev172 |
| 29 | A/S템플릿코드 위치 문제 | 신규(9/16) | ✅완료(정정) | 위치는 정확, 변환함수 누락이 진짜원인, curl 확인 | rev172 |
| 30 | 반품배송비 편도처리(6000고정) | 신규(9/16) | ✅완료 | curl+openpyxl, 4개파일 수정 | rev172 |
| 31 | 롱테일키워드 고정출력 | 신규(9/16) | ✅완료 | curl d1파라미터 전후 비교 | rev173 |
| 32 | 태그 콤마입력+실시간자동완성 | 원본(최초) | ✅완료 | 브라우저 실측(3태그분할+신호등) | rev176 |
| 33 | 상품명 다듬기 원클릭 적용/복사 | v.01/v.02 | ✅완료 | 근본원인: '적용'(onApplyFix, ProductNameDiagnostics.tsx)은 이미 완벽 구현됐으나 '복사'가 이 컴포넌트에만 없었음(태그·SEO훅문구엔 이미 navigator.clipboard.writeText+toast 패턴 존재 — UX 비일관성). 기존 패턴 그대로 복사 버튼 추가. 브라우저 실측(2단계): ①결함있는 상품명 입력→"AI 다듬은 이름 후보" 제시→"이 이름으로 다듬기" 클릭→입력창 값 정확히 교체 확인 ②"복사" 클릭→토스트"복사했어요" 렌더 확인(setTimeout+title트릭으로 async결과 캡처)+navigator.clipboard.readText()로 실제 클립보드 내용이 다듬은 이름과 정확히 일치함을 확인 | rev191 |
| 34 | 마진역산기+10원단위 단수정돈 | v.02 | ✅완료 | 브라우저 실측(28000원→31120원) | rev177(정정, 원래완료) |
| 35 | 엑셀 사전검수 신호등 | v.01/v.02 | ✅완료 | curl 2시나리오(정상/결함상품) | rev178 |
| 36 | OCR 스캔 진단 신호등(🟢🟡🔴) | v.02 | ✅완료 | curl 10장 재현(7.5초 소요 확인)+브라우저 실측("🟢 스캔 완료 — 3개 항목을 찾았어요" 렌더 확인) | rev179 |
| 37 | 태그 SEO점수 신호등(🟢🟡🔴) | v.01/v.02(제미나이제안) | ✅완료 | rev176에서 이미 근본구현된 상태였음(#32와 동일 인프라, /api/tags/verify+tag-dictionary.ts: verified≥30회/weak 10~29회/missing<10회 3단계). 이번 세션 브라우저 재실측: "강아지용품"→🟢 월6,790회 SEO유효, 무작위문자열 2종→🟡 월10회 검색량낮음(네이버 검색광고 API가 10미만을 전부 10으로 반환하는 파서특성상 🔴 재현은 실질적으로 API가 키워드를 아예 못찾을 때만) 정확 확인. "검증 자취" 패널 클릭 시 SEO유효/약함/미등재 건수 집계+개별 신호등 상세 노출까지 확인 | rev176(구현)/이번세션(재확인) |
| 38 | 데일리 2회 재고크론 | v.02 §5 | ✅완료 | Vercel Hobby 플랜 "동일크론 하루1회" 제약 확인(cron/order-sync 기존주석) → inventory-sync(자정)+stock-alert-pm(21시KST) 별도크론 2개로 우회, stock-alert-pm이 pollAppRegisteredInventory() 실제 재조회 호출 확인(코드 56줄) — "재고 데일리 2회"의 핵심(오후 신규품절을 당일 포착)이 실제로 성립. 실행시각 근거: Gemini제안 "오전9시/오후2시"는 근거없는 추정치로 판단해 기각(#357), cron/daily와 반나절 이상 시차만 확보 | 코드주석 GAP_FIX_2026-09-09(구현)/이번세션(재확인) |
| 39 | 다중 품절안전망(1→N개 대체) | v.02 §5 | ✅완료 | SubstituteEditor.tsx에 다중 추가 로직(priority: prev.length+1) + "대체상품 추가" 버튼 확인. substitute-types.ts의 substitutes[] 배열구조(v1 단일객체→v2 배열, 하위호환 변환헬퍼 포함) 확인. cron/stock-alert-check.ts가 substitutes[]를 최대 2개까지 디스코드 알림 alternatives로 실제 연결(2차방어선 — 대체상품 자체 품절여부까지 확인) 확인 | rev161(구현)/이번세션(재확인) |
| 40 | 옵션별 대체발주 알림 | v.02 §5 | ❌미착수 | 원본명세(B11_B13_B15_HANDOFF_SPEC) 재확인 결과 이건 품절알림(stock-alert-check.ts)이 아니라 **주문 수신 시점**의 별개 알림("화이트 옵션 주문 들어왔으니 이 대체상품으로 처리하세요") — 트리거 지점은 /api/cron/order-sync. grep 실측: order-sync에 옵션값(optionValue/optionName) 처리 로직 자체가 0건, optionMatches 데이터(저장 UI·읽기 함수)는 있으나 소비처가 전혀 없음. 명세 자체가 "실제 발송 전 dry-run 필수·비가역 발송 위험" 강한 경고 포함 — 즉흥 구현 대신 별도 작업으로 분리, 착수 전 order-sync 흐름 실측부터 필요 | - |
| 41 | Grok 실시간 트렌드 키워드 전담 | v.02 §3 | ✅완료 | grep 확정: Grok(xAI)은 naver-seo/ai-generate/route.ts 주석에 "DEPRECATED chains — xAI Grok(cost-capped, not in primary stack)"으로 명시적 폐기 확인(현재 Groq+Claude). 다만 원본 요구사항의 본질("실시간 데이터 기반 트렌드 키워드")은 미충족 발견 — "황금키워드"(NaverSEOWorkflow)가 LLM 추정 생성값을 네이버 실검색량 검증 없이 그대로 노출(원칙#357 위반 소지). 근본수정(rev192): aiKeywords를 기존 /api/tags/verify(#37 검증된 3단계 신호등)로 자동 교차검증하도록 useEffect 추가, 화면에 🟢🟡🔴+실검색량 노출. 브라우저 실측(실제 워크플로우 End-to-End): "여성 여름 냉감 매트 긴팔 티셔츠" → 7개 키워드 생성→적용→5🟢2🟡 확인. **추가근본수정(rev195)**: Naver_Smart_Store_Product_Identity_Keyword_Extraction 리서치(SOURCE_DOCS_INDEX) 대조 결과, 리서치는 "검색량 0/극저 후보는 표시 전에 폐기"를 권고하는데 rev192는 "표시는 하되 신호등만"이라 갭 확인 — 완전 hide는 API 일시오류로 멀쩡한 키워드가 사라질 위험(#37에서 확인된 파서특성) 때문에 채택 안 하고, 대신 missing 상태 키워드를 흐리게(opacity 0.55)+"(검색량 없음)" 명시 텍스트로 안전하게 절충 반영. **최종 브라우저 재검증(rev196)**: 실제 워크플로우 2차 실행("여성 여름 냉감 매트 긴팔 티셔츠") — 황금키워드 8개 생성, 7🟢1🟡(opacity:1 정상) DOM 실측 확인. missing 상태는 fetch('/api/tags/verify') 직접호출 2회(무작위 한글조합, 영숫자혼합)로도 전부 weak(월10회)만 나와 실전 재현 한계 확인(네이버 검색광고 API가 사실상 모든 문자열에 최소 10회를 부여하는 파서 특성, #37에서 이미 확인된 사실과 일치) — isMissing 조건부 렌더링 3개 라인(조건식/opacity 스타일/텍스트 접미사) 코드리뷰로 정확성 확인, tsc 0에러로 타입 정확성 보장. missing 자연재현은 실전 데이터 축적 시 재확인 예정, 정직하게 한계로 기록 | rev192, rev195, rev196(재검증) |
| 42 | 보안: .save 백업파일 유출 재발차단 | 작업중 발견 | ✅완료 | git rm+.gitignore 패턴추가 확인 | rev169 |
| 43 | 카테고리 "액세서리"류 오분류 — 헤어핀 계열 | 신규(9/17), 원칙#379/380/381/382 | ✅완료 | #1과 동일 사안, rev183에서 최종근본수정+curl 5종 재검증 완료 | rev183 |
| 44 | 카테고리 "헤어핀" 동음이의 — 유아용(출산/육아)이 성인용(패션잡화) 대신 1위 | 신규(9/17), rev183 재검증중 발견 | ✅완료 | 근본원인: homonymUnconfirmed 안전장치가 "자식0개 d3-as-리프" 경쟁후보를 rival로 인정 못 함(원래 "자식있는 진짜branch"만 요구) → rival 조건 확장. curl 3회 클린배포판 재검증(CLEANVERIFY1~3) — 안정적으로 성인용 1위, needsConfirmation:true(정직한 동음이의 표시), Vercel로그로 Gemini valid=true 채택 확인. 사전 리스크점검(원칙#379): 118건 잠재충돌 중 7개 대표사례(바지/티셔츠/카디건/점퍼/우산) 회귀 없음 확인 | rev184 |
| 45 | 카테고리 "휴대폰 스트랩 액세서리" 오분류 — 카메라/캠코더용품으로 오분류 | 신규(9/17), rev183 재검증중 발견 | ✅완료 | 근본원인: headNounWeight의 strongHeadMatch가 슬래시합성어 부분매칭(partial)을 무시하고 무조건 고신뢰 부여하던 비대칭(weakHeadMatch는 이미 partial 체크함). 마스터에 이런 이질적 슬래시그룹 70건 확인(단건 아닌 클래스). 2단계 수정: ①partial discount 추가 ②즉시 재검증으로 "아로마 디퓨저" 회귀 발견 → modifier가 매칭 안 된 형제파트를 뒷받침하면(우산 사례 d2-corroboration과 대칭) discount 면제하는 modifierCorroborates 추가. 클린배포 curl 8종 재검증(CLEANFINAL 포함) — 무관 카테고리로 새는 사례 0건 | rev185 |
| 46 | Gemini OCR 업그레이드 — 상세페이지 통이미지 멀티모달 스펙 추출(소재/텍스처/실측/KS규격) | 신규(9/17), 제미나이 제안+대표님 원본메모 | ✅완료 | 코드실측(2026-09-22) 정확한 현황: ①"대표썸네일뿐 아니라 상세페이지 통이미지" 요구는 이미 구현됨 — src/app/products/new/page.tsx의 ocrSourceImages가 detailImagesArr(상세이미지 최대4장)를 우선사용, "원산지 표기가 각각 다른 이미지에 나뉘어 있는 경우가 흔함" 주석까지 리서치 문제의식과 정확히 일치. /api/ai/image-ocr→extractAttributesFromImage(gemini-ocr.ts)가 다중이미지(MAX_OCR_IMAGES까지) 처리 확인. ②소재(material)/실측(size,capacity)/원산지(origin) 필드는 기존 구현됨. ③근본수정(rev198): ExtractedAttributes에 texture(질감/결)·certification(안전확인/KC/KS신고번호) 2개 필드 신규 추가 — SYSTEM_PROMPT/USER_PROMPT JSON스키마 확장, normalizeAttributes 파싱 추가, 씨앗심기 화면 OCR결과 패널에 표시 UI 연결(texture는 material/size/capacity와 동일 박스, certification은 저장UI 부재로 복사버튼만 우선 제공 — #57 참조), 신호등 filledCount 계산에도 반영. 네이버 썸네일 텍스트면적15~20% 검사(별도 요구)는 seo-guard-linter.ts가 흰배경만 체크하고 텍스트면적은 미구현 — 이건 OCR과 무관한 별개 과제(Sharp 픽셀분석 신규 필요)로 범위 밖 확정, 이번 근본수정은 정확히 "소재/텍스처/실측/KS규격 4종 추출"에 집중 | 기존구현(9/3~9/16)/rev198(texture+certification 추가) |
| 47 | 스마트에디터 ONE 반응형 HTML 상세페이지 자동생성(clamp폰트/카드UI/컬러테이블) | 신규(9/17), 제미나이 제안+대표님 원본메모, #59(대표님 9/22 세로형슬롯 지시) 통합 | 🔶부분 | 정정: "완전 신규 기능"이 아니었음 — 코드실측(2026-09-22)으로 6,677줄 규모의 정교한 자동화 파이프라인(src/lib/automation/, 섹션빌더+12종 레이아웃스켈레톤+스펙테이블렌더러 등)이 이미 존재함을 확인. 단, 이 대규모 인프라는 PNG 이미지 렌더링 전용(createCanvas/sharp-composite, CANONICAL_WIDTH 고정픽셀)이라 "반응형" 개념 자체가 불필요한 경로 — 진짜 요구사항(스마트에디터 ONE 복사-붙여넣기용 HTML)은 별도의 훨씬 단순한 detail-html-serializer.ts(123줄, PNG와 병렬 경로)가 담당하는데 여기에 clamp()/구분선이 빠져있었음이 정확한 갭. 근본수정(rev201): renderSection의 헤딩(h2)/본문(p) 폰트를 고정px에서 clamp(모바일최저~PC고정값 유지)로 교체, 섹션 padding도 clamp화, 섹션-섹션 사이(첫 블록 제외)에 sectionDivider() 1px 구분선 삽입(#59 세로형 슬롯 시각강조), 컨테이너 폴백폰트도 clamp화. 스마트에디터가 &lt;style&gt;태그를 통째로 제거하는 사례(Research_Report.md)를 고려해 인라인 style 속성 안에 직접 clamp() 적용. 검증(3건): ①node로 renderSection/serializeDetailHtml 로직 미러링해 clamp 존재+구분선 정확히 1개(2섹션 기준, 중복없음) 확인 ②동일 HTML을 브라우저 iframe(400px 모바일폭)에 실제 렌더링 — 스크린샷으로 헤딩 반응형 크기+배경색 전환+구분선 전부 정상 시각확인 ③tsc 0에러. 🔶부분인 이유: clamp()/구분선/모바일반응형은 해결했으나, "컬러/스펙 모바일 테이블"(제미나이 요구, PNG경로의 specTable.ts와 동등한 구조화 스펙표)은 HTML경로에 아직 없음(section-builder.ts의 SerializeSection.copy가 자유텍스트 key-value일 뿐 스펙 테이블 전용 구조가 없어 별도 확장 필요) — 다음 단계로 남김 | rev201 |
| 48 | 'Aesthetic Wit' 3단계 카피라이팅(Hook/Detail/Attitude) 채팅형 생성 | 신규(9/17), 제미나이 제안+대표님 원본메모 | ✅완료 | 코드실측(2026-09-22): Hook(공감형 도발)=problem.ts(generateProblemCopy, 이미 구현), Detail(핵심 팩트)=spec.ts(generateSpecRows, 이미 구현) 확인 — Attitude(시크한 클로징)만 정확히 빠져있던 갭. 근본수정(rev202): section-copy.ts에 generateAttitudeCopy() 신설(기존 함수와 동일패턴, filterDarkPatterns 적용, cta.ts와 톤 의도적 분리), strings.ko.json 폴백문구 추가, /api/ai/aesthetic-wit 신규 API로 3개 카피 함수 텍스트 미리보기 제공. **실측검증 중 전상품공통 결함 발견·근본수정(rev203)**: API가 매번 AI 대신 폴백 템플릿만 반환 — 원인추적으로 Groq가 llama-3.1-8b-instant를 카탈로그에서 완전제거(404, GET /openai/v1/models 실측확인)했는데 section-copy.ts를 포함한 5개 파일이 이미 UCE-2(2026-08-27)가 정정한 정본(src/lib/ai/groq.ts, openai/gpt-oss-120b+reasoning_effort:low)을 안 쓰고 각자 로컬로 죽은 모델명을 복제 중이었음(#295 단일권위 위반) — section-copy.ts/groq-client.ts/review-sentiment-analyzer.ts/shopping-search.ts/upload-readiness-filler.ts 5개 파일 일괄 정정. 최종 브라우저(curl) 실측: 실제 AI생성 카피 확인(Hook "여름에 땀에 눅눅해 불편하지 않나요?", Detail 폴리에스터85%·스판15% 등 구체스펙, Attitude "시원함을 담은 여름 티셔츠/지금 선택해 보세요") — 이전 폴백문구와 완전히 다른 결과로 정상작동 확정 | rev202, rev203(전상품공통 근본수정) |
| 49 | '앱 전용 스킬 프리셋' — 레퍼런스 스타일+카피셋 저장 후 다른 상품에 재적용 | 신규(9/17), 대표님 원본메모(옥반지 작업 예시) | ❌미착수 | 완전 신규 기능(DB 스키마+UI 신설 필요) — 대표님이 명시적으로 "MCP 스킬을 앱 전용으로 만드는 것"이라 비유. 별도 설계 논의 필요(우선순위/UX 확정 후 착수) | - |
| 50 | Canva Connect API 연동 — 템플릿 자동 채움(썸네일/배너 배치생성) | 신규(9/17), 제미나이 제안 | ❌미착수 | Canva API 키/계정 연동 여부부터 확인 필요. 대표님 원본메모는 "캔바 사용 불가시 자체 HTML 코드 제공"을 대안으로 명시 — 우선순위상 #47(자체 HTML)이 선행 권장 | - |
| 51 | 씨앗심기 "재고수량"이 실제 재고와 무관하게 표시·저장되던 문제(#16 조사 중 신규발견) | 신규(9/21) | ✅완료 | 대표님 지시(9/21) 정확히 구현: 옵션 있는 상품=옵션별 재고 합산, 옵션 없는 단품=크롤/공급사 폴링 재고(InventorySnapshot 최신 qty)가 진짜값. 신설 src/lib/products/effective-stock.ts(resolveEffectiveStock 단일권위 순수함수)를 엑셀생성 API(DB조회+즉석다운로드 양쪽)·상품조회 API(GET, 수정모드 hydrate용)·씨앗심기 저장payload 3곳에 연결. curl 실측: LED가습기(오너클랜,옵션없음) GET effectiveStock=9999(source=supplier_snapshot), 사무실가습기(옵션2개 999+999) GET effectiveStock=1998(source=option_sum). 엑셀다운로드 openpyxl 직접검증 — 재고수량 컬럼에 9999·1998 정확히 반영(이전엔 전부 999로 강제됐던 버그). 브라우저 스크린샷으로 씨앗심기 화면 "재고수량" 필드도 9999로 정상 hydrate 확인 | rev188 |
| 52 | 이스터에그 하단바(핑크 라인)가 화면 최하단에 고정 안 되고 스크롤 시 콘텐츠를 가림(대표님 스크린샷 신고) | 신규(9/22) | ✅완료 | 근본원인: footer가 &lt;main&gt;(overflowY:auto 스크롤 컨테이너) 안쪽에 있었고 marginTop:'auto'로 "콘텐츠 짧으면 바닥에 붙는다" 트릭을 썼는데, rev186(#23 Studio수정)에서 콘텐츠 div에 flex:1을 준 것과 충돌 — flex:1인 형제가 남는 공간을 전부 차지해 marginTop:auto가 실제로는 항상 0px로 계산됨(computed-style 실측 확인). 근본수정: footer를 &lt;main&gt; 스크롤 콘텐츠 밖으로 완전히 빼내 그 형제(Header/main 감싸는 flex-column wrapper)의 직계자식으로 이동 — 스크롤과 무관하게 항상 화면 최하단 고정. 브라우저 실측(2건): ①씨앗심기 SEO탭에서 scrollTop=0/scrollHeight 양쪽 다 footer.bottom이 정확히 viewportHeight와 일치 확인 ②Studio(#23 회귀위험 재검증) — aside clientHeight 1079px 정상 유지(2180px 폭주 재발 없음) + 이스터에그 footer도 화면 최하단 정확히 고정 확인(단, 페이지에 동명 &lt;footer&gt; 태그가 3개 있어 querySelector 1건만 잡으면 다른 컴포넌트(ThumbnailCard의 Phase2-B-3 안내)를 오인할 수 있음 — 검증 시 전체 footer 목록에서 텍스트로 정확히 식별 필요, 향후 유사검증 시 유의) | rev193 |
| 53 | 네이버 정보고시(productInfoProvidedNotice) — 발행실패(BAD_REQUEST) 방지 | SOURCE_DOCS_INDEX 카테고리2(9/22 발견, Research_Report.md) | ✅완료(기존 구현 재확인) | 2026-06-02 P0로 이미 완전 구현돼 있었음(코드 실측): src/lib/naver/product-builder.ts의 buildProductInfoProvidedNoticeEtc()가 ETC유형 8필드(returnCostReason/noRefundReason/qualityAssuranceStandard/compensationProcedure/troubleShootingContents/itemName/modelName/manufacturer)+customerServicePhoneNumber 전부 채움, "상품상세참조" 표준문구 패턴 적용, buildNaverProductPayload() 1039줄에서 호출→1087줄 detailAttribute에 정확히 포함되어 최종 발행 payload에 실림. 주석에 "RESEARCH §1: 템플릿코드 참조 미지원(공식), 매 상품 인라인이 유일"까지 명시 — Research_Report.md 원문 결론과 정확히 일치. SOURCE_DOCS_INDEX 카테고리1(반영됨)로 재분류 필요 | 커밋이력 2026-06-02(기존) |
| 54 | 도매꾹 Open/Private API 활용도 — 재고폴링/경쟁사추적 vs 자동발주 | SOURCE_DOCS_INDEX 카테고리2(9/22 발견, Domeggook_Open_API_and_Private_API_Integration_Strategy.md) | ✅완료(기존 설계 재확인) | 코드 실측: domemae-adapter.ts에 getItemView(단건+multiple=true배치)·getItemList(→searchItems, dome-competitor-tracker.ts에서 실제 사용 확인) 둘 다 이미 구현·연결됨. placeOrder()(자동발주)만 "requires Private API, throws until Sprint 8"로 명시적 보류 — Research 자료가 권고한 우선순위(재고폴링=Open API로 충분, 자동발주만 Private API 필요)와 정확히 일치하는 설계였음. "미반영"이 아니라 "의도된 단계적 구현" — Private API 신청 자체는 Sprint 8(자동발주 착수 시점)까지 불필요 | 기존 구현(도입시점 불명, 재확인 완료) |
| 55 | 카테고리 매처 — "신체부위+청소기/클리너" 조합이 디지털/가전으로 오분류(한국어_이커머스_소싱_카테고리_전환_동음이의어_필터링 리서치 대조 중 실측 발견) | SOURCE_DOCS_INDEX 카테고리2(한국어_이커머스_소싱의_카테고리_전환_동음이의어_필터링.md, "축A-신체부위" 권고) | 🔍조사완료·근본수정보류(데이터공백 판정) | curl 실측: "귀청소기 실리콘 이어클리너"→디지털/가전>PC액세서리>클리너(오분류), "애견 강아지 털청소기"/"차량용 무선 청소기"는 정확(리서치의 축B/축C는 이미 정상 작동). 근본원인 확정: 네이버 마스터 카테고리 자체에 "귀"+클리너류 leaf가 없음(생활/건강>건강관리용품에 코클리너·구강위생용품에 혀클리너는 존재하나 귀 대응 leaf 부재) — 코드 로직 결함이 아니라 카테고리 체계의 데이터 공백. headNoun 선정 로직에 "신체부위 감지" 규칙 자체가 없다는 것도 확인(리서치의 "축A" 개념이 매처에 없음). 즉흥 코드수정 보류 — 유사 사례가 더 쌓이면(체계적 패턴으로 확인되면) 신체부위 헤드노운 감지 규칙 신설 검토, 지금은 셀러 수동 카테고리 선택으로 대응 | - |
| 56 | SEO 훅문구(이벤트필드용)에 "구체적 수치 혜택만 통과, 추상 홍보문구는 거부" 검증 누락(상품명진단엔진+SEO훅문구 리서치 대조 중 발견) | SOURCE_DOCS_INDEX 카테고리2(꽃틔움_가든_네이버_상품명_진단_엔진과_SEO_훅문구_개선_설계_리서치_2026-06.md, 목표2) | ✅완료 | 근본원인: SEO훅문구 필드는 정확히 네이버 이벤트필드(혜택형) 용도("네이버 쇼핑 검색 결과 홍보문구")인데, 상품명(product-name-diagnosis.ts)과 달리 리서치가 강조한 검증이 전혀 없었음 — "끝까지 최선을 다하겠습니다" 같은 추상 문구가 경고 없이 저장돼 실제 이벤트필드 검수 반려 위험. 신설: src/lib/seo/hook-phrase-guard.ts(lintHookPhrase 순수함수) — 수치혜택(아라비아/한글숫자+단위) 존재여부+리서치 원문의 거부예시 시드 사전+장식특수문자 3종 검사. 배포전 로컬테스트하네스(9→12케이스)로 자체검증 중 한글숫자 정규식이 "당일배송"의 "일"을 숫자로 오인하는 false-positive 발견해 "숫자+단위 인접" 패턴으로 즉시 수정, 12/12 통과 후 배포. 브라우저 실측(2건): ①"품질보증 무료A/S" 입력→경고문구 정확히 렌더 확인 ②"3만원 이상 무료배송" 입력→경고 정확히 사라짐(오탐없음) 확인 | rev197 |
| 57 | naver_certification(안전확인/KC/KS 신고번호) 저장 UI가 앱 전체에 없음(#46 확장 중 발견) | #46 근본수정 작업 중 신규발견(2026-09-22) | ✅완료 | 실제 필요성 확정(법령+데이터 교차검증): 국가기술표준원(KATS) 공식자료로 "전기용품 및 생활용품 안전관리법" 안전확인대상 전기용품(가습기 등, 별표4 제1호)에 해당 신고번호 표시 의무 확인. curl로 이 스토어 실제 상품 36개 중 6개(17%)가 가습기류이고 카테고리 50002540("디지털/가전>계절가전>가습기>초음파식가습기")로 분류돼 있으며 naver_certification 전부 비어있음을 실증 — 즉흥 판단 아닌 실질적 발행리스크로 확정. 근본수정(rev205): naverCertification state 신설, 씨앗심기 D3.5 "상품정보제공고시" 섹션(배송정책 탭 내부)에 입력필드 추가("전기·생활용품만 해당, 해당없으면 비워두세요" 안내로 강제감 제거), GET hydrate(수정모드 로드)+저장payload(신규/수정 공통) 양쪽 연결(API는 이미 받을 준비 완료돼 있었음, rev198 grep 확인 그대로). #46의 OCR certification "복사"버튼도 저장필드 생겼으므로 "적용"(setNaverCertification 직접호출)으로 업그레이드. 브라우저 실측(실제 상품 edit모드): 배송정책 탭 전환 후 필드 렌더링 확인, "제2026-055678호" 입력→즉시 state 반영 확인→테스트값 원복(저장 안 눌러 DB무영향) | rev205 |
| 58 | 정원창고/꽃밭돌보기(좀비부활소) — 상품 클릭 시 전체페이지 이동이 아니라 우측 슬라이딩 씨앗심기 튜닝 캔버스로 열려야 함(#21 판정오류 정정 중 확정) | 제미나이_꽃틔움_시스템_고도화_2026-09-17.md 섹션7 정확한 원문("우측 슬라이딩 씨앗심기 튜닝 캔버스") | ✅완료 | 정정 경위: 이전 세션이 이 요구사항을 Studio 3-Domain 구조(#21)와 혼동해 "완료"로 오판정했던 걸 정정, 실제로는 미구현이었음(grep 0건, 전체페이지 이동만 존재). 근본수정(rev200): /naver-seo 페이지에 이미 검증된 SeoEditDrawer(position:fixed+translateX 슬라이딩 패널, 코드주석 "레거시 전체페이지 이동 대체용")가 있음을 발견 — 좀비부활소는 이 컴포넌트를 안 쓰고 있었을 뿐, 필요 데이터(naver_title 등)는 /api/products가 이미 select로 내려주고 있었음(프론트 타입 선언만 누락). Product 인터페이스 확장, SeoEditDrawer import, drawerProductId state 추가, "SEO 최적화" 버튼을 <a href="/naver-seo?ids="> 전체이동에서 <button onClick={setDrawerProductId}>로 교체, 파일 끝에 드로어 렌더링(items에서 product 매핑, /naver-seo와 동일 patternshape). 실측 검증(2건): ①/naver-seo에서 동일 컴포넌트 실제 클릭 — "씨앗 심기 | 실시간 SEO 편집" 우측 드로어가 페이지 이동 없이 정확히 슬라이드로 열림 스크린샷 확인 ②좀비부활소 페이지 자체는 정상 렌더링(콘솔에러 0, JS로 확인) + SeoEditDrawer가 if(!open) return null 조건부라 닫힌 상태에서 DOM에 없는 것도 정상 동작임을 코드로 확인. 정직한 한계: 현재 스토어에 score_drop(점수급락) 사유 상품이 0개라 이 페이지에서 직접 버튼 클릭 재현은 못함(DB aiScore 인위조작은 비가역위험 판단해 보류) — 동일 컴포넌트 실제작동+정확한 props shape 일치+tsc 0에러로 대체검증 | rev200 |
| 59 | 상세페이지 생성 시 세로형 슬롯 배치 구조(대표님 9/22 신규 지시 — #47과 결합) | 대표님 원본지시(2026-09-22, 9/17파일 아님 — project_knowledge에 없는 신규 요구사항) | ❌미착수 | 대표님 정확한 지시: 썸네일(1000x1000 정사각) 규격은 기존 그대로 유지하되, 상세페이지(#47 반응형HTML 생성기) 슬롯 배치는 모바일 세로 스크롤 환경에 맞춰 세로로 길게 이어지는 슬롯 구조로 설계. 모바일/PC 비율 차이도 함께 고려. #47(반응형 HTML 생성기) 설계 착수 시 정확히 통합 반영 — 별도 항목으로 분리해 누락 방지 | - |
| 60 | 경쟁사 인사이트(시장분석) — 네이버 쇼핑검색 Open API가 404 SE05(존재하지 않는 검색api) 반환(rev203 후속 브라우저검증 중 발견) | rev203 후속검증(2026-09-22, review-sentiment/shopping-search/upload-readiness 3종 실사용 검증 중) | ❌미착수(코드 밖 원인) | curl 실측: GET /api/naver/market-analysis?q=... → "Naver Shopping Search failed: 404 — Invalid search api(SE05)". src/lib/naver/shopping-search.ts의 searchShopping()이 https://openapi.naver.com/v1/search/shop.json을 호출하는데, 이건 Groq 결함(rev203)과 무관 — Groq AI인사이트(generateMarketInsight) 앞단인 네이버 쇼핑검색 자체가 막혀 AI 로직 도달조차 못 함. 근본원인 추정: NAVER_DATALAB_CLIENT_ID/SECRET(Vercel실제값 확인)는 네이버 데이터랩 API 전용으로 발급된 자격증명이고, 쇼핑검색 Open API는 네이버 개발자센터에서 별도로 앱에 등록해야 하는 다른 권한 — SE05 "존재하지 않는 검색 api" 메시지가 정확히 이 미등록 상태를 가리킴. 코드 결함이 아니라 네이버 개발자센터 설정 확인이 필요한 외부요인(egress차단으로 로컬 직접검증은 못 함, Vercel배포서버 실측 404로 확정) — 대표님께 네이버 개발자센터에서 해당 앱에 "검색" API가 등록돼 있는지 직접 확인 요청 필요 | - |

## 다음 작업 우선순위 제안(의존성 없음, 순서 무관 — 단 #46~50은 규모가 커서 별도 논의 권장)
1. #47+#59 통합설계 — 반응형 HTML 상세페이지 생성기(#47) + 세로형 슬롯 배치(#59, 대표님 9/22 지시로 #47과 결합 요청) — 신규 기능+UI라 규모 있음
2. #40 옵션별 대체발주 알림 — order-sync 옵션값 흐름 실측부터 필요, 규모 있는 별도작업
3. #18-20 꽃수레 드래그정렬/그룹화/시각화 — 신규 UI, 규모 있음
4. #49 스킬 프리셋 저장소 — 신규 DB스키마+UI, 규모 큼, 별도 스프린트 권장
5. #50 Canva API 연동 — 계정/키 확인부터, #47 이후 착수 권장
6. #60 경쟁사 인사이트 — 대표님 네이버 개발자센터 "검색" API 등록여부 확인 대기 중(코드 작업 불가)

## 문서 갱신 규칙(재발 방지)
- 매 응답마다 이 파일을 실제로 열람(memory 아님, 파일 자체)해 표를 갱신한다.
- "완료"로 바꿀 때 검증방법 칸을 비워두지 않는다 — 비어있으면 그 표시는 무효.
- 새로운 지적사항이 들어오면 반드시 이 표에 새 행을 추가한다(별도 트래커에만
  적고 여기 누락하는 것을 금지).
