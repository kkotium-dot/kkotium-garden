# PRINCIPLES_LEARNED — 작업원칙 (현행)

> **분할 이력(#31)**: 이 파일이 임계(1500줄)에 근접할 때마다 앞부분을 archive로 이동합니다. **삭제된 내용은 없습니다.**
> - 2026-07-22 분할: `archive/PRINCIPLES_LEARNED_archived-2026-07-22.md` (#1~#45 구간 대응 — PRINCIPLES_CODE.md 이관분과 별개)
> - 2026-07-30 분할: `archive/PRINCIPLES_LEARNED_archived-2026-07-30.md` (**#46~#227 구간**, 131건)
>
> **이 파일(현재)**: #254부터 시작. 원칙을 찾을 때는 archive 두 파일과 함께 grep 하세요:
> `grep -rn "작업원칙 #NNN" docs/plan/PRINCIPLES_LEARNED.md docs/plan/archive/PRINCIPLES_LEARNED_archived-2026-07-22.md docs/plan/archive/PRINCIPLES_LEARNED_archived-2026-07-30.md`
>
> **신규 원칙은 이 파일 맨 아래에 추가합니다.**

---

## 작업원칙 #254 — 상품 IA=라이프사이클 단계 구성·역할버킷·포커스모드 (2026-07-12/13, PRODUCT_IA_REDESIGN_V2 확정)

**규칙**: 상품 관리 메뉴 구조는 상품의 라이프사이클 단계(소싱→작성중→발행연동→사후관리)를 따라 구성하고, 각 메뉴는 "역할 버킷"(그 단계에서 필요한 기능 전부를 흡수)과 "포커스 모드"(다른 단계 관심사는 노출하지 않음)를 가진다. 나눔 기준 = 발행 여부. 확정 명명(SoT): 꿀통 창고(소싱+창고 통합·크롤링탭+작성중탭) / 온실아틀리에(씨앗심기+꽃단장 작업실) / 꽃밭 돌보기(연동+앱등록 통합·SEO진단+품절대체 흡수). 권위: `docs/design/PRODUCT_IA_REDESIGN_V2_CONFIRMED_2026-07-12.md`.

## 작업원칙 #255 — 네이버 미제공 지표는 자체지수로 우회하되 정직 caveat 필수 (2026-07-12/13)

**규칙**: 네이버가 API로 제공하지 않는 운영 지표(예: 재고 회전율, 판매 추세 세부)는 자체 산출 지수(튜닝 필요도 지수·좀비 감지 점수 등)로 우회할 수 있으나, UI에 반드시 "이 지수는 근사치이며 실제 네이버 데이터와 다를 수 있다"는 정직 caveat를 노출한다. 추정치를 확정 사실처럼 표시 금지(#46 거짓 라벨 금지의 연장).

## 작업원칙 #256 — 메뉴 명명은 운영자 확정 은유가 SoT, 라우트는 보존 (2026-07-12/13)

**규칙**: 사이드바 메뉴명(꿀통 창고/온실아틀리에/꽃밭 돌보기 등)은 운영자가 확정한 은유적 명명이 유일한 SoT이며, 절대 재질문 없이 그대로 사용한다. 단 내부 라우트 경로(`/crawl`, `/products/new`, `/products/link` 등)는 이름 변경과 무관하게 보존한다(딥링크·북마크·코드 참조 안정성). 명명 변경 시 표시 라벨만 바꾸고 라우트는 유지.

## 작업원칙 #257 — "작동하지만 조용함" vs "고장"을 구분하고 가시화로 신뢰 확보 (2026-07-12/13)

**규칙**: 기능이 정상 작동하지만 결과가 사용자에게 보이지 않는 상태("조용히 작동")와, 실제로 고장난 상태("고장")를 UI에서 명확히 구분한다. 조용히 작동하는 기능은 결과를 가시화하는 UI(배지·인디케이터·로그)를 추가해 운영자가 "이게 되고 있나?"를 재차 묻지 않게 한다. #54(적용현황 상시 명시)의 연장 — 결과가 안 보이면 운영자 입장에서는 고장과 구분 불가능하다.

## 작업원칙 #258 — 알림 개선은 실물 발송 확인이 필수(코드 존재 ≠ 실제 노출) (2026-07-12/13)

**규칙**: 디스코드/카카오 등 알림 콘텐츠 개선(문구·페르소나·데이터 정확도)은 코드 배포만으로 "완료" 처리하지 않는다. 실제로 1회 이상 발송해 운영자가 눈으로 확인해야 완료(#88 완료=검증의 알림 도메인 적용). 코드가 정상이어도 실제 발송 트리거(cron 조건·환경변수·시크릿)가 막혀 있으면 무용지물이므로, 실발송 검증을 발행 게이트처럼 취급한다.

## 작업원칙 #259 — 꼬띠 페르소나 SoT는 KKOTTI_PERSONA_VOICE_GUIDE.md, 디스코드+앱 전체 적용 (2026-07-12/13)

**규칙**: 꼬띠(마스코트)의 목소리/톤/모드 분기(정원사🌷 vs 카우걸🤠)는 `docs/design/KKOTTI_PERSONA_VOICE_GUIDE.md`가 유일한 권위 문서다. 디스코드 알림뿐 아니라 앱 UI 전체(빈 상태·토스트·에러 문구)에 일관 적용한다. 신규 사용자 대면 문구 작성 시 이 가이드를 먼저 참조하고, 가이드에 없는 새 상황은 가이드 자체를 갱신해 드리프트를 막는다.

---

## 작업원칙 #260 — 재고 스냅샷 qty=-1/status=unknown은 UI에 "재고 N개"로 노출 금지(폴링 실패와 진짜 0 재고 혼동 방지) (2026-07-14 Desktop 발견)

**배경**: `/products`(꽃밭돌보기) 상품 카드가 명화 상품에 "재고 -1개"를 표시. 근본 원인 추적 결과 `inventory_snapshots.qty=-1`은 도매매(domeggook) `getItemView` API가 해당 productNo를 찾지 못했을 때(`dcode:ITEM_ERROR, dmessage:"해당 정보가 존재하지 않습니다"`) 어댑터가 반환하는 **명시적 "폴링 실패/알 수 없음" 센티널 값**이지 실재고가 아니다(`src/lib/sources/domemae-adapter.ts` getInventory 함수 참조). 폴링 대상 3개 상품(명화·달항아리·1건 추가) 전부가 2주 이상(2026-06-28~07-13 확인) 연속으로 이 상태다 — `source='NATIVE'`(직접 등록) 상품에 채워진 `supplier_product_code`가 실제 도매매 상품번호와 매핑되지 않는 것으로 보임(소싱 당시 값 오기입 또는 매핑 정책 미비 추정, 미확정).

**규칙**: (1) `qty=-1` 또는 `status='unknown'`인 스냅샷은 UI에 **절대 숫자 그대로 노출 금지** — "재고 확인 실패" 또는 "폴링 대기" 같은 명시적 상태 문구로 표시한다(#46 거짓 라벨 금지 연장 — 음수 재고는 사용자에게 데이터 이상으로 오인되거나, 반대로 무시되어 진짜 폴링 결함을 숨긴다). (2) `source='NATIVE'`(직접 등록) 상품을 도매매 실시간 재고 폴링 대상에 포함시킬지 여부를 재검토한다 — 크롤링 기원이 아닌 상품은 애초에 도매매 실물 매핑이 보장되지 않으므로, 폴러 대상 필터(`loadProductsToPoll`, `dome-inventory-poller.ts`)가 `source`를 무시하고 `supplier_product_code IS NOT NULL`만 보는 현재 로직이 근본 결함일 가능성이 있다. (3) 폴링이 N회 연속 실패하는 상품은 개입 대기열에 "재고 확인 불가 — 소싱 코드 재확인 필요" 카드로 표면화한다(#56 자연스러운 개입 노출).

**상태**: 코드 수정 미실시(진단만 완료 — 명화 발행과 무관한 별도 이슈이므로 운영자 확인 후 착수, 명화 발행 자체는 보류 중이며 이 항목도 발행과 독립적으로 처리 가능). 실측 근거: `inventory_snapshots` 테이블 SQL 조회(Supabase MCP) + `/api/crawler/domemae` POST 라이브 호출로 도매매 API 원문 에러 확보(2026-07-14). 전상품 확인 완료(#62) — 폴링 대상 3건 전부 동일 증상, 명화 단건 이슈 아님.


## 작업원칙 #271 — 공급처 단절(소싱처 소멸)은 재고 실패 센티널의 "지속성"으로 판정한다 (2026-07-18)

**맥락**: qty=-1(#260 조회 실패 센티널)만으로는 "일시적 폴링 실패"와 "공급사가 도매꾹에서 상품을 내린 영구 소멸"을 구분할 수 없다. 실제로 명화·아이스틀·달항아리 모두 2주+ 연속 -1이었으나(#270 파서 버그), 파서 정상화 후 아이스틀·달항아리는 다음 폴에서 실재고로 복구되고 명화만 -1이 지속됐다.

**판정 규칙(전상품 범용·#55)**:
1. 최신 스냅샷 qty=-1 **이면서** 최근 연속 N회(현재 3) 이상 -1이 지속되면 `sourceGone=true`(공급처 단절).
2. 살아있는 상품의 일시 실패는 1~2폴 안에 자가 복구되므로 걸리지 않는다. 하차 상품은 -1이 무한 지속돼 걸린다.
3. 판정은 스냅샷 히스토리 in-memory 집계로(추가 DB 쿼리 0). 명화 전용 하드코딩 금지(#55·CLAUDE.md §0).

**UX**: 공급처 단절은 재고 신호가 아니라 **운영 신호**("이 상품은 다시 못 들여오니 정리하세요") → "공급처 단절 · 삭제 권장" 자주빛 배지(활성 재고 빨강과 색 구분). 배지가 삭제를 유도하고, 삭제 게이트(#46 강경고)가 안전판.

**연계 개선(미착수)**: 좀비/건강 판정(zombie-verdict)에도 sourceGone을 반영해야 함 — 공급처 소멸 상품에 "잘 자라는 중" 건강 배지가 붙는 모순 방지(#62 전체 정합).


## 작업원칙 #272 — 상품 삭제는 "지킬 자산이 있는가"로 판단한다. 판매 이력이 있으면 삭제가 아니라 대체 소싱 (2026-07-18)

**맥락**: 명화(소싱처 소멸) 삭제 후 운영자 질문 — "품절 상품도 삭제가 좋은가, 보관이 좋은가?"

**핵심 사실**: 네이버 스마트스토어의 실질 자산은 **상품 URL에 누적된 이력**이다.
- 리뷰·구매평 (삭제 시 영구 소멸, 복구 불가)
- 판매 건수·찜 수 (0으로 리셋)
- 상품ID 기준으로 축적된 검색 순위 (처음부터 다시)

리뷰 50개 달린 상품을 지우고 재등록하면 **리뷰 0개 신규 상품**이 되고 순위 회복에 수개월이 걸린다. 파워셀러가 품절 상품을 절대 지우지 않는 이유이며, 유료 셀러툴들도 삭제 대신 "판매중지 + 대체 소싱"을 민다.

**판단 규칙(전상품 범용·#55)**:

| 상황 | 권장 액션 |
|---|---|
| 일시 품절 | 품절 처리(OUTOFSTOCK) — 상품 살려두고 재고만 0 |
| 장기 품절 | 판매중지(SUSPENSION) — 검색 하락하나 URL·리뷰 보존 |
| 소싱 단절 + 판매 이력 O | **판매중지 + 대체 소싱** — 같은 URL에서 공급처만 교체해 순위·리뷰 승계. **삭제 금지** |
| 소싱 단절 + 판매 이력 X | 삭제 안전 — 지킬 자산이 없음 |

**앱 구현 원칙**:
1. 자산 유무 판정은 `sales-assets.ts` 단일 권위(salesCount>0 또는 lastSaleDate 존재).
2. 공급처 단절 배지는 자산 유무로 권장 액션을 분기한다("삭제 권장" vs "대체 소싱 권장").
3. 삭제 경고는 **잃을 것에 비례**한다 — 자산이 있으면 구체적 손실(리뷰·순위)을 명시하고 대안을 제시, 없으면 단순 확인. 무조건 강경고는 경고 피로를 만들어 오히려 위험하다.

**부수 원칙(클라/서버 경계)**: 클라이언트 컴포넌트(`'use client'`)에서 쓰는 판정 로직은 prisma 의존 모듈에서 분리한다. tsc는 통과하지만 build/런타임에서 터지므로 **build 검증이 필수**(#32 재확인).

---

## 작업원칙 #273 — "신호"까지가 아니라 "결론"까지 앱이 낸다. 처분 권고는 단일 엔진이 판정한다 (2026-07-21)

**맥락**: #272에서 삭제 vs 보관 정책을 문서로 정리했으나, 앱은 여전히 *신호*(재고 배지·공급처 단절·좀비 지수)만 보여주고 *결론*은 운영자 머릿속에 있었다. 운영자 요청 — "권고사항대로 앱이 이 판단을 대신하게 해달라".

**발견한 실제 결함**: `/products/out-of-stock` 페이지의 `getOosAdvice`가 판매 이력을 모른 채 "30일 품절 + 꿀통 낮음 = 정리 권장"을 띄우고 있었다. 리뷰·순위가 쌓인 상품에도 그대로 떠서 **#272를 정면으로 위반**했다. 정책을 문서에만 두면 코드가 조용히 어긋난다.

**규칙**:
1. 처분 결론은 `disposition.ts` 단일 권위가 낸다(#62). 소비처(배지·품절 페이지·삭제 게이트)는 자체 규칙을 두지 않는다.
2. 판정은 **가중치 합산이 아니라 우선순위 분기**다. 소싱 단절은 확정 신호로 다른 조건을 덮어쓴다(#271과 같은 사상).
3. **이미 조치된 상품에는 권고하지 않는다.** 네이버 상태가 SUSPENSION이거나, 단기 품절인데 이미 OUTOFSTOCK이면 `NONE`. 처리 끝난 상품에까지 배지가 남으면 경고 피로가 생겨 진짜 권고까지 무시하게 된다(#272의 "경고는 잃을 것에 비례" 연장).
4. **판단 근거와 무관한 점수를 판단에 섞지 않는다.** 꿀통지수는 *재입고 우선순위*에만 쓰고 *처분 여부*에는 쓰지 않는다 — 점수가 높다고 자산이 지켜지는 것도, 낮다고 지워도 되는 것도 아니다.
5. 권고는 **기존 배지를 승격**해 싣는다. 배지를 새로 추가하지 않는다(#233 과밀).

**판정표**(전상품 범용 #55):

| 조건 | 권고 | 색 |
|---|---|---|
| 소싱 단절 + 판매이력 O | 대체 소싱 | 자주 |
| 소싱 단절 + 판매이력 X | 삭제 | 자주 |
| 품절 + 스토어 판매중 | 품절 처리 | 빨강 |
| 품절 14일 이상 | 판매중지 | 주황 |
| 미발행 / 조회실패 / 조치완료 / 재고정상 | 권고 없음 | — |

**임계값 근거(14일)**: 네이버는 장기 품절 상품의 노출을 점진적으로 낮춘다. 품절로 방치하면 순위가 서서히 깎이지만 판매중지는 검색에서만 내려가고 URL·리뷰는 보존된다. 2주면 공급사 재입고 여부가 판가름나는 시점과, 순위 손상이 누적되기 전인 시점이 맞물린다(운영자 결정).

**검증 방식(참고)**: 프로덕션 DB에 테스트 데이터를 주입하는 대신 **임시 프리뷰 라우트**에 전 분기를 한 화면에 렌더해 육안 확인 후 라우트를 삭제했다. 실데이터 오염 위험 0이고 분기 누락도 한눈에 보인다 — 배지·카드류 검증의 기본형으로 삼는다. 로직은 별도 15케이스 테이블 테스트로 경계값(임계 정확히/임계-1)까지 확인.

---

## 작업원칙 #274 — 배지는 지우지 말고 줄을 세워라. 순서 기준은 "돈이 새는 순서" (2026-07-21)

**맥락**: 상품 행 이름칸에 배지가 7개까지 쌓여 행 높이 178px. 매일 보는 목록이라 스캔 비용이 그대로 운영 비용이 된다(#233).

**핵심 판단**: 배지는 **각자 필요해서 생긴 것**이라 지우면 정보가 사라진다. 삭제가 아니라 **순서 + 접기**가 답이다 — 상시 노출은 들어가는 만큼만, 나머지는 "+N"으로 접는다. 정보 손실 0, 스캔 비용만 감소.

**순서의 기준**(`badge-priority.ts` 단일 권위 #62): 디자인 취향이나 구현 편의가 아니라 **"이 배지를 못 보고 지나치면 얼마를 잃는가"**로 줄을 세운다.

| 급 | 성격 | 예 | 값 |
|---|---|---|---|
| 1 | 사고 — 되돌릴 수 없는 손실 | 처분 권고(#273) | 10 |
| 2 | 매출 — 지금 돈이 새는 중 | 좀비·마진 위기 | 20 |
| 3 | 신뢰 — 판단 근거가 틀어짐 | 앱↔네이버 불일치 | 30 |
| 4 | 상태 — 사실 정보 | 재고·발행상태·준비도 | 40~60 |
| 5 | 개선 — 여지 | 상품명 등급 | 70 |
| 6 | 메타 — 분류 | 출처 태그 | 80 |
| 7 | 식별자 | SKU | 90 |

값을 10단위로 띄우는 이유: 새 배지가 생겨도 기존 값을 안 건드리고 사이에 끼운다.

**★ 고정 개수가 아니라 실측이다**: 처음엔 "상위 3개 고정 노출"로 만들었으나 좁은 칸에서 3개가 안 들어가 잘렸다. 배지 폭은 내용에 따라 34~199px로 제각각이라 **개수로는 절대 못 맞춘다**. 미러 측정(visibility:hidden) + ResizeObserver로 컨테이너 폭과 각 배지 실제 폭을 재서 들어가는 만큼만 노출한다. 화면폭·컬럼구성·문구가 바뀌어도 스스로 맞는다.

**부수 규칙**: 행 전체가 클릭 가능한 목록에서 쓰이므로 "+N" 트리거와 팝오버는 반드시 `stopPropagation` — 배지를 보려다 패널이 열리면 안 된다. 팝오버 구현 관례(포털·viewport clamp·Escape·바깥클릭)는 기존 `OverflowMenu`를 그대로 계승한다(#62 — 같은 앱에서 같은 종류의 UI가 다르게 동작하면 안 된다).

---

## 작업원칙 #275 — 데이터 테이블에서 열이 "조용히 사라지는" 것보다 가로 스크롤이 언제나 낫다 (2026-07-21)

**발견 경위**: #274 작업 중 브라우저로 행 높이를 실측하다 **상품명 칸의 width가 0px**인 것을 발견했다. 배지 정리 때문이 아니라 **원래 그랬다**.

**원인**: `grid-template-columns: 36px 1fr 90px 110px 130px 62px 90px 72px 70px`에서 `1fr`은 `minmax(auto, 1fr)`이다. 고정 트랙 합(660px) + gap(64px) = 724px가 컨테이너 가용폭(약 710px)을 넘는 순간 `1fr` 트랙이 **0px로 붕괴**한다. 목록에서 가장 중요한 상품명과 SKU가 모든 데스크톱 폭에서 보이지 않았고, 배지만 블록 요소라 밖으로 흘러넘쳐 "뭔가 보이니까 정상"처럼 착시를 일으켰다.

**규칙**:
1. 데이터 테이블의 주요 정보 열에는 **반드시 하한**을 준다 — `1fr`이 아니라 `minmax(260px, 1fr)`.
2. 그래도 안 들어가면 **침묵 붕괴가 아니라 가로 스크롤**로 넘긴다. 없어진 정보는 운영자가 없어진 줄도 모른다. 스크롤은 최소한 "더 있다"는 사실을 알려준다.
3. 최소 테이블 폭은 **컬럼 정의 문자열에서 계산**한다. 상수를 따로 두면 컬럼을 바꿀 때 어긋난다(#62).

**검증 방법(중요)**: 이 결함은 스크린샷만으로는 못 잡는다(배지가 흘러넘쳐 채워 보임). **DOM 실측**(`getBoundingClientRect().width`)이 필요했다. 앞으로 레이아웃 변경 시 스크린샷과 DOM 실측을 **둘 다** 본다 — #265("수치 PASS ≠ 화면 정상")의 역방향 짝: **화면이 그럴듯해도 수치가 0일 수 있다.**

**전수 확인**: 같은 패턴을 앱 전체(대시보드·주문·소싱·재활성화·크롤·시장·성장·관제탑 등)에서 스캔 — 0폭 트랙을 가진 그리드는 상품 목록 하나뿐이었음을 브라우저 실측으로 확인. 단건 수습이 아니라 전수 확인까지가 한 세트다.

---

## 작업원칙 #276 — 밀집 목록의 배지는 "무엇"만 말하고 "왜"는 hover로 내린다 (2026-07-21)

**맥락**: 배지 레일(#274)을 적용했는데도 배지 하나가 199px를 먹어 다른 신호를 전부 밀어냈다. 범인은 `TuningBadge`의 인라인 사유("좀비 발견 · 순마진 0.0% — 마진 위기").

**규칙**: 목록 행처럼 밀집된 맥락의 배지는 **라벨(무엇)만** 인라인으로 두고, 사유·수치·제안(왜)은 hover 툴팁으로 내린다. 툴팁에 이미 전문이 있으므로 **정보 손실 0**이면서 폭은 199px → 75px로 줄어든다.

**구현 관례**: 배지 컴포넌트는 `compact?: boolean` prop을 받아 밀집 맥락에서 켠다. 상세 화면처럼 여유 있는 곳에서는 기본값(전체 노출)을 그대로 쓴다 — 같은 컴포넌트가 맥락에 따라 밀도를 바꾸는 것이지, 컴포넌트를 두 벌 만드는 게 아니다(#62).

**일반화**: "한 요소가 공용 공간을 독점하면, 그 요소를 빼는 게 아니라 그 요소의 밀도를 낮춘다."

---

## 작업원칙 #277 — 판단(권고)과 실행(쓰기)은 한 흐름으로 잇되, 실행의 방아쇠는 항상 사람이 당긴다 (2026-07-22)

**맥락**: 처분 권고 엔진(#273)이 "판매중지하세요"라고 판단해도 앱에 그 버튼이 없으면, 운영자는 결국 스마트스토어센터를 따로 열어야 한다 — 앱이 판단을 대신해준 가치가 절반만 남는다. 권고와 실행 사이의 간극을 없앤다.

**규칙**:
1. **권고를 실행 지점에 녹인다.** 처분 판정(disposition.ts)을 실제 반영 UI(반영 탭) 상단에 배너로 노출하고, 추천 액션 버튼을 시각적으로 강조한다("추천"). 운영자가 "무엇을 할지" 다시 생각할 필요가 없다.
2. **실행은 미리보기 → 명시적 GO의 2단.** 앱은 절대 자동으로 스토어에 쓰지 않는다(#46). dryRun(GET+merge, 읽기 전용) 미리보기로 "무엇이 바뀌는지"를 먼저 보이고, 운영자의 확인 다이얼로그 승인 후에만 `dryRun:false + confirm:true` 이중 게이트로 실제 PUT이 나간다.
3. **변경 없으면(no-op) 실행 경로 자체를 숨긴다.** 이미 목표 상태인 상품(예: 이미 SUSPENSION)은 `changedTopLevelFields=[]`로 판정돼 GO 버튼이 뜨지 않는다. 무의미한 쓰기 기회를 원천 차단 — 실수의 표면적을 줄이는 게 경고 문구보다 낫다.

**상태 target의 성격 차이(#197 확장)**: 네이버 상태 3종은 쓰기 방식이 다르다.
| target | 방식 | 재고 |
|---|---|---|
| OUTOFSTOCK(품절) | statusType는 재고 파생값 → 재고를 0으로 (옵션상품은 optionCombinations) | 0으로 |
| SUSPENSION(판매중지) | statusType 직접 세팅 | **무변경** — 재입고 시 즉시 재판매 성립 |
| SALE(재판매) | statusType 직접 세팅 | 무변경(재입고는 네이버 측) |

**검증의 안전 경계(중요)**: dryRun(읽기 전용)과 UI 렌더는 프로덕션에서 끝까지 실측할 수 있지만, **실제 GO(비가역 스토어 쓰기)는 검증자가 누르지 않는다** — 그 클릭 자체가 운영자의 #46 승인이자 유일한 실쓰기 테스트다. 검증은 "GO 버튼이 올바른 조건에서 뜨고/숨고, 올바른 payload(dryRun:false+confirm:true)로 배선됐는가"까지다. 이 경계를 지키면 "테스트 못 한 코드를 배포"가 아니라 "운영자와 공동 실행할 지점까지 안전하게 준비"가 된다.

---

## 작업원칙 #278 — "대기함"은 상태가 아니라 판정으로 모은다. 상태로 거르면 가장 급한 건이 통째로 빠진다 (2026-07-22)

**발견**: 처분 결정 대기함(구 품절 상품 개선)이 `status=OUT_OF_STOCK`인 상품만 불러오고 있었다. 그런데 **공급처가 끊긴 상품은 앱 상태가 ACTIVE로 남아 있을 수 있다** — 재고 폴링이 -1을 반환할 뿐 앱 status는 바뀌지 않기 때문. 결과적으로 **가장 급한 처분 대상(대체 소싱 필요)이 이 화면에서 통째로 누락**되고 있었다.

**규칙**: 할 일을 모으는 화면은 **판정 결과로 모은다**. `status`·`flag` 같은 저장된 상태로 거르면, 판정에 쓰이는 다른 입력(재고 신호·자산 유무)이 바뀌어도 목록에 나타나지 않는다. 대기함의 정의는 "이 상태인 것"이 아니라 **"앱이 조치가 필요하다고 판단한 것"**이다.

**일반화**: 목록 화면의 필터 축이 판정 엔진의 입력과 다르면, 그 화면은 조용히 일부를 놓친다. 필터 축은 항상 **판정의 출력**과 일치시킨다.

**부수 규칙(무엇도 버리지 않기)**: 재정의하면서 기존 화면의 가치(꿀통지수 기반 재입고 우선순위)를 삭제하지 않고 "재입고 검토" 그룹으로 내려 보존했다. 화면을 재정의할 때 기존 기능을 없애면 그 기능을 쓰던 동선이 갈 곳을 잃는다.

---

## 작업원칙 #279 — 환경변수 오염은 "코드가 멀쩡한데 안 되는" 증상으로 온다. 재발 방지는 스크립트에 못박는 것 (2026-07-22)

**증상**: `next dev`가 `globals.css`의 `@tailwind`를 파싱하지 못함(`Module parse failed: Unexpected character '@'`). `npm run build`는 정상. `.next` 완전 삭제·`npm ci` 후에도 지속. 코드 변경(`.ts` 2개, CSS 무접촉)과 무관.

**근본 원인**: **셸 환경변수 `NODE_ENV=production`**. 이 값에서
1. Next dev가 개발용 CSS 파이프라인을 비활성화하고,
2. `npm ci`가 devDependencies(tailwindcss·postcss·typescript)를 건너뛴다.

`.env`에 `NODE_ENV=development`가 있어도 **셸 환경변수가 우선**하므로 무력했다.

**진단이 늦어진 이유(교훈)**: "빌드는 되는데 dev만 안 된다"는 신호를 코드 문제로 계속 의심했다. **빌드와 dev가 갈리면 코드가 아니라 환경을 먼저 본다** — 두 경로의 차이는 대부분 NODE_ENV·캐시·설치 범위다. dev 서버 기동 로그의 경고(`non-standard NODE_ENV`, `typescript 미설치 → 자동 설치`)에 답이 이미 있었다. **로그 상단을 먼저 읽는다.**

**재발 방지(단건 수습 금지)**: `package.json`의 dev 스크립트에 `NODE_ENV=development`를 못박았다. 셸이 오염돼 있어도, 누가(Code/Desktop/Cowork) 어디서 실행해도 같은 함정에 빠지지 않는다. 개인 셸을 고치는 것은 그 사람만 고치는 것이고, 스크립트를 고치는 것은 프로젝트 전체를 고치는 것이다.

---

## 작업원칙 #280 — 같은 데이터를 두 화면이 그리면, 아이템 구성은 한 함수에서 만든다 (2026-07-22)

**발견**: 배지 레일(#274)을 데스크톱 목록 행에만 적용했더니, **모바일 카드에는 처분 권고·좀비·상품명·허브 배지가 아예 없었다.** 같은 상품인데 폰으로 보면 앱의 판단이 통째로 사라지는 정보 비대칭. 원인은 배지 구성이 JSX 안에 인라인 배열로 박혀 있어서 "한쪽만 고치는" 게 기본값이었기 때문.

**규칙**: 같은 도메인 데이터를 두 개 이상의 화면이 렌더하면, **무엇을 보여줄지(아이템 구성)는 함수 하나로 모으고, 어떻게 보여줄지(폭·개수·레이아웃)만 소비처가 다르게 한다.** `buildProductBadgeItems(p, ctx)`가 그 경계다 — 데스크톱은 SKU 포함, 모바일은 제외처럼 **맥락 플래그**로만 갈린다.

**판별법**: JSX 안에 10줄 넘는 배열 리터럴이 보이면 그건 대개 "다른 화면에도 있어야 하는데 없는 것"의 신호다.

---

## 작업원칙 #281 — 공간이 물리적으로 부족하면 "밀어내기"가 아니라 "줄이기"다 (2026-07-22)

**맥락**: R-3(사이드패널이 목록 컬럼을 가림)이 여러 세션 미해결로 남아 있었다. rev58에서 "압축 모드 대신 밀어내기(margin-right)"를 택했는데, 실측해보니 **패널 720px가 열리면 목록에 495px만 남고 9컬럼 테이블은 최소 1020px가 필요**했다. 밀어내기로는 애초에 불가능한 요구였다.

**규칙**: 컨테이너가 콘텐츠보다 작을 때 선택지는 셋이고, 우선순위가 있다.
1. **줄이기(축소·열 제거)** ← 정보가 다른 곳에 있으면 최선
2. **스크롤** ← 정보가 여기밖에 없으면 차선(#275)
3. **침묵 붕괴** ← 절대 금지

패널을 보는 중에는 목록에 "이게 어떤 상품인지 + 핵심 판단"만 있으면 되고, 숨긴 열의 정보는 **패널 안에 전부 있다** → 정보 손실 0이므로 1번이 정답.

**부수 규칙**: 그리드에서 열을 조건부로 숨길 때는 **트랙 정의와 헤더 라벨과 셀 개수를 반드시 함께** 분기한다. 셋 중 하나만 바꾸면 전 행의 정렬이 어긋나며, 이 오류는 타입 검사에 안 걸린다.

---

## 작업원칙 #282 — React 렌더 결과를 측정할 때는 커밋 완료를 기다린다. 조급한 측정은 없는 버그를 만든다 (2026-07-22)

**사건**: 배지 레일의 자동 접힘을 검증하려고 카드 폭을 여러 단계로 줄이며 700ms 간격으로 측정했더니 "배지가 안 접히고 잘린다"는 결과가 나왔다. **버그로 판단하고 원인 분석에 들어갔는데, 단일 단계로 1.6초 대기 후 재측정하니 완벽하게 동작했다** (420px→5개 / 340px→3개+"+2" / 260px→2개+"+3" / 200px→1개+"+4", 잘림 0).

**원인**: ResizeObserver → setState → 리렌더 → 커밋까지의 비동기 사슬. 연속 변경을 빠르게 주면 측정 시점이 커밋 이전에 걸린다.

**규칙**: ResizeObserver·IntersectionObserver·SWR 등 **비동기 사슬을 거치는 렌더 결과는 충분히 기다린 뒤 측정**한다(1.5초 이상 권장). 그리고 **"버그다" 결론 전에 반드시 단일 단계로 재현**한다 — 연속 조작 중의 측정값은 증거로 쓰지 않는다.

**일반화**: 검증 도구가 만든 거짓 실패는 진짜 결함보다 비싸다. 없는 버그를 쫓느라 시간을 쓰고, 멀쩡한 코드를 "고쳐서" 진짜 결함을 심을 수 있다.

---

## 작업원칙 #283 — 페르소나는 셀러가 보는 앱 UI에만. 고객이 보는 상품 카피에는 절대 금지 (2026-07-22)

**사건**: D1(꼬띠 페르소나 적용) 인계 문서가 `section-renderers/strings.ko.json`을 "관제 상태표"라고 설명했으나, 💻 Code가 실제 문구를 추출해 확인한 결과 **네이버 고객이 보는 상품 상세페이지 카피**였다("평일 오후 2시 이전 주문 시 당일 출고", "단순 변심 7일 이내 무료 반품", "브랜드 철학"). `preset/samples.ko.json`·`detail-content-templates.ko.json`도 마찬가지로 `noticeCards`(교환·반품 안내)·`scents[].desc`(향 설명) 등 실제 판매 카피였다. **지시대로 강행했으면 교환·반품 고지에 "까꿍💖"이 들어갈 뻔했다.**

**규칙 — 문구의 독자를 먼저 정한다**:

| 독자 | 예 | 페르소나 |
|---|---|---|
| **셀러(운영자)** — 앱 내부 UI | 빈 상태, 에러, 토스트, 힌트, 위젯 안내 | **적용** (까꿍/이랴/해유) |
| **고객** — 네이버 상세페이지·고지 | 상세 본문, 배송/교환·반품 안내, 향 설명, 브랜드 철학 | **금지** |

꼬띠는 **앱 내부 마스코트**다. 고객은 꼬띠를 모른다. 마스코트 보이스가 상품 페이지에 들어가면 브랜드 신뢰를 해치고, 교환·반품 같은 고지 문구는 **법적 성격**까지 있어 장난스러운 어조가 부적절하다. #262("셀러 실무 용어로 번역")가 다루는 "화면 문구"는 **셀러가 보는 앱 UI**를 뜻하지 고객이 보는 등록 상품 카피가 아니다.

**근본 수정(단건 수습 금지)**: 원인은 지시가 아니라 **`persona-audit.py`에 독자 축이 없던 것**이었다. 스크립트가 키 이름(`desc|notice|hint`)만 보고 대상을 골라 고객 대면 파일을 "적용 대상"으로 올렸고, 인계 문서는 그 출력을 그대로 옮겼다. → 스크립트에 `CUSTOMER_FACING` 제외 목록을 넣되 **침묵 제외가 아니라 별도 섹션으로 노출**했다(조용히 빠지면 다음 사람이 또 헷갈린다). 잔여 0건 6개 → 3개로 정리.

**메타 교훈 — 인계 문서는 증거가 아니다**: 인계 문서의 파일 설명이 실제 내용과 달랐다(`envWarn`/`empty` 키는 아예 다른 파일에 있었다). **작업 전 target 문구를 직접 추출해 눈으로 확인**하는 절차가 이 사고를 막았다. 지시받은 파일이라도 **내용을 먼저 열어보고 성격을 판정**한다.

---

## 작업원칙 #284 — 병행 세션에서 남의 작업 중 파일을 만나면 손대지 않고 자기 파일만 개별 add (2026-07-22)

**사례**: 💻 Code가 D1 작업 중 `src/app/products/page.tsx`에 132줄의 미커밋 변경을 발견했다(🌸 Cowork의 C2 작업 진행 중 스냅샷). Code는 **건드리지 않고 자기 담당 파일만 개별 지정해 add**했고, 그 사이 Cowork가 정상 커밋(`ab3702e`)해 충돌 0으로 끝났다.

**규칙**:
1. `git add -A`/`git commit -a`를 병행 세션에서 쓰지 않는다. **자기 담당 파일만 경로로 지정**해 add한다.
2. 예상 못 한 미커밋 변경을 발견하면 **되돌리거나 stash하지 말고 그대로 둔 채 보고**한다. 남의 작업 중인 변경을 stash하면 그 세션이 통째로 날아간다.
3. 보드(`TASK_BRIDGE §3-A`)의 "파일 영역 겹침 0" 확인은 착수 전에 한다 — 겹치면 그 조합은 병행 대상이 아니다.

**왜 중요한가**: 병행 작업의 이득은 속도인데, 한 번의 덮어쓰기로 그 이득을 전부 잃고 신뢰까지 깎인다. 위 사례는 **규율이 지켜지면 병행이 안전하다는 실증**이다.

---

## 작업원칙 #285 — "되살릴까?"를 묻는 화면에는 "왜 죽었나"가 함께 있어야 한다 (2026-07-22)

**발견**: 좀비 부활소(재활성화 목록)는 되살릴 상품을 고르는 화면인데 **상품명 진단 배지만 있고 공급처 단절·처분 권고가 없었다.** 공급처가 끊긴 상품을 되살리려 시간을 쓰는 일이 구조적으로 막히지 않았다. 부활 작업은 이미지·상품명·키워드 재작업까지 포함해 상품당 수십 분이 드는데, **매입이 불가능한 상품이면 그 시간이 통째로 헛수고**다.

**규칙**: **행동을 유도하는 화면에는 그 행동을 말려야 할 신호도 함께 놓는다.** "되살리기" 버튼 옆에는 "되살려도 소용없음" 신호가, "발행하기" 옆에는 "발행하면 안 되는 이유"가 있어야 한다. 긍정 신호만 있는 화면은 운영자를 손실로 안내한다.

**구현 원칙(재사용)**: 새 배지를 만들지 않고 기존 `InventoryBadge`를 그대로 붙였다. 판정은 `disposition.ts` 단일 권위이므로 이 화면은 자체 규칙을 두지 않는다(#62/#280). **개입점을 늘릴 때 로직을 늘리지 않는 것**이 확장의 핵심이다 — 화면마다 판정이 갈라지면 같은 상품에 다른 답이 나온다.

**점검 질문(다른 화면에도 적용)**: 이 화면이 유도하는 행동은 무엇인가? 그 행동이 **틀린 선택이 되는 조건**은 무엇인가? 그 조건이 이 화면에 보이는가?

---

## 작업원칙 #286 — "준비됐는가"와 "지금 팔 수 있는가"는 다른 질문이다. 발행은 둘 다 통과해야 한다 (2026-07-22)

**발견(#285 전수 점검 중)**: 발행 검증(`NaverRegisterModal`)이 **준비도만** 봤다 — 대표이미지·카테고리·필수필드. 그래서 **공급사가 도매처에서 내린 상품도 준비도 100%면 일괄 발행에 섞였다.**

**손실 경로(돈이 새는 결함)**:
1. 발행 즉시 매입 불가 → 주문이 들어오면 **취소** → 취소율이 스마트스토어 등급을 깎는다
2. 신규 상품 **7일 노출 부스트**를 매입 불가 상품에 날린다
3. 발행 직후 품절 처리하면 그 상품 URL은 **시작부터 죽은 상태**가 된다

**규칙**: 발행 게이트는 두 축을 따로 본다.
| 축 | 묻는 것 | 권위 |
|---|---|---|
| 준비도 | 상품 **데이터의 완성도** (이미지·카테고리·필수필드) | publish-readiness |
| 공급 가능성 | **지금 매입할 수 있는가** (공급처 단절·공급사 품절) | `publish-gate.ts` |

둘 다 통과해야 발행이다. 하나만 보면 "완벽하게 준비된, 팔 수 없는 상품"을 올린다.

**★모르는 것으로 막지 않는다**: 재고 신호가 아예 없으면(폴링 전) 통과시키고, `qty<0`(조회 실패 센티널 #260)도 통과시킨다. 지속되면 `sourceGone`이 잡는다(#271). 모른다고 막으면 **신규 소싱 상품을 영영 못 올린다** — 게이트가 업무를 막는 순간 운영자는 게이트를 끄고, 그러면 게이트가 없는 것만 못하다(#82 degrade 사상).

**차단의 UX**: 기본 제외하되 **운영자가 토글로 뒤집을 수 있게** 하고, 뒤집으면 무엇을 감수하는지 명시한다(#46 사상 — 앱은 권하고 결정은 사람이). 강제 차단은 우회를 부르고, 우회는 기록에 남지 않는다.

**일반화(다른 게이트에도)**: 어떤 행동을 검증할 때 "형식이 갖춰졌는가"만 보고 있지 않은지 확인한다. **형식 검증과 실행 가능성 검증은 다른 축**이며, 대개 후자가 빠져 있다.

---

## 작업원칙 #287 — 일괄 작업은 단건 작업보다 규율이 느슨해지기 쉽다. 같은 규율을 강제하라 (2026-07-22)

**발견(#285/#286 전수 점검 중)**: `/products`의 일괄 상태 변경이 이랬다.

```
await Promise.all(ids.map(id => fetch(...PATCH...)));
setRawProducts(... status 반영 ...);   // 결과를 보지 않는다
```

**결과를 전혀 보지 않았다.** 실패해도 알림 없이 로컬 상태만 바뀌어 **화면과 DB가 어긋났다.** 같은 파일의 단건 경로(`handleProductMutate`)에는 optimistic + rollback + alert가 다 있었는데, **일괄 경로만 빠져 있었다.** 게다가 같은 기능인 `/products/sourced`는 실패를 제대로 보고하고 있었다 — **같은 동작이 두 곳에 다르게 구현된 #62의 반대 사례.**

**왜 일괄에서 규율이 무너지는가**: 단건은 결과가 눈에 보이지만(한 행이 바뀜), 일괄은 "여러 개 처리했다"는 느낌만 남는다. 구현할 때도 `Promise.all` 한 줄이 편해서 결과 수집을 건너뛰기 쉽다. **`Promise.all`은 하나라도 실패하면 전체가 reject되지만, 이미 날아간 요청은 되돌아오지 않는다** — 부분 성공 상태가 조용히 남는다.

**규칙**:
1. 일괄 작업은 `Promise.allSettled`로 **개별 결과를 수집**한다. `Promise.all`은 부분 성공을 표현할 수 없다.
2. 실패분만 롤백하고, **"N개 중 M개 완료, K개 실패"를 반드시 보고**한다. 부분 성공은 정직하게 부분 성공으로 표시한다.
3. 단건 경로에 있는 안전장치(확인·롤백·보고)는 **일괄 경로에도 그대로** 있어야 한다. 일괄이 오히려 피해 규모가 크다.
4. 상태를 바꾸는 일괄 작업에는 단건과 **동일한 게이트**를 적용한다(#286) — 여러 건을 한 번에 잘못 바꾸는 게 더 위험하다.

**점검 질문**: 코드베이스에서 `Promise.all(`을 grep 해서, 그 결과를 쓰지 않는 곳이 있는지 본다. 결과를 안 쓰는 `Promise.all`은 대개 조용한 실패를 숨기고 있다.

---

## 작업원칙 #288 — 라벨과 문장은 다른 축이다. 라벨은 표기를 고정하고, 문장은 자연스러운 한국어를 쓴다 (2026-07-22)

**발견**: 상태 드롭다운 라벨이 `판매 중`(공백)인데 코드베이스의 다른 107곳은 `판매중`이었다. `InventoryBadge`의 `stopped`도 `판매 중지` vs 나머지 `판매중지`.

**왜 문제인가**: 라벨은 **필터·비교·검색·정렬**에 쓰인다. 표기가 흔들리면 문자열 비교가 조용히 어긋나고, 그 어긋남은 타입 검사에 안 걸린다. 반면 문장 속 서술("공급사 재고가 없는데 스토어는 아직 **판매 중**이에요")은 **띄어쓰기가 맞는 한국어**다.

**규칙**:
| 축 | 예 | 표기 |
|---|---|---|
| **라벨**(값·상태·버튼) | `판매중` `판매중지` `발행대기` | **붙여쓰기 고정** — 식별자로 취급 |
| **문장**(설명·툴팁·경고) | "아직 판매 중이에요" | **자연스러운 한국어** |

라벨을 일괄 치환할 때 문장까지 바꾸면 문법이 깨진다. **치환 전에 라벨 문맥(`label:`, JSON 값)과 문장 문맥을 반드시 구분**한다.

**부수 교훈(검증)**: 이 불일치는 `#282`의 자기 교정 과정에서 발견됐다 — 드롭다운에서 "판매중"을 찾는 정규식이 매치되지 않아 "옵션이 없다"고 오판했고, 확인해보니 **코드가 아니라 테스트가 틀렸으며 동시에 진짜 표기 불일치도 있었다.** 거짓 실패를 추적하다 진짜 결함을 만나는 일은 드물지 않다 — 오판을 확인할 때 대충 넘기지 않는다.

---

## 작업원칙 #289 — 병행 세션의 순번은 충돌한다. 식별자는 사람이 세지 않는 값으로 (2026-07-22)

**사건**: `TASK_BRIDGE §3 ACTIVE`에 `(124)` 엔트리가 **두 개** 생겼다. 🌸 Cowork와 💻 Code가 각자 "다음 번호는 124"라고 판단해 동시에 붙였다. 서로의 작업을 보지 못하는 병행 세션에서는 **순번 충돌이 예외가 아니라 필연**이다.

**규칙**:
1. 엔트리 헤더에 **커밋 SHA를 반드시 포함**한다. SHA는 충돌하지 않으며, 순번이 겹쳐도 어느 작업인지 식별된다.
2. 순번이 겹치면 **시간순(커밋 시각)으로 뒤쪽에 +1**을 준다. 병합하는 쪽이 조정한다.
3. 앞으로 신규 엔트리는 `(번호) 환경 — 제목 [SHA]` 형식을 지키되, **번호보다 SHA가 정본**이다.

**일반화**: 병행 작업에서 **사람이 세는 값(순번·버전·순서)은 반드시 충돌한다.** 식별자는 기계가 만든 값(SHA·타임스탬프·UUID)을 쓰고, 사람이 세는 값은 보조 표시로만 둔다.

---

## 작업원칙 #284 보강 — 파일 영역 소유권은 "동시 편집 방지"이지 영구 배타가 아니다 (2026-07-22 보강)

`*.strings.ko.json` = 💻 Code, `products/page.tsx` = 🌸 Cowork로 영역을 나눴으나, 이는 **같은 시간에 두 세션이 같은 파일을 고치는 것**을 막기 위한 규칙이지 영구 소유권이 아니다.

**상대 영역 파일을 건드려야 할 때**:
1. 상대 작업 큐가 비어 있는지 보드(`§3-A`)에서 확인
2. 미커밋 변경이 없는지 `git status`로 확인
3. **인계 메시지에 "상대 영역을 건드렸다"고 명시** — 상대가 다음 세션에 놀라지 않게

(실사례: F3 라벨 통일에서 Cowork가 `InventoryBadge.strings.ko.json`을 수정. Code 큐가 비어 있어 위험 0이었고 인계에 명시함.)

---

## 작업원칙 #290 — 판정 로직이 PURE여도, 그 입력을 화면에서만 모으면 서버 집계는 갈라진다 (2026-07-22)

**발견**: `#278`에서 "대기함은 상태가 아니라 판정으로 모은다"를 확립하고 대기함 화면을 고쳤다. 그런데 **대시보드 카운트는 여전히 `status='OUT_OF_STOCK'`로 세고 있었다.** 공급처가 끊긴 상품은 앱 status가 ACTIVE로 남으므로, **가장 급한 처분 대상이 앱 첫 화면의 카운트에서 통째로 빠졌다.** 앱이 자기 할 일을 실제보다 적게 보고하고 있었다.

**왜 놓쳤나**: 판정 로직(`disposition.ts`)은 PURE로 잘 분리했는데, **그 입력(재고 스냅샷)을 모으는 경로를 화면(클라이언트 훅)에만 만들어 뒀다.** 서버는 같은 판정을 쓸 방법이 없어 옛 방식(status 집계)에 머물렀다. **로직을 단일 권위로 만드는 것만으로는 부족하고, 입력 수집 경로도 양쪽에 있어야 한다.**

**규칙**:
1. 판정 로직을 PURE로 분리했으면, **입력을 모으는 로더도 서버·클라이언트 양쪽에 만든다.** 한쪽만 있으면 다른 쪽은 반드시 옛 방식에 머문다.
2. 서버 로더는 화면과 **동일한 계산 함수**를 재사용한다. 다른 쿼리로 "비슷하게" 만들면 수치가 어긋나고, **화면과 서버가 다른 수를 말하면 운영자는 어느 쪽도 믿지 않게 된다.**
3. 집계 로더는 best-effort(#82) — 실패 시 빈 결과로 degrade하고 throw하지 않는다. 카운트 하나 때문에 대시보드 전체가 죽으면 안 된다.

**점검 질문**: 새 판정 엔진을 만들 때 —"이 판정을 서버에서도 써야 하는 곳이 있나?" 대시보드·크론·디스코드 다이제스트·API 집계가 대표적이다.

---

## 작업원칙 #291 — 인계 메시지에는 "작성 시점의 상대 HEAD"를 적는다. 완료 보고와 인계는 교차한다 (2026-07-22)

**사건**: 🌸 Cowork가 💻 Code에게 `G1-A` 인계를 보냈는데, **Code는 이미 그 작업을 완료·배포한 상태**였다(`a692b7c`). Cowork의 인계는 Code의 완료 보고가 도착하기 전에 작성됐다. Code는 "이미 완료됐고 추가 작업이 없다"고 정확히 회신했지만, **한 턴이 낭비됐다.**

**왜 필연인가**: 두 세션은 서로의 진행을 실시간으로 보지 못한다. 운영자를 경유해 메시지가 오가므로 **작성 시점과 도착 시점 사이에 상대가 일을 끝낼 수 있다.** 순번 충돌(#289)과 같은 뿌리다.

**규칙**:
1. 인계 메시지 첫 줄에 **`BASELINE: main <SHA>` + "이 시점 기준"**을 명시한다.
2. 받는 쪽은 **착수 전 `git pull` 후 보드(`§3-A`)에서 해당 항목이 아직 `READY`인지 확인**한다. `DONE`이면 교차로 판단하고 즉시 회신한다.
3. 보내는 쪽은 인계 전 **보드에서 상대 항목 상태를 먼저 확인**한다 — 상대가 이미 `DONE`으로 바꿔 놨을 수 있다.

**일반화**: 비동기 협업에서 메시지는 **작성 시점의 스냅샷**이지 현재 상태가 아니다. 항상 **정본(보드·SHA)을 다시 확인**하고 움직인다.

---

## 작업원칙 #292 — 전수 점검 전에 "이게 실제로 렌더되는가"를 먼저 묻는다. 죽은 코드는 결함의 냉동고다 (2026-07-22)

**사건**: E2(대시보드 위젯 정합성) 점검 중 `KkottiWidget`의 "품절 현황"에서 E1과 동일한 결함(status 기준 카운트 + 목적지 오류)을 찾아 수정했다. 그런데 **브라우저로 검증하려 하니 화면에 없었다** — `#228`로 대시보드에서 제거된 뒤 어디에도 마운트되지 않는 **죽은 코드**였다.

전수 스캔 결과 대시보드에만 **미마운트 컴포넌트 13개**가 남아 있었다.

**두 가지 교훈**:

**1. 검증 순서 — 렌더 여부를 먼저 확인한다.**
수정 → 브라우저 검증 시도 → "화면에 없네" 순서로 갔다. 반대로 갔어야 했다. 전수 점검 대상을 고를 때 **"이 컴포넌트가 실제로 마운트되는가"를 먼저 확인**하면 검증 시간을 낭비하지 않는다.
```bash
# 마운트 여부 확인 (자기 파일 제외 참조 검색)
grep -rn "ComponentName" src --include="*.tsx" | grep -v "그_컴포넌트_파일" | grep -vE "^\s*//"
```

**2. 죽은 코드는 결함의 냉동고다.**
미마운트 컴포넌트는 결함이 있어도 **아무도 발견하지 못한 채 보존**된다. 그리고 언젠가 되살릴 때 **이미 고친 결함이 함께 부활한다.** 원칙을 새로 세워도(#278/#285/#290) 죽은 코드에는 적용되지 않으므로, 되살리는 순간 앱이 과거로 돌아간다.

**규칙**:
- 미마운트 컴포넌트를 발견하면 **삭제 대신 `@unmounted` 표식 + "되살리기 전 확인할 것" 체크리스트**를 파일 상단에 남긴다. 삭제는 의도적 보존일 수 있어 **운영자 판단 영역**이다.
- 체크리스트에는 **그 사이 새로 생긴 원칙**을 적는다(판정 기준·목적지·용어·페르소나 경계). 되살리는 사람이 무엇을 확인해야 하는지 그 자리에서 알 수 있어야 한다.
- 원칙을 새로 세울 때 **"이 원칙이 적용돼야 하는데 죽은 코드라 빠진 곳"**을 별도로 센다.

**일반화**: 코드베이스에는 세 종류가 있다 — 살아 있는 코드, 죽은 코드, **죽은 줄 모르는 코드**. 세 번째가 가장 비싸다. 검증 도구는 두 번째와 세 번째를 구분해 줘야 한다.

---

## 작업원칙 #293 — 앱은 모든 채널에서 같은 말을 해야 한다. 알림 채널이 가장 늦게 정합된다 (2026-07-22)

**사건**: `#290`(대시보드 카운트)을 고친 뒤 **디스코드 일일 알림**을 확인하니 여전히 `status === 'OUT_OF_STOCK'` 기준이었다. 공급처가 끊긴 상품은 앱 status가 ACTIVE로 남으므로 **알림이 아예 울리지 않았다.**

같은 결함의 **세 번째 인스턴스**였다: 대기함 화면(#278) → 대시보드(#290) → 알림(#293).

**왜 알림이 가장 늦게 발견되나**:
1. 화면은 열면 보이지만 **알림은 조건이 맞아야 발송**된다 — 결함이 있으면 "안 울리는" 형태로 나타나고, **안 울리는 것은 눈에 띄지 않는다.**
2. 알림 코드는 크론·서버에 있어 **브라우저 검증 루틴에서 빠진다.**
3. 발송을 함부로 테스트할 수 없어(운영자에게 실알림) 검증이 미뤄진다.

**왜 가장 위험한가**: 알림은 **운영자가 앱을 열지 않아도 받는 유일한 채널**이다. 화면이 "처분 대기 3건"이라고 말해도 운영자가 그날 앱을 안 열면 못 본다. 알림이 침묵하면 **가장 급한 건을 통째로 놓친다.**

**규칙**:
1. 판정·집계 기준을 바꾸면 **화면 / 서버 집계 / 알림 채널 세 곳을 같은 턴에** 확인한다. 하나만 고치면 앱이 채널마다 다른 말을 한다.
2. **알림 대상과 이벤트 기록 대상은 다른 축**이다. 이벤트(`productEvent type=OOS`)는 "status가 바뀜"의 사실 기록이므로 status 기준을 유지하고, 알림은 "지금 조치가 필요함"이므로 판정 기준을 쓴다. 둘을 섞으면 이벤트 이력의 의미가 흐려진다.
3. 알림 로직은 **발송 없이 대상 선정만 검증**한다 — 합성 데이터로 필터 로직을 재현하고, 실제 발송은 하지 않는다(운영자에게 실알림이 간다).
4. 알림 경로는 best-effort로 degrade한다(#82). 판정 실패 시 옛 기준으로라도 보내는 게 **아예 안 보내는 것보다 낫다.**

**점검 목록(판정 기준 변경 시)**: 화면 목록 · 대시보드 카운트 · KPI · 알림(디스코드/이메일) · 크론 다이제스트 · 사이드바 배지 · 외부 API 응답.

---

## 작업원칙 #294 — 체크리스트는 만든 다음에 "실제로 돌려야" 값을 한다 (2026-07-22)

**사건**: `#293`에서 "판정 기준 변경 시 점검 목록"(화면·대시보드·KPI·알림·크론·사이드바·외부 API)을 만들었다. 그 목록을 **실제로 한 번 돌렸더니 사각지대 2곳이 더 나왔다**:

| 위치 | 문제 |
|---|---|
| `/api/notifications` | 앱 헤더 **종 알림**이 `status='OUT_OF_STOCK'` — 공급처 단절 상품이 알림에서 누락 |
| `/api/weekly-report` | **주간 리포트** 카운트·대체계획 대상 둘 다 status 기준 |

같은 결함의 **다섯 번째·여섯 번째 인스턴스**였다(#278→#290→#292→#293→#294).

**교훈**: 체크리스트를 **문서에 적는 것과 실제로 돌리는 것은 다른 일**이다. `#293`을 쓰면서 "점검 목록"을 만들었지만 그 순간 목록대로 훑지 않았고, 다음 턴에 돌리자마자 2건이 나왔다. **원칙을 세운 턴에 그 원칙을 자기 코드베이스에 적용해 보는 것**까지가 한 세트다.

**규칙**:
1. 점검 목록을 새로 만들면 **그 턴에 최소 1회 전수 실행**하고 결과를 기록한다. 실행하지 않은 체크리스트는 "다음에 하자"로 남고, 대개 안 한다.
2. 전수 실행은 **기계적 방법(grep 등)으로** 한다. 눈으로 훑으면 이미 아는 곳만 본다.
3. 실행 결과에서 **"정상인 곳"도 분류해 기록**한다. 그래야 다음 사람이 같은 곳을 또 뒤지지 않는다.

**분류 예시(이번 스윕)** — `status === 'OUT_OF_STOCK'` 매치 20곳 중:
- **정상**: 필터 탭(사용자가 status로 거름) · 상태 배지 표시 · status를 **쓰는(write)** 경로 · 이벤트 기록 · 네이버 statusType 축
- **결함**: 알림 대상 선정 2곳

**일반화**: "무엇을 확인해야 하는가"(체크리스트)와 "확인했는가"(실행 기록)는 별도로 관리한다. 전자만 있으면 안심하게 되고, 그 안심이 가장 위험하다.



---

## 2026-07-23 (Desktop) 신규 원칙 #295~#299

### #295 — 처분·생애주기 판정은 단일 함수가 유일 진실
모든 상품 노출 화면은 `deriveLifecycleState(product)` 또는 `decideDisposition()`의 반환값만 소비한다. 화면이 `product.status`/`naver_status_type`을 직접 해석해 자체 분류하면 화면마다 판정이 갈라진다. 2026-07-23 부활소 실측: 배지(InventoryBadge)는 중앙신호를 썼지만 탭/카운트/버튼 노출조건은 자체판정(getReactivationReason)이라 "삭제 권장 배지 + 등록 완료 버튼" 모순 발생. 배지가 중앙신호를 써도 그 화면의 필터/카운트/액션 노출조건이 자체판정이면 이탈이다.

### #296 — 발행여부 분리는 생애주기 파생이어야
꿀통창고(미발행)↔꽃밭돌보기(발행/연동) 분리(스펙 §1·#256)는 화면별 자체 판정이 아니라 lifecycle state에서 파생돼야 한다. 위반 사례(2026-07-23 실측): 좀비 부활소가 "등록 미완료(미발행)" 상품 2건을 노출 — 발행여부 분리가 부활소/좀비감지까지 전파 안 됨. 상품 분류/카테고리 규칙은 단일 파생함수로 수렴시켜 구조적으로 재발 불가로 만든다.

### #297 — sourceGone 연속판정은 스냅샷 타이밍에 취약
`countLeadingNegatives()`(source-gone.ts:29)는 최신부터 훑다 첫 양수에서 즉시 seal → 실단절 중 폴러가 일시적 양수 1건만 읽어도 카운트가 리셋되어 sourceGone이 고착 false로 오판. 테스트 주입 시에도 실 스냅샷과 끼이면 불발(2026-07-23 교정: now/-1d/-2d → now/-1h/-2h로 실 최신 양수 위에 안착). 정책 재검토 대상: "연속 N" vs "최근 M중 N음수" vs "연속+고립 스파이크 1회 허용"(SOURCE_GONE_ROBUSTNESS_TICKET_2026-07-23.md). 판정 로직은 스냅샷 노이즈 내성을 설계에 포함한다.

### #298 — 커넥터 하나가 막히면 즉시 대체 커넥터로 우회
Filesystem MCP가 다운(#26)이어도 **Desktop Commander MCP**(read_multiple_files/write_file/edit_block/start_process)가 살아있으면 .md 읽기·쓰기·git 정찰이 전부 가능하다. 하나의 커넥터/스킬이 막히면 "못 한다"고 보고하기 전에 대체 경로(Desktop Commander·iterm·terminal)를 전수 시도한다. 단, 실제로 못 하는 것을 지어내지는 않는다 — 우회 성공/실패를 실측으로 확인 후 보고.

### #299 — 처분 정합 스모크 테스트 = 회귀 자산
DESKTOP-1 수동검증(qty=-1 3행 주입 → 채널 count=1 → 원복)을 `scripts/smoke-disposition-channels.ts`로 자산화. 판정 로직(source-gone·disposition·발행여부) 변경 시 필수 실행. 안전조건: 대상=등록상품(naverProductId 有)+사전 스냅샷 0건, 위반 시 즉시 abort(파괴적 fallback 없음).

---

## 2026-07-23 (Desktop) 신규 원칙 #300~#302

### #300 — 상태 축(진단)과 권고 축(처방)은 분리한다
"지금 무엇인가"(LifecycleState·배지·필터·카운트)와 "아직 안 한 일"(DispositionAction·주 액션 버튼·알림)은 서로 다른 질문이다. 섞으면 모순이 난다. 근거: `disposition.ts:139`는 이미 판매중지 상품에 `NONE`을 돌려주는데(할 일 없음), 배지까지 사라지면 "공급처 단절"이라는 사실이 소실된다. 반대로 배지 기준으로 버튼을 만들면 이미 조치한 일을 또 시키게 된다. 3초룰의 "배지=진단 / 버튼=처방"이 이 분리의 UI 표현이다.

### #301 — 화면은 보관함(위치 축)과 작업 큐(조건 축) 두 성격으로 나뉜다
- **보관함**: 꿀통창고(미발행) / 꽃밭돌보기(발행). 발행여부로 분리(#296). 모든 상품이 정확히 한 곳에 존재.
- **작업 큐**: 처분 대기함 · 부활소 · 대시보드 할일. 조건 충족 상품만 수집. 위치와 무관하게 중복 등장 가능.
- **단, 작업 큐도 발행 전용이다**: `disposition.ts:129 if (!p.naverProductId) return NONE` — 앱의 확립된 규약. 미발행 상품이 작업 큐에 나타나면 경계 위반(2026-07-23 부활소 `draft_incomplete` 위반 확정).
- 큐에서 항목을 제거할 때는 **그 항목이 갈 곳을 반드시 만든다**(부활소에서 뺀 "등록 미완료"는 꿀통창고 '이어서 작성' 큐로). 제거만 하면 상품이 조용히 사라진다(#56).

### #302 — 설계를 외부 세션에 위임할 때는 도메인 사실을 함께 넘긴다
Cowork가 코드 접근 없이 생애주기를 설계한 결과, 원자 7필드 중 실재 0개·핵심 축(sourceGone) 소실·도메인 오인("화훼 이커머스")이 발생했다(2026-07-23 실측). 설계 위임 시 최소 첨부: (1) 실제 스키마 발췌 (2) 기존 판정 함수 시그니처 (3) 도메인 한 줄 정의 (4) 미사용 개념 목록. 다만 **반환된 설계를 폐기하지 않는다** — 구조적 골격(파생 단일함수·권한 매트릭스·금지조합 테스트)은 도메인과 무관하게 유효하므로, 갭을 브리지 문서로 접합해 살린다. 검증 없는 채택도, 검증 없는 폐기도 둘 다 낭비다.

### #303 — 설계 세션은 "사실 확인 → 없으면 보고"가 착수 조건이다
접근 불가 자체는 문제가 아니다. **접근 불가를 숨기고 그럴듯하게 채우는 것**이 사고를 만든다. 2026-07-23 Cowork는 "첨부된 지식 폴더가 비어 있어 완전 독립 설계로 진행"이라 **밝히긴 했으나 그대로 진행**해, 원자 7필드 중 실재 0개인 설계를 산출했다. 규칙: (1) 설계·기획 착수 전 `docs/DOMAIN_FACTS.md` + `CLAUDE.md` 정독 (2) 설계에 쓰는 모든 필드는 **파일·라인 또는 SQL 결과로 실재 확인** (3) 확인 불가 시 설계를 멈추고 **필요한 사실 목록을 요청** (4) 인계 블록에는 항상 `권위 문서 경로 + 착수 전 필독 지시`를 명시한다. 검증 없는 산출은 검증 비용을 뒤로 미룰 뿐이며, 그 비용은 항상 더 크다.

### #304 — 외부 세션은 저장소 폴더에 연결해서 쓴다 (커넥터 설정도 재발방지의 일부)
Cowork/Code 등 외부 레인은 **연결된 폴더의 파일만** 읽고 쓴다. 채팅 프로젝트에서 가져온 Cowork 프로젝트는 리서치 PDF·MD만 들어있고 **실제 코드베이스가 없다** — 이것이 2026-07-23 갭의 물리적 원인이었다. 규칙: **기존 Cowork 프로젝트 설정에서 로컬 폴더를 "추가"**해 `/Users/jyekkot/Desktop/kkotium-garden`을 붙인다(프로젝트 구성은 생성 후 변경 가능 — 신규 생성은 차선. 신규로 만들면 누적 메모리·작업 이력이 끊긴다). 채팅 프로젝트(리서치 자산)는 **링크**로 함께 붙여 병합 없이 참조만 시킨다. 폴더 지침에 "착수 전 docs/DOMAIN_FACTS.md·CLAUDE.md 정독"을 넣는다. 프로세스(인계 문구)만으로 막지 말고 **환경 설정으로 먼저 막는다.**

---

## 2026-07-23 (Desktop) 신규 원칙 #305~#306

### #305 — localhost 통과는 검증 완료가 아니다 (프로덕션 실측이 권위 · #45 확장)
개발서버(`npm run dev`)에서 콘솔 에러 0을 확인한 것은 **컴파일·런타임 스모크**일 뿐 검증 종결이 아니다. 2026-07-23 사례: Code가 부활소·정원창고 브라우저 확인을 "완료"로 보고했으나, 그 시점 로컬 HEAD가 origin/main보다 **5커밋 앞선 미push 상태**여서 프로덕션에는 변경이 전혀 없었다. 규칙: (1) "브라우저 테스트 완료"라고 쓸 때는 **localhost인지 프로덕션인지 반드시 명시**한다 (2) 기능 검증 종결은 **push → Vercel 배포 → 프로덕션 URL 실측** 이후에만 선언한다 (3) Desktop이 Chrome으로 읽는 화면은 항상 프로덕션이므로, 배포 전 화면을 읽고 "반영 안 됨"이라 판단하지 않도록 **먼저 배포 SHA를 확인**한다.

### #306 — 세션 종료 전 push 상태를 반드시 확인한다 (미push 누적 금지)
커밋만 하고 push하지 않으면 (1) 프로덕션에 반영되지 않고 (2) 다른 레인이 최신 코드를 못 받고 (3) 로컬 사고 시 전량 유실된다. 2026-07-23에 5커밋이 미push로 누적됐다. 규칙: Code 세션은 작업 종료 시 `git rev-list --left-right --count HEAD...origin/main`을 실행해 **왼쪽 값이 0이 아니면 push 여부를 반드시 운영자에게 확인**하고, 인계 문서에 미push 커밋 수를 명시한다. Desktop은 세션 개시 점검에 이 항목을 포함한다.

---

## 2026-07-23 (Desktop) 신규 원칙 #307 — 운영자 확정

### #307 — 준비도는 발행 승인이 아니다 (미발행→발행은 사람 검수 필수)
`준비도 %` / `발행 N/8` / `상품명 S등급`은 **필수 항목이 채워졌는지에 대한 체크섬**일 뿐, **그 내용이 팔릴 만한지에 대한 판단이 아니다.** 시스템은 절대 "발행 가능 → 발행하러 가기"로 유도해서는 안 된다.

**워크플로우 (운영자 확정 2026-07-23)**
```
정원 창고(미발행) → [준비도 체크 = 빠진 항목 없음]
   → ★씨앗심기 검수(운영자 필수): SEO 상품명·키워드·썸네일·상세
   → 발행 결정(운영자만)
```
- **예외 1건**: 이미 발행된 좀비 상품 중 **일부 정보 수정 → 튜닝 리셋 → 재활성화**로 끝나는 수준. 신규 검수 대상이 아니라 기존 자산 회복이므로 시스템 처리 가능.
- **경계**: 미발행→발행 = **사람 검수 필수** / 발행됨→재활성화 = 자동화 가능.

**UI 규칙**
1. 미발행 상품의 주 액션은 "발행"이 아니라 **"검수(씨앗심기로 이동)"**이다.
2. 배지·넛지 문구에 발행을 재촉하는 표현을 쓰지 않는다. 예: "발행 가능 N건 — 발행하러 가기" ✗ / **"검수 대기 N건 — 씨앗심기에서 확인"** ○
3. **일괄 발행은 "검수 완료" 표시가 된 상품에만** 열린다. 준비도만으로 일괄 발행 경로를 여는 것은 검수 우회 유도이며 금지.
4. 잘못 발행된 상품은 노출·순위·리뷰까지 오염되는 **비가역 손실**이다. 되돌릴 수 없는 경로일수록 개입점을 앞에 둔다(#46 연장).

> 배경: 2026-07-23 Desktop이 "준비도 84%·8/8이니 발행하러 가기 배지를 달자"고 제안 → 운영자 정정. 준비도 통과를 발행 승인으로 오독한 사례.

---

## 2026-07-23 (Desktop) 신규 원칙 #308

### #308 — 배포 후 첫 확인은 반드시 강제 새로고침(캐시 무효화) 후에 한다
2026-07-23 실사고: F1/F2 수정이 배포(`543f38b`, SHA 3중 확인)됐음에도 프로덕션 화면이 **구버전을 렌더**해 Desktop이 "수정 실패"로 오판할 뻔했다. 코드·DB·API 응답을 전부 실측해 조건이 모두 충족됨을 확인한 뒤 **강제 새로고침(reload)** 하자 즉시 정상 동작(부활소 판매중지 1건, 배지 "검수 대기 2건")이 확인됐다.

**규칙**
1. 배포 SHA 일치를 확인했더라도, 화면 검증 전에 **reload를 먼저 수행**한다. reload 없이 본 화면으로 "미반영" 판정을 내리지 않는다.
2. "안 고쳐졌다"고 결론 내리기 전 점검 순서: **배포 SHA → 강제 새로고침 → DB 실측 → API 응답 실측 → 코드 경로**. 이 중 하나라도 안 보고 결론 내면 오판이다.
3. **운영자도 같은 함정에 빠진다.** 배포 완료를 알릴 때는 "새로고침(Cmd+Shift+R) 후 확인"을 함께 안내한다.
4. 향후 개선 후보: 배포 버전 변경 시 앱이 감지해 "새 버전이 있어요 — 새로고침" 안내를 띄우는 것. 1인 운영자가 구버전 화면을 보고 잘못 판단하는 것을 구조적으로 차단한다.

> 교훈: 코드가 맞고 데이터가 맞는데 화면이 틀리면, 화면을 의심하기 전에 **내가 보고 있는 화면이 최신인지**를 먼저 의심한다.

---

## 2026-07-23 (Desktop) 신규 원칙 #309~#310

### #309 — API 응답 검증은 브라우저 렌더 텍스트가 아니라 원본(curl)으로 한다
2026-07-23 실사고: Desktop이 Chrome으로 `/api/version`을 읽었더니 `{"샤":"bbc1a97..."}`가 나와 "JSON 키가 한글로 깨진 버그"로 오판할 뻔했다. **Chrome 자동번역이 JSON 키까지 번역**한 것이었고, `curl` 원본은 `{"sha":"..."}`로 정상이었다.

**규칙**
1. API 엔드포인트 검증은 반드시 `curl`(또는 fetch 원본 파싱)으로 한다. 브라우저 주소창으로 열어 화면 텍스트를 읽고 판단하지 않는다.
2. 브라우저 확장·자동번역·리더모드는 **DOM 텍스트를 변조**할 수 있다. 화면 텍스트는 "사용자가 보는 것"의 근거지 "서버가 보낸 것"의 근거가 아니다.
3. 이상 징후를 발견하면 **최소 2개 경로로 교차 확인**한 뒤 결함을 선언한다(브라우저 + curl, 또는 화면 + DB).

### #310 — 검증 불가는 "통과"가 아니라 "미검증"으로 기록한다
2026-07-23: 새 버전 배너의 **양성 동작**("다른 버전일 때 실제로 뜨는가")을 fetch 가로채기 + 이벤트 강제 발생으로 시도했으나, 60초 스로틀과 React ref 타이밍 때문에 **기능 결함인지 테스트 방법 부적합인지 구분할 수 없었다.**

**규칙**
1. 확인하지 못한 것은 **PASS로도 FAIL로도 적지 않는다.** `미검증(사유·재검증 시점)`으로 명시한다.
2. 자연 발생 시점에 검증되는 기능(예: 배너는 다음 배포 시)은 **다음 세션 필수 확인 항목**으로 인계에 올린다.
3. "구현했고 빌드가 통과했다"는 **양성 동작의 증거가 아니다.** 음성(오탐 없음)만 확인하고 양성을 확인하지 않으면 절반만 검증한 것이다.
4. 테스트 자체가 관측을 방해할 수 있음을 인지한다(스로틀·캐시·ref 타이밍). 인위적 주입이 실패했다고 기능 결함으로 단정하지 않는다.

---

## 2026-07-23 (Desktop) 신규 원칙 #311

### #311 — 게이트는 "화면"이 아니라 "경로"에 건다
검수·승인 같은 게이트를 특정 화면에 만들어두는 것만으로는 우회를 막지 못한다. **그 화면을 거치지 않는 경로가 하나라도 있으면 게이트는 없는 것과 같다.**

**2026-07-23 실측 사례 (F3)**
- 검수 화면 `/products/[id]/preview`("발행 전 검수")는 `canPublish = readinessOk && canRegister && imageWarningCount === 0`을 계산해 잘 만들어져 있다.
- 그러나 **최초 발행 경로**(`NaverRegisterModal`)는 이 화면을 전혀 거치지 않으며, `publish-preview` 참조가 0건이다.
- **일괄 발행**(`/api/products/batch-register`)은 `productIds`만 받아 발행하며 readiness/canPublish 검사가 없다.
- 즉 검수 화면의 발행 버튼은 **재발행(`/update`) 전용**이고, 미등록 상품엔 409를 반환한다. "경고 0건"과 "실제 발행 가능"이 **분리된 두 경로**였다.

**규칙**
1. 게이트를 설계할 때는 먼저 **그 행위에 도달하는 모든 경로를 열거**한다(단건 UI·일괄 UI·API 직접 호출·크론).
2. 게이트는 UI가 아니라 **서버 진입점(API route)**에 둔다. UI 게이트는 안내용, 서버 게이트는 강제용.
3. 경로가 여러 개면 **공통 함수 하나를 모든 경로가 호출**하게 한다(#295 연장).
4. "화면을 만들었다"는 게이트 완료의 증거가 아니다. **우회 경로 0건을 확인**해야 완료다.

---

## 2026-07-24 (Desktop) 신규 원칙 #312~#313

### #312 — 미커밋 파일의 출처를 추정하지 말고 확인한다
2026-07-24: Code가 작업 중 `docs/README.md`·`CLEANUP_PLAN_2026-07-23.md`·`PUBLISH_REVIEW_GATE v2`를 발견하고 **"Desktop 추정"**이라 보고했으나, 실제로는 **Cowork가 쓴 파일**이었다(Cowork 보고서에 동일 3파일 명시). 다행히 Code가 손대지 않고 플래그만 올려 사고는 없었다.

**규칙**
1. 내가 만들지 않은 미커밋 파일을 발견하면 **손대지 않고 보고**한다(Code의 판단은 옳았다).
2. 출처는 **추정하지 말고** 각 레인의 최근 보고서와 대조해 확인한다. 잘못된 출처 표기는 트래커에 남아 나중에 잘못된 인과를 만든다.
3. 3레인이 같은 시간대에 같은 저장소를 만지므로, **인계 블록에 "이번에 내가 만들 파일 목록"을 미리 명시**해 충돌·오인을 예방한다.

### #313 — 문서는 자산이 아니라 비용이다 (맥락 예산)
Chroma 실험(194,480회 호출, 18개 모델)에서 **집중된 300토큰 프롬프트가 전체 113k 토큰보다 모든 항목에서 더 좋았고, Claude 계열에서 격차가 가장 컸다.** 논리적으로 정리된 자료 더미가 뒤섞은 것보다 오히려 나빴다. 어텐션은 **기호의 유효 여부를 구분하지 못하므로**, 살아있는 폴더에 있는 낡은 문서는 폐기됐더라도 계속 성능을 깎는다("색인병").

**규칙**
1. 문서를 늘리기 전에 **"이걸 지우면 실수하게 되는가"**를 먼저 묻는다. 아니면 만들지 않는다.
2. `CLAUDE.md` **≤200줄**(목표 150). 초과하면 즉시 `.claude/rules/`(paths 범위 지정)로 분산한다. `@import`는 그대로 펼쳐지므로 절약 효과가 **없다**.
3. 세션 시작 필독은 **4문서로 고정**. 늘리지 않는다.
4. 낡은 문서는 **지우지 말고 물리적으로 격리**(archive)한다. 대체 시 `> SUPERSEDED BY:` 표기 필수.
5. 기계가 반복 갱신하는 상태는 **JSON**에 둔다(모델이 Markdown보다 덜 덮어쓴다 — Anthropic 실험 근거).

---

## 2026-07-24 (Desktop) 신규 원칙 #314

### #314 — 문서 정리에서 "삭제"는 별도 승인 대상이다 (추가·이동과 다르게 다룬다)
문서 작업은 위험도가 완전히 다른 세 종류다. 하나로 묶어 처리하면 사고가 난다.

| 유형 | 예 | 위험 | 승인 |
|---|---|---|---|
| **추가** | 새 문서·원칙 append·rev 삽입 | 없음(맥락 비용만) | 불필요 |
| **이동/격리** | handoff → archive/ | 없음(git·디스크에 잔존) | 불필요 |
| **삭제/재작성** | CLAUDE.md 다이어트·통합 시 줄 제거 | **규칙 소실** | **필수** |

**삭제 절차 (예외 없음)**
1. **삭제 전 전문 보존**: `docs/archive/<파일명>_FULL_YYYY-MM-DD.md`로 원본 통째 저장.
2. **삭제 후보 목록을 먼저 제시** → 운영자 확인 → 그다음 실행. 임의 판단으로 규칙을 지우지 않는다.
3. **삭제 후 검증**: 지운 규칙이 다른 문서에 살아있는지 grep으로 확인하고 **결과를 보고**한다.
4. 파일을 v2로 재작성할 때는 **같은 파일명에 덮어쓰지 말고** 구본에 `> SUPERSEDED BY:` 표기 후 새 파일을 만든다(DOCS_STANDARD §3).

**2026-07-24 감사 결과(참고)**: 이번 문서 정리 사이클에서 파일 삭제 0건, 원칙 수정·삭제 0건, 실제 줄 삭제는 2곳(DOMAIN_FACTS 중복 통합 31줄 — 내용은 통합본에 전부 잔존 / PUBLISH_REVIEW_GATE v1→v2 재작성 89줄 — git `7858694`에서 복구 가능). 핵심 규칙("일괄 발행은 검수 완료 상품에만")은 DOMAIN_FACTS:67 · #307 · PRODUCT_LIFECYCLE_FLOW 3곳에 잔존 확인.

---

## 2026-07-29 (Desktop) 신규 원칙 #315~#316

### #315 — 새 파일·규칙은 "없으면 어떤 반복 문제가 생기는가"에 답할 수 있을 때만 만든다
`.claude/` 자산(Skill·Agent·Hook·Rule)과 문서 모두 동일하다. 답이 모호하면 **아직 만들지 않는다.** 만든 뒤에는 소유 기준을 함께 남긴다: Skill=목적·입력·산출물·**마지막 검토일** / Hook=트리거·실행명령·**실패 시 동작** / Settings=변경 이유와 승인자.

**우리 프로젝트 특칙 — hooks는 도입하지 않는다.** 일반 프로젝트에서도 "자동 실행 위험"으로 신중 대상인데, 우리는 훅이 네이버 PUT/POST를 자동 실행하면 **비가역 사고**(#46), 디스코드 훅은 **실제 알림 발송**이 된다. 도입이 필요하면 반드시 ADR로 근거·범위·실패 시 동작을 먼저 확정한다.

### #316 — 게이트가 둘 이상이면 판정은 AND, 표시는 분리
발행 허용처럼 여러 조건이 걸리는 자리에서는 **차단 판정은 AND로 결합**하되, **셀러에게 보여줄 사유는 각각 구분**한다(#300 상태축↔권고축 분리의 연장).

- 2026-07-29 사례: `publish-gate.ts`("팔 수 있는 물건인가" — 공급 단절/품절)와 신설 `publish-review-gate.ts`("팔아도 되는 상태인가" — 검수 승인)는 **서로 다른 질문**이다. 이름이 비슷하다고 합치면 판정 축이 뒤섞인다.
- 사유를 "발행 불가" 하나로 뭉뚱그리면 셀러가 **무엇을 해야 할지 모른다.** 진단도 처방도 각각 준다.
- **동명 파일에 덮어쓰지 않는다.** 신규 게이트는 새 파일명으로 분리하고, 각 파일 상단에 역할을 한 줄 주석으로 명시한다.

### #316-A — 승인 만료 기준은 "지킬 수 있는 규칙"이어야 한다
검수 승인 신선도(REVIEW_STALE)를 `updatedAt` 전체로 잡으면 **재고 폴러가 돌 때마다 승인이 풀린다.** 그러면 운영자는 매번 재검수를 강요당하고 결국 게이트를 꺼버린다. 승인 만료는 **검수에서 실제로 보는 필드(화이트리스트)** 변경 시에만 발동한다. 규칙은 지킬 수 있어야 지켜진다.

---

## 2026-07-30 (Desktop) 신규 원칙 #317

### #317 — 화면 문구는 발견 즉시 고친다 (개발자 은어·영어 잔존 금지, #262 강화)
화면에 사용자가 보는 모든 텍스트(배지·버튼·툴팁·경고·에러메시지·빈상태문구)에서 개발자 은어·영어 필드명·기술 코드가 그대로 노출되면 **발견한 즉시(작업 목적과 무관해도) 고친다.** 별도 티켓으로 미루지 않는다 — 화면 문구는 발견 비용이 가장 싼 시점이 "지금 보고 있는 이 순간"이기 때문이다.

**금지 패턴** (실사례)
- 영어 필드명 노출: `reviewChecklist`, `NOT_REVIEWED`, `FIELD_ABSENT` 같은 코드 상수가 화면에 그대로 보임
- 기술 용어 직역: "canPublish", "readiness score" 같은 표현
- 이모지·의성어 과다(꼬띠 캐릭터 대사 제외 — 그건 브랜드 보이스로 별도)

**판별 기준** (3초룰 §0 연장)
- 배지·경고는 **명사/형용사**로, 이커머스 실무자가 바로 이해하는 단어. 예: "검수 대기" ○ / "NOT_REVIEWED" ✗
- 버튼은 **동사**로 끝나는 다음 행동. 예: "검수 승인" ○ / "Approve" ✗
- 원인은 **왜(무엇이 문제인지)** → 행동은 **무엇을 하면 되는지**. 코드 사유 상수를 그대로 붙이지 않고 한글 문장으로 번역.

**적용 범위**: 이번 세션 이후 Code·Cowork가 화면 요소를 만들거나 수정할 때마다 발견되는 즉시 적용. 별도 지시 없이도 상시 적용(#40 확장).

---

## 2026-07-30 (2) (Desktop) 신규 원칙 #318

### #318 — 꼬띠 페르소나에 "표면(Surface)" 축 추가 — 판단 표면은 사투리 감탄사 제거
정원사🌷/카우걸🤠 모드축(`KKOTTI_PERSONA_VOICE_GUIDE.md` §1~5, #259)과 별개로, 화면의 목적에 따라 "친밀 표면"(인사·빈상태·성공토스트 — 사투리 허용)과 "판단 표면"(검수·발행 게이트·차단사유·에러 — 셀러가 즉시 판단해야 하는 문구, 사투리 감탄사 제거)을 구분한다.

**사건**: `persona-audit.py`가 "사투리(까꿍|빵야|이랴|어유|에유|해유) 존재 여부"만으로 페르소나 적용 완료를 판정하는 얕은 기준이었다. 그 결과 검수·발행 게이트처럼 정확한 판단이 필요한 화면에도 "이랴, 발행에 실패했어유!" 같은 감탄사가 기계적으로 채워졌다. 캐릭터를 지우지 않으면서(꼬띠 인격은 유지) 판단 표면에서만 톤을 정확·신뢰가는 방향으로 조정하는 것이 해법.

**근본 수정**: `persona-audit.py`에 `JUDGMENT_SURFACE_KEYS` 화이트리스트 추가 — 이 목록에 있는 파일·키는 "사투리 존재"가 아니라 "사투리 부재"가 정상으로 반전 판정된다(#283의 `CUSTOMER_FACING` 제외 패턴과 동일 원리 재사용, #62). 조용히 빠지지 않고 리포트에 "판단 표면 — 사투리 위반" 별도 섹션으로 명시(#283 사례와 동일하게 침묵 제외 금지).

**적용 대상 등록 시**: 새 판단표면 문구(발행/삭제/승인처럼 되돌리기 어려운 액션과 관련된 화면)를 추가할 때는 `JUDGMENT_SURFACE_KEYS`에 등록하고 `KKOTTI_PERSONA_VOICE_GUIDE.md` §6 판별기준("이 문구를 읽고 셀러가 즉시 판단을 내려야 하는가?")으로 재확인한다.

전문: `docs/design/KKOTTI_PERSONA_VOICE_GUIDE.md` §6.

---

## 2026-07-30 (3) (Desktop) 신규 원칙 #319

### #319 — 세션 종료 시 문서 3종 점검을 완료 조건으로 삼는다
의미 있는 작업 단위(기능 구현·버그수정·설계 확정)를 완료로 보고하기 전에 반드시 아래 3가지를 확인한다. 하나라도 스킵했으면 "완료"라고 말하지 않는다.

1. `docs/handoff/CURRENT.md` — 매 세션 종료 시 덮어쓰기(누적 아님, DOCS_STANDARD 만료성 문서 규칙). 다음 세션이 여기서부터 이어받는다.
2. `docs/plan/PARALLEL_WORK_TRACKER.md` — 새 rev 블록 추가(무엇을 했는지 누적 기록, 삭제 없이 append).
3. `docs/plan/PRINCIPLES_LEARNED.md` — 새로 확립된 규칙이 있으면 번호 등재.

**사건**: 2026-07-30 세션에서 CLAUDE.md 축소 2단계와 `#311` 발행게이트 배선을 "미완료"로 오판해 재작업을 준비하다가, git log·grep 실측으로 **둘 다 이미 완료돼 있었음**을 발견했다(`docs/handoff/CURRENT.md` 2026-07-30 세션 기록 참조). 원인은 `docs/plan/TASK_BRIDGE.md` §3-A 작업큐 보드가 2026-07-22 이후 갱신되지 않고 방치된 것 — 그 사이 20개 이상 커밋이 진행됐는데 보드는 낡은 스냅샷 그대로였다.

**근본 수정**: 문서 갱신을 "여유 있으면 하는 것"이 아니라 **작업 완료의 정의에 포함**시킨다(#319). 동시에 **착수 전에는 인계 문서의 "미완료/대기중" 표기를 그대로 믿지 않고 실제 코드·git log로 먼저 확인**하는 것을 상시 원칙으로 굳힌다 — 문서가 아무리 잘 갱신돼도 다음 세션 사이에 다른 레인(Code/Cowork)이 먼저 끝냈을 가능성은 항상 있다.

**AI 인프라 확장 시 재검토**: 스킬·서브에이전트·MCP 서버 등을 새로 도입할 때는 그 도구가 이 문서 3종 점검 규율과 충돌하지 않는지 확인한다(예: 자동으로 문서를 쓰는 서브에이전트를 붙일 경우, 한글 sentinel 검증과 덮어쓰기 규칙이 그대로 적용되는지). CLAUDE.md "절대 금지·원칙 인덱스" 섹션에 이 재검토 의무를 명시해둠.

---

## 2026-07-30 (4) (Desktop) 신규 원칙 #320

### #320 — 브랜치·머지 리듬: 저녁 세션 우선 검토, 짧은 브랜치 수명
운영자+Claude 협업 환경에서 feature 브랜치를 언제 main에 합칠지에 대한 운영자 확정 규칙.

**규칙**: 저녁 작업 세션 때 그날 만든 feature 브랜치를 **우선적으로 검토** — 문제없으면 그 자리에서 main에 merge한다. 저녁 시간이 아니어도 급하거나 중요한 판단이 필요한 사안은 그때그때 알린다. **브랜치를 며칠~몇 주 방치하지 않는다.**

**근거**: 업계 조사(2026-07-30) — CI/CD·트렁크 기반 개발 표준 문헌들이 공통적으로 "브랜치 수명은 짧게, 하루~며칠 이내 merge"를 권장한다. 오픈소스 데이터 기준 merge의 약 20%가 충돌로 이어지고 그중 75% 이상이 개발자가 멈춰서 로직을 재검토해야 하는 수준 — 브랜치가 오래 살수록 이 위험이 커진다("새싹셀러라 제대로 만드는 게 중요하다"는 운영자의 신중함과, 업계의 "짧은 브랜치" 권장이 상충하는 것처럼 보이지만 실제로는 상충하지 않는다: 신중한 검토는 브랜치를 오래 묵혀서가 아니라 merge *전에* 철저히 검증해서 얻는다).

**적용**:
- 세션 브리핑(CLAUDE.md STEP 2)에 push된 미merge 브랜치가 있으면 "며칠째 대기 중"인지 반드시 포함.
- 저위험(문서만, 소규모 문구수정 등)은 main 직접 push, 화면 재설계·다중파일 묶음은 feature 브랜치 — 최종 PR 검토·merge 승인은 항상 운영자 본인.
- AI 인프라(스킬·서브에이전트 등) 도입 시 이 리듬과 충돌하지 않는지 재검토(#319와 동일한 재검토 트리거를 CLAUDE.md에 명시).

전문: `CLAUDE.md` "HOW — 작업 흐름 규칙" §브랜치·머지 리듬.

---

## 2026-07-30 (5) (Desktop) 신규 원칙 #321~#322

### #321 — "에이전트"는 3계층으로 구분해 말한다 (개념 혼동 방지)
운영자와 개발 레인이 "에이전트"를 다르게 이해하면 몇 주를 낭비한다. 이 프로젝트에서는 항상 아래 셋 중 무엇인지 명시한다.

| 계층 | 정체 | 위치 | 24시간 자동 실행 |
|---|---|---|---|
| A. Claude Code 서브에이전트 | 개발 보조 AI | `.claude/agents/*.md` | ❌ (세션 중에만) |
| B. Desktop MCP 도구 | 리서치·검증 AI | Claude Desktop | ❌ (대화 중에만) |
| C. 앱 내부 파이프라인 | 프로덕션 코드 | `src/lib/` + Vercel Cron | ✅ |

**핵심**: 운영자가 "내 일을 대신하는 에이전트"라고 할 때 원하는 것은 거의 항상 **C**다. A·B는 C를 *만드는* 도구일 뿐 C를 대체하지 못한다(아침 8시 디스코드 알림을 A나 B가 보낼 수는 없다). 새 기능 요청을 받으면 먼저 "이건 A/B/C 중 무엇인가"를 확정하고 시작한다.

**부수 규칙**: MCP(계층 B)의 최적 활용은 "실시간 자동화"가 아니라 **1회성 리서치로 풍부한 시드 데이터셋을 구축**하고 그것을 앱(C)이 매일 사용하게 하는 것이다(예: 시즌 캘린더). 근거: wikidocs 「Claude 기초부터 고급까지 100」 092.

### #322 — 병렬 작업은 write set(수정 파일 집합) 기준으로 나눈다
두 작업을 동시에 진행해도 되는지 판단하는 기준은 "주제가 다른가"가 아니라 **"수정하는 파일이 겹치는가"**다. write set이 겹치지 않으면 병렬 안전, 겹치면 순차.

**작업 분해 시 반드시 명시할 7가지**(wikidocs 098):
1. 목표 2. 담당 레인 3. 읽을 파일 4. **수정 가능 파일(write set)** 5. **수정 금지 파일** 6. 산출물 7. 병합 시 충돌 위험

**병렬화하면 안 되는 것**: 같은 파일 동시 수정 · 요구사항이 모호한 작업 · DB migration처럼 순서가 중요한 작업 · 인증/권한/발행처럼 단일 책임자 판단이 필요한 작업.

**동시 작업 중 필수 지시문**(wikidocs 097): "다른 레인이 동시에 작업 중일 수 있다. 담당 write set 밖의 파일은 되돌리거나 정리하지 말 것. 범위 밖 변경을 발견하면 수정하지 말고 요약만 남긴다." — 한 레인이 다른 레인의 변경을 "불필요한 diff"로 판단해 되돌리는 사고를 막는다.

**적용**: 작업 스케줄은 `docs/plan/WORK_SCHEDULE_BOARD.md`가 단일 권위. 각 작업에 write set과 PARALLEL-OK 여부를 표기한다.

---

## 2026-07-31 (Desktop) 신규 원칙 #323

### #323 — 부재 증명은 전수 검색으로 한다 (head/tail 잘린 출력으로 "없다" 단정 금지)
"X가 연결돼 있지 않다", "그 코드는 없다" 같은 **부재(不在) 결론**은 시스템 진단의 방향을 통째로 바꾼다. 따라서 부재를 주장하기 전에는 반드시 **잘리지 않은 전수 검색**으로 확인한다.

**사건(2026-07-31)**: Desktop이 `grep ... | head -20`으로 `cron/daily/route.ts`를 조사한 뒤 "소싱 추천 엔진은 어떤 크론에도 연결돼 있지 않다"고 `CURRENT.md`에 기록했다. 그러나 실제로는 **440행에 E-7 소싱 블록이 존재**했고(파일 총 484줄), `head -20`이 407행에서 잘려 보이지 않았을 뿐이다. 이 오진 위에 P1 스펙 전체가 세워졌고, Code 레인이 구현 중 실측으로 발견해 정정했다.

**규칙**:
- 부재를 결론짓기 전 `grep -c`(건수) 또는 `wc -l`(전체 줄수)로 **탐색 범위가 파일 전체를 덮었는지** 먼저 확인한다.
- 파일 크기를 모르는 상태에서 `head`/`tail`/`sed -n 'A,Bp'`로 본 결과만으로 "없다"고 쓰지 않는다.
- 부재 주장은 문서에 기록할 때 **확인 방법을 함께 남긴다**(예: "grep 전수, 매치 0건").

**부수 확인 사실**: 레인 간 교차검증(Desktop 설계 → Code 구현 중 실측)이 이 오류를 잡아냈다. 단일 레인이었으면 잘못된 전제 위에 계속 쌓였을 것이다. **핑퐁 구조의 실효성이 입증된 사례**이며, Code/Cowork가 Desktop 분석과 다른 실측 결과를 발견하면 **즉시 보고하는 것이 정상 동작**임을 확인한다.

---

## 2026-08-01 (Desktop) 신규 원칙 #324

### #324 — 외부 API "실패"는 내 설정을 의심하기 전에 공급자 공지를 실측한다
외부 API가 갑자기 실패하면 "내 키/권한/설정 문제"로 단정하기 쉽다. 그러나 공급자가 API를 종료·변경했을 수 있다. **설정 문제로 결론짓기 전에 공급자 공식 공지를 실측**한다.

**사건(2026-08-01)**: 네이버 쇼핑검색 API가 SE05(404)를 반환하자 Desktop이 "운영자가 개발자센터에서 검색 API 권한을 등록해야 한다"고 단정해 CURRENT.md에 기록했다. 운영자가 "개발자센터에 문제 없어 보인다"고 지적하고 공식 공지 링크를 제공. 확인 결과 **네이버가 2026-07-31자로 쇼핑/책/전문자료 검색 API를 영구 종료**(API HUB 이관 대상에서도 제외)한 것이었다. 운영자 설정 문제가 전혀 아니었다.

**규칙**:
- 외부 API 오류 코드는 그대로 검색해 공급자 공지/변경 이력을 먼저 확인한다.
- "운영자가 X를 설정해야 한다"고 쓰기 전에, 그 설정이 실제 원인인지 실측 근거를 확보한다(#310 연계).
- 공급자 사이트가 web_fetch로 차단되면 `curl -A "Mozilla/5.0"`로 우회해 본문을 확인할 수 있다(이번에 사용).
- API 종료는 **단건이 아니라 그 API를 쓰는 모든 파일에 영향**한다 — 즉시 전수 검색으로 영향 범위를 파악한다(#62). 이번 건은 `searchShopping`/`analyzeCompetition` 소비처 11개 파일.

**부수 효과**: 폐기된 기능을 "더 나은 방식으로 갈아탈" 기회로 삼는다. 쇼핑검색 productCount 기반 경쟁분석은 원래도 부실했고, 검색광고 경쟁지수(살아있음)로 대체하는 것이 더 정확하다. 대응 설계: `docs/design/NAVER_SHOPPING_API_SUNSET_RESPONSE.md`.

---

## 2026-08-04 (Desktop) 신규 원칙 #325~#326

### #325 — 정적 라벨 상수에 "0"·"없음" 같은 기본값을 하드코딩하면, 실제 값이 달라져도 화면은 계속 기본값을 말한다
2026-08-04 실사고: 검수화면(`/products/[id]/preview`)의 "이미지 경고 N" 배지가 `t.gate.imageClean = "이미지 경고 0"`이라는 **정적 문자열**이었다. 아이콘(불리언 `ok`)은 `imageWarningCount===0`으로 정확히 계산됐지만, 라벨 텍스트는 실제 카운트를 절대 반영하지 않고 항상 "0"을 표시했다. 실제 경고가 3건이어도 화면은 "이미지 경고 0"이라 말하면서 아이콘만 빨간 X였다 — 숫자와 색이 모순되는 화면.

**발견 경위**: main merge 직후 프로덕션 브라우저 검증(#305 연장 — 프로덕션 실측) 중 curl 원본 API·브라우저 fetch·DOM 렌더링 **3중 대조**로 확정. API(`gateReasons.imageWarningCount: 3`)와 화면 경고목록(3건 정확 표시)은 일치했는데 배지 텍스트만 불일치했다.

**규칙**:
1. 국제화(i18n) 문자열에 **동적 개수·값이 포함된 라벨**을 만들 때는 정적 완성 문장(`"이미지 경고 0"`)이 아니라 **prefix + 변수 조합**(`"이미지 경고 " + count`)으로 설계한다. 완성 문장 하나만 두면 "그 값일 때만" 맞는 문장이 된다.
2. 상태 배지에서 **아이콘(불리언)과 텍스트(값)를 다른 소스에서 가져오지 않는다.** 아이콘이 실제값을 참조하면 텍스트도 반드시 같은 실제값을 참조해야 한다 — 하나는 계산값, 하나는 상수면 반드시 어긋나는 경우가 생긴다.
3. 이런 결함은 **정상 케이스(카운트=0)에서는 절대 발견되지 않는다.** 0건일 때는 상수와 실제값이 우연히 일치하기 때문이다. 검증할 때는 반드시 **비정상 케이스(카운트>0)를 가진 실제 데이터**로 확인한다.
4. 시스템 레벨 확인(#62): 같은 프로젝트에서 동일 패턴(정적 라벨+미반영 동적값)이 다른 화면에도 있는지 grep으로 전수 확인한다 — 이번엔 이 화면 1곳에 국한됨을 확인.

### #326 — Chrome 확장 자체의 통신 잡음("message channel closed")과 앱 결함을 구분한다
브라우저 콘솔에서 `"A listener indicated an asynchronous response by returning true, but the message channel closed before a response was received"` 에러가 보이면, 이는 대부분 **Chrome 확장 프로그램(자동번역·보안·개발도구 확장 등) 자체의 알려진 통신 패턴**이지 앱 코드의 결함이 아니다.

**판별 기준**:
- 스택트레이스가 `파일:0:0`(위치 정보 없음)이면 확장 프로그램 자체 잡음일 가능성이 높다.
- 앱 자체 네트워크 요청(`read_network_requests`)이 전부 200이면 이 에러는 앱과 무관하다고 결론지어도 된다.
- 웹서치로 이 정확한 에러 메시지를 검색하면 "Chrome 확장이 원인"이라는 다수의 독립 사례가 나온다(2026-08-04 확인).

**규칙**: 이 에러 하나만으로 앱을 의심해 원인 조사에 들어가지 않는다. 반드시 **앱 API 응답(network_requests)을 별도로 확인**해 실제 요청 실패가 있는지부터 본다. #309(API 검증은 curl 원본으로)의 연장 — 브라우저 콘솔의 모든 에러가 앱 신호는 아니다.


---

## 2026-08-05 (Desktop) 신규 원칙 #327

### #327 — 키워드 전문검색 매칭은 "본품 vs 부속품·소모품"을 별도 축으로 판별한다
도매처 API처럼 상품명 키워드로 검색하는 매칭은, 키워드가 상품명에 포함되기만 하면 통과하므로 **본품이 아니라 그 상품의 부속품·소모품·호환용품**이 대량 섞인다. 그중 최저가가 "대표 공급가"로 노출되면 완전히 잘못된 정보가 된다.

**사건(2026-08-05)**: 아침 소싱봇 교체 검증 중, 도매매칭이 "가습기"→"가습기 청소솔" 170원, "제습기"→"습기제거제" 870원, "청소기"→"차량유리 걸레" 1,000원을 최저가 대표로 노출하는 것을 dry-run 실측으로 발견. 기존 필터는 "키워드 글자가 상품명에 포함되는가"만 봐서 부속품이 전부 통과했다. #326(캐리어 90원 이종상품)이 "가격 이탈"(priceOutlier) 축이라면, 이건 "상품 종류가 본품이 아님"(accessoryRisk)이라는 **다른 축**이다 — 부속품은 가격이 정상 범위라 priceOutlier로는 안 잡힌다.

**규칙**:
1. 키워드 매칭 결과에 **본품 판별 축**을 별도로 둔다. 부속품/소모품/호환용품 신호어(필터·브러쉬·커버·거치대·제거제·방향제·리필·호환·전용 등)를 상품명에서 감지해 `accessoryRisk` 플래그를 붙인다. **특정 상품을 하드코딩하지 않는 범용 신호어 사전**으로 만든다(#55 전 상품 공통).
2. **완전 배제가 아니라 후순위+경고**로 처리한다 — 진짜 부속품을 찾는 경우(예: 키워드 자체가 "필터")도 있으므로. 본품(accessoryRisk=false)을 우선 정렬하되, 부속품밖에 없으면 그거라도 노출한다(정보 손실 방지). 키워드 자체가 부속품류면 그 신호어는 무시한다.
3. 이 판별은 **전 채널 정합**(#293)으로 디스코드·웹앱·DB에 동시 반영한다 — 매칭 엔진(단일 함수)을 고치면 세 곳이 동시에 개선되고, 각 표시 지점(embed·위젯)에 경고 배지를 함께 붙인다.

**부수 규칙 — 검색량은 많을수록 좋은 게 아니다**: 블루오션 점수에서 초대형 검색량(월 10만 건 초과)은 대기업·브랜드 각축장이라 1인 새싹셀러에게 오히려 **감점** 요인이다("제습기 월 43만 건"은 블루오션이 아니라 레드오션). "적정 검색량 + 낮은 경쟁"이 진짜 블루오션이다.

**미해결(다음 개선)**: "청소기"→"귀청소기"처럼 키워드는 맞으나 카테고리가 완전히 다른 동음 케이스는 본품 신호어 필터로 못 잡는다. 키워드 확장 시 카테고리 정합성 검사가 별도로 필요하다(#324 SE05로 쇼핑검색 카테고리 API가 죽어 현재는 우회 불가 — 향후 과제).


---

## 2026-08-05 (2) (Desktop) 신규 원칙 #328~#329

### #328 — 새 이동 동선을 만들 때는 목적지가 이미 받는 파라미터를 먼저 찾는다 (수신부 중복 생성 금지)
화면 A에서 화면 B로 데이터를 넘기며 이동시키는 기능(예: 소싱 발굴 → 상품 등록)을 만들 때, **새 쿼리 파라미터를 만들기 전에 목적지가 이미 받는 파라미터가 있는지 먼저 확인**한다. 목적지에 이미 같은 목적의 수신부가 있으면 그것을 재사용한다 — 새 파라미터를 만들면 목적지에도 새 수신 코드를 붙여야 하고, 같은 목적의 경로가 둘이 되어 유지보수가 갈라진다(#62).

**사건(2026-08-05)**: 트랙B 소싱 드로어의 "이 키워드로 소싱 시작" 버튼을 처음엔 `/products/new?sourcingKeyword=X`로 만들었다. 그러나 `/products/new`는 이 파라미터를 받지 않아 아무 일도 일어나지 않았다. grep으로 확인하니 그 페이지는 이미 `prefill`·`prefillName`·`crawlUrl`·`edit`·`focus`·`autoSeo` 등 6개 프리필 파라미터를 받고 있었고, `?prefillName=`이 정확히 상품명 입력란(`setProductName`)을 채우는 기능이었다. seedUrl을 `?prefillName=`으로 바꾸자 새 수신부 코드 없이 즉시 상품명이 채워졌다. 브라우저 실측: `/products/new?prefillName=가습기` → 상품명 input value "가습기" 확인.

**규칙**:
1. 이동 링크·리다이렉트를 만들기 전, 목적지 페이지에서 `searchParams.get(` 또는 `useSearchParams`를 grep해 **이미 받는 파라미터 목록을 확인**한다.
2. 같은 목적의 파라미터가 있으면 재사용한다. 없을 때만 새로 만들고, 그 경우 **수신부도 같은 커밋에 반드시 함께 구현**한다(송신만 만들고 수신을 빠뜨리면 조용히 무동작 — #270 무음실패의 UI 이동판).
3. 이동 기능은 **브라우저로 실제 이동시켜 목적지 상태(입력란 값 등)를 실측**해야 완료다. 링크 URL이 그럴듯한 것은 동작 증거가 아니다.

### #329 — JSX 블록 삽입은 tsc로 구조 정합을 즉시 검증한다 (닫는 태그 어긋남은 눈으로 안 잡힌다)
기존 JSX 트리 중간에 새 요소(버튼·블록)를 삽입할 때, 삽입 위치가 조건부 블록·map·중첩 div의 닫는 태그와 어긋나면 전체 트리가 깨진다. 이 오류는 **문자열 편집 도구의 diff만 봐서는 잡히지 않고**, 반드시 `tsc --noEmit`로 구조 정합을 검증해야 한다.

**사건(2026-08-05)**: 트랙B 드로어 트리거 버튼을 확장 상세 블록 끝에 넣으려 했으나, edit_block이 도매매칭 `map()` 내부의 경고 배지(`⚠️ 부속품?`) 바로 뒤에 삽입해 닫는 태그 5개(가격div·링크a·map·컨테이너div·조건부블록)가 누락됐다. tsc가 `TS17008: JSX element 'div' has no corresponding closing tag` 등 17개 에러로 즉시 잡아냈고, 버튼을 올바른 위치(확장 상세 블록 끝)로 재배치해 해결.

**규칙**:
1. JSX 트리 중간에 요소를 삽입한 직후에는 **반드시 `tsc --noEmit`를 돌린다.** 삽입이 성공한 것처럼 보여도(도구가 "적용됨"이라 보고해도) 구조가 깨졌을 수 있다.
2. 삽입 위치를 정할 때는 **삽입 지점 앞뒤의 닫는 태그 개수와 들여쓰기**를 확인한다 — 특히 `map()` 내부와 조건부 렌더(`{cond && (...)}`) 경계는 헷갈리기 쉽다.
3. tsc 에러의 **첫 번째 위치(줄 번호)를 보고 그 지점의 태그 짝을 직접 확인**한다. "no corresponding closing tag"는 대개 삽입으로 짝이 어긋난 신호다.
4. 이 검증은 build보다 빠른 tsc로 먼저 하고, 통과하면 build로 최종 확인한다(#32).


---

## 2026-08-05 (3) (Desktop) 신규 원칙 #330

### #330 — 이 프로젝트에서 신규 파일은 Desktop Commander:write_file로 만든다 (create_file은 로컬 맥이 아닌 별도 샌드박스에 쓴다)
신규 파일을 만들 때 `create_file` 도구를 쓰면 **로컬 맥 파일시스템이 아니라 별도 샌드박스(bash_tool과 같은 환경)에 기록**된다. 그 결과 iterm·Desktop Commander·`npm run dev`가 보는 실제 로컬 맥에는 파일이 존재하지 않아, 새 API route라면 런타임 404가 난다. 반드시 **`Desktop Commander:write_file`**(디렉토리는 iterm `mkdir -p` 선행)로 만들어야 로컬 맥에 생긴다. 기존 파일 편집(`Desktop Commander:edit_block`)은 로컬 맥에 정상 반영되므로 문제없다.

**사건(2026-08-05)**: 트랙C-1에서 신규 `src/app/api/sourcing-recommend/status/route.ts`(PATCH 낙점 엔드포인트)를 `create_file`로 만들었다. 도구는 "File created successfully"를 반환했고 tsc·build도 통과했으나, 브라우저에서 PATCH 호출 시 404가 났다. 진단 결과: `bash_tool`(`cat`, `find`)로는 파일이 보이는데 iterm `test -f`·Desktop Commander `list_directory`로는 `MISSING_ON_LOCAL_MAC`. 즉 파일은 샌드박스에만 있고 로컬 맥에 없었다. `mkdir -p`(iterm) + `Desktop Commander:write_file`로 로컬 맥에 재생성하자 PATCH가 정상 동작(`ok:true`)했고, build 결과에도 `/api/sourcing-recommend/status` 라우트가 포함됐다.

**왜 tsc·build가 못 잡았나**: 그 route를 호출하는 코드(훅의 `fetch('/api/sourcing-recommend/status')`)는 **런타임 문자열**이라 컴파일러가 파일 존재 여부를 검사하지 않는다. import로 참조되는 모듈이 아니면 tsc·build는 파일 부재를 통과시킨다. 따라서 신규 route는 **브라우저 실호출로만 검증 가능**하다.

**규칙**:
1. **신규 파일 생성 = `Desktop Commander:write_file`만 사용한다. `create_file` 금지.** (디렉토리가 없으면 iterm `mkdir -p` 먼저.)
2. 신규 파일을 만든 직후 **로컬 맥에 실존하는지 iterm `test -f` 또는 Desktop Commander `list_directory`로 확인**한다. `bash_tool`/`create_file`의 성공 메시지는 로컬 맥 존재의 증거가 아니다(별도 샌드박스일 수 있음).
3. 신규 API route는 tsc·build 통과만으로 완료로 보지 않고 **브라우저 실호출(PATCH/GET 등)로 200 응답을 실측**해야 완료다(#310 양성 검증 연장).
4. `bash_tool`은 별도 샌드박스이므로 **로컬 맥 파일 검증에 신뢰하지 않는다** — 실제 로컬 맥은 iterm·Desktop Commander가 본다. 두 환경이 갈리면(#279 "빌드는 되는데 dev만 안 됨"의 파일시스템판) 환경 차이를 먼저 의심한다.

**부수 효과**: 기존 파일 편집은 `edit_block`이 로컬 맥에 정상 반영되므로 이번 트랙C-1의 위젯·훅·타입 수정(3개 파일)은 전부 로컬 맥에 올바르게 적용됐다(git status로 확인). 문제는 오직 `create_file`로 만든 신규 1개 파일이었다.

**메타 교훈(이 원칙을 쓰다 재발)**: 이 #330을 처음 `Filesystem:write_file`로 파일 끝에 추가하려다, 그 도구가 **전체 덮어쓰기**로 동작해 #254~#329 전체(896줄)를 19줄로 날렸다. `git checkout`으로 즉시 복구 후 `Desktop Commander:edit_block`(마지막 줄 앵커 이어붙이기)으로 재등재했다. **대용량 MD에 내용을 추가할 때는 `write_file` 전체 덮어쓰기가 아니라 `edit_block` 앵커 방식으로 이어붙인다**(#29 한글 대용량 MD 안전 규칙의 연장 — 전체 덮어쓰기는 마지막 write가 이전 전체를 담고 있을 때만 안전하다).


---

## 2026-08-06 (Desktop) 신규 원칙 #331

### #331 — 목록 조회 API는 날짜 범위(gte)가 아니라 "가장 최신 1회분"으로 조회한다 (날짜 경계·타임존 중복 방지)
하루 1회 배치로 재생성되는 데이터(소싱 스캔 등)를 조회하는 API가 `where: { date: { gte: todayStart } }` + `take: N`으로 조회하면, 날짜 경계·타임존 차이로 **어제/오늘 레코드가 DB에 함께 남아 있을 때** 두 날짜에서 상위 rank끼리 섞여 **같은 키워드가 중복 반환**된다.

**사건(2026-08-06, 트랙C-2 검증 중 발견)**: `/api/sourcing-recommend` GET이 `date: { gte: todayStart }` + `take: 5`로 조회했다. POST(스캔)가 `deleteMany({date: todayDate})`로 **오늘 것만** 지우고 과거는 안 지워 8/3~8/6이 DB에 누적된 상태였고, 그 결과 GET이 "가습기"를 8/5·8/6 두 건으로 반환했다(rank=0끼리 교차). 위젯의 setStatus는 recordId 기준이라 낙점 자체는 정확했지만, 같은 키워드가 화면에 2번 렌더돼 혼란을 줬다.

**근본 수정**: `gte todayStart` + `take` 대신 **"DB에서 가장 최신 date 하나"를 먼저 구하고(findFirst orderBy date desc), 그 date의 레코드만 findMany**한다. 소싱 스캔은 하루 1회 배치이고 POST가 매번 같은 날짜를 deleteMany 후 재생성하므로 "최신 date 하나 = 오늘의 스캔 1회분"이라는 정확한 의미가 된다. 날짜 자체를 조건으로 넣지 않아 타임존 경계 문제를 원천 차단한다.

**규칙**:
1. 하루 1회(또는 N회) 배치로 갱신되는 데이터를 조회할 때, "오늘 이후 전부"(gte)로 긁지 말고 **"가장 최근 1회분"**만 조회한다 — 배치 경계에서 이전 회차가 남아 있으면 gte는 반드시 섞인다.
2. 날짜를 쿼리 조건으로 직접 넣으면(gte todayStart) **서버 타임존과 DB 저장 타임존(UTC) 차이**로 경계가 어긋난다. 실제 날짜 값을 DB에서 읽어 그 값으로 매칭하면 타임존과 무관해진다.
3. **누적 자체는 별도 문제다.** GET을 최신 1회분으로 고쳐 화면은 안전해지지만, POST가 과거를 안 지우면 DB는 계속 는다 — 조회 수정과 별개로 오래된 레코드 정리(N일 이전 deleteMany 또는 주간 크론)를 함께 설계한다.

**부수 확인(UI는 원래 안전)**: 이 사건에서 위젯은 항상 recordId로 상태를 변경했으므로 낙점 로직 자체는 정확했다. 문제는 오직 GET이 같은 키워드를 2번 렌더한 것 — "데이터 조회 계층의 중복"과 "쓰기 계층의 오작동"은 다른 축이며, 조회 중복이 곧 쓰기 오류는 아니다.


---

## 2026-08-06 (Desktop) 신규 원칙 #332

### #332 — 화면에 노출되는 값이 내부 코드면 표시 계층에서 한글 라벨로 변환한다 (코드값은 데이터로만 유지)

플랫폼 코드·enum·상태값 같은 내부 식별자를 화면에 **그대로** 렌더하면 셀러(운영자)는 그 약어가 무슨 뜻인지 즉시 알 수 없다. 화면 문구는 개발자 은어가 아니라 셀러 실무 용어여야 한다(#317). 내부 코드값은 데이터 계약(DB·API)에 그대로 두고, **표시 계층에만 한글 라벨 매핑 함수**를 둔다.

**사건(2026-08-06, 소싱 위젯 UX 종합점검 중 프로덕션 실측)**: 소싱 위젯 도매 매칭 목록의 플랫폼 배지가 내부 코드 `DMM`·`DMK`를 그대로 노출했다(DMM=도매매, DMK=도매꾹). 셀러는 이 약어로 어느 도매처인지 알 수 없다. 카드·드로어 두 화면, 배지·헤더 4곳에서 동일 노출.

**근본 수정**: `getPlatformLabel(platform)` 단일 헬퍼(카드·드로어 공유, #62 단일 권위)를 두어 DMK→도매꾹(초록)·DMM→도매매(빨강)로 변환. 배지 `{w.platform}` → `getPlatformLabel(w.platform).label`. **DB·API의 코드값('DMM'|'DMK')은 무변경** — 데이터 계약은 그대로 두고 사람이 읽는 표현만 표시 계층에서 만든다.

**연장 발견(#325 계열, 빈 괄호)**: 배포 후 프로덕션 검증에서 헤더가 "도매 매칭 ()"처럼 빈 괄호로 나왔다. 원인은 헤더가 참조하던 요약 필드 `wholesalePlatforms`를 API가 실제로는 안 채운다는 것(항상 undefined) — 실제 플랫폼 정보는 목록 데이터 `wholesaleMatches[].platform`에 있었다. **죽은 요약 필드 대신 실제 목록 데이터에서 유도**(new Set 고유화 후 라벨 매핑)하고, 없으면 괄호 자체를 생략하도록 수정.

**규칙**:
1. 화면에 나가는 값이 내부 코드/enum/상태값이면 표시 계층에 라벨 매핑 함수를 두고, 여러 화면이 그 단일 함수를 공유한다(#62·#317). 코드값 자체는 DB·API에 그대로 유지한다(데이터 계약 무변경).
2. 표시용 "요약 필드"(예: platforms 배열)를 만들었으면 그 필드가 실제로 채워지는지 확인한다. 안 채워지면 죽은 필드다(#325) — 대신 실제 목록 데이터에서 유도한다.
3. 데이터가 없을 때 "(  )"·"[]" 같은 빈 껍데기를 남기지 말고 해당 장식(괄호 등) 자체를 생략한다.
4. 이런 표시 결함은 tsc/build로 안 잡힌다(타입은 맞고 렌더 문자열만 이상). **프로덕션 브라우저 실측**으로만 확인된다(#45/#275) — 배포 후 실제 화면을 눈으로 본다.




---

## 2026-08-08 (Code) 신규 원칙 #333

### #333 — 크론 미실행 의심 시 대시보드 "Enabled" 표기를 믿지 말고 Vercel Runtime Logs로 실제 invocation 유무를 직접 조회한다

Vercel 대시보드의 Cron Jobs가 "Enabled"·정상 등록으로 보여도 **실제로 스케줄대로 실행됐는지는 별개 사실**이다. Hobby 플랜은 대시보드 UI 로그 보관이 1시간이라 "어젯밤 크론이 불렸는지"를 UI만으로는 확인할 수 없다고 오판하기 쉽다.

**사건(2026-08-08, 아침 소싱 알림 미발송 조사)**: Desktop이 대시보드에서 크론 "Enabled"·`0 23 * * *` 정상 등록을 확인했으나, 실제 발송은 안 됐다. Code가 Vercel MCP `get_runtime_logs`(group_by=statusCode, query=경로)로 직접 조회한 결과 **등록된 크론잡 5개 전부 지난 7일간 예정 스케줄 시각의 실행 로그가 0건**임을 확인 — "등록됨"과 "실행됨"이 다른 사실이었다.

**규칙**:
1. Vercel MCP의 `get_runtime_logs`/`get_runtime_errors`는 대시보드 UI가 표기하는 1시간 제한보다 **긴 기간(최소 7일)** 조회 가능하다 — 크론·백그라운드 작업 미실행 의심 시 이 도구를 1순위로 사용한다.
2. 경로별 `group_by: requestPath` 또는 `query`로 필터링해 "그 경로가 예정 시각에 실제로 찍혔는지" 직접 대조한다 — 등록 상태(dashboard "Enabled")만으로 실행을 단정하지 않는다.
3. 4xx/5xx 로그조차 0건인데 요청도 0건이면, 앱 코드(route.ts) 내부 실패가 아니라 **Function에 도달하기 전 엣지 단(Deployment Protection·SSO 등)에서 막혔을 가능성**을 함께 의심한다(`get_project_deployment_protection`으로 확인).

---

## 2026-08-10 (Code) 신규 원칙 #334~#335

### #334 — AI/캐시 파이프라인에서 생성 로직을 고쳐도 이미 저장된 캐시는 안 바뀐다 — 캐시가 생성 호출보다 먼저 조회되는 구조인지 항상 확인한다

생성 로직(AI 프롬프트 등)의 버그를 고쳐도, 그 결과를 캐싱해 재사용하는 구조라면 **이미 캐시에 저장된 과거 오답은 새 로직이 실행조차 되지 않아 계속 서빙된다.**

**사건(2026-08-10, 씨앗심기 카테고리 "가구/인테리어" 대량 오분류 수정 중 발견)**: `api/category/suggest/route.ts`는 `dome_category_cache`(name_hash/dome_code)를 AI 호출보다 먼저 조회한다. AI 프롬프트의 강제 폴백 지시("모르면 가구/인테리어로")를 삭제했지만, 이미 스캔된 적 있는 상품명은 캐시 히트로 즉시 반환되어 **수정 전 프롬프트가 만든 오답을 그대로 계속 서빙**했다(브라우저 curl 재현 중 `cacheHit: "name_hash"` 확인, 신규 상품명으로 바꾸자 비로소 새 프롬프트 경로 확인됨).

**규칙**:
1. 캐시를 쓰는 생성 파이프라인의 버그를 고칠 때는 "캐시가 생성 호출 앞에 있는지" 코드 흐름을 반드시 확인한다. 앞에 있다면 수정은 **신규 입력에만** 적용되고, 기존 캐시 오염은 별도 정리(무효화/재계산)가 필요하다 — 코드 수정 하나로 끝났다고 보고하지 않는다.
2. 검증 시 캐시 히트 여부(`cacheHit` 필드 등)를 반드시 확인한다. 캐시 히트로 "고쳐진 것처럼" 보이거나 "안 고쳐진 것처럼" 보이는 결과 둘 다 착시일 수 있다 — 신규(캐시 미스) 입력으로 재현해야 실제 수정 효과를 검증한 것이다.
3. 캐시 오염 정리(기존 오답 행 삭제/재계산)는 DB 쓰기이므로 운영자 결정 없이 임의 실행하지 않는다 — 발견 사실만 보고하고 방침은 위임한다.

### #335 — "정보가 리셋된다"는 UX 버그는 복원(hydrate) 로직이 아니라 저장(write) 로직의 데이터 유실이 원인일 수 있다 — 저장 경로가 실제로 무엇을 쓰는지부터 확인한다

폼을 다시 열었을 때 필드가 비어 보이면 보통 "복원 로직이 그 필드를 안 읽는다"고 의심하기 쉽지만, **애초에 저장 시점에 그 필드가 DB에 기록되지 않았을 수 있다.** 복원 로직은 무죄이고 저장 로직이 원인인 경우, 복원 코드를 아무리 고쳐도 증상은 그대로다.

**사건(2026-08-10, 씨앗심기 이동 시 정보/이미지 리셋 버그 조사)**: `products/new/page.tsx`의 `?edit=` 로더(`productFormHydrate`)는 DB에 있는 값을 빠짐없이 정확한 컬럼명으로 복원하고 있었다(버그 없음). 실제 원인은 `api/products/import/route.ts`가 네이버 원상품 상세를 가져오면서 `name`/`salePrice`/`mainImage`/`status` **4개 필드만 골라 저장**하고, 이미 응답에 들어있던 카테고리·설명·추가이미지·태그를 전부 버리고 있었던 것 — 복원할 데이터 자체가 DB에 없었다.

**규칙**:
1. "저장했다가 다시 열면 비어있다" 류 버그는 먼저 DB를 직접 조회해(또는 저장 API 응답을 확인해) **그 필드가 애초에 저장돼 있는지**부터 확인한다. 있는데 안 읽으면 복원 버그, 없으면 저장 버그 — 두 원인은 수정 위치가 완전히 다르다.
2. 외부 API(네이버 등) 응답을 DB로 매핑하는 import/ingestion 라우트를 고칠 때는, 응답에 이미 존재하는 필드 중 **앱 스키마와 형식이 100% 동일한 것만** 추가로 채운다(예: 네이버 leafCategoryId → 앱 naverCategoryCode는 같은 코드 체계라 안전). 코드 체계가 다른 필드(원산지 코드, AS 정보 등)는 오매핑 위험이 있으므로 형식 불일치가 확인되기 전엔 채우지 않는다.
