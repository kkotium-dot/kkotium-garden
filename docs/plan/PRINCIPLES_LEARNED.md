# PRINCIPLES_LEARNED — 작업원칙 (현행)

> **분할 이력(#31)**: 이 파일이 임계(1500줄)에 근접할 때마다 앞부분을 archive로 이동합니다. **삭제된 내용은 없습니다.**
> - 2026-07-22 분할: `archive/PRINCIPLES_LEARNED_archived-2026-07-22.md` (#1~#45 구간 대응 — PRINCIPLES_CODE.md 이관분과 별개)
> - 2026-07-30 분할: `archive/PRINCIPLES_LEARNED_archived-2026-07-30.md` (**#46~#227 구간**, 131건)
> - 2026-09-04 분할: `archive/PRINCIPLES_LEARNED_archived-2026-09-04.md` (**#254~#331 구간**, 1437줄→임계 근접 대응)
>
> **이 파일(현재)**: #332부터 시작. 원칙을 찾을 때는 archive 세 파일과 함께 grep 하세요:
> `grep -rn "작업원칙 #NNN" docs/plan/PRINCIPLES_LEARNED.md docs/plan/archive/PRINCIPLES_LEARNED_archived-2026-07-22.md docs/plan/archive/PRINCIPLES_LEARNED_archived-2026-07-30.md docs/plan/archive/PRINCIPLES_LEARNED_archived-2026-09-04.md`
>
> **신규 원칙은 이 파일 맨 아래에 추가합니다.**

---

## 📑 주제별 클러스터 인덱스 (착수 전 관련 원칙 소환용)

> 작업 착수 시 관련 클러스터의 원칙을 먼저 읽는다. 번호로 흩어진 원칙을 주제로 묶어 "이 작업엔 뭘 봐야 하나"를 한눈에. 신규 원칙 등재 시 해당 클러스터에도 번호를 추가한다. **번호가 위 archive 구간(#254~#331)에 있으면 archive 파일에서 찾는다.**

| 클러스터 | 관련 원칙 | 핵심 |
|---|---|---|
| **카테고리 매처·분류** | #295·#346·#349·#350·#351·#352·#353·#356·#357 | 결정론 우선·캐시무효화·테스트≠전수안전·고신뢰만 자동·이원필드·하드코딩기본값 주석신뢰금지 |
| **AI 의존성·모델** | #324·#348·#349·#350·#358·#359 | 모델 생존확인·대체품도 출처검증·폴백=정상·재현성으로 판정 |
| **검증·실측(구조≠내용)** | #45·#318·#350·#352·#356·#357·#359·#362·#369·#371·#374·#377 | 문서/보고 아닌 실측·구조≠내용·전수 dryRun·브라우저·주석신뢰금지·재현성·손트레이스금지·크론실행≠소스재조회·같은이름다른시스템병치금지·코드정확성≠실사용경로·함수명grep 0건≠미구현 |
| **비가역 쓰기·안전** | #46·#299·#352·#353·#355·#356·#373 | 운영자 GO·dryRun·개악>미개선·파생은 delta時만·발행전 정합성·발행쓰이는필드 청소·저장은 payload+허용목록+로딩복원 3지점 |
| **워크트리·git 위생** | #319·#320·#347·#354 + COMPLETION_GATE C6 | 세션종료 정리·lock 잔재·커밋0≠작업0(미커밋 확인) |
| **시스템 승격(단건 금지)** | #62·#293·#295·#351·#370·#372·#375·#379·#380·#381 | 전 상품 공통·채널 일관성·단일 권위·근본 무효화·같은 API 호출부 전수확인·데이터전달 송수신 필드 1:1대조·단일API 다종슬롯 구분전달·단건→패턴 일반화 4~5개 확장검증·출처교차검증+전상품공통 표준절차·AI크로스체크는 유효성으로 채택 |
| **문서·인계·완료판정** | #17·#29·#31·#319·#344·#345·#376·#378 | 대용량MD 덮어쓰기·해시 없는 완료는 완료 아님·완료표는 원본전체 재대조·제안은 응답받고 실행 |
| **외부 API 연동·검증** | #367 | GraphQL 200=필드명 정확 아님·공식스키마 1:1 대조·인증성공≠데이터정확 |
| **인증정보 처리 안전** | #368 | 저장성공≠값이 맞음·비번은 절대 안 봄·검증 컨텍스트의 CSP 등 제약 사전확인 |

> 상위 방법론은 `docs/playbook/CORE_WORKING_PRINCIPLES.md`(3대 기둥). 절대 금지선은 `docs/CORE_PRINCIPLES.md`.

---

## 2026-08-06 (Desktop) 신규 원칙 #332

### #332 — 화면에 노출되는 값이 내부 코드면 표시 계층에서 한글 라벨로 변환한다 (코드값은 데이터로만 유지)

플랫폼 코드·enum·상태값 같은 내부 식별자를 화면에 **그대로** 렌더하면 셀러(운영자)는 그 약어가 무슨 뜻인지 즉시 알 수 없다. 화면 문구는 개발자 은어가 아니라 셀러 실무 용어여야 한다(#317). 내부 코드값은 데이터 계약(DB·API)에 그대로 두고, **표시 계층에만 한글 라벨 매핑 함수**를 둔다.

**사건(2026-08-06, 소싱 위젯 UX 종합점검 중 프로덕션 실측)**: 소싱 위젯 도매 매칭 목록의 플랫폼 배지가 내부 코드 `DMM`·`DMK`를 그대로 노출했다(DMM=도매매, DMK=도매꾹). 셀러는 이 약어로 어느 도매처인지 알 수 없다. 카드·드로어 두 화면, 배지·헤더 4곳에서 동일 노출.

**근본 수정**: `getPlatformLabel(platform)` 단일 헬퍼(카드·드로어 공유, #62 단일 권위)를 두어 DMK→도매꾹(초록)·DMM→도매매(빨강)로 변환. 배지 `{w.platform}` → `getPlatformLabel(w.platform).label`. **DB·API의 코드값('DMM'|'DMK')은 무변경** — 데이터 계약은 그대로 두고 사람이 읽는 표현만 표시 계층에서 만든다.

**연장 발견(#325 계열, 빈 괄호)**: 배포 후 프로덕션 검증에서 헤더가 "도매 매칭 ()"처럼 빈 괄호로 나왔다. 원인은 헤더가 참조하던 요약 필드 `wholesalePlatforms`를 API가 실제로는 안 채운다는 것(항상 undefined) — 실제 플랫폼 정보는 목록 데이터 `wholesaleMatches[].platform`에 있었다. **죽은 요약 필드 대신 실제 목록 데이터에서 유도**(new Set 고유화 후 라벨 매핑)하고, 없으면 괄호 자체를 생략하도록 수정.

**규칙**:
1. 화면에 나가는 값이 내부 코드/enum/상태값이면 표시 계층에 라벨 매핑 함수를 두고, 여러 화면이 그 단일 함수를 공유한다(#62·#317). 코드값 자체는 DB·API에 그대로 유지한다(데이터 계약 무변경).
2. 표시용 "요약 필드"(예: platforms 배열)를 만들었으면 그 필드가 실제로 채워지는지 확인한다. 안 채워지면 죽은 필드다(#325) — 대신 실제 목록 데이터에서 유도한다.
3. 데이터가 없을 때 "(  )"·"[]" 같은 빈 껍데기를 남기지 말고 해당 장식(괄호 등) 자체를 생략한다.
4. 이런 표시 결함은 tsc/build로 안 잡힌다(타입은 맞고 렌더 문자열만 이상). **프로덕션 브라우저 실측**으로만 확인된다(#45/#275) — 배포 후 실제 화면을 눈으로 본다.




---

## 2026-08-08 (Code) 신규 원칙 #333

### #333 — 크론 미실행 의심 시 대시보드 "Enabled" 표기를 믿지 말고 Vercel Runtime Logs로 실제 invocation 유무를 직접 조회한다

Vercel 대시보드의 Cron Jobs가 "Enabled"·정상 등록으로 보여도 **실제로 스케줄대로 실행됐는지는 별개 사실**이다. Hobby 플랜은 대시보드 UI 로그 보관이 1시간이라 "어젯밤 크론이 불렸는지"를 UI만으로는 확인할 수 없다고 오판하기 쉽다.

**사건(2026-08-08, 아침 소싱 알림 미발송 조사)**: Desktop이 대시보드에서 크론 "Enabled"·`0 23 * * *` 정상 등록을 확인했으나, 실제 발송은 안 됐다. Code가 Vercel MCP `get_runtime_logs`(group_by=statusCode, query=경로)로 직접 조회한 결과 **등록된 크론잡 5개 전부 지난 7일간 예정 스케줄 시각의 실행 로그가 0건**임을 확인 — "등록됨"과 "실행됨"이 다른 사실이었다.

**규칙**:
1. Vercel MCP의 `get_runtime_logs`/`get_runtime_errors`는 대시보드 UI가 표기하는 1시간 제한보다 **긴 기간(최소 7일)** 조회 가능하다 — 크론·백그라운드 작업 미실행 의심 시 이 도구를 1순위로 사용한다.
2. 경로별 `group_by: requestPath` 또는 `query`로 필터링해 "그 경로가 예정 시각에 실제로 찍혔는지" 직접 대조한다 — 등록 상태(dashboard "Enabled")만으로 실행을 단정하지 않는다.
3. 4xx/5xx 로그조차 0건인데 요청도 0건이면, 앱 코드(route.ts) 내부 실패가 아니라 **Function에 도달하기 전 엣지 단(Deployment Protection·SSO 등)에서 막혔을 가능성**을 함께 의심한다(`get_project_deployment_protection`으로 확인).

---

## 2026-08-10 (Code) 신규 원칙 #334~#335

### #334 — AI/캐시 파이프라인에서 생성 로직을 고쳐도 이미 저장된 캐시는 안 바뀐다 — 캐시가 생성 호출보다 먼저 조회되는 구조인지 항상 확인한다

생성 로직(AI 프롬프트 등)의 버그를 고쳐도, 그 결과를 캐싱해 재사용하는 구조라면 **이미 캐시에 저장된 과거 오답은 새 로직이 실행조차 되지 않아 계속 서빙된다.**

**사건(2026-08-10, 씨앗심기 카테고리 "가구/인테리어" 대량 오분류 수정 중 발견)**: `api/category/suggest/route.ts`는 `dome_category_cache`(name_hash/dome_code)를 AI 호출보다 먼저 조회한다. AI 프롬프트의 강제 폴백 지시("모르면 가구/인테리어로")를 삭제했지만, 이미 스캔된 적 있는 상품명은 캐시 히트로 즉시 반환되어 **수정 전 프롬프트가 만든 오답을 그대로 계속 서빙**했다(브라우저 curl 재현 중 `cacheHit: "name_hash"` 확인, 신규 상품명으로 바꾸자 비로소 새 프롬프트 경로 확인됨).

**규칙**:
1. 캐시를 쓰는 생성 파이프라인의 버그를 고칠 때는 "캐시가 생성 호출 앞에 있는지" 코드 흐름을 반드시 확인한다. 앞에 있다면 수정은 **신규 입력에만** 적용되고, 기존 캐시 오염은 별도 정리(무효화/재계산)가 필요하다 — 코드 수정 하나로 끝났다고 보고하지 않는다.
2. 검증 시 캐시 히트 여부(`cacheHit` 필드 등)를 반드시 확인한다. 캐시 히트로 "고쳐진 것처럼" 보이거나 "안 고쳐진 것처럼" 보이는 결과 둘 다 착시일 수 있다 — 신규(캐시 미스) 입력으로 재현해야 실제 수정 효과를 검증한 것이다.
3. 캐시 오염 정리(기존 오답 행 삭제/재계산)는 DB 쓰기이므로 운영자 결정 없이 임의 실행하지 않는다 — 발견 사실만 보고하고 방침은 위임한다.

### #335 — "정보가 리셋된다"는 UX 버그는 복원(hydrate) 로직이 아니라 저장(write) 로직의 데이터 유실이 원인일 수 있다 — 저장 경로가 실제로 무엇을 쓰는지부터 확인한다

폼을 다시 열었을 때 필드가 비어 보이면 보통 "복원 로직이 그 필드를 안 읽는다"고 의심하기 쉽지만, **애초에 저장 시점에 그 필드가 DB에 기록되지 않았을 수 있다.** 복원 로직은 무죄이고 저장 로직이 원인인 경우, 복원 코드를 아무리 고쳐도 증상은 그대로다.

**사건(2026-08-10, 씨앗심기 이동 시 정보/이미지 리셋 버그 조사)**: `products/new/page.tsx`의 `?edit=` 로더(`productFormHydrate`)는 DB에 있는 값을 빠짐없이 정확한 컬럼명으로 복원하고 있었다(버그 없음). 실제 원인은 `api/products/import/route.ts`가 네이버 원상품 상세를 가져오면서 `name`/`salePrice`/`mainImage`/`status` **4개 필드만 골라 저장**하고, 이미 응답에 들어있던 카테고리·설명·추가이미지·태그를 전부 버리고 있었던 것 — 복원할 데이터 자체가 DB에 없었다.

**규칙**:
1. "저장했다가 다시 열면 비어있다" 류 버그는 먼저 DB를 직접 조회해(또는 저장 API 응답을 확인해) **그 필드가 애초에 저장돼 있는지**부터 확인한다. 있는데 안 읽으면 복원 버그, 없으면 저장 버그 — 두 원인은 수정 위치가 완전히 다르다.
2. 외부 API(네이버 등) 응답을 DB로 매핑하는 import/ingestion 라우트를 고칠 때는, 응답에 이미 존재하는 필드 중 **앱 스키마와 형식이 100% 동일한 것만** 추가로 채운다(예: 네이버 leafCategoryId → 앱 naverCategoryCode는 같은 코드 체계라 안전). 코드 체계가 다른 필드(원산지 코드, AS 정보 등)는 오매핑 위험이 있으므로 형식 불일치가 확인되기 전엔 채우지 않는다.

---

## 2026-08-10 (Code) 신규 원칙 #336

### #336 — 여러 층위(AI·검증·폴백)가 얽힌 분류 버그는 각 층위를 임시 로그로 직접 찍어 어느 층위인지 확정한 뒤 고친다 — 가장 정교해 보이는 층위가 범인이라고 추정하지 않는다

카테고리 추천처럼 "AI 호출 → 검증/보정 로직 → 폴백 규칙"이 순서대로 얽힌 파이프라인에서 오답이 나오면, 가장 복잡해 보이는 중간 로직(fuzzy 매칭 등)을 의심하기 쉽다. 하지만 **실제로는 훨씬 단순한 층위(폴백 규칙 데이터 자체, 또는 AI의 단순 사실 오답)가 원인일 수 있다** — 추정으로 고치면 엉뚱한 층위를 건드리고 진짜 원인은 남는다.

**사건(2026-08-10, "반려동물 자동급식기"가 계속 "가구/인테리어"로 나오는 잔여 오분류 조사)**: Desktop은 `validateSuggestion()`의 fuzzy 매칭(d1 exact, d2 char-overlap 50%)이 오답을 통과시키는 것으로 의심했다. Code가 AI 원본 응답과 검증 전/후 값을 임시 `console.warn`으로 찍어 재현한 결과, **fuzzy 매칭은 전혀 관여하지 않았다** — AI가 진짜로 실재하는 카테고리("가구/인테리어>주방가구>그릇장/컵보드")를 답해 1번 exact-match 분기로 바로 통과한 것이었다. 진짜 원인은 완전히 다른 곳: `FALLBACK_RULES`(AI 실패 시 안전망)의 반려동물 규칙이 `d1:'반려동물'`이라는, DB에 아예 존재하지 않는 카테고리 축을 가리키고 있어 안전망 자체가 매번 무력화되고 있었다(실제 축은 `생활/건강>반려동물`). fuzzy 매칭 코드는 손댈 필요가 없었다.

**규칙**:
1. 다층 파이프라인의 분류/판정 버그는 각 층위의 입출력을 임시 로그(작업 후 제거)로 실제로 찍어서 확정한 뒤 고친다. "이 로직이 복잡하니 여기가 범인일 것"이라는 추정만으로 코드를 고치면, 진짜 원인(더 단순한 데이터 테이블의 오류 등)을 놓친다.
2. 폴백/안전망 규칙 테이블(하드코딩된 카테고리·매핑 등)을 만들 때는 그 값이 **실제 데이터 소스(DB의 진짜 카테고리 트리 등)에 존재하는지 기계적으로 검증**한다 — 사람이 손으로 적은 규칙은 실제 스키마와 어긋나기 쉽고, 어긋나면 "안전망이 있다고 믿었지만 실제로는 매번 드롭되고 있었다"는 조용한 실패가 된다.

---

## 2026-08-10 (Code) 신규 원칙 #337

### #337 — Vercel Hobby 크론은 "실행 이력을 사후 조회할 방법이 코드/API 레벨에 없다" — 원인 조사가 막히면 재현 시도 대신 회피 설계(안전망·가드)로 전환한다

Vercel Hobby 플랜 크론은 (1) 로그 보관이 1시간뿐이고 (2) 크론 실행 이력(invocation history)을 알려주는 공식 API/MCP 툴이 없다(대시보드 "Recent Invocations" UI만 있고, 그마저 로그인 세션이 있는 사람만 볼 수 있음). 즉 "어젯밤 크론이 정말 안 불렸는지" 자체를 사후에 코드로 증명할 방법이 구조적으로 없다.

**사건(2026-08-10, 아침 소싱 알림 정규 스케줄 미실행 3세션째 조사)**: `maxDuration`+독립크론 분리(fec8759)로 코드 레벨 원인은 다 없앴는데도 8/9·8/10 정규 실행이 재발. 이번 세션에 Vercel 공식문서 재확인(스케줄 정밀도 ±59분, 크론 슬롯 100개 — 기존 "5개 상한" 우려 기각)과 Runtime Logs/Errors API 재조회(7일 에러 0건, 로그는 1시간 한계로 여전히 조회 불가)까지 했지만 **근본 원인은 끝내 확정하지 못했다** — 조회 자체가 안 되기 때문.

**규칙**:
1. Vercel Hobby 크론이 "됐다/안 됐다"를 다투는 문제는 코드 정적분석·환경변수·수동 curl 검증까지 다 정상으로 나오면 그 이상 "원인 확정"을 목표로 계속 파고드는 게 비생산적일 수 있다 — 대시보드 로그인 세션이 있는 사람(운영자/Desktop)의 UI 확인이 유일한 추가 증거원이라면, 그 확인을 명시적으로 위임하고 Code 쪽은 **재발해도 피해가 없는 설계**(당일 중복실행 가드, 외부 안전망 트리거 등)로 방향을 튼다.
2. 안전망(외부 스케줄러로 같은 엔드포인트를 다시 부르는 방식)을 추가하기 전에는 반드시 **멱등성 가드**(같은 날 이미 처리됐으면 스킵)를 먼저 넣는다 — 안전망이 정상 크론과 겹쳐 실발송이 중복되는 사고를 코드로 원천 차단한다.
3. 매일 자동으로 실행되며 실제 외부 발송(디스코드 등)을 일으키는 CI 스케줄 워크플로(GitHub Actions 등)의 신규 추가·활성화는 "디스코드 실발송"과 동급의 승인 대상이다 — 코드 작성까지는 진행해도(승인 대기 상태로 미병합/미활성 유지), 실제로 매일 도는 상태로 켜는 것은 운영자 승인 후에만 한다. 프로덕션 시크릿(CRON_SECRET 등)을 다른 시스템(GitHub repo secret)에 복제하는 것도 같은 승인선 안에 둔다.

4. **self-fetch(자기 자신의 다른 API 라우트를 HTTP로 호출)는 별개 서버리스 함수 홉을 만든다**(#338, 2026-08-11) — 호출자 라우트의 `maxDuration`은 self-fetch로 호출되는 대상 라우트에는 적용되지 않는다(별개 Vercel 함수이기 때문). 실제 사고: `cron/sourcing-daily`(maxDuration=60)가 무거운 실작업을 하는 `/api/sourcing-recommend`를 self-fetch로 호출했는데 그 대상 라우트엔 `maxDuration` 지정이 빠져 있어 Hobby 기본 10초에 걸렸다(dryRun만으로도 8~10초 실측) — DB 저장은 조용히 중단되는데 상위 응답은 정상처럼 보였다. 근본수정은 self-fetch 자체를 제거하고 로직을 같은 프로세스 안에서 직접 호출하는 함수로 합치는 것 — "무거운 작업을 별도 함수로 쪼갤 때는 각 함수마다 개별적으로 maxDuration을 확인할 것", 더 근본적으로는 "가능하면 self-fetch보다 in-process 함수 호출을 우선할 것".


---

## 2026-08-11 (Code) 신규 원칙 #339

### #339 — 인계 문서가 "코드체계가 다를 수 있다"고 경고해도 실제로 검증 함수를 추적하기 전엔 사실로 단정하지 않는다 — 위험 경고는 확인 대상이지 그 자체로 결론이 아니다

이전 세션들이 "원산지 코드는 네이버와 앱이 다른 체계일 수 있어 오매핑 위험이 있다"고
반복 경고해왔고(#335에서도 같은 이유로 import route가 원산지를 의도적으로 비워뒀다),
그 경고를 그대로 받아들이면 "확인 안 되니 손대지 않는다"는 보수적 선택만 남는다. 하지만
경고 자체가 실측 없이 만들어진 추정일 수 있다.

**사건(2026-08-11, 씨앗심기 원산지 import 미연동 근본수정)**: 실제로 코드를 끝까지
추적한 결과 `src/lib/naver/naver-origin-codes.ts`의 `NAVER_ORIGIN_CODES`(네이버 공식
원산지코드.xls 518건)가 앱의 **유일한** 원산지 코드 소스였고, `product-builder.ts`의
`OFFICIAL_ORIGIN_CODES`(발행/수정 시 `Product.originCode`를 검증하는 바로 그 집합)가
정확히 그 표에서 만들어졌다 — 즉 네이버 원산지 코드와 앱 원산지 코드는 **처음부터 같은
코드표**였다. 변환이 필요하다는 가정 자체가 틀렸고, 그 틀린 가정 때문에 원산지 정보가
계속 비어 있었다.

**규칙**:
1. "두 시스템의 코드체계가 다를 수 있다"는 경고를 마주하면, 검증 함수(이 경우
   `resolveOriginAreaCode`/`OFFICIAL_ORIGIN_CODES`)를 실제로 열어 **그 값이 어디서
   왔는지 끝까지 추적**한다. 소스가 같은 표에서 파생됐다면 변환 없이 매핑해도 안전 —
   추정만으로 기능을 계속 비워두지 않는다.
2. 반대로 추적 결과 실제로 다른 체계로 확인되면 그때 변환 테이블을 설계한다 — 이 규칙은
   "무조건 매핑하라"가 아니라 "확인 없이 단정하지 말라"는 것.
3. 이런 조사는 보통 짧게 끝난다(이번 건은 grep 4~5회로 확정) — "위험할 수 있다"는
   경고를 이유로 조사 자체를 생략하지 않는다.


---

## 2026-08-11 (Code) 신규 원칙 #340

### #340 — 네이버에 실제로 전송되는 필드와 씨앗심기 폼이 저장하는 DB 컬럼이 이름은 비슷해도 다른 컬럼일 수 있다 — dirty-field 화이트리스트는 반드시 payload 빌더가 실제로 읽는 컬럼명으로 확정한다

폼 상태 → DB 컬럼 → 네이버 payload로 이어지는 체인에서, 이름이 비슷한 두 컬럼이
실제로는 서로 다른 값을 나른다면 "필드가 바뀌었다"는 신호가 조용히 틀려질 수 있다.

**사건(2026-08-11, "네이버에도 반영" dirty-field 화이트리스트 확정)**: 씨앗심기 폼의
"과세/면세/영세율" 선택값은 `productFormSerialize`를 통해 `Product.taxType` 컬럼에
저장된다. 그런데 `buildNaverProductPayload`(네이버로 실제로 나가는 payload를 만드는
함수)는 `taxType`이 아니라 **`Product.naver_tax_type`**이라는 별개 컬럼을 읽는다
(`product-builder.ts:1091`). 즉 씨앗심기에서 과세유형을 바꿔도 네이버 payload는
안 바뀔 수 있다 — 이름만 보고 "당연히 이 필드가 그 필드겠지"라고 넘겨짚었다면
dirty-field 감지기가 "과세유형 변경됨"이라고 잘못 알리거나(실제로 전송 안 됨), 반대로
`naver_tax_type`을 놓쳐서 실제로 바뀌는 필드를 빠뜨렸을 것이다.

**규칙**:
1. "폼 필드 X → 네이버에 반영된다"는 화이트리스트/매핑을 만들 때는 반드시 payload
   빌더 함수(`buildNaverProductPayload` 등)를 열어 **그 함수가 실제로 참조하는 DB
   컬럼명**을 확인한다. 폼 상태 변수명·저장 컬럼명·payload가 읽는 컬럼명, 이 셋이
   전부 같은 이름일 거라고 추정하지 않는다.
2. 확인 결과 셋 중 하나가 어긋나면(이번 사례처럼), 그 필드는 dirty-field 감지 대상에서
   **제외**하고(오표시 방지) 발견한 사실을 결과 문서에 남겨 다음 라운드 근본수정
   후보로 넘긴다 — 이번 작업 범위를 넘어서는 별개 버그를 그 자리에서 고치지 않는다
   (요청 범위만 수정 원칙).
3. 이런 어긋남은 grep 1~2회로 빠르게 확인 가능하다(`product.taxType` vs
   `product.naver_tax_type` 사용처 검색) — "이름이 비슷하니 같겠지"라는 가정으로
   건너뛰지 않는다.

---

## 2026-08-12 (Code) 신규 원칙 #341

### #341 — DB 필드가 비어있다고 해서 "네이버 값도 비어있다"고 가정하면 안 된다 — 부분 수정/재연동 라우트는 항상 GET-merge 방어 대상에 포함시킨다

**사건**: "듀얼 무선 가습기" 부분재연동 dryRun 미리보기에 "카테고리코드: -"(앱 DB
공백)로 나왔는데, 실제 네이버에는 정확한 카테고리가 설정돼 있었다(운영자가 클로드
크롬으로 직접 확인, confirm:true 실행 전 발견). 원인 추적 결과 초기버전 import route가
카테고리·원산지를 아예 채우지 않은 채 임포트된 상품들이 존재(발행 6건 중 카테고리
5건·원산지 라벨 6건 앱 DB 공백, 네이버에는 전부 정상 설정). v2 PUT은 FULL REPLACE라
빈 값을 그대로 보내면 네이버의 실제 값을 지운다.

**추가로 확인된 근본원인**: `/api/naver/products/update` 라우트가 `validateForRegistration()`이
계산한 `validation.canRegister`(카테고리 빈값이면 false)를 응답에만 실어 보낼 뿐 PUT
실행 여부 결정에 전혀 쓰지 않았다 — `register`/`batch-register` 라우트는 이 게이트로
막는데 `update` 라우트만 빠져 있었다. 게다가 기존 §4-C GET-merge 방어(detailContent/
sellerTags/metaDescription)는 카테고리·원산지를 커버하지 않았고, dryRun 경로에는
GET-merge 자체가 없어 미리보기가 실제 전송값과 달랐다.

**규칙**:
1. "발행된 상품을 부분 수정/재연동"하는 라우트를 만들거나 손댈 때, DB 필드가
   비어있는 것을 "원래 비어있었다"(= 네이버도 비어있다)고 가정하지 않는다. 특히
   `@default("")`/`@default(null)`류 컬럼은 "채워진 적 없음"과 "의도적으로 비움"을
   구분할 수 없으므로 위험하다.
2. v2 PUT(FULL REPLACE) 경로에서 DB-built 값이 비어있거나 형식이 무효면, 실 PUT
   직전 GET으로 네이버 현재값을 확인해 병합(GET-merge)한다. 이 병합은 **dryRun
   경로에도 동일하게 적용**해야 미리보기가 실제 전송값과 일치한다(dryRun에만 없으면
   운영자가 "미리보기는 괜찮았는데 실행하니 지워졌다"를 겪게 된다).
3. GET-merge 이후에도 값이 여전히 무효하면(GET 실패 포함) 그 필드를 요구하는 실
   PUT을 하드 블록한다 — 추측으로 채우지 않는다(원산지 코드 하드블록 #95와 같은
   원칙을 카테고리에도 동일 적용).
4. `validateForRegistration()` 같은 검증 함수가 이미 계산해주는 게이트(`canRegister`
   등)가 있다면, 새로 만드는 라우트가 그 게이트를 실제로 써서 막는지 반드시 확인한다
   — 계산만 하고 응답에 얹어 보내기만 하는 건 게이트가 아니다(이번 update 라우트의
   실제 구멍).
5. 이런 위험을 발견했을 때, 즉시 방어(GET-merge 확장 + 하드블록)와 별개로 DB 자체를
   실제 값으로 백필하는 일회성 스크립트도 함께 마련해두면(dry-run 기본) 재발 방지와
   기존 오염 데이터 정리를 동시에 다룰 수 있다 — 단 production mutation(`--apply`)은
   #41 두 환경 핑퐁 프로토콜에 따라 Code가 직접 실행하지 않는다.

---

## 2026-08-12 (Code) 신규 원칙 #342

### #342 — "네이버 API에 이 개념이 있을까?"는 실제 GET 응답을 찍어보고 판단한다 — 필드명이 그럴듯하다고 존재를 가정하지 않는다

**사건**: import route 필드 완전성 갭 10건을 조사하며, 발행 6개 상품 전부를
실제 `getProduct()`로 GET해 구조를 직접 확인한 결과: asPhone/asGuide/brand/
sellerCode/unitPrice 5건은 정말 네이버가 구조화된 필드로 돌려주지만, 나머지
5건(detailImages/detailImageUrl/hookPhrase/keywords/shippingTemplateId)은
네이버 API 자체에 대응 개념이 없거나(HTML 하나로 이미 합쳐짐) 우리 시스템의
내부 개념(로컬 FK)이었다. 특히 hookPhrase/detailImages/detailImageUrl은
`detailContent`라는 하나의 완성된 HTML 블록으로 발행 시점에 합쳐지고, 네이버는
그 HTML만 돌려준다 — "이 이미지가 상세페이지 이미지였다"를 문자열 파싱으로
되짜는 건 추측이지 매핑이 아니다.

**추가 발견(#340과 같은 패턴)**: `sellerCode` 필드를 추적하다가, 씨앗심기
폼의 "판매자 상품코드" 입력란은 `Product.sku`에 저장되는데
`buildNaverProductPayload`는 실제로 `Product.sellerProductCode`(별개 컬럼)를
읽어 PUT한다는 것을 발견 — 부분재연동 dirty-field 감지기가 지금 추적 중인
컬럼과 실제 네이버로 나가는 컬럼이 다르다. `brand`/`naver_brand`는 한술 더
떠서, 두 컬럼 다 outgoing PUT 페이로드 어디에도 안 들어간다(내부 완결성
점수용으로만 읽힘) — 화이트리스트가 추적은 하지만 실제로 네이버에 반영되지
않는 필드다.

**규칙**:
1. "네이버 API에 X라는 필드/개념이 있을 것이다"는 절대 추정하지 않는다.
   `getProduct()`(또는 등록/수정 응답)를 실제로 호출해 `JSON.stringify`로
   구조를 직접 확인한 뒤 매핑한다(#82의 구체적 실행 방법). 가능하면 발행된
   상품 여러 건을 GET해서 필드 존재 여부가 상품마다 우연이 아닌지 확인한다.
2. 응답에 없는 필드는 "아직 못 찾았다"가 아니라 "네이버가 이 개념 자체를
   구조화된 형태로 주지 않는다"는 결론일 수 있다 — 특히 여러 개의 로컬
   컬럼(hookPhrase/detail_image_url/detail_images/description)이 발행
   시점에 하나의 HTML(`detailContent`)로 합쳐지는 구조라면, 그 역방향
   복원은 파싱 추측이니 시도하지 않는다.
3. dirty-field 화이트리스트처럼 "이 필드가 바뀌면 네이버에도 반영된다"고
   운영자에게 보여주는 UI가 있다면, 그 필드가 실제로 payload 빌더(`build
   NaverProductPayload` 등)의 어느 컬럼을 읽는지 반드시 추적해 폼 저장
   컬럼과 일치하는지 확인한다(#340 연장) — 일치하지 않거나 payload에
   아예 안 들어가면 운영자에게 "바뀜"이라고 잘못 알리는 셈이니 발견 즉시
   기록하고(수정은 별도 스코프 판단), 방치하지 않는다.

## 작업원칙 #343 — Hobby 크론은 "시각"이 아니라 "시간대"로 발동한다. 창이 닫히기 전의 미수신은 실패가 아니다 (2026-08-20)

**사건**: 소싱 알림이 8일간 "안 온다"고 판정돼 5개 가설을 세우고 전부 기각했다. 실제로는 rev120의 self-fetch 제거 수정이 처음부터 작동하고 있었고, 크론은 매일 정상 실행되고 있었다. `cron_invocation_log` 실측: `/api/cron/sourcing-daily | 08-20 08:55:50 KST | auth_ok=t | ok | 6719ms`.

**원인**: Vercel Hobby 크론은 `0 23 * * *`을 23:00~23:59 UTC(08:00~08:59 KST) **사이 임의 시점**에 발동한다. 그런데 형제 크론(`cron/daily`)이 23:0x에 찍히는 걸 보고 "소싱도 같은 시각"이라 전제한 뒤, **창이 끝나기 12분 전인 08:47에 확인하고 "재발"로 판정**했다.

**규칙**:
- 스케줄러 미수신을 판정하려면 **발동 창이 완전히 닫힌 뒤**에 본다.
- 형제 크론의 실행 시각을 다른 크론의 기준으로 삼지 않는다(플랫폼이 부하 분산한다).
- 관측 수단 없이 가설만 반복하지 않는다. `cron_invocation_log` 같은 **자체 계측을 먼저 심는다**. 플랫폼이 이력을 안 주면 우리가 기록한다(#337의 벽을 뚫는 방법).

## 작업원칙 #344 — 완료 판정은 "사용 지점에서의 증명"으로만 한다. tsc 통과는 완료가 아니다 (2026-08-20)

**사건**: 이틀 사이 같은 유형의 실패가 5회 발생했다. 전부 작업은 실제로 했고 코드도 옳았는데 효과가 0이었다.
- F1 커밋 누락 — KST 유틸 11파일·수수료 15파일이 worktree에 미커밋 방치
- F2 병합 누락 — 커밋·push는 됐으나 브랜치에만 존재, main은 옛 코드 그대로
- F3 연결 누락 — 8렌즈·`searchad-volume.ts`가 존재하나 호출부 0건
- F4 데이터 누락 — `dropship-fitness.ts`가 완벽하나 `category_id`가 전 상품 NULL이라 전부 기본값
- F5 문서 미동기화 — 설계안이 Desktop 로컬에만 있어 Code가 착수 불가

**규칙**: 완료 보고 전 5게이트를 **명령 실행 결과로** 증명한다.
1. **커밋** — `git status --porcelain` 비어 있고 해시를 보고에 명시. **해시 없는 완료는 완료가 아니다.**
2. **병합** — `git merge-base --is-ancestor <hash> origin/main`. 브랜치 커밋은 보존이지 반영이 아니다.
3. **연결** — 새 모듈의 호출부를 grep으로 증명. 호출부 없는 모듈은 죽은 코드다(#292 사전 차단판).
4. **데이터** — 로직이 읽는 컬럼의 실제 채움률을 센다. 전 건이 기본값이면 그 로직은 작동하지 않는 것이다.
5. **실행** — 실제로 돌리고 출력값을 원문 그대로 보고에 붙인다. 못 돌렸으면 "못 돌렸다"고 명시하고 검증 스크립트를 인계한다.

**전문**: `docs/playbook/COMPLETION_GATE.md` (보고 템플릿·안티패턴·검증 우선순위 포함)

## 작업원칙 #345 — 문서를 근거로 인계할 때는 커밋·push 후 해시와 함께 넘긴다 (2026-08-20)

**사건**: Desktop이 `PRODUCT_CATEGORY_BACKFILL` 설계안을 작성하고 "정독하라"고 인계했으나, 커밋하지 않아 Code 워크트리에서 파일이 보이지 않았다.

**규칙**:
- 설계안·리서치·인계 문서도 **코드와 동일하게 커밋·push해야 다른 레인이 읽는다.**
- 인계받은 쪽은 문서가 없으면 **요약만 보고 착수하지 말고 부재를 먼저 보고한다.** (2026-08-20 Code의 대응이 모범 사례 — 검증 불가한 요약으로 움직이지 않아 잘못된 전제의 작업을 막았다)
- 문서 레인(Cowork)도 G1·G2 게이트 적용 대상이다.

## 작업원칙 #346 — 분류는 "생성"이 아니라 "선택"으로 만든다. 정답지가 있으면 오분류는 구조적으로 불가능해진다 (2026-08-20)

**배경**: 네이버 카테고리는 오지정 시 미지정보다 큰 페널티를 받는다(적합도=카테고리 선호도, 리서치 항목1). AI에게 카테고리 문자열을 생성시키면 네이버에 없는 값이 나올 수 있다.

**실측**: `src/lib/naver/naver-categories-full.ts`에 2026-07-09자 네이버 공식 XLS 기반 **5,021엔트리**(D1 11개, D4까지 3,475개, 중복 code 0건)가 이미 존재한다.

**규칙**:
- 유한한 정답 집합이 존재하는 분류 문제는 **생성이 아니라 선택**으로 설계한다.
- 3단 구조: ① 규칙으로 후보 압축 → ② AI는 후보 **안에서만** 선택 → ③ 선택값의 실재를 강제 검증.
- 검증 실패 시 **저장하지 않고 `needs_review`**. 추측으로 채우지 않는다.
- 이 원칙은 카테고리뿐 아니라 브랜드·속성·단위 등 마스터 데이터가 있는 모든 분류에 적용한다.

## 작업원칙 #347 — 세션 시작 시 워크트리·lock 위생을 확인한다. 크래시 잔재가 다음 세션의 git 명령을 조용히 실패시킨다 (2026-08-27)

**사건**: 2026-08-20 크래시로 남은 `.git/HEAD.lock`·`.git/index.lock`(0바이트)이 이후 여러 세션 동안 방치됐다. 동시에 `git worktree list`의 워크트리 8개 중 5개가 이미 main에 완전 반영(ahead=0)됐는데도 정리되지 않고 남아있었고, 그중 3개는 "정리 대상"으로 보였지만 실제로는 미커밋 잔재(중복 `" 2.json"` 파일, 이미 아카이브된 내용의 staged diff, 미추적 로컬 설정)를 품고 있어 무심코 지웠으면 손실 여부를 확인 못 할 뻔했다.

**규칙**:
- 세션 시작 시(또는 이번처럼 위생 점검 지시 시) `.git/*.lock` 파일의 존재·크기·mtime을 확인하고, 살아있는 git 프로세스가 없음을 `ps`로 확인한 뒤에만 제거한다.
- `git worktree list`로 워크트리를 감사할 때 **ahead/behind 커밋 수만으로 판단하지 않는다** — 각 워크트리에서 `git status --short`로 미커밋(staged/untracked 포함) 여부를 반드시 함께 확인한다.
- ahead=0이어도 미커밋 변경이 있으면, 그 내용이 이미 main 등 다른 곳에 존재하는 중복인지(`grep`으로 원문 대조) 확인하기 전엔 삭제하지 않는다.

## 작업원칙 #348 — 외부 AI 모델명은 살아있는 의존성이다. 공급자가 조용히 폐기할 수 있고, 그 순간 관련 기능 전체가 침묵한다 (2026-08-27)

**사건**: UCE(Universal Category Engine) 세션에서 `category/suggest`의 `usedAI:false`를 코드 버그로 의심하고 파싱 로직을 개선했으나, 프로덕션 런타임 로그(`vercel logs`) 실측 결과 진짜 원인은 `GROQ_API_KEY`도 파싱도 아니었다 — Groq가 `llama-3.3-70b-versatile`을 카탈로그에서 완전히 제거해 모든 호출이 404였다(`GET /v1/models` 실측으로 확인, 대체 모델도 없이 그냥 사라짐). 같은 하드코딩 모델명을 쓰던 `sourcing-recommender.ts`의 `llama-3.1-8b-instant` 호출도 같은 이유로 동시에 죽어 있었다 — 카테고리 제안 하나만의 문제가 아니라 앱 전역 Groq 의존 기능(SEO 키워드·제목·설명·소싱 인사이트)이 전부 침묵하고 있었다.

**규칙**:
- 모델명은 라이브러리 버전처럼 **시간이 지나면 깨질 수 있는 외부 의존성**이다 — 코드에 흩어 하드코딩하지 말고 단일 상수(`GROQ_MODEL` 같은)로 격리해, 교체가 grep 한 번으로 끝나게 한다.
- AI 의존 기능이 원인 불명으로 침묵/저품질 결과를 낼 때, **파싱·프롬프트를 의심하기 전에 모델 자체의 생존을 먼저 확인**한다 — `GET {provider}/v1/models`로 현재 카탈로그에 그 모델명이 실제로 있는지 찍어본다. 이게 최우선 의심 대상이어야 하는 이유는 조사 비용이 가장 싸고(API 호출 1번), 발생 시 영향 범위가 가장 크기 때문이다(그 모델을 쓰는 모든 기능이 한 번에 죽는다).
- 프로덕션 로그 문구를 코드의 최신 문구와 대조한다 — 로그가 옛 에러 텍스트를 그대로 뱉고 있다면 애초에 최신 코드가 배포되지 않았을 가능성도 함께 검증한다(#350과 함께 적용).

## 작업원칙 #349 — AI에 의존하는 기능은 결정론적 매칭을 1순위로 두고 AI는 실패했을 때만 보조로 부른다 (2026-08-27)

**배경**: `category/suggest`는 원래 "AI 우선 → 실패 시 하드코딩 57건 폴백" 구조였다. AI가 조용히 죽어도(#348) 알아채기 어렵고, 하드코딩 폴백은 유지보수 대상이 늘어나며 커버리지가 작았다.

**실측**: 마스터(`NAVER_CATEGORIES_FULL`, 5,021개 리프)와 상품명을 직접 substring/synonym-split 매칭하는 결정론적 매처(`category-deterministic-matcher.ts`)로 전면 교체한 뒤, 프로덕션 실호출 20종(카탈로그 10 + 카탈로그 밖 10: 달항아리·수세미·우산꽂이 등)이 전부 `usedAI:false`로 유효 카테고리를 반환했다 — AI를 한 번도 부르지 않고도 100% 커버, 비용 0.

**규칙**:
- 유한한 정답 집합(마스터 데이터)이 있는 분류/매칭 문제는 AI보다 **결정론적 매칭을 1순위**로 두고, AI는 결정론적 매칭이 완전히 실패했을 때만 보조로 강등한다(#346의 "생성이 아니라 선택" 원칙의 구체적 배선 패턴).
- `usedAI:false`는 실패 신호가 아니라 **성공 신호일 수 있다** — 어떤 경로로 답을 찾았는지(`source` 필드 등)로 판단하지, AI 호출 여부만으로 성패를 판단하지 않는다.
- 결정론적 매처가 마스터에서 직접 파생되면, 마스터가 갱신될 때(UCE-5류 재생성 파이프라인) 별도 유지보수 없이 자동으로 최신화된다 — 하드코딩 규칙 목록은 항상 이 방식으로 대체를 검토한다.
- **단서(UCE-7, 2026-08-27) — 저신뢰 결정론은 AI 교차확인**: "결정론이 뭐라도 반환하면 AI 미호출"을 무조건 적용하면 결정론의 **오답**을 교정할 기회가 없다(실측: `실리콘주걱`→접착용품>실리콘, `차량용방향제`→여가/생활편의>원데이클래스처럼 존재하는 카테고리로 틀리게 매칭). 결정론 결과가 (a) 약한 티어(전체 리프 정확매칭이 아닌 d3-only/역포함 폴백)이거나 (b) 최고점이 임계 미만이거나 (c) 2위 후보가 다른 d1에서 근소 차로 붙어있으면, **그 슬라이스에 한해서만** Groq을 교차확인으로 호출해 `usedAI:true`로 승격한다 — 고신뢰 결정론(정확 리프 매칭)은 여전히 AI 미호출·비용 0을 유지한다(구현: `src/app/api/category/suggest/route.ts` `isDeterministicLowConfidence`, 설계: `docs/design/UCE7_MATCH_QUALITY_2026-08-27.md`).

## 작업원칙 #350 — 표면적인 "실패처럼 보이는 결과"(source:fallback, 빈 응답 등)를 실측 없이 실패로 단정하지 않는다. 그 값을 만든 코드의 반환 조건을 먼저 확인한다 (2026-08-27)

**사건**: `kkotti-comment` 같은 AI 응답 라우트가 특정 입력(예: 대상 상품 0건)에서 폴백/빈 값을 반환하는 것을 보고 "AI 실패"로 오판할 뻔했다 — 실제로는 그 조건에서 폴백을 반환하도록 **의도적으로 설계된 정상 경로**였고, 상품이 있는 입력으로 다시 호출하면 `source:groq`(실측)로 정상 동작했다.

**규칙**:
- `usedAI:false`·`source:fallback`·빈 배열/문자열 같은 "실패처럼 보이는" 응답 필드를 마주치면, 그 값을 **어떤 코드 경로가 왜 반환했는지**(가드 조건·early return)를 먼저 읽는다 — "실패했나 보다"라고 추측(#82 위반)하지 않는다.
- 같은 엔드포인트를 **입력을 바꿔가며 재현**해 "이 입력에서는 정상적으로 폴백" vs "이 입력에서는 원래 성공해야 하는데 실패"를 구분한다.
- #348(모델 생존 확인)·#349(결정론 우선)와 함께 적용: 표면 신호(usedAI/source) → 코드 반환조건 확인 → (필요시) 외부 의존성 생존 확인 → 런타임 로그 실측, 순서로 좁혀간다(`docs/playbook/AI_DEPENDENCY_DIAGNOSIS.md` 참조).
- 워크트리·브랜치 삭제, `.git/*.lock` 제거처럼 되돌리기 어려운 정리 작업은 **사용자 승인 후에만** 실행한다(safety 원칙과 동일선상).

## 작업원칙 #351 — 캐시 있는 로직을 수정할 때는 캐시 무효화가 완료조건이다. 코드수정 ≠ 실효 (2026-08-27)

**사건(UCE-7)**: `category-deterministic-matcher.ts` + `category/suggest/route.ts`를 고치고 main 병합·프로덕션 배포까지 확인했는데, 프로덕션 실측에서 "차량용방향제"·"칫솔살균기"·"전동칫솔"·"요가매트" 4건이 **여전히 옛 오답**을 반환했다. 원인은 `category_mappings`(name_hash 학습 캐시)에 옛 코드가 저장해 둔 오답 로우가 캐시 히트로 그대로 서빙된 것 — 코드는 100% 고쳐졌고 배포도 됐는데, **캐시가 "고쳐지기 전 세계"를 기억**하고 있었다. 1차 조치로 해당 로우 4건을 수동 `DELETE`했지만, 이는 **이번 건에서만 통하는 임시방편**이다 — 다음에 매처 로직을 또 고치면 같은 사고가 반복된다.

**근본 수정**: `dome-category-cache.ts`에 `CATEGORY_MATCH_LOGIC_VERSION` 상수를 신설하고, 캐시 저장/조회 키(`normalizeKey`)에 `.v<버전>` 접미사로 항상 포함시켰다. 매처의 스코어링·티어·AI 교차확인 게이트 등 **"같은 입력에 대해 다른 정답을 낼 수 있는" 로직**을 고칠 때마다 이 숫자를 올리면, 옛 버전 접미사가 붙은 로우는 새 조회 키와 영영 매치되지 않아 **자동으로 재계산**된다(수동 DELETE 사냥 불필요, 옛 로우는 그냥 고아로 남아 무해하게 방치).

**규칙**:
- **캐시가 낀 로직을 수정할 때, "코드 수정 완료"와 "실제 효과 발생"은 별개 사건이다.** 캐시가 있다면 그 캐시를 어떻게 무효화할지가 완료조건의 일부다 — 매 배포마다 수동으로 캐시를 뒤지고 지우는 절차는 **재발이 예정된 미봉책**이지 해결이 아니다.
- 판별법: "이 로직의 출력을 저장해 두고 재사용하는 캐시/학습 테이블이 있는가?"에 예라면, 로직을 고치는 커밋에는 **버전 태그 방식의 무효화(또는 TTL·명시적 캐시 클리어 스크립트)를 함께 넣는다** — 사람이 매번 기억해서 지우는 것에 의존하지 않는다.
- 완료 보고 전 **캐시 히트 경로를 직접 실측**한다(`cacheHit` 같은 응답 필드, 또는 DB 조회) — 응답이 맞아 보여도 그게 "새 로직이 방금 계산한 것"인지 "옛 로직이 예전에 캐싱해 둔 것"인지 구분 없이는 완료로 인정하지 않는다(#350과 연결 — 표면 신호만으로 성패를 판단하지 않는다).
- #349(결정론 우선)·#350(표면 신호 재검증)과 한 세트: 결정론적 매칭·학습 캐시처럼 "정답을 저장해 재사용"하는 설계는 강력하지만, **그 저장소가 로직 버전과 묶여 있지 않으면 캐시가 곧 회귀의 온상**이 된다.


## 작업원칙 #352 — 테스트 green ≠ 전수 안전. 비가역 쓰기는 "임의 표본 전수 dryRun"을 통과해야 한다 (2026-08-27)

**사건(category_id backfill)**: `naver_categories` 마스터 적재 후, 상품의 과거 `naverCategoryCode`(오라벨)를 그대로 FK 연결한 backfill이 아이스트레이→"홍합", 디퓨저→"교자상" 같은 오라벨을 만들었다. 이를 고치려 UCE 매처 기반 resolver + 정합성 테스트(`test:category-integrity`)를 만들어 **5/5 → 16/16 green**을 받았다. 그런데 Desktop이 **--apply 전 dryRun을 실제로 돌려보니**, 전 상품 15종 중 4건이 오히려 **새 오분류로 개악**될 상황이었다(듀얼가습기→"완구>기차", 달항아리→현재 맞는 "도어벨"에서 틀린 "도서>인테리어"). resolver를 confidence 게이트로 보수화(애매하면 NULL)한 뒤에도, **카탈로그 밖 임의 20종 테스트에서 무선청소기→"공구", 물티슈→"반려동물" 등 오분류가 남았다.**

**핵심 교훈 3가지**:
1. **테스트가 커버한 표본 ≠ 실제 전수.** 정합성 테스트가 16/16 green이어도, 그 테스트에 든 케이스만 검증한 것이다. "green = 안전"이 아니라 "green = 이 케이스들만 안전"이다. 진짜 검증은 **카탈로그 밖 임의 상품 20~30종 전수**로 한다(기둥 3의 "임의 표본" 강화).
2. **dryRun은 비가역 쓰기의 필수 방어선.** `--apply`(DB 직접 쓰기) 전에 dryRun으로 "무엇이 어떻게 바뀌는가"를 전량 출력해 육안 검수한다. dryRun이 "정정하려다 개악"을 배포 전에 잡았다 — dryRun 없는 비가역 쓰기는 금지.
3. **보수화(NULL)로는 근본을 못 고친다.** resolver를 아무리 보수적으로 만들어도(애매하면 비움), 그 아래 **공통 엔진(매처)이 수식어('인테리어')를 핵심명사('달항아리')보다 높게 치는 결함**이 있으면 개악은 계속 난다. 진짜 뿌리는 여러 소비처(backfill·소싱·/api/suggest·발행넛지)가 공유하는 **단일 엔진**이다 — 거기를 고쳐야 전부 풀린다(기둥 2 = 단건 수습 금지).

**규칙**:
- 비가역 데이터 쓰기(DB backfill/migration/일괄 UPDATE) 완료조건 = **① 카탈로그 밖 임의 표본 20종+ 전수 dryRun 오분류 0 + ② 실제 카탈로그 전수 dryRun 개악 0 + ③ 그 케이스들을 회귀 테스트에 고정**. 이 셋을 통과하기 전 `--apply` 금지.
- "green"을 완료로 인정하지 않는다. **green이 검증한 표본의 범위**를 명시하고, 그 밖의 임의 표본으로 재검증한다.
- 개악(원래 맞던 값을 틀리게 바꿈)은 미개선(빈손 유지)보다 나쁘다. 확신 없으면 **NULL 유지(정직)**가 개악보다 낫다(#350 정직표시와 연결).
- 여러 화면/경로가 같은 판정을 쓰면, 소비처마다 우회 보수화하지 말고 **공통 엔진 한 곳**을 고친다(#295 단일 권위·#62 시스템 수정).
- CORE_WORKING_PRINCIPLES 기둥 1(구조≠내용: 테스트 green은 구조, 전수 정확은 내용)·기둥 3(임의 표본 전수)의 구체 적용 사례.


## 작업원칙 #353 — 자동분류는 "고신뢰만 자동, 애매하면 사람 확인". 완벽한 자동화보다 안전한 반자동 (2026-08-27)

**사건(카테고리 매처)**: 카테고리 매처 정확도를 100%로 올리려 여러 세션에 걸쳐 매처를 반복 수정했으나, 임의 30종 전수 dryRun 결과 정확도 77%(정확17·NULL8·오분류5)에서 벽에 부딪혔다. 매처를 6종 고치면 5종이 새로 터지는 **무한 두더지잡기** — '사료'가 관상어사료(d3 완전일치)에 낚여 강아지사료(부분매칭)를 이기는 등, 부분토큰 매칭의 태생적 한계다. Code에 매처를 계속 갈게 하는 건 ROI가 낮다고 판단, "고신뢰만 자동 + 애매하면 사람 확인" 반자동으로 전환했다.

**출처 교차검증(2026-08-27)**: 이 전환이 감이 아니라 업계 표준·네이버 공식 방식임을 웹으로 확인:
- 스마트스토어 카테고리는 **한번 등록하면 재수정·변경이 어렵다(비가역)** → 자동 오분류는 치명적. 틀리게 자동입력하느니 비워두고 사람이 확인하는 게 맞다(mangotree/sjnim manual).
- 네이버 상품등록 자체도 **모델명이 정확히 매칭될 때만(고신뢰)** 카테고리를 자동입력하고, 그 값은 변경 불가. 매칭 안 되면 셀러가 직접 선택(dubuplus).
- 업계 관행: **카테고리가 애매하면 네이버쇼핑에 검색해 타사 결과로 확인**한다 = 사람 확인 루프가 표준(sjnim).

**규칙**:
- 자동분류/자동입력 기능의 완료조건은 "정확도 100%"가 아니라 **"오분류 0%(고신뢰만 MATCH, 애매하면 비움)"**이다. 100% 자동보다 **개악 0%**가 우선(#352 개악>미개선의 연장).
- 비가역 대상(스마트스토어 카테고리처럼 되돌리기 어려운 것)일수록 이 원칙이 강하다. 확신 없으면 자동으로 쓰지 말고 **NULL 유지 → 사람 개입큐(UCE-4)로 흘린다**.
- "완벽한 자동화"를 목표로 무한정 엔진을 튜닝하는 것은 새싹셀러 ROI상 함정일 수 있다. 어느 지점에서 **"후보 제시 + 사람 최종선택" 반자동으로 전환**하는 게 매출 레버(발행·SEO·리뷰)에 시간을 쓰는 길이다.
- 판별: "이 자동 결과가 틀렸을 때 되돌리기 어려운가?"에 예라면 → 고신뢰 임계를 높이고 애매한 건 사람에게. 아니오라면 → 자동 폭을 넓혀도 됨.
- 개입점은 이미 있는 것을 쓴다: 매처 후보 → 씨앗심기 UI 후보 3개 → 사람 선택(UCE-4 개입큐). 새 UI 만들지 말고 기존 흐름에 연결.
- CORE_WORKING_PRINCIPLES 기둥 3(개입점 자연스럽게)의 구체 적용. #350(정직표시)·#352(개악>미개선)와 한 세트.


## 작업원칙 #354 — 브랜치/워크트리 상태는 git log(커밋)만이 아니라 working tree(미커밋)까지 봐야 판정된다 (2026-09-02)

**사건(Desktop 오판 2건, Code가 교정)**: category 반자동 작업 중 Desktop이 두 가지를 잘못 판정했다.
1. **"elegant-kirch는 빈 브랜치(커밋0)"** — `git rev-list origin/main..branch`(커밋 이력=구조)만 보고 "비었으니 정리가능"이라 상신. 실제로는 `git status`상 **미커밋 변경 2파일(dashboard-product.ts·DailyPlanWidget.tsx)**이 있었다. Code가 "다른 세션 작업중 상태를 임의로 안 지운다"로 올바르게 멈췄다.
2. **"DailyPlanWidget에 카테고리채우기 뜨는지 실측하라"** — 그 파일은 헤더에 `⚠️ @unmounted(2026-07-22 전수확인)` = **죽은 코드**라 브라우저 실측 자체가 불가능. Code가 발견해 진짜 라이브 화면(/growth UploadReadinessWidget)으로 정확히 리다이렉트했다.

**핵심 교훈**:
- 브랜치가 "비었다/정리가능"을 판정할 때 `git log`(커밋 이력)만 보면 **working tree의 미커밋 변경을 놓친다**. 반드시 `git -C <worktree> status --porcelain`까지 확인한다. 커밋0 ≠ 작업0.
- UI 컴포넌트에 "실측하라"고 인계하기 전, 그 컴포넌트가 **실제 마운트되는 라이브 화면인지** 먼저 확인한다(파일 헤더 @unmounted 주석·라우트 grep). 죽은 코드 실측은 시간 낭비이자 허위 완료의 씨앗.
- 이건 CORE_WORKING_PRINCIPLES 기둥1("구조≠내용·내가 틀릴 수도 있다")의 정확한 실례 — Desktop이 구조(git log)만 보고 내용(working tree/마운트 여부)을 안 봤고, 핑퐁 교차검증(Code)이 교정했다. **레인 간 상호검증이 단일 레인의 오판을 잡는다는 증거.**

**규칙**:
- 워크트리 삭제/정리 상신 전 체크리스트: (1)`git rev-list`(ahead/behind) (2)`git status --porcelain`(미커밋) (3)다른 세션 활성 여부. 셋 다 확인 후에만 "정리가능" 판정.
- 컴포넌트 실측 인계 전: 라이브 마운트 확인(@unmounted 주석·페이지 import grep). 죽은 코드면 진짜 라이브 표면을 지목.
- 클러스터 인덱스 "워크트리·git 위생"에 #354 추가.


## 작업원칙 #355 — autosave/파생 저장은 "실제 변경 시에만". 비가역 행위 직전엔 정합성 게이트 (2026-09-02)

**사건 2건(category_id 발행 직전)**:
1. **재발 버그**: clear-known-wrong로 지운 오라벨(홍합 category_id)이, 편집화면을 **열기만 해도**(아무 클릭 없이) autosave가 stale naverCategoryCode를 재전송 → category_id가 홍합으로 **자동 부활**. 원인: 파생 조건이 "요청에 naverCategoryCode 포함되면"이었고 "실제로 값이 바뀌었을 때"가 아니었음. 수정(41342ef): `updateData.naverCategoryCode !== prevData.naverCategoryCode`일 때만 derive.
2. **발행 직전 오분류**: 발행임박 상품 "라이트 LED 저소음 사무실 가습기"가 category "인테리어소품>조명>LED모듈"로 오분류('LED'가 조명에 낚임). 발행게이트가 이 상충(가습기≠조명)을 못 잡음 → 이대로 발행되면 참사.

**핵심 교훈**:
- **autosave/파생 로직은 "요청에 필드가 있음"이 아니라 "값이 실제로 바뀜"을 조건으로 한다.** 안 그러면 화면을 여는 것만으로 stale 값이 되살아나 애써 고친 데이터가 자동 오염된다. "포함됨 ≠ 변경됨".
- **비가역 행위(발행·네이버 전송) 직전엔 "구조≠내용" 정합성 게이트를 둔다.** 데이터가 형식상 유효해도(category_id FK 유효) 내용이 상품과 상충(가습기↔조명)하면 그 행위를 막고 사람 확인을 요구한다. #352(구조≠내용)를 비가역 게이트에 박제.

**규칙**:
- 파생/자동저장 필드는 이전 값과 비교해 **delta가 있을 때만** 쓴다. no-op 요청(같은 값 재전송)은 파생을 트리거하지 않는다.
- 발행/전송 등 되돌리기 어려운 게이트(publish-review-gate)에 정합성 검사 추가: category full_path의 핵심명사 ↔ 상품명 핵심명사가 명백히 상충하면 차단+확인. 개별 오분류를 그때그때 잡는 두더지잡기 대신, 게이트 한 곳에서 전 상품 공통으로 막는다(#62·#352).
- clear-known-wrong류 "오라벨만 NULL" 정리는 재발 경로(autosave 파생)를 함께 막지 않으면 무효 — 데이터 정리와 재발 차단은 세트.
- 클러스터 인덱스 "비가역 쓰기·안전"에 #355 추가.


## 작업원칙 #356 — 오라벨 청소는 category_id + naverCategoryCode 둘 다. 발행에 실제 쓰이는 필드를 청소해야 한다 (2026-09-02, 브라우저 실측)

**사건(브라우저 실측으로 발견)**: Chrome 재연결 후 씨앗심기에서 "64구 아이스트레이"(cmtilmjxo, 09-01 신규) 편집화면을 열자, 마진계산기·엑셀매핑·황금키워드 **모든 표면에 카테고리가 "홍합"(50004716)**으로 표시됨. DB 확인: category_id는 NULL(clear-known-wrong로 비움)인데 **naverCategoryCode는 홍합(50004716) 그대로**. 즉 clear-known-wrong가 category_id만 비우고, **화면·발행에 실제 쓰이는 naverCategoryCode는 안 비웠음**. 이대로 발행하면 아이스트레이가 홍합 카테고리로 네이버에 등록될 뻔.

**추가 발견**: 아이스트레이가 3중 중복(05-28·08-08·09-01), 08-08(cmsjzlf0r)만 카테고리 정확. 신규 생성 때마다 stale naverCategoryCode가 따라옴.

**핵심 교훈**:
- 카테고리는 **두 필드로 이원화**: `category_id`(naver_categories FK·앱 내부 표시 일부) + `naverCategoryCode`(엑셀매핑·마진계산기·**실제 네이버 발행에 쓰이는 코드**). 오라벨 청소는 **둘 다** 해야 실효. 하나만 비우면 나머지가 화면·발행을 오염시킨다.
- **"발행에 실제 쓰이는 필드가 무엇인가"를 먼저 확인**하고 그것을 청소·검증한다. 내부 FK(category_id)만 고치고 만족하면, 정작 발행 코드(naverCategoryCode)는 틀린 채 남는다.
- **API·DB 쿼리만으론 이 이원화 오염을 놓친다.** 브라우저에서 실제 편집화면(마진계산기·엑셀매핑)을 봐야 "화면이 어느 필드를 읽어 무엇을 표시하는가"가 드러난다. #355의 발행 직전 정합성 게이트는 **두 필드 모두** 검사해야 한다.

**규칙**:
- 오라벨/카테고리 정리 스크립트는 category_id NULL과 **naverCategoryCode 재확인(또는 함께 클리어)** 을 세트로. clear-known-wrong류에 naverCategoryCode 처리 추가.
- 발행 정합성 게이트(#355)는 category_id의 full_path뿐 아니라 **naverCategoryCode가 가리키는 카테고리**도 상품명과 상충하는지 검사(발행에 쓰이는 건 naverCategoryCode이므로 이게 더 중요).
- 카테고리 관련 검증은 반드시 **브라우저 편집화면 실측**을 포함(마진계산기·엑셀매핑 표시값 = 발행 실값). DB만 보지 말 것.
- 중복 상품은 신규 생성 시 stale naverCategoryCode를 물고 오므로, 중복 정리와 카테고리 청소는 함께.
- 클러스터 인덱스 "비가역 쓰기·안전"·"카테고리 매처"에 #356 추가.


## 작업원칙 #357 — 하드코딩 기본값의 주석과 실제가 다를 수 있다. 주석 신뢰 금지 (2026-09-03)

**사건**: `KKOTIUM_DEFAULTS.categoryCode='50004716'`가 주석엔 "꽃다발"인데 실제 마스터 기준 "식품>수산물>홍합". POST route가 미입력 시 이 값을 자동 주입 → **몇 세션간 쫓던 홍합 오염의 진짜 발원지가 매처 버그가 아니라 이 하드코딩 기본값이었음**. 매처를 아무리 고쳐도 이 기본값이 계속 홍합을 뿌렸다.

**규칙**:
- 하드코딩 상수의 **주석을 신뢰하지 말고 실제 값이 가리키는 것을 마스터로 대조**한다. 주석은 작성 시점 의도이고 마스터는 바뀐다.
- "자동 기본값(default)"은 사각지대다 — 미입력 시 조용히 주입돼 화면·발행을 오염시킨다. 확신 없으면 기본값 주입 대신 빈값+개입큐(#353·#355 연장).
- 반복되는 오염(홍합)의 근본을 찾을 때, 증상 발생 지점(매처)뿐 아니라 **값이 최초 주입되는 지점(기본값·프리필)**까지 거슬러 올라간다.
- 클러스터 인덱스 "검증·실측"·"카테고리 매처"에 추가.

## 작업원칙 #358 — 죽은 모델·API를 교체할 땐 대체품도 출처로 검증한다 (2026-09-03)

**사건**: `gemini-2.0-flash`가 2026-06-01 shutdown된 죽은 모델(#348 재발). Code가 대체로 `gemini-3.6-flash`를 넣었는데, Desktop이 **대체 모델도 Google 공식 changelog로 GA 여부 출처 확인**(1일전 문서). 검증 없이 대체품을 믿었으면 또 죽은/미존재 모델일 수 있었다.

**규칙**:
- 죽은 모델/API를 고칠 때 "새 이름을 넣었다"로 끝내지 말고, **대체품이 실재·GA·현재 유효한지 1차 출처(공식 changelog/docs)로 확인**한다. 교체는 새 함정의 시작일 수 있다.
- 모델명은 상수 1곳 격리(#348) + 대체 시 출처 주석(왜 바꿨는지·근거 URL). 이번 gemini.ts 주석이 모범.
- 클러스터 인덱스 "AI 의존성"에 추가.

## 작업원칙 #359 — 하이브리드/폴백 시스템에서 1회 다른 결과는 실패가 아니다. 재현성으로 판정한다 (2026-09-03)

**사건**: quality 프로필(Gemini-first) 첫 호출에서 Groq가 응답 → "Gemini 안 되나?" 의심. 그러나 **3회 연속 재현 테스트하니 전부 gemini-3.6-flash**. 첫 Groq는 일시적 429→Groq 폴백(하이브리드 설계대로 정상 작동)이었다.

**규칙**:
- round-robin·failover·프로필 기반 provider 선택 시스템에서 **1회 관찰로 "실패/오작동" 단정 금지**. 폴백은 정상 동작이다.
- provider·키·모델 작동 여부는 **연속 N회(≥3) 재현성**으로 판정한다. 1회는 일시적 상태(429·지연)일 수 있다.
- "provider가 예상과 다름"이 관찰되면 → 코드 로직(프로필 순서) 확인 + 재현 테스트. 폴백이 섞이는 건 오히려 이중화가 작동한다는 증거.
- 클러스터 인덱스 "AI 의존성"·"검증·실측"에 추가.


## 작업원칙 #360 — 반응형(모바일) UI 검증은 OS 창 리사이즈가 아니라 실제 뷰포트 반영 여부를 먼저 확인한다 (2026-09-04)

**사건(B2 검증)**: Claude-in-Chrome `resize_window`(390x844) 요청 후 `getComputedStyle`로 액션바를 확인했더니 `bottom:20px, zIndex:40, className:""` — 수정 전 구버전처럼 보였다. 프로덕션 배포 SHA는 이미 `verify-vercel-deploy.sh`로 정확히 확인된 상태라 "배포 안 됐나?"로 잘못 의심할 뻔했다. 근본원인은 `window.innerWidth`를 직접 찍어보니 여전히 1834px — **`resize_window`가 OS 창 크기만 바꿨고 실제 렌더링 뷰포트(devicePixelRatio 포함)는 반영되지 않았다.** 동일 호출을 재시도하니(414x800) 이번엔 `innerWidth:625, matchesLg:false`로 정확히 반영됐고, 그 상태에서 액션바가 `bottom:76px, zIndex:65`(수정 코드 그대로)로 정상 렌더됨을 확인했다.
**규칙**:
- Tailwind `lg:`류 반응형 클래스를 검증할 때, OS 창 크기 조정 도구(`resize_window`)만 믿지 말고 **`window.innerWidth`와 `window.matchMedia('(min-width: Npx)').matches`로 실제 반영 여부를 먼저 확인**한다 — CSS 미디어쿼리는 뷰포트 픽셀 기준이지 OS 창 픽셀 기준이 아니다.
- 1차 시도가 기대와 다른 결과(구버전처럼 보임)를 내면, 배포 문제로 단정하기 전에 **검증 도구 자체의 성공 여부를 먼저 재확인**한다(#359 연장 — 검증 도구도 1회 실패로 단정하지 않고 재시도해 재현성을 본다).
- 모바일 UI 회귀 검증 표준 절차: ① `resize_window`로 목표 폭 요청 → ② `window.innerWidth`/`matchMedia`로 실제 반영 확인(불일치 시 재시도) → ③ 반영 확인 후에만 스크린샷·`getComputedStyle`로 실제 UI 검증 → ④ 필요 시 `getBoundingClientRect()`로 요소 간 겹침(overlap) 여부를 좌표로 정량 확인(육안 스크린샷 판단보다 정밀).
- 클러스터 인덱스 "검증·실측"·"UI/반응형"에 #360 추가.


## 작업원칙 #361 — 병합 직후 검증 스크립트의 "OK"도 도구 결과다. 배포는 QUEUED/BUILDING 구간이 있어 원본 상태를 재확인해야 한다 (2026-09-04)

**사건(UCE-10 병합 검증)**: main fast-forward 병합 직후 `verify-vercel-deploy.sh --wait`를 실행해 "OK — production is on {새커밋}"을 받았다. 그런데 곧바로 프로덕션 API를 호출해보니 결과가 수정 전(구버전) 그대로였다. `Vercel:get_deployment`로 배포 ID를 직접 조회하니 새 배포가 그 시점에 `state:QUEUED`였고, 스크립트는 그 직전 배포(구버전, 이미 READY)를 "정확한 최신"으로 오판했다. 약 2분 뒤 배포가 `READY`+프로덕션 `alias`에 실제로 걸린 뒤에야 API가 정상 결과를 반환했다.

**같은 세션의 별도 사건(발행게이트 병합)**: 원래 미병합 브랜치(`b077cbe`, #355/#356 정합성검사)를 병합하려다 3개 파일에서 merge conflict가 났다. 조사 결과 **같은 날 다른 세션이 같은 목적을 다른 구현으로 이미 main에 병합·배포 완료**한 상태였다(`274bbf2`). conflict가 우연히 이를 드러냈을 뿐, 사전에 확인하지 않았다면 어느 쪽이 "정답"인지 판단 없이 강제 병합했을 위험이 있었다.

**규칙**:
- 병합 직후 검증 스크립트가 "OK"를 반환해도, **그 응답이 도구의 판단이지 원본 배포 상태 자체가 아님을 기억한다.** API 응답이 기대와 다르면(수정이 반영 안 된 것처럼 보이면) 스크립트를 재실행하기 전에 `get_deployment`/`list_deployments`로 배포의 실제 `state`(QUEUED/BUILDING/READY)와 `alias`(프로덕션 도메인 포함 여부)를 직접 조회해 원본으로 재확인한다.
- 병합 전 스크립트 "OK"를 받은 즉시 검증하지 말고, 특히 빌드 시간이 있는 배포 환경에서는 배포가 완전히 READY됐다고 확신할 때까지(수 분 소요 가능) 프로덕션 실측을 늦추거나, 배포 상태를 폴링한다.
- **오래 대기 중인 미병합 브랜치를 병합하기 전, 반드시 `git log --all -- <관련 파일/디렉토리 경로>`로 같은 목적의 코드가 이미 main에 다른 형태로 들어와 있지 않은지 사전 확인한다.** 병렬 세션(Desktop/Code/Cowork)이 동시에 같은 문제를 다룰 수 있고, merge conflict가 우연히 이를 드러내지 않을 수도 있다(예: 완전히 다른 파일명으로 구현됐다면 conflict조차 안 남).
- 두 구현이 충돌해 발견되면, 실측(테스트 커버리지·설계원칙 부합도·이미 배포됐는지)으로 어느 쪽을 채택할지 판단하고, 폐기하는 쪽은 브랜치·워크트리를 안전하게 정리한다(#354 working tree 확인 절차 그대로).
- 클러스터 인덱스 "배포·검증"·"병렬작업 위생"에 #361 추가.


## 작업원칙 #362 — 매처/스코어링 코드는 손으로 읽고 결과를 예단하지 않는다. 반드시 실행해 확정한다 (2026-09-04)

**사건(UCE-11 결함A)**: "넥타이"→"넥타"로 스트리핑되는 트레일링 "이" 결함을 COMPOUND_NOUNS 등재로 고치려던 중, `headNounWeight()`의 부분포함 관용성(`p.includes(term) || term.includes(p)`) 때문에 "넥타"도 "넥타이"에 포함되므로 이 수정이 "넥타이" 자체의 최종 분류 결과엔 영향이 없을 것이라고 코드를 손으로 읽고 예단했다. 그런데 실제로 `test:category-match`를 돌려보니 정반대 — 수정 후 "넥타이"가 출산/육아>유아동잡화(오답)에서 패션잡화>패션소품(정답)으로 실제 전환됐다. 여러 스코어링 항(termMatchScore·headNounWeight·branchBreadthBonus 등)이 얽힌 코드는 손트레이스로 최종 결과를 예측하기 어렵다.

**규칙**:
- 여러 스코어링/가중치 항이 조합되는 매처·랭킹 로직을 수정할 때는, 그 영향을 "코드를 읽고 판단"하지 말고 **반드시 실행해서 실제 출력을 확인**한다(`test:category-match` 같은 회귀 스위트 실행, 또는 대상 함수를 직접 호출하는 1회성 스크립트).
- "이 수정은 이 케이스엔 영향 없을 것"이라는 사전 판단은 코드 리뷰/설계 논의에서는 유효해도, **완료 보고나 스코프 판단의 근거로 쓰지 않는다** — 실행 결과가 나오기 전까지는 가설일 뿐이다.
- #350(표면 신호 실측 없이 실패로 단정하지 않음)·#352(테스트 green ≠ 전수 안전)와 같은 계열 — "코드를 읽었으니 안다"는 착각을 실행으로 교정한다는 점에서 #350의 반대 방향(과소평가가 아니라 과소평가/과대평가 둘 다 실행으로만 교정 가능) 확장.
- 클러스터 인덱스 "검증·실측(구조≠내용)"에 #362 추가.

## 작업원칙 #363 — 상품 검증은 "전 상품 적용 시 문제없는가"로 확장, 개선/오류체크는 확실한 출처로 교차검증해 환각 제거 (2026-09-06, 운영자 작업룰 격상)

**운영자 명시 작업룰(반복 강조 → 정식 원칙)**: 상품을 테스트·검증할 때
특정 상품 하나만 보지 말고 **"어떤 상품을 적용해도 문제없는가"로 확장**해
전 상품 공통(#62) 관점으로 체크하고, 그게 앱에 실제 적용되는지 다시
확인한다. 개선·오류체크 시 **확실한 출처로 교차검증**해 환각을 없애고
최신 정보로만 적용한다.

**이번 세션 실증 사례(왜 이 원칙이 결정적인가)**:
- 카테고리 백필: code 반환만 보고 "됐다" 안 하고 4개 전부 상품↔마스터
  대조 → LED"차량용"가습기가 일반가습기 아닌 자동차용품>차량용가습기로
  정확 구분됨을 확인(네이버 실등록값 교차검증). 코드만 믿었으면 놓쳤을 것.
- 결함B 우산: Code 리포트만 믿지 않고 독립 dryRun + 프로덕션 API 재검증
  → 넥타이 회귀0 직접 확인.
- disposition 결함1: 명화 1건이 아니라 발행 6개 전수 대입 → 5개 오판
  발견(전 상품 확장). 나아가 grep으로 publish-gate에도 동일 버그 확인.
- "재고있음+판매종료"·"유리받침"·무한업데이트 경고: 표면을 결함으로
  단정 않고 크롤소스·캐시DB·로컬재현 교차검증 → 전부 "정상" 확정(환각 제거).

**실행 규칙**:
1. 단일 상품 통과 = 검증 완료 아님. 카탈로그 밖 임의표본 or 전수로 확장(#352).
2. "코드가 반환함/테스트 통과함" ≠ "실제 올바름". 결과값을 권위 출처
   (마스터·네이버GET·DB실측)와 대조해 의미까지 검증.
3. 다른 세션(Code) 리포트는 독립 재현으로 교차검증 후에만 신뢰(#45).
4. 표면 이상신호는 3중(프로덕션/DB·캐시/로컬재현) 중 2+ 교차 없이
   결함/정상 단정 금지(#350, CORE 기둥1).
5. 이 원칙으로 얻은 통찰은 즉시 학습화(문서 커밋)한다.
- 클러스터 인덱스 "검증·실측"·"전상품공통"에 #363 추가.

## 작업원칙 #364 — Code 세션 착수 전 "이미 main에 반영됐는지" 필수 확인(worktree 재활용 중복작업 방지) (2026-09-07)

**반복 관찰**: worktree가 소실/재활용되면 Code 세션이 오래된 스냅샷에서
시작해 **이미 병합·검증 완료된 작업을 처음부터 재구현**하는 낭비가
반복됨. 이번(supplier-code 즉시폴링): Desktop이 045aff8로 병합 + 프로덕션
실측(접이식트렁크 재고1285)까지 rev152에서 마쳤는데, 다음 Code 세션이
같은 pollSingleProduct/route/UI를 워크트리에서 재구현("이번에 구현 완료"
리포트). 다행히 main은 무손상(중복 미병합)이나 토큰·시간 낭비.

**규칙**:
1. Code(및 모든 세션) 착수 전 반드시 `git fetch && git log origin/main`으로
   최신 확인 + 인계받은 작업의 핵심 심볼이 이미 있는지 grep
   (예: `grep -c pollSingleProduct src/...`). 있으면 "이미 완료"로 보고,
   재구현 금지.
2. 인계 메시지에 "이 작업이 이미 병합됐는지 먼저 grep 확인" 체크를 명시.
3. worktree HEAD가 origin/main보다 뒤처졌으면(behind) 먼저 sync 후 판단.
4. Desktop은 인계 시 "완료된 것/미완인 것"을 커밋해시와 함께 명시해
   Code가 중복 안 하도록(rev 트래커 최신 상태 항상 유지).
- 클러스터 인덱스 "핑퐁·병렬작업 위생"에 #364 추가(#361 연장).

## 작업원칙 #365 — 병렬작업 누락 방지: 착수 전 미완목록 전수확인, 완료는 실측증거로만 마킹 (2026-09-07, 운영자 지시)

**배경**: 병렬작업이 많아지면 "계획엔 있으나 실제 진행 안 된" 항목이
누락됨. 실증: 트리아지 B4~B15가 문서에 정리됐으나 1군(B1/2/3/8/10)만
완료되고 2·3·4군은 통째 미착수인 걸 운영자가 지적("누락된 것 같다")해서야
발견. 크롤UPSERT는 반대로 grep오탐으로 미완 오판할 뻔.

**규칙**:
1. **단일 미완 레지스트리 유지**: PARALLEL_WORK_TRACKER 최신 rev에 "완료/
   미완/대기(의존)"를 항상 최신화. 새 세션·병렬작업 착수 전 이 목록을
   먼저 읽어 "빠진 것 없나" 전수 확인(문서가 진실원천 #319).
2. **완료 마킹은 실측증거 필수**: "완료"로 쓰려면 코드 grep+DB/프로덕션
   실측+(가능하면)브라우저 중 최소 2개 증거. grep 단일 오탐 주의 —
   키워드가 안 맞아 미완 오판(크롤UPSERT) or 심볼만 있고 미작동 오판
   양방향 모두 재확인(#363).
3. **의존성 태깅**: 각 미완 항목에 [독립/의존:X] 표기. 병렬 착수 시
   독립 항목만 동시 진행, 의존 항목은 선행 완료 확인 후. 의존성 오판으로
   빈껍데기 만들지 않기(개선2/3/4가 폴링데이터 의존인 걸 뒤늦게 안 사례).
4. **핑퐁 중복 방지(#364 연계)**: Code 인계 항목이 이미 완료됐는지
   착수 전 grep. 완료된 걸 재인계하지 않기.
5. **세션 종료 시 미완 목록 갱신 필수**: 이번 세션에서 완료/발견/이관된
   것을 rev로 남겨 다음 세션이 누락 없이 이어받음.
- 클러스터 인덱스 "핑퐁·병렬작업 위생"에 #365 추가(#361·#364 연장).

## 작업원칙 #366 — 토큰비용 vs 운영자 직접처리: "운영자가 빠른 것"은 대신하지 말고 권고 (2026-09-07, 운영자 지시)

**배경**: supplier_code 5개 연결 시 도매꾹 상품번호를 Claude가 찾으려면
검색+추정+오판위험(#환각)까지 감수하며 토큰을 많이 쓰는데, 운영자는
자기 화면(도매꾹 판매내역·주문서)에서 몇 초면 확인 가능. 이런 "정보
자체를 운영자만 확정할 수 있거나, 운영자가 훨씬 빠른 작업"을 Claude가
무리해서 대신하려 하면 비효율+오판 위험 둘 다 커짐.

**규칙**:
1. 작업 착수 전 자문: "이 정보/조작을 운영자가 직접 하면 미미한 시간
   (수초~수분)인데, Claude가 하면 검색·추정·왕복 확인으로 훨씬 오래
   걸리거나 오판 위험이 있는가?" → 그렇다면 **먼저 운영자에게 권고**하고
   대신하지 않는다.
2. 특히 다음 케이스는 항상 운영자 권고 우선: (a) 운영자만 아는 식별정보
   (도매꾹/공급사 상품번호, 계정정보), (b) 운영자 화면에서 즉시 보이는
   값(주문내역·판매자센터), (c) 잘못 추정하면 되돌리기 어려운 식별자 연결.
3. 권고 시 "왜 Claude가 못/안 하는지"를 정직히 설명(추측 금지 사유
   포함) + 운영자가 그대로 따라할 수 있는 단계별 가이드 제공(비개발자
   기준, 화면 캡처로 위치 확정).
4. 반대로 "코드 3줄 고치면 되는데 운영자가 수동으로 매번 반복하는" 것은
   Claude/Code가 시스템화(#62 전상품공통 개입점)해서 반복노동 자체를 없앤다.
   #366은 "이번 1회성 정보값"에 한정 — 반복작업 자동화 원칙(#62)과 배치되지 않음.

**실증**: supplier_code 5개 — Claude가 도매꾹 검색으로 추정하면 오판위험
(엉뚱한 상품 재고연결)+토큰비용 큰데, 운영자는 본인 판매내역에서 바로 확인
가능 → 가이드 문서(SUPPLIER_CODE_CONNECT_GUIDE)로 권고, 대신 시도 안 함.
- 클러스터 인덱스 "핑퐁·역할분담"에 #366 추가.

## 작업원칙 #367 — GraphQL·스키마 기반 외부API는 "성공 응답"이 "필드명이 맞다"의 증거가 아니다 (2026-09-09, 실전발견)

**배경**: 오너클랜 GraphQL 어댑터에서 `item.stock`이라는 존재하지 않는
필드를 쿼리했는데, 인증도 성공하고 HTTP 200에 `data` 필드까지 정상 반환
됐음. 하지만 `stock`은 스키마에 없는 필드라 응답에서 조용히 null/누락
처리되어, 실제로는 "쿼리 실패"인데 코드 상으로는 "성공 + 값만 항상 -1"로
보여 원인을 못 찾고 헤맴(인증 문제로 오인해 되돌아가는 시행착오 발생).

**규칙**:
1. **REST 4xx/5xx만 에러 신호로 믿지 말 것**: GraphQL·유사 쿼리 언어
   기반 API는 최상위 HTTP 상태가 200이어도 개별 필드 요청이 스키마에
   없으면 그 필드만 조용히 null/누락될 수 있다. "인증 성공+에러 없음"은
   "요청한 필드명이 실재한다"의 증거가 아니다.
2. **필드명은 반드시 공식 스키마 문서와 1:1 대조 후 사용**: 운영자가
   제공한 공식 매뉴얼(PDF 등)이 있다면 그 문서의 Method/Type 섹션에서
   정확한 필드명을 그대로 인용하고, "합리적으로 추정한 필드명"(stock,
   inventory 같은 일반적인 이름)을 마음대로 붙이지 않는다. 문서에 없는
   추정 필드는 즉시 표시(#231)하고 검증 전까지 신뢰하지 않는다.
3. **"인증 성공"과 "데이터 정확"은 별개 검증 단계**: 3rd-party API 연동
   시 (a)인증 성공 여부 (b)스키마 필드 정확성 (c)실제 값의 합리성, 이
   3단계를 순서대로 개별 확인. 1단계 통과했다고 2·3단계를 건너뛰지 않는다.
4. **의심스러운 -1/null/0 값은 "정상적으로 없는 값"과 "쿼리가 잘못돼서
   못 가져온 값"을 구분**: 모든 상품이 예외 없이 같은 기본값을 반환하면
   필드명 오류를 의심한다(#231 연장 — 침묵 실패의 새 유형).

**실증**: 오너클랜 Seller Manual v0.11.0(운영자 제공 PDF, 74p) 대조 결과
Item 타입에 top-level 재고 필드 자체가 없고 `options[].quantity`(SKU
단위)만 존재. 어댑터를 options.quantity 합산으로 재작성 후 실제 재고
(29,997개·9,999개) 정상 확보, DB 스냅샷 교차검증까지 완료.
- 클러스터 인덱스 "외부 API 연동·검증"에 #367 신규 추가.

## 작업원칙 #368 — 사람이 입력한 인증정보는 "저장 성공"과 "값이 맞다"를 분리 검증 (2026-09-09, 실전발견)

**배경**: 오너클랜 계정을 웹 UI에 입력→암호화 저장까지는 매번 성공
(HTTP 200, DB에 값 존재)했지만, 실제 인증은 계속 401 실패. DB에 저장된
암호화값을 직접 복호화해 문자코드를 검사한 결과 **한글이 섞인, 명백히
비밀번호가 아닌 다른 텍스트**가 저장돼 있었음(클립보드에 다른 텍스트가
남아있다가 잘못 붙여넣기 됐을 가능성). 저장 로직 자체는 무결했음 — 입력값
자체가 오염된 채로 들어온 것.

**규칙**:
1. **"저장 API가 200을 반환했다"는 "사람이 의도한 값이 저장됐다"의
   증거가 아니다**: 폼 입력값은 클립보드 잔존 내용, IME 조합 중 오타,
   자동완성 개입 등으로 오염될 수 있다. 반복 실패 시 저장된 값 자체를
   (권한 범위 내에서, 값을 노출하지 않고) 길이·문자종류·특수문자
   존재 여부만 점검해 "명백히 잘못된 값"인지 먼저 배제한다.
2. **비밀번호를 Claude가 직접 다루지 않는 원칙은 절대 타협하지 않되,
   검증 방법은 유연하게 설계**: 운영자가 원해도 Claude가 값을 보거나
   대신 입력하지 않는다. 대신 운영자가 안전하게 직접 검증할 수 있는
   절차(예: 신뢰 가능한 빈 컨텍스트에서 콘솔로 직접 API 호출)를
   제공한다. 단, 그 절차 자체도 사전 검증 없이 권고하면 안 된다(#368-3).
3. **콘솔/스크립트 기반 자가검증 절차를 제안하기 전, 그 환경의 제약을
   먼저 확인**: 대상 사이트 자체의 콘솔에서 실행하면 그 사이트의 CSP
   (Content-Security-Policy)에 막혀 검증 자체가 실패할 수 있다(오너클랜
   웹사이트 콘솔에서 자기 API로도 요청이 CSP에 의해 차단된 사례). 외부
   API를 브라우저 콘솔로 직접 두드리게 할 때는 CSP 없는 컨텍스트
   (about:blank 등)를 사용하도록 안내한다.
4. **재입력을 요청할 땐 "복사 붙여넣기 대신 직접 타이핑"을 권고**:
   클립보드 오염이 의심되는 경우, 같은 방식(붙여넣기)으로 재시도하면
   같은 오류가 재발할 수 있다.

**실증**: 저장된 암호화값 복호화 → 문자코드 검사(첫 3자·끝 3자 charCode)로
"한글 포함 = 비밀번호 아님" 판정 → 정직 보고 → 운영자가 about:blank
콘솔에서 직접 fetch로 실제 오너클랜 서버에 인증 성공 확인(JWT 발급됨) →
"아이디/비번 자체는 맞다"를 먼저 확정한 뒤 재입력 요청 → 성공.
- 클러스터 인덱스 "인증정보 처리 안전"에 #368 신규 추가.

## 작업원칙 #369 — "크론이 N번 도는가"와 "그 크론이 실제로 소스까지 닿아 새 데이터를 가져오는가"는 별개 검증 (2026-09-09, 재점검으로 발견)

**배경**: B15("재고 데일리 2회") 완료를 코드리뷰+브라우저실측으로 확정
판정했으나, 운영자 재점검 지시로 다시 파고든 결과 실제 갭 발견. Code의
구현(`cron/stock-alert-pm`)은 크론이 실제로 하루 2번 도는 것까진 정확
했지만(vercel.json 등록 확인), **오후 크론이 재고를 새로 가져오지 않고
오전 크론이 쌓은 스냅샷을 그대로 재사용해 알림만 다시 보내는 구조**였다.
즉 "크론 실행 횟수 2회"는 맞았지만 "재고 재조회 횟수 2회"는 거짓이었다
— 오후에 새로 품절된 상품은 다음날 자정까지 시스템이 전혀 몰랐음.

**근본 원인**: 커밋 메시지·코드 주석이 "크론 2회"라고만 표현해, 검증하는
쪽(Desktop)이 "크론이 2번 돈다=요구사항(재고 2회 갱신) 충족"으로 등치
판단해버림. 크론 등록·트리거 확인에서 검증을 멈추고, 그 크론이 호출하는
함수 체인이 실제로 외부 API(도매매/오너클랜)까지 닿는지 끝까지 추적하지
않았다.

**규칙**:
1. **"자동화됐다"는 주장을 검증할 때 체크리스트를 반드시 끝까지**:
   (a) 크론/스케줄이 등록됐는가 (b) 그 스케줄대로 실제로 트리거되는가
   (c) **트리거된 핸들러가 실제로 소스(외부 API·DB원본)까지 호출하는가**
   (d) 그 결과가 후속 로직(알림·판정)에 실제로 반영되는가. (a)(b)만
   확인하고 "자동화 완료"로 판정하지 않는다 — (c)가 빠지면 "실행은 되는데
   새 데이터는 없는" 크론이 완성돼도 알아채지 못한다.
2. **"알림/로그가 N번 발생한다"와 "원본 데이터가 N번 갱신된다"를 혼동하지
   않는다**: 같은 데이터를 재사용해 알림만 반복하는 것과, 실제로 데이터를
   다시 가져와 알림을 보내는 것은 사용자 관점에서 전혀 다른 가치를 준다.
   요구사항 문구에 "체크"·"확인"·"갱신" 같은 동사가 있으면 그 동사가
   가리키는 대상(재고 수치 자체)이 실제로 새로 바뀌는지 함수 호출
   그래프로 끝까지 추적한다.
3. **"완료 확인"이 코드리뷰+일부 실측(브라우저)이었다고 완전 검증이
   끝난 게 아니다**: 이번 사례는 코드 diff를 정밀 검토하고 브라우저로
   B11·B13을 실측했음에도, B15의 "재조회" 부분은 함수 호출 그래프까지
   추적하지 않아 놓쳤다. 특히 "크론"·"자동화" 관련 항목은 한 번 더
   "이 핸들러가 실제로 외부 API를 호출하는 코드가 있는가"를 grep으로
   재확인하는 습관화가 필요하다.

**실증**: `stock-alert-pm/route.ts`에 `pollAppRegisteredInventory()`
(도매매/오너클랜 API를 실제로 호출하는 함수) 호출이 없었음을 grep으로
확인 → 재고 재조회 없이 알림만 재발송하는 구조임을 확정 → 알림 로직
전에 그 함수를 호출하도록 근본수정(d2c5927)하여 실제 "재고 재조회
2회/day"를 완성.
- 클러스터 인덱스 "검증·실측(구조≠내용)"에 #369 추가.

## 작업원칙 #370 — "1곳 수정 완료"는 "같은 API/함수의 전체 소비처 수정 완료"와 다르다 (2026-09-10, 재점검 지시로 발견)

**배경**: 원본메모 실측 재검증으로 카테고리 오분류("강아지 얼굴망"→"출산/육아")
근본원인(needsConfirmation 플래그를 UI가 무시)을 정확히 찾아 씨앗심기의
"카테고리 자동 추천" 버튼 1곳을 수정·배포·브라우저 검증까지 마치고
"완료"로 보고했다. 그런데 운영자가 "테스트한 상품 말고 전체 상품에도
문제없는지 확장 확인"을 지시하자 재검증한 결과, **같은 API
(`/api/category/suggest`)를 호출하는 지점이 실제로는 3곳**이었고
(products/new/page.tsx 자동프리필 경로, crawl/page.tsx 크롤 자동추천),
고친 것은 그중 1곳뿐이었다. 나머지 2곳 중 하나(자동 프리필)는 오히려
사용자 클릭 없이 조용히 실행되는 더 위험한 경로였다.

**규칙**:
1. **API 엔드포인트나 공용 함수의 동작을 고칠 때는 먼저 그 호출부 전체를
   grep으로 센 뒤, 발견된 개수만큼 전부 고쳤는지 확인하고 나서 "완료"라고
   말한다.** "이 화면에서 재현된 버그를 고쳤다"는 "이 API를 쓰는 모든
   화면에서 안전하다"를 의미하지 않는다.
2. **버그를 처음 발견한 진입점(사용자가 보고한 화면)이 우연히 가장 눈에
   띄는 곳일 뿐, 가장 위험한 곳이 아닐 수 있다** — 이번 사례에서 실제로
   더 위험했던 건 사용자가 버튼을 누르지 않아도 자동 실행되는 경로였다.
   신고된 진입점만 고치고 멈추면 그보다 위험한 자동 실행 경로가
   미수정 상태로 남는다.
3. **수정 완료 보고 전 체크리스트**: (a) 이 API/함수를 호출하는 모든
   위치를 grep했는가 (b) 각 위치에서 문제되는 조건(이번엔
   `needsConfirmation`)을 동일하게 확인·처리하는가 (c) 이미 이 조건을
   올바르게 처리하고 있던 곳이 있다면(이번 sourcing-recommender.ts처럼)
   그 기존 구현 패턴을 그대로 재사용했는가, 신규 발명하지 않았는가.
4. **연관된 다운스트림 인프라(이번엔 `internalTags`의
   `category_confirm_needed` 태그, 대시보드 게이트)도 함께 확인**한다 —
   백엔드에 이미 있는 안전장치가 실제로 새 수정과 정합성 있게 작동하는지
   (이번엔 이미 올바르게 설계돼 있었음을 확인) 검증 없이는 "완료"가 아니다.

**실증**: grep으로 `/api/category/suggest` 호출부 전수 확인 → 3곳
확정 → 1곳은 이미 수정, 2곳 신규 발견·수정(products/new 자동프리필에
`unconfirmed` 상태 신설, crawl 페이지 배지 조건부화) → 별도 소비처
sourcing-recommender.ts는 이미 올바르게 구현돼 있음을 확인(재발명 방지)
→ 백엔드 `category_confirm_needed` 태그 인프라 정합성 확인.
- 클러스터 인덱스 "전상품공통·재발명금지"에 #370 신규 추가.

## 작업원칙 #371 — 같은 이름을 쓰는 서로 다른 시스템이 설명 없이 병치되면 "버그"로 보인다 (2026-09-10)

**배경**: rev163~164에서 카테고리 오분류를 근본수정(Groq+Gemini 이중검증)
했는데도 대표님이 "여전히 매칭 오류"를 재현·보고. 실제 재현 결과 상단
배너(네이버 카테고리, 이미 정확해짐)와 우측 패널(내부 참고용 분류,
별개 시스템)이 서로 다른 카테고리명을 동시에 보여줘 혼란 발생 — 실제
매칭 로직에는 이미 문제가 없었고, "설명 없는 두 시스템 병치"가
진짜 원인이었다.

**규칙**:
1. **"같은 화면에 비슷한 이름의 값이 여러 개 뜬다"는 그 자체로 검증
   대상**이다 — 각각이 어느 시스템에서 왔는지, 왜 다를 수 있는지
   설명이 없으면 사용자는 무조건 "오류"로 인식한다. 기능이 정확히
   설계대로 작동해도 그렇다.
2. **버그 신고를 받으면 "코드가 맞는지"만 확인하지 말고 "사용자가
   실제로 보는 화면 전체"를 재현**해야 한다. API 레벨 검증(이번엔
   `/api/category/suggest` 응답이 정확함)만으로 "해결됨"이라 결론
   내리면 안 된다 — 같은 화면의 다른 위젯이 다른 값을 보여주고 있을
   수 있다.
3. **의도적으로 분리된 두 시스템이 우연히 같은 필드명·같은 UI 항목
   슬롯을 공유하고 있다면, 그 공유 자체가 실제 버그일 수 있다**
   (이번 사례: `upload-readiness.ts`의 `categoryPassed`가 두 독립
   신호를 AND로 묶어, 하나(내부 참고용)가 약해도 다른 하나(네이버
   카테고리, 실제 상품등록 필수)까지 "실패"로 끌어내렸다). "왜 다른
   시스템인데 하나의 판정으로 합쳐지는가"를 반드시 확인한다.
4. **가중치·합계 같은 부수 수치도 이 기회에 재검산**한다(이번에
   WEIGHTS 합이 100이어야 한다는 주석과 달리 실제로 110이었던 것을
   발견) — 관련 코드를 열어본 김에 인접한 불변조건도 검증하는 습관.

**실증**: `resolveConfidentCategory`(내부참고, AI없음)와
`/api/category/suggest`(네이버노출, AI크로스체크)가 완전히 독립적으로
설계돼 코드 주석에도 명시돼 있었으나, `calcUploadReadiness`의
`categoryPassed` 계산 하나가 둘을 묶어 UX 혼란을 유발. `category_db_ref`
항목으로 분리(가중치 1, 참고용) + `categoryPassed`는 네이버 카테고리만
반영하도록 수정 후 브라우저로 실측 검증 완료.
- 클러스터 인덱스 "검증·실측(구조≠내용)"에 #371 추가.

## 작업원칙 #372 — 데이터가 A화면→B화면으로 "넘어간다"는 기능은 송신·수신 필드를 1:1 대조해야 한다 (2026-09-10)

**배경**: "크롤 정보가 씨앗심기로 넘어갈 때 제대로 안 들어간다"는 보고.
crawl이 prefill로 보내는 필드와 씨앗심기가 읽는 필드를 grep으로 1:1
대조하니 crawlProductNo(공급처 상품번호)·crawlInventory·crawlNaverFeeRate
등이 송신은 되는데 수신부에 아예 없어 조용히 버려지고 있었다. 게다가
진입점(단건 vs bulk)마다 보내는 필드셋·타입(options 객체 vs 문자열)이
달라 일부 경로만 정상이었다.

**규칙**:
1. **"데이터 이관/prefill/전달" 기능은 반드시 송신측 필드 목록과 수신측
   읽는 필드 목록을 나란히 놓고 대조**한다. 송신 코드만 보고 "다 보내니까
   되겠지"라고 판단하면 안 된다 — 수신측이 그 필드를 안 읽으면 조용히
   버려진다(에러도 안 남).
2. **직렬화로 전달되는 데이터(base64/JSON/쿼리스트링)는 타입 검증기가
   없어 필드명 오타·타입 불일치가 런타임까지 안 잡힌다.** 반드시 공용
   스키마 타입 하나(interface + encode/decode 헬퍼)로 정의해 송·수신이
   같은 정의를 import하게 만든다(#295 단일권위) — 그래야 필드 추가 시
   한쪽만 빠지는 걸 컴파일러가 잡는다.
3. **같은 "전달" 기능에 진입점이 여러 개면(단건/일괄/재시도 등) 전부
   같은 스키마·같은 필드셋을 쓰는지 확인**(#370 연장) — 이번엔 단건은
   options를 객체로, bulk는 문자열 배열로 보내 bulk 경로만 옵션 재고가
   누락됐다.
4. **수신 로직이 여러 곳으로 쪼개져 있으면(이번엔 3개 useEffect가 같은
   payload를 각자 재파싱) 그 자체가 필드 누락의 온상**이다 — 한 곳에서
   한 번 파싱해 구조화된 객체로 만들고 그걸 나눠 쓰도록 통합 권장.

**실증**: crawl/page.tsx 4개 진입점 vs products/new/page.tsx 3개 수신
useEffect를 grep 전수 대조 → 결함 A(미수신 필드)·B(진입점별 스키마
불일치)·C(수신 로직 분산) 확정. prefill 공용 스키마 도입으로 근본
해결하도록 진단서(docs/plan/CRAWL_PREFILL_GAP_HANDOFF_2026-09-10.md)
작성·Code 인계.
- 클러스터 인덱스 "시스템 승격(단건 금지)"에 #372 추가.

## 작업원칙 #373 — "저장 안 됨" 결함은 프론트 payload·백엔드 허용목록·로딩 복원 3지점을 모두 확인해야 한다 (2026-09-10)

**배경**: "상품 수정/저장 시 정보가 제대로 안 들어간다"는 보고. 조사
결과 supplier_product_code가 DB엔 있는데 씨앗심기에서 유실되는 원인이
한 곳이 아니라 3개 지점의 조합이었다 — (1)저장 payload에 필드 없음
(2)편집 로딩 시 폼에 복원 안 함 (3)백엔드 허용목록. 다행히 (3)은
Prisma DMMF 자동추출이라 이미 통과됐지만, (1)(2)가 동시에 빠져 있었다.

**규칙**:
1. **"저장이 안 된다"는 결함은 반드시 3지점을 각각 확인**한다:
   (a) 프론트가 그 필드를 저장 payload에 넣는가
   (b) 백엔드(API/ORM sanitize/allowlist)가 그 필드를 받아 쓰는가
   (c) 다시 열었을 때 그 필드를 폼에 복원하는가.
   하나만 고치면 "저장은 되는데 안 보임" 또는 "보이는데 저장 안 됨"이
   된다. 사용자에겐 둘 다 "저장 안 됨"으로 보인다.
2. **"DB에 값이 있는가"부터 먼저 확인**(Supabase 직접 쿼리)하면 (b)
   백엔드 문제인지 (a)(c) 프론트 문제인지 즉시 갈린다 — 이번엔 DB에
   값이 있는 상품(31763951)과 없는 상품(null)이 공존해, "백엔드는 되는데
   프론트 특정 경로에서만 누락"임을 빠르게 특정할 수 있었다.
3. **양방향(저장·로딩)이 짝을 이루는 필드는 공용 매핑 한 곳에서 관리**
   (이 프로젝트의 product-form-mapping.ts serialize/hydrate 쌍) — 그래야
   필드 추가 시 한 곳만 고쳐 양방향이 동시에 복구되고, 한쪽만 빠지는
   #373류 결함이 구조적으로 방지된다(#62).
4. **PATCH-style PUT에서 "값 있을 때만 emit"**: 빈 값을 payload에 넣으면
   기존 DB 값을 ''로 덮어쓸 수 있다 — 특히 재고추적 연결키처럼 유실되면
   치명적인 필드는 반드시 non-empty 가드를 건다.

**실증**: DB 직접쿼리로 supplier_product_code가 상품별로 있음/없음
확인 → GET API는 값 반환하나 폼 미복원 확인 → product-form-mapping.ts
serialize+hydrate 양방향에 필드 추가 + 두 저장경로 일관 적용 → 브라우저
실측(편집열람 시 입력창에 코드 정확히 로드) 검증.
- 클러스터 인덱스 "비가역 쓰기·안전"에 #373 추가.

## 작업원칙 #374 — "이 코드는 올바르다"를 확인해도, 그게 실제 프로덕션에서 쓰이는 경로인지 별도로 확인해야 한다 (2026-09-15)

**배경**: 원본메모 "엑셀 추가이미지가 콤마로 잘못 들어간다" 조사 중,
`excel-generator.ts`를 열어보니 이미 정확히 고쳐져 있었다("images.
slice(1).join('\n')", 주석까지 "newline-separated per spec"). 이걸 보고
"이미 해결됨"으로 판단할 뻔했으나, 실제로 화면의 다운로드 버튼을 추적한
결과 그 파일은 **어느 API 라우트에서도 호출되지 않는 죽은 코드**였고,
진짜 쓰이는 엔진(`naverExcelJS.ts`)에는 결함이 그대로 남아있었다.

**규칙**:
1. **"이 함수/파일의 로직이 올바르다"를 확인하는 것과 "이 함수/파일이
   실제로 사용자가 누르는 버튼에서 실행된다"를 확인하는 것은 완전히
   별개의 검증**이다. 후자를 생략하면 죽은 코드를 보고 "고쳐졌다"고
   오판할 수 있다.
2. **버그 재조사 시 반드시 "화면의 버튼/액션 → 정확히 어느 API 라우트
   → 정확히 어느 함수"까지 실제 import 체인을 따라 추적**한다. 같은
   기능을 하는 것처럼 보이는 파일이 여러 개 있으면(이번엔
   excel-generator.ts와 naverExcelJS.ts), 이름이나 위치가 그럴듯해
   보여도 실제 호출 여부를 grep으로 확정하기 전엔 그중 어느 것도
   "이것이 진짜"라고 가정하지 않는다.
3. **실제 프로덕션 경로를 찾은 뒤에는, 그 경로의 필드 매핑을 원본 스키마
   (Prisma 모델)와 1:1 재대조**한다 — 존재하지 않는 필드명을 참조하면
   TypeScript가 `any` 타입 등으로 인해 컴파일 시점에 못 잡을 수 있고
   (이번 사례: `toNaverProduct(p: any)`), 런타임에도 조용히 undefined가
   되어 값이 빠진 채로 통과된다.
4. **"죽은 코드가 정확하다"는 사실 자체는 참고 가치가 있다** — 그 죽은
   코드가 "정답이 무엇인지"의 근거 자료 역할을 하므로, 실제 프로덕션
   경로를 고칠 때 그 죽은 코드의 정확한 로직을 그대로 재사용/이식하면
   된다(이번엔 재사용은 안 했지만 검증 기준으로 참고했음).

**실증**: `products/page.tsx`에서 `ExcelExportButton` 컴포넌트 →
`/api/naver/excel-export` route → `generateNaverExcelBuffer`
(naverExcelJS.ts) import까지 실제 코드로 추적해 진짜 경로를 확정. 그
경로의 `toNaverProduct` 함수를 Prisma 스키마와 1:1 대조해 3개 필드명
버그(`additionalImages`→`images`, `deliveryTemplateCode`→
`shipping_template_id`, 옵션필드 전무)를 확정·수정 후 curl+openpyxl로
실제 셀 값까지 실측 검증.
- 클러스터 인덱스 "검증·실측(구조≠내용)"에 #374 추가.

## 작업원칙 #375 — 하나의 업로드/처리 API가 여러 "슬롯 종류"를 서빙하면 종류 정보 자체를 서버에 전달해야 한다 (2026-09-15)

**배경**: 상세페이지 이미지 업로드가 원인 불명으로 실패한다는 보고.
조사 결과 /api/upload/image 하나가 대표(main)·추가(additional)·상세
(detail) 3가지 다른 목적의 이미지를 전부 처리하는데, 클라이언트가
서버에 boolean(isMain) 하나만 넘겨 나머지 두 종류(additional/detail)를
서버가 구분할 방법이 없었다. 그 결과 대표이미지 전용 규격(500x500px
최소)이 무차별로 전부에 적용돼 상세이미지가 부당하게 거부됐다.

**규칙**:
1. **하나의 API가 "종류가 다른 여러 대상"을 처리한다면, 그 종류
   자체를 명시적 파라미터로 전달**해야 한다. boolean 플래그 하나로
   N개(N>2) 종류를 표현하려 하면 정보 손실이 생긴다(이번엔 3종류를
   isMain 하나로 표현하려다 2종류가 뭉개짐).
2. **검증 규칙(크기·형식·용도별 제약)을 추가할 때 "이 규칙이 정말
   모든 호출 케이스에 동일하게 적용돼야 하는가"를 먼저 확인**한다.
   이번 500x500 규격은 네이버 대표이미지 규정에서 온 것인데, 상세
   이미지에도 그대로 적용된 것은 규격의 출처(대표이미지 전용)를
   따라가지 않고 "이미지니까 다 똑같겠지"라고 가정한 결과였다.
3. **catch 블록에서 `String(e)`나 원본 에러 객체를 그대로 사용자에게
   보여주지 않는다** — 실패 원인별(HTTP 상태 있음/응답 파싱 실패/
   네트워크 완전 단절)로 사람이 이해할 수 있는 문구로 분기한다.
   서버가 이미 좋은 한국어 error 메시지를 내려줬다면 그걸 최우선
   사용하고, 그것마저 없을 때만 클라이언트 쪽 fallback 문구를 쓴다.

**보안 부수 발견**: 이번 조사 중 디렉토리를 훑다가 `.save` 확장자
백업 파일 2건이 실제로 git에 커밋되어 공개 저장소에 있는 것을 발견
(원칙#43이 정확히 경고했던 클래스의 재발, 다른 확장자로). `.gitignore`
의 백업 패턴 목록은 한 번 만들고 끝이 아니라, 새로운 백업 확장자
습관(`.save`, `.orig` 등)이 생길 때마다 계속 갱신해야 하는 살아있는
목록임을 재확인.

**실증**: ImageUploadDropzone이 `slotType` 필드를 신규 전달하도록
수정, 서버가 `slotType === 'detail'`일 때만 크기검증을 스킵하도록
분기. 브라우저 fetch로 100x100px 이미지를 detail 슬롯엔 성공(200),
main 슬롯엔 정확히 거부(400, 규격 유지) — 회귀 없이 실측 검증.
`.save` 파일 2건 git rm + `.gitignore`에 `*.save`/`*.orig` 추가.
- 클러스터 인덱스 "시스템 승격(단건 금지)"에 #375 추가.

## 작업원칙 #376 — "완료 브리핑"은 이번 세션에서 실제로 조사한 항목만이 아니라 원본 지시서 전체와 재대조해야 한다 (2026-09-16)

**배경**: 여러 세션에 걸쳐 원본메모+v.01+v.02의 지적사항을 순서대로
처리하며 "완료 현황표"를 매번 갱신해왔다. 그런데 대표님이 "다시 한번
전체적으로 더블체크"를 지시하자, v.02 문서 전체를 처음부터 재정독하고
각 항목을 실제 코드로 재대조한 결과 — 마진 역산기(원하는 실판매가→
필요 판매가 역산+10원단위 절사), 태그 콤마입력+실시간 자동완성, 엑셀
사전검수 신호등, OCR 스캔 신호등 등 v.02가 명시적으로 요구한 4개
이상의 항목이 **완전히 미구현 상태**였는데, 이전 "완료 현황표"에는
이 항목들이 아예 언급조차 안 돼 마치 전부 끝난 것처럼 보이는 오류가
있었다.

**원인 분석**: 완료 현황표를 "이번 세션에서 내가 실제로 조사·수정한
항목 리스트"로 작성했을 뿐, "원본 지시서 전체 목록과 1:1 대조해
각 항목의 실제 상태를 표시"하지 않았다. 즉 표 자체가 암묵적으로
"내가 다룬 것 = 전부"라는 잘못된 전제 위에 있었다 — 다루지 않은
항목은 표에 아예 나타나지 않으니 사용자 입장에선 "언급이 없다=이미
끝났거나 원래 없던 요구사항"으로 오인하기 쉬웠다.

**규칙**:
1. **여러 세션에 걸친 긴 작업(원본 지시서 기반 프로젝트)에서 "완료
   현황"을 보고할 때는, 매번 원본 지시서 전체(모든 md 파일 최신본)를
   처음부터 다시 열어 각 조항을 하나씩 코드/DB로 재대조**한다. 지난
   세션 요약이나 자신의 기억에 의존한 표는 "다룬 것"과 "빠뜨린 것"을
   구분하지 못한다.
2. **지시서가 여러 버전(v.01→v.02처럼 누적/확장)으로 왔다면, 최신
   버전이 이전 버전의 상위집합인지 실제로 라인 단위 대조로 확인**한다
   — 이번 사례에서 v.02는 v.01보다 훨씬 넓은 범위(Studio, 정원창고,
   Grok+Gemini 라우팅 등)를 추가했는데, v.01 기준으로 "완료"라고
   판단한 항목들이 v.02 기준으로는 부분완료에 불과했다.
3. **"유사한 기능이 있다"와 "요구된 정확한 UX/기능이 구현됐다"를
   구분**한다. 상품명 다듬기 요청("옆에 버튼→적용/복사")에 대해 이미
   있던 "3가지 전략 제안" 기능을 보고 "완료"로 오판할 뻔했다 — 목적은
   비슷해도 요구된 정확한 형태(단일 버튼, 적용/복사 두 액션)와 다르면
   "유사기능 있음, 요구 UX와는 차이 있음"으로 정직하게 구분 표시한다.
4. **완료 표의 각 행은 반드시 검증 방법(어떤 grep/curl/DB조회/브라우저
   테스트로 확인했는지)을 함께 적는다** — 검증 근거가 없는 "완료"
   표시는 그 자체로 위험 신호다.

**실증**: v.02 전문 재정독 → grep으로 `smartRound`(씨앗심기 없음, crawl
페이지에만 존재), "적용하기"/"복사하기" 버튼 텍스트(0건), 콤마분리
태그입력(Enter 단건 입력만 존재) 등을 하나하나 재확인해 4개 이상의
누락 항목을 신규 확정. 완료 표를 "검증방법 포함" 형식으로 재작성.
- 클러스터 인덱스 "문서·인계·완료판정"에 #376 추가.

## 작업원칙 #377 — "기능 존재 확인"은 예상 함수명 grep 하나로 끝내지 말고 화면 텍스트/실제 계산결과까지 실측해야 한다 (2026-09-16)

**배경**: 원칙#376(완료표 전체 재대조) 적용 중 "마진 역산기+단수정돈"을
재검증하면서 `grep -rn "smartRound"` 한 번으로 "씨앗심기엔 없다, 크롤
페이지에만 있다"고 판정했다. 하지만 실제로는 MarginCalculator.tsx에
`reverseCalc`/`reverseTarget`/`Math.ceil(x/10)*10` 형태로 완전히
구현되어 있었다 — 단수정돈을 `smartRound`라는 함수명이 아니라 인라인
계산으로 짰을 뿐이었다. 브라우저로 실제 공급가·목표실판매가를 입력해
보고서야 "필요 정가 31,120원"이 정확히 계산됨을 확인했다.

**규칙**:
1. **"이 기능이 있는가"를 확인할 때 특정 함수/변수 이름 하나로 grep해서
   0건이 나와도 "미구현"으로 단정하지 않는다** — 같은 로직을 다른
   이름·다른 형태(인라인 계산, 다른 컴포넌트, 다른 파일)로 구현했을
   가능성이 항상 있다. 관련 도메인 키워드(이번엔 "역산", "reverse",
   "10원", 화면에 보였던 라벨 문구)로 여러 각도에서 재검색한다.
2. **가장 확실한 검증은 결국 화면 텍스트 존재 확인 + 실제 입력값으로
   계산 결과를 실측**하는 것이다. 코드에 함수가 있어 보여도 실제로
   렌더 조건(이번엔 `supplierPrice > 0`)을 못 넘기면 화면엔 안 보이고,
   반대로 코드에 원하는 함수명이 없어도 기능은 이미 완성돼 있을 수
   있다 — 코드 검색과 브라우저 실측 둘 다 필요하고, 어느 한쪽만으로
   "없다"/"있다"를 확정하면 안 된다.
3. **"미착수로 재분류"하기 전에 한 번 더 다른 키워드로 재검색**하는
   습관을 들인다. 이번 사례는 두 번째 검색(도메인 키워드 "역산") 없이
   첫 검색(함수명)만으로 결론 내려 정직하지만 부정확한 재분류를 만들
   뻔했다.

**실증**: `smartRound` grep(0건, 씨앗심기 기준) → "미구현"으로 오판정
직전 → `MarginCalculator.tsx` 재조회에서 `reverseCalc`/10원단위
`Math.ceil` 발견 → 브라우저 실측(공급가 10,000원+목표실판매가 28,000원
+할인10%) → "필요 정가 31,120원" 정확 계산 확인. 최종 정정: 이 기능은
이미 완료 상태였음(원 브리핑이 맞았음, 재점검의 검색 방법이 부정확했음).
- 클러스터 인덱스 "검증·실측(구조≠내용)"에 #377 추가.

## 작업원칙 #378 — 개선 제안은 반드시 대표님의 명시적 응답을 받은 후에만 실행한다 (2026-09-16)

**배경**: 대표님이 명확히 지시 — "앞으로 추가적인 제안사항들은 항상
요청 후에 진행하도록 한다. 답변을 누락하고 지나칠 수 있으니, 제안에
대한 답을 무조건 받고 진행 여부를 파악한 후 넘어가도록 유도하라."

**규칙**:
1. **좋은 개선안이나 아이디어가 떠올라도, 그 자리에서 바로 실행하지
   않는다.** 먼저 제안 내용을 명확히 제시하고, 대표님의 응답을 기다린다.
2. **응답을 받지 못한 채(대표님이 다른 화제로 넘어가거나 메시지에서
   빠뜨린 경우) 절대 "동의한 것으로 간주"하고 진행하지 않는다.** 다음
   응답에서라도 반드시 "지난번 제안한 OO, 진행할까요?"처럼 다시 명시적
   으로 묻는다 — 대표님이 답을 놓쳤을 가능성을 항상 전제한다.
3. **`ask_user_input_v0` 같은 명시적 확인 도구를 적극 활용**해 제안 후
   바로 "예/아니오" 또는 선택지를 받는다. 텍스트로만 묻고 다음 문단으로
   넘어가면 답변이 묻히기 쉽다.
4. 이 규칙은 **코드를 고치는 실행 자체**에 적용되는 것이지, 기존에
   합의된 병렬작업 목록(예: MASTER_CHECKLIST.md의 항목들)을 순서대로
   이어가는 것과는 다르다 — 이미 승인된 작업 큐를 진행하는 것은 매번
   재확인이 필요 없다. 재확인이 필요한 것은 **새로 떠오른 제안**
   (원래 목록에 없던 아이디어, 구조 변경, 신규 기능 제안)이다.

**실증**: 2026-09-16 대표님 명시적 지시로 신규 등재.
- 클러스터 인덱스 "문서·인계·완료판정"에 #378 추가.

## 작업원칙 #379 — 오류 확인은 "등록된 상품"이 아니라 "그 카테고리/기능을 쓸 수 있는 모든 상품 유형"으로 확장 재현해야 한다 (2026-09-17)

**배경**: 대표님이 명확히 지시 — "등록된 상품뿐만이 아니라 모든 상품을
적용했을 때도 문제가 없도록 확장하여 체크하고 앱에 적용하고 있는지
다시 한번 체크하라." 실제로 "천연 비취 옥반지" 오분류를 조사하며 이
지시를 그대로 실행한 결과 — "옥반지" 하나만 고쳤다면 놓쳤을, "액세서리"
라는 단어가 들어간 최소 5개 이상의 완전히 다른 상품군(차량용방향제/
헤어핀/목걸이/휴대폰스트랩)에서 동일 클래스의 결함이 재현됨을 확정했다.
단건으로 접근했다면 "옥반지 카테고리 매핑을 하드코딩으로 고정"하는
식의 임시방편으로 끝났을 것이고, 다른 액세서리류 상품은 계속 오분류
됐을 것이다.

**규칙**:
1. **하나의 상품명에서 결함을 발견하면, 그 결함이 걸리는 "패턴"
   (특정 단어, 특정 상품 구조, 특정 카테고리 조합)을 먼저 식별**한다.
   "옥반지가 이상하다"가 아니라 "'액세서리'라는 단어가 들어간 상품이
   전반적으로 이상한 것 아닌가"라는 가설을 세우고 검증한다.
2. **그 패턴에 해당하는 서로 다른 상품군을 최소 4~5개 골라 동시에
   재현 테스트**한다 — 같은 카테고리 내 변주가 아니라 완전히 다른
   카테고리/용도의 상품들로 검증해야 "그 단어 자체가 문제"인지 "그
   상품만의 우연"인지 구분된다.
3. **수정 후에도 같은 패턴의 여러 사례로 재검증**한다 — 한 사례가
   고쳐졌다고 패턴 전체가 고쳐졌다고 단정하지 않는다(이번 사례:
   병합 수정 후 "핀셋"·"걸이"는 캐시 때문에 착시 개선처럼 보였지만,
   완전히 새로운 변형으로 재검증하니 여전히 결함이 재현됐다).
4. **부분개선과 완전해결을 명확히 구분해서 기록**한다. "후보가 목록
   에서 사라지지 않게 됐다"는 것과 "1순위가 항상 정확해졌다"는 것은
   다른 수준의 성과다 — 부분개선을 완료로 과장하지 않고, 남은 근본
   원인과 다음 조사 방향을 정직하게 문서에 남긴다.

**실증**: "천연 비취 옥반지" 단건 조사 대신 액세서리류 5개 상이한
상품군으로 확장 재현 → 시스템적 결함 확정 → Vercel 로그 실측으로
근본원인(AI검증 성공시 결정론적 매처의 좋은 후보를 통째로 덮어쓰는
설계) 확정 → 병합으로 부분수정 → 재현 검증 결과 일부 사례는 여전히
미해결임을 정직하게 기록, 완료로 과장하지 않음.

## 작업원칙 #380 — 오류·개선 확인은 항상 확실한 출처로 교차검증하여 환각을 제거하고, 단건 수습이 아닌 전 상품 공통 시스템 관점에서 진행한다 (2026-09-17, 대표님 명시 지시 종합)

**배경**: 대표님이 이번 세션 전체에 걸쳐 반복적으로 강조 — "개선과
오류 체크할 때는 확실한 출처로 교차 검증을 통해 제대로 확인하여
환각을 없애고 제대로된 최신 정보로 적용", "단건 수습으로 끝내지 말고
전 상품 공통 시스템 관점으로 프로젝트 전체에 확장 적용." 이 세션에서
실제로 지킨 방식과 어겼을 때의 대가를 함께 기록해 향후 모든 작업의
기본 절차로 삼는다.

**"확실한 출처로 교차검증" — 실제로 이렇게 했다**:
- 네이버 엑셀 규격 확인 시 대표님이 준 실제 공식 템플릿 파일
  (ExcelSaveTemplate_20260324.xlsx)을 직접 openpyxl로 파싱해 5행
  규격설명을 근거로 사용 — 지침서 문서(v.02)의 주장과 실제 파일이
  충돌했을 때 실제 파일을 우선(v.02의 "조건부무료 공백제거" 주장은
  실제 파일 대조 결과 틀렸음을 확인하고 정정).
- 도매매 selectOpt 필드 스키마처럼 공식 문서에 없는 내용은 그 API를
  실사용하는 제3자 오픈소스의 실제 테스트 코드로 교차확인(추측 금지).
- 코드가 "맞아 보인다"고 결론짓지 않고 Vercel 런타임 로그로 실제
  프로덕션 실행 결과를 확인 — 이번 카테고리 오분류 조사에서 로그 없이
  코드만 읽었다면 "d3 fuzzy매칭 실패시 첫번째 반환" 결함을 절대
  못 찾았을 것(코드만 봐서는 그럴듯해 보이는 로직이었음).
- 함수명 grep 0건으로 "미구현"이라 결론짓지 않고(#377), 브라우저
  실측·API 실측·DB조회 등 다각도로 재확인.

**"단건 수습 금지, 전 상품 공통 관점" — 실제로 이렇게 했다**:
- 같은 API의 다른 호출부까지 전수확인(#370) — 이번에도 validateSuggestion
  의 3개 호출부(category/suggest, seo-workflow, sourcing-recommender)가
  모두 한 함수를 재사용하는 구조임을 확인하고서야 "이 함수 하나만
  고치면 3곳 다 정정된다"는 확신을 가질 수 있었다.
- "옥반지"라는 단건 상품명이 아니라 "액세서리"라는 패턴으로 일반화해
  5개 상이한 상품군으로 확장검증(#379).
- 완료 표를 "이번에 만진 것"이 아니라 "원본 지시서 전체"와 대조
  (#376) — 그렇지 않았다면 마진역산기·태그자동완성·엑셀사전검수 등
  다수 항목이 계속 누락된 채로 "완료"라고 보고했을 것.

**이 두 원칙을 어겼을 때 실제로 벌어졌던 일(반면교사)**:
- "완료"라고 커밋 메시지에 적었지만 실제 파일은 그대로였던 사고
  (#376 계기, 이후 재발도 확인) — 검증 없이 주장한 결과.
- "미구현"이라 판단했던 마진역산기가 사실 이미 구현돼 있었던 오판정
  (#377) — 다각도 재확인 없이 단일 검색 방법에 의존한 결과.
- "개껌 오분류가 고쳐졌다"고 이전 세션에서 보고했지만 재현 결과
  여전히 남아있었던 사고 — 실측 검증(curl 여러 번 재현) 없이 코드
  리뷰만으로 결론지었던 결과로 추정.

**규칙(위 실증들을 종합한 표준 절차)**:
1. 결함 원인을 코드만 읽고 추측하지 않는다 — 가능하면 항상 실제
   실행 로그/API 응답/DB 조회로 확정한다.
2. 문서(지침서)와 실제 파일/실제 동작이 충돌하면, 실제 파일/실제
   동작을 우선한다 — 문서가 최신이 아닐 수 있다는 것을 항상 전제한다.
3. 하나의 상품/사례에서 결함을 발견하면 그 패턴을 일반화해 최소
   4~5개 다른 사례로 확장 검증한다.
4. 같은 함수/API/로직의 다른 소비처를 전수 확인한다(#370).
5. "완료"라고 쓰기 전에 그 근거(검증방법)를 함께 적고, 그 근거가
   없으면 "완료"라고 쓰지 않는다(#376).
6. 부분개선과 완전해결을 구분해서 정직하게 기록한다.

- 클러스터 인덱스 신규: "전 상품 확장·교차검증"에 #379·#380 추가.

## 작업원칙 #381 — AI 크로스체크에서 "먼저 응답한 엔진"이 아니라 "실제로 유효한 답을 준 엔진"을 신뢰해야 한다 (2026-09-17)

**배경**: #379/#380(전 상품 확장검증) 원칙을 그대로 실천하며 "액세서리"류
오분류를 더 깊이 재조사한 결과, rev181의 1차 수정(AI결과 덮어쓰기→병합)
만으로는 해결 안 되는 더 근본적인 결함을 발견했다. Vercel 런타임 로그
실측: Groq와 Gemini 둘 다 호출됐고 agreement=false였는데, 기존 로직은
무조건 "Groq가 뭐라도 답하면 Groq 우선"이었다. 그런데 이번 실측에서
Groq의 답 자체가 실제 네이버 카테고리 체계에 존재하지 않는 d1+d2
조합("패션의류 > 여성패션소품" — 실존하는 카테고리명은 "패션잡화 >
여성패션소품"의 유사구조였으나 정확한 조합 자체가 마스터에 없었음)을
만들어낸 환각이었음을 카테고리 마스터 직접 대조로 확정했다. 같은 질문에
Gemini는 정확히 실존하는 조합을 줬다.

**규칙**:
1. **여러 AI 엔진을 크로스체크로 쓸 때, "어느 엔진이 먼저 응답했는지"
   또는 "어느 엔진을 기본으로 정했는지"가 아니라 "각 엔진의 답이 실제로
   유효한(사실과 대조 가능한) 것인지"를 우선 기준으로 삼는다.** 이번
   사례처럼 검증 가능한 마스터 데이터(카테고리 목록)가 있으면, AI 답을
   그대로 쓰기 전에 최소한 "이 조합이 실제로 존재하는가"라는 경량
   존재확인을 먼저 거친다.
2. **"둘 다 유효하면 원래 기본 엔진 우선, 한쪽만 유효하면 유효한 쪽
   채택"** 식으로 설계하면 기존 정상 동작(대부분의 경우 둘 다 유효)에는
   영향을 주지 않으면서, 환각 케이스만 정확히 걸러낼 수 있다 — 이번
   실제 수정이 정확히 이 패턴(validateExists로 최소 침습적 개선).
3. **AI 두 엔진의 "agreement"(일치 여부)만으로는 신뢰도를 담보할 수
   없다** — 한쪽이 유효하고 다른 쪽이 환각이면 애초에 일치할 리 없고
   (agreement=false로 정직하게 표시는 되지만), 그 뒤에 "그래도 둘 중
   하나를 골라야 한다"는 폴백 로직 자체가 이번처럼 환각을 그대로
   통과시키는 지점이 될 수 있다. agreement 판정과 별개로, 최종 채택
   답 자체의 유효성을 반드시 한 번 더 검사한다.
4. **하나의 근본 수정이 통했다고 그 결함 클래스 전체가 해결됐다고
   단정하지 않는다.** 이번에도 "목걸이"·"방향제" 등 여러 사례는
   개선됐지만 "헤어핀" 사례는 여전히 실패했다 — 같은 패턴 안에서도
   개별 사례별로 다른 하위경로(예: 결정론적 매처가 애초에 confident로
   판정해 AI크로스체크 자체를 안 타는 경로)를 탈 수 있으므로, 부분
   개선 확인 후에도 반드시 여러 사례로 재검증하고 남은 실패를 정직하게
   추적한다(#379).

**실증**: category-ai-suggest.ts의 suggestWithCrossCheck()에 validateExists()
(d1+d2 조합의 카테고리 마스터 실존 여부 확인) 신설 — Groq 1순위가
무효하고 Gemini 1순위가 유효하면 Gemini 채택, 그 반대나 둘 다
유효/무효면 기존 Groq 우선 유지. 실측 결과 "골드 목걸이"·"차량용
방향제"는 정확한 카테고리로 개선 확인, "헤어핀" 계열은 원인 미확정
상태로 다음 세션에 인계.
- 클러스터 인덱스 "시스템 승격(단건 금지)"에 #381 추가.

## 작업원칙 #382 — AI크로스체크 계층 이전 단계(결정론적 매처 headNoun 선정)가 확신도 게이트를 완전히 우회할 수 있다 (2026-09-17, rev183 최종확정)

**배경**: #381로 "헤어핀" 사례를 다음 세션에 인계했으나, 실제로는
validateExists() 수정(#381의 대상)과 전혀 다른, 더 앞단계의 결함이었다.
Vercel 런타임 로그 실측(CATDBG4~7): "우아한 헤어핀 액세서리"에서
extractNouns()의 tail-noun 휴리스틱이 headNoun="액세서리"를 선택했다
(진짜 식별자 "헤어핀"은 modifierNouns로 강등). 카테고리 마스터에 무관한
d1 두 곳(디지털/가전>계절가전>전기매트/장판>액세서리, 생활/건강>
반려동물>패션용품>액세서리)에 bare "액세서리" 리프가 우연히 존재해,
headNoun 완전일치 경로(EXACT_BRANCH_SCORE_PER_CHAR)가 동점 135점
고신뢰(tier=1, **lowConf=false**)로 확정됐다 — AI크로스체크 분기
자체가 발동하지 않아, #381에서 고친 안전장치(validateExists)에
도달하기도 전에 오분류가 확정되고 있었다.

**규칙**:
1. **"AI 검증 계층에 안전장치를 추가했다"는 것이 "그 계층에 실제로
   도달한다"를 보장하지 않는다.** 확신도 게이트(isDeterministicLowConfidence
   등)가 있는 파이프라인에서는, 안전장치를 고친 후에도 반드시 그
   안전장치가 실제로 호출되는지(`usedAI` 등 플래그) 실측으로 확인한다
   — lowConf=false로 판정되면 그 이후 계층의 어떤 수정도 무의미하다.
2. **같은 증상(오분류)의 재발이 반드시 같은 원인이라는 보장은 없다.**
   #381은 AI크로스체크 계층의 결함(환각 답 채택)이었고, #382는 그
   계층에 도달하기도 전인 결정론적 매처 자체의 결함(headNoun 오선정)
   이었다 — 겉보기 증상이 같아도 근본 원인 조사를 처음부터 다시 해야
   한다(코드 리뷰만으론 계층 구분이 안 보임, 실측 로그로만 구분됨).
3. **후행 장르 동격어("액세서리", "세트", "용품" 등 STOP_NOUNS급 단어)는
   leaf 매칭 단계뿐 아니라 headNoun 선정 단계에서도 반드시 같은 사전을
   공유해야 한다.** 두 단계가 서로 다른 사전(HEAD_NOUN_EXCLUDE 1단어
   로컬셋 vs STOP_NOUNS_SET)을 쓰면, 한쪽만 고쳐도 다른 쪽에서 같은
   결함이 재발한다(#295 단일권위 원칙의 구체 사례) — 이번 수정으로
   HEAD_NOUN_EXCLUDE가 STOP_NOUNS_SET을 흡수하도록 통합.
4. **마스터 데이터에 bare(단독) leaf로 존재하는 범용 단어는 grep으로
   전수 확인한다.** "액세서리"가 서로 무관한 2개 d1에 bare leaf로
   존재한다는 사실은 코드 추론만으로는 안 나오고, naver-categories-full.ts
   직접 grep으로만 확인됐다 — 유사 위험군(세트/케이스 등)도 같은 방식
   으로 전수 확인 후 안전 여부 판단.

**실증**: identity-dictionary.ts의 STOP_NOUNS에 '액세서리' 추가,
category-deterministic-matcher.ts의 HEAD_NOUN_EXCLUDE가 STOP_NOUNS_SET을
스프레드로 흡수. 최종 5개 사례 재검증(원칙#379): "헤어핀"·"목걸이"·
"방향제" 3건은 무관 카테고리 오분류 완전 해소 확인(curl 실측). 다만
정직한 잔여 이슈 2건 신규 발견 — (a) "헤어핀"이 정답 후보 2개
(성인용 패션잡화>헤어액세서리 vs 유아용 출산/육아>유아동잡화) 중
유아용이 1위로 선택되는 동음이의 이슈(오늘 수정 범위 밖, 별도 결함),
(b) "휴대폰 스트랩 액세서리"가 카메라/캠코더용품>삼각대가방/스트랩
으로 오분류되는 신규 별건("스트랩"이 카메라 액세서리 leaf로 마스터에
존재) — 둘 다 이번 rev183 수정과 무관한 별도 결함이므로 신규 항목으로
MASTER_CHECKLIST에 등재하고 이번 수정을 "완료"로 확정한다(원칙#376 —
관련 없는 잔여 이슈 발견이 이번 수정의 완료 여부를 무효화하지 않음).
- 클러스터 인덱스 "시스템 승격(단건 금지)"에 #382 추가.

## 작업원칙 #383 — 대칭이어야 할 두 분기 중 하나만 안전장치가 있으면 반드시 재발한다 + 배포 직후 재검증은 전파지연을 의심하라 (2026-09-21, rev185)

**배경**: #45(휴대폰 스트랩→카메라 오분류) 조사에서 확인. headNounWeight
함수의 strongHeadMatch(완전일치)와 weakHeadMatch(부분일치) 두 분기는
"슬래시 합성어의 일부만 매칭됐을 때 신뢰를 낮춘다"는 같은 목적을
가지는데, weakHeadMatch만 partialMatch를 체크하고 strongHeadMatch는
그 체크가 빠져있었다 — #382(HEAD_NOUN_EXCLUDE/STOP_NOUNS 이원화)와
정확히 같은 클래스의 결함이다: "같은 목적의 두 갈래 로직이 한쪽만
안전장치를 갖고 있으면, 그 갭을 정확히 찌르는 입력이 반드시 나타난다."

**규칙**:
1. **판정 함수 안에 목적이 같은 두 개 이상의 분기(if/else if)가 있으면,
   각 분기가 같은 안전장치(discount/exclude/corroboration 등)를
   동일하게 적용하는지 반드시 대조한다.** 하나를 고칠 때 "형제 분기도
   같은 결함을 갖고 있지 않은가"를 항상 같이 확인 — 이번처럼 grep
   한 번으로 발견 가능했다.
2. **부분매칭 안전장치를 추가할 때, 그 안전장치가 정당한 케이스까지
   덮어버리지 않는지 반드시 즉시 재검증한다.** "스트랩"을 고치는
   순간(discount 추가) "아로마 디퓨저"(정당한 슬래시 동의어)가
   회귀했다 — 이건 미리 예측 가능한 트레이드오프였고, 실제로
   재검증 절차(원칙#379) 덕에 배포 직후 바로 잡아냈다. discount
   추가형 수정은 항상 "이 discount가 정당하게 걸려야 할 반대
   케이스"를 함께 실측해야 한다.
3. **discount 면제 조건은 이미 존재하는 대칭 신호를 재사용한다.**
   "골프 우산"의 d2-corroboration(modifier가 경쟁 카테고리의 d2와
   겹치면 동음이의 아님을 확증)과 정확히 대칭되는 신호
   (modifierCorroborates: modifier가 매칭 안 된 형제 파트를 뒷받침
   하면 discount 면제)를 새로 만들지 않고 같은 구조로 추가 — #295
   단일권위 원칙의 "완전히 새 판정 신설 금지"를 지킨 사례.
4. **배포 직후 재검증에서 "여전히 안 고쳐진 것처럼" 보이면, 코드
   버그를 의심하기 전에 배포 전파 지연(lambda 콜드/웜 인스턴스 혼재)
   을 먼저 의심한다.** rev185에서 2차 수정 배포 후 curl 재검증이
   여전히 이전 값(20점)을 보여줘 "로직이 틀렸다"고 오판할 뻔했으나,
   내부 계산값을 직접 로그로 노출(CATDBG10)해 실제로는
   modifierCorroborates=true로 정확히 계산되고 있음을 확인 — 몇 초
   후 재요청하니 정상 값(60점)이 나왔다. 배포 상태(dep=커밋해시)가
   최신이어도 실제 실행 중인 lambda 인스턴스가 이전 코드일 수 있으니,
   "고쳤는데 안 됨"이 보이면 (a) 배포가 진짜 완료됐는지, (b) 내부
   계산값 자체가 틀렸는지를 분리해서 확인한다 — 둘을 섞으면 잘못된
   원인으로 잘못된 재수정을 할 위험이 있다.

**실증**: category-deterministic-matcher.ts의 headNounWeight —
strongHeadMatch 분기에 partial discount + modifierCorroborates 추가
(커밋 79a1448, a97e564). 클린 배포판 curl 8종 재검증으로 최종 확정.
- 클러스터 인덱스 "시스템 승격(단건 금지)"에 #383 추가.

## 작업원칙 #384 — "DOM에 있다"는 "화면에 보인다"의 증거가 아니다: 시각적 렌더링은 computed style로 직접 확인한다 (2026-09-21, rev186)

**배경**: #23(Studio 상세캔버스 콘텐츠 실종) 조사에서, DetailPageCard/
SlotFunnelBoard/DetailAssemblyBoard가 `document.body.innerText`로
정확히 확인됐음에도 실제 화면(스크린샷)에는 완전히 안 보였다. 1차로
레이아웃 높이 상속 결함(layout.tsx/AtelierShell.tsx의 minHeight:0/
height:100% 누락)을 찾아 고쳤지만 그것만으로는 설명이 안 됐고,
`elementFromPoint` + `getComputedStyle` 조상 체인 전수 스캔으로
진짜 원인(StepGroup 인라인 컴포넌트 재생성 → CSS 애니메이션 무한
리셋, opacity:0 고착)을 찾았다.

**규칙**:
1. **"콘텐츠가 DOM에 있다"(`innerText`, `querySelector` 매칭)와
   "콘텐츠가 화면에 보인다"는 서로 다른 주장이며, 전자는 후자의
   필요조건일 뿐 충분조건이 아니다.** UI 버그를 "완료"로 확정하기
   전에는 반드시 후자까지 확인한다.
2. **스크린샷과 `elementFromPoint`/`getComputedStyle`이 다른 결과를
   주면, 그 불일치 자체가 신호다** — 어느 한쪽을 무시하고 넘어가지
   말고 왜 다른지부터 규명한다. 이번 사례에서 `elementFromPoint`는
   정상 텍스트를 반환했는데 스크린샷엔 안 보였다 — 이 모순이
   `opacity:0` 애니메이션 고착을 찾는 단서였다.
3. **진단 순서**: (1) DOM 존재 확인(`innerText`) → (2) 그래도
   안 보이면 좌표 기반 존재 확인(`elementFromPoint`) → (3) 그래도
   안 보이면 조상 체인의 `getComputedStyle`(opacity/visibility/
   transform/animationPlayState/display)을 전수 스캔. 각 단계를
   건너뛰지 않는다 — (3)에서 찾은 원인이 (1)(2)에서는 절대 안
   드러난다.
4. **CSS 진입 애니메이션(`animation: ... both`, opacity 0→1류)이
   걸린 요소는 특히 의심 대상이다** — `animation-fill-mode: both`는
   애니메이션 시작 전에도 `from` 상태(보통 opacity:0)를 유지하므로,
   애니메이션이 정상 재생을 못 하면 그 요소가 "영원히 투명한 채로
   DOM에 존재"하는 상태가 되고, 표준 디버깅으로는 절대 못 잡는다.
5. **레이아웃 결함(rev186 part1/2)과 렌더링 결함(rev186 part3)은
   서로 독립적으로 존재할 수 있다** — 하나를 고쳤다고 문제가 완전히
   해결됐다고 단정하지 말고, 매 수정 후 반드시 재검증해 "이걸로
   충분한지" 스스로 확인한다(원칙#379의 확장 적용).

**실증**: `src/app/studio/page.tsx`의 `StepGroup`을
`useCallback([step, a])`로 감싸 함수 참조 안정화(커밋 05a1ee7).
`docs/playbook/RISKY_IDIOMS.md`에 IDIOM-7(인라인 자식 컴포넌트 +
CSS 진입 애니메이션)로 이 패턴 자체를 신설, 재사용 가능한 진단
절차를 문서화.
- 클러스터 인덱스 "시스템 승격(단건 금지)"에 #384, RISKY_IDIOMS
  IDIOM-7 추가.

## 작업원칙 #385 — 레이아웃 수정은 "그 자리"만이 아니라 "같은 flex 컨테이너 안 형제 전체"를 재검증한다 (2026-09-22, rev193)

**배경**: rev186(#23)에서 Studio 상세캔버스가 뷰포트에 맞게 줄어들도록
`<main>` 콘텐츠 div에 `flex:1`을 추가해 근본수정했다. 이 수정 자체는
정확했고 그날 충분히 검증했지만, **그 형제인 이스터에그 footer가
`marginTop:'auto'`(콘텐츠 짧으면 바닥에 붙는 트릭)를 쓰고 있다는
사실을 그때는 몰랐다** — footer는 그날 건드린 영역이 아니었으므로.
결과: `flex:1`이 남는 공간을 전부 차지해버려 `marginTop:auto`가
조용히 `0px`로 무력화됐고, 몇 세션 뒤(9/22) 대표님이 스크린샷으로
"핑크바가 화면 중간에 고정돼 콘텐츠를 가린다"고 신고할 때까지 아무도
몰랐다.

**규칙**:
1. **flex 컨테이너의 자식 하나에 `flex:1`/`flex-grow`를 추가하거나
   바꿀 때, 그 컨테이너의 다른 자식 전체를 훑어 `margin:auto`나
   "남는 공간"에 의존하는 스타일이 있는지 확인한다.** 코드에 에러가
   안 나고 그 순간엔 화면도 멀쩡해 보일 수 있다 — 문제가 되는 시점은
   "콘텐츠가 뷰포트보다 길어져서 스크롤이 생기는" 그 다음 상황이라,
   당장 눈에 안 띄고 나중에 신고로만 발견되기 쉽다.
2. **수정한 요소가 속한 flex 컨테이너의 범위를 정확히 파악한다** —
   이번 사고에서 `flex:1`을 준 대상(`<main>` 안의 콘텐츠 div)과
   footer는 서로 다른 컴포넌트 파일처럼 느껴지지만 실제로는 **같은
   `<main>` flex 컨테이너의 형제**였다. 파일이 다르다고 안전하다고
   착각하지 않는다 — DOM 트리 상의 실제 부모-자식 관계가 기준이다.
3. **UI 레이아웃 버그의 브라우저 검증 시, 같은 태그가 페이지에 여러
   개 있을 수 있음을 항상 의심한다.** 이번 재검증 중
   `document.querySelector('footer')`가 진짜 대상이 아니라 완전히
   다른 컴포넌트(`ThumbnailCard`의 "Phase 2-B-3" 안내 `<footer>`)를
   잡아서 한 차례 오판할 뻔했다 — `querySelectorAll`로 개수를 먼저
   세고, 텍스트 내용으로 정확한 대상을 특정해야 한다.

**실증**: `src/app/layout.tsx`의 footer를 `<main>`(스크롤 컨테이너)
밖으로 완전히 이동시켜 스크롤과 무관하게 항상 화면 최하단에 고정되도록
재구조화(커밋 f8efdd3). `docs/playbook/RISKY_IDIOMS.md`에 IDIOM-8
(flex:1이 형제의 margin:auto 트릭을 무력화)로 이 패턴 자체를 신설,
grep 커맨드로 재사용 가능하게 함.
- 클러스터 인덱스 "시스템 승격(단건 금지)"에 #385, RISKY_IDIOMS
  IDIOM-8 추가.

## 작업원칙 #386 — project_knowledge는 검색될 때마다 부분 청크만 보여준다: 전체 반영현황은 별도 인덱스 파일로 관리한다 (2026-09-22)

**배경**: 대표님이 9/17자 원본메모/제미나이메모를 재첨부하며 "혹시 모든 원본
파일을 다시 올려야 하는지"를 물으셨다. project_knowledge_search를 실제로
여러 쿼리로 돌려본 결과, 검색될 때마다 관련도 높은 청크 몇 개만 반환되고
"이 저장소에 총 몇 개 문서가 있고 그중 뭐가 이미 반영됐는지"를 알려주는
인덱스가 없음을 확인 — 그 결과 이미 완료된 옛 이슈(이미지업로드 400에러 등)
자료와 아직 미반영인 자료(정보고시 productInfoProvidedNotice, 도매꾹
Private API, 대규모 UX 리서치 다수)가 검색결과에 뒤섞여 나왔다.

**규칙**:
1. **project_knowledge에 새 문서가 쌓일 때마다, 그 문서명·핵심요구사항·
   MASTER_CHECKLIST 매핑·반영상태를 `docs/plan/SOURCE_DOCS_INDEX.md`
   한 표에 기록한다.** 이 표는 project_knowledge 원본을 대체하지 않고,
   "재검색 전에 먼저 훑어보는 목차" 역할만 한다.
2. **새 md 파일을 받으면 순서를 지킨다**: ①이 인덱스에서 이미 있는
   파일/주제인지 먼저 확인 → ②있으면 project_knowledge 재검색 없이 바로
   해당 MASTER_CHECKLIST 항목번호로 이동 → ③없으면 project_knowledge_search로
   원문 확인 후 신규 항목 등재 + 인덱스에도 새 행 추가.
3. **"이미 반영됨"이라고 적기 전에 반드시 코드/DB로 실측 재확인한다** —
   인덱스의 "반영됨" 표시 자체가 또 다른 환각의 소스가 될 수 있으므로,
   상태를 "반영됨"으로 적을 때는 그 근거(커밋/rev/실측방법)를 함께
   남긴다(원칙#357/#376과 동일 규율).
4. **인덱스 갱신은 코드 수정과 마찬가지로 별도 커밋으로 즉시 반영한다** —
   "다음에 정리해야지"로 미루면 다시 검색 반복 문제가 재발한다.

**실증**: `docs/plan/SOURCE_DOCS_INDEX.md` 신설(2026-09-22). project_knowledge
전수 검색으로 확인한 결과, project_knowledge에는 대규모 UX/디자인시스템
리서치(ADHD 대시보드, Studio 리팩터, 디자인 토큰, 슬라이스 기반 자동조립 등)
와 실무 리스크(정보고시 productInfoProvidedNotice, 도매꾹 Private API)가
MASTER_CHECKLIST에 전혀 반영 안 된 채 존재했음을 발견 — 이 전부를 카테고리
2("재확인필요")로 인덱스에 기록해 다음 조사 우선순위로 편입.
- 클러스터 인덱스 "시스템 승격(단건 금지)"에 #386 추가.

## 작업원칙 #387 — 여러 출처 문서의 유사 문구는 반드시 원문 재대조로 정확한 소속을 확인한다 (2026-09-22)

**배경**: MASTER_CHECKLIST #21("ADHD 3단 스플릿뷰 슬라이딩 씨앗심기")이
실제로는 제미나이_꽃틔움_시스템_고도화_2026-09-17.md의 **섹션5(Studio
3-Domain 공간 구조)**와 **섹션7(정원창고 — 우측 슬라이딩 씨앗심기 튜닝
캔버스)**이라는 서로 다른 두 요구사항을 하나로 합쳐 "완료"로 오판정하고
있었음을 대표님 지적으로 발견. 두 문구 모두 "3분할/슬라이딩/씨앗심기"라는
비슷한 어휘를 쓰지만, 하나는 Studio(꽃단장 작업실) 페이지 자체의 좌우
3분할 레이아웃이고 다른 하나는 정원창고(좀비부활소)에서 상품을 클릭했을
때 페이지 이동 없이 옆에서 슬라이드로 열리는 별개의 편집 패널이다.
실제 코드 확인 결과 후자는 완전히 미구현 상태(grep 0건, 전체페이지
이동만 존재)였다.

**규칙**:
1. **여러 출처 문서(원본메모/제미나이메모/여러 리서치)를 다룰 때, 비슷한
   키워드가 서로 다른 섹션에 나올 수 있음을 항상 경계한다.** 항목을
   "완료"로 표시하기 전에 그 항목이 정확히 어느 섹션의 어느 문장에
   대응하는지 원문을 다시 펼쳐 확인한다 — 요약이나 기억에 의존하지 않는다.
2. **출처 인용 시 정확한 섹션 번호/제목까지 명시한다** — "제미나이메모"
   같은 모호한 출처가 아니라 "섹션7 정원창고" 처럼 구체적으로 적어야,
   나중에 다른 섹션과 혼동됐는지 스스로 검증할 수 있다.
3. **대표님이 지적하신 오류(파일명 오귀속, 섹션 혼동)는 그 자리에서
   고치는 것으로 끝내지 않는다** — 같은 패턴으로 잘못 매칭된 다른 항목이
   없는지 MASTER_CHECKLIST 전체를 재훑는다(#376의 "원본전체 재대조
   필수"와 동일 규율을 출처 오귀속에도 적용).
4. **판정을 정정할 때는 "삭제하고 다시 쓰기"가 아니라 "무엇이 왜 잘못
   됐는지"를 그 항목에 그대로 남긴다** — 다음에 같은 혼동이 재발하는 걸
   막는 최선의 방법은 실수의 흔적을 지우지 않는 것이다.

**실증**: MASTER_CHECKLIST #21을 "Studio 3-Domain 공간 구조"로 정확히
재명명하고 판정 정정 경위를 명시. #58(정원창고 슬라이딩 튜닝캔버스)을
신규 분리 등재, 실제 미구현 상태(grep 실측)로 정직 기록.
- 클러스터 인덱스 "시스템 승격(단건 금지)"에 #387 추가.

## 작업원칙 #388 — "타입이 호환된다"는 "아직 연결이 안 됐다"를 의미하지 않는다: 새 브릿지 함수 전에 소비처부터 grep한다 (2026-09-22)

**배경**: #47(반응형 HTML 상세페이지)+#59(세로형 슬롯) 설계 조사
중, `buildDetailPage()`(PNG 파이프라인, 6,677줄 규모 기존 시스템)와
`serializeDetailHtml()`(HTML 시리얼라이저, rev201에서 근본수정한
파일)의 반환/인자 타입이 정확히 호환됨을 코드로 확인하고, "이 둘을
연결하는 브릿지가 없다"고 판단해 새 파일(detail-page-to-html.ts)을
작성했다. tsc가 0에러로 통과해 "정확한 설계였다"고 느껴질 정도로
근거가 탄탄해 보였다. 하지만 배포 직전 마지막 확인 단계로
`src/app/api/`에서 이 두 함수의 실제 소비처를 grep한 순간,
**`generate-detail/route.ts`가 이미 정확히 같은 방식으로 두 함수를
연결하고 있었음**(코드 주석에 "STEP 3 — Parallel HTML output —
additive" 명시, 즉 이전 스프린트에서 의도적으로 단계별로 추가된
기능)이 드러났다. 만든 파일은 즉시 삭제하고 설계를 정정했다.

**규칙**:
1. **"A와 B를 연결하는 코드가 없다"는 결론은, 타입 호환성 확인이
   아니라 실제 소비처(호출부) grep으로만 내릴 수 있다.** 두 함수의
   시그니처가 정확히 맞아떨어진다는 사실은 "만들 수 있다"는 근거는
   되지만 "아직 안 만들어졌다"는 근거는 절대 되지 않는다 — 오히려
   시그니처가 깔끔하게 맞아떨어질수록, 이미 누군가 그 연결을 염두에
   두고 설계했을 가능성이 높다.
2. **새 "브릿지/통합/연결" 함수를 작성하기 전에, 연결하려는 두 핵심
   함수 각각의 이름으로 `src/app/api/`를 grep하고, 두 결과의 교집합
   (두 함수를 모두 import하는 파일)을 확인한다.** 하나만 grep해서
   "없다"고 판단하지 않는다.
3. **이 grep은 설계 착수 시점이 아니라, 코드를 다 쓴 뒤 배포 직전
   "마지막 확인"으로도 유효하다** — 이번 사례가 정확히 그랬다. 늦게
   발견해도 배포 전에만 잡으면 손실은 작다(파일 삭제 한 줄); 배포
   후 발견했다면 중복 로직 두 벌을 유지보수하는 더 큰 비용이 됐을
   것.
4. 이 규칙은 원칙#295(단일권위)의 실행 절차다 — #295가 "중복을 만들지
   말라"는 원칙이라면, #388은 "중복을 만들기 전에 정확히 무엇을
   확인해야 중복인지 판별할 수 있는가"에 대한 구체적 절차다.

**실증**: `docs/playbook/RISKY_IDIOMS.md`에 IDIOM-9로 이 패턴 자체를
신설, 재사용 가능한 grep 커맨드(`grep -rln "함수A" src/app/api/ |
xargs grep -l "함수B"`) 포함 — 실행해 정확히 1건(`generate-detail/
route.ts`)이 나옴을 검증.
- 클러스터 인덱스 "시스템 승격(단건 금지)"에 #388, RISKY_IDIOMS
  IDIOM-9 추가.

## 작업원칙 #389 — 대표님이 실제로 쓰는 외부 도구의 "정확한 사용 방식"을 먼저 확인한다: 내가 아는 유사 개념으로 대체 추정하지 않는다 (2026-09-22)

**배경**: 대표님이 "제미나이의 캔바를 활용"이라고 하신 것을, 처음엔 Claude
자신의 도구 목록에 있던 "Canva MCP 커넥터"(프레젠테이션 전용)나 "Canva
Connect API의 autofill"(Enterprise 전용 유료 API)로 잘못 해석했다.
실제로는 2026년 5월 19일 Google I/O에서 출시된 **"Canva Connected App
for Gemini"**(Gemini 앱 UI 안에서 `@Canva`로 호출하는 기능)를 말씀하신
것이었다 — 완전히 다른 제품이었다.

**핵심 실수**: "캔바"라는 단어를 들었을 때, 내가 아는 캔바 관련 개념(내
도구함의 MCP 커넥터, 또는 흔히 알려진 Connect API) 중 하나로 성급하게
매칭했다. 정확한 확인 없이 "아마 이걸 말씀하시는 거겠지"로 넘어간 것이
근본원인 — 대표님이 "다시 설명해주는" 수고를 두 번이나 하시게 만들었다.

**규칙**:
1. **사용자가 특정 외부 서비스/기능을 언급하면, 그게 정확히 어느
   제품·어느 접근 경로(웹 UI 전용인지, API로도 되는지, 어느 플랜이
   필요한지)를 가리키는지 먼저 웹 검색으로 확인한다** — 내가 아는
   유사 개념으로 대체 추정하지 않는다. 특히 "제미나이 안에서 쓰는
   캔바"처럼 "A 안에서 쓰는 B"라는 표현은 A의 확장 기능이지 B의 API를
   직접 뜻하는 게 아닐 수 있다.
2. **"API로 가능한 것"과 "그 서비스의 웹/앱 UI에서만 가능한 것"을
   명확히 구분한다.** 이번 사례처럼, 화려한 기능(Gemini 채팅 중
   `@Canva` 호출)이 실제로는 공식 API 리소스 목록(`v1beta.*`)에
   전혀 없고 UI 전용일 수 있다 — API 문서의 전체 리소스 목록을 직접
   확인해야 정확히 판별된다(2차 출처 블로그의 "가능하다"는 서술만
   믿지 않는다).
3. **여러 후보가 헷갈릴 때는 정확한 날짜·출처로 교차검증한다** — 이번
   확인에서 Android Authority, Canva 공식 뉴스룸, Business Wire,
   Google 공식 지원문서(support.google.com) 4개 독립 출처가 모두
   "2026-05-19 출시, `@Canva` 트리거, Gemini 앱 설정에서 연결"이라는
   동일한 사실을 보고해 신뢰도를 확정했다.
4. **한 번 잘못 짚었다면, 정정 후 반드시 사용자의 재설명을 기다리지
   말고 먼저 "제가 정확히 이렇게 이해했습니다"로 명시적으로 재확인
   받는다** — 애매한 상태로 다음 작업을 진행하면 같은 오해가 반복
   증폭된다.

**실증**: Gemini API 공식 리소스 목록(ai.google.dev/api/all-methods)에
Canva 관련 리소스가 없음을 직접 확인 — `@Canva`는 Gemini 앱 UI 전용
기능이며, 우리 앱의 백엔드가 Gemini REST API를 호출하는 방식으로는
재현 불가능함을 확정. 설계를 "우리 앱이 만든 이미지를 Canva에서
열어 편집하는 링크 제공" 방식으로 현실화.
- 클러스터 인덱스 "시스템 승격(단건 금지)"에 #389 추가.
