# 디스코드 알림 실물 진단 — 스크린샷 기반 개선 (2026-07-13)

Authoring: DESKTOP(운영자 제공 실물 5장 분석). 계기: 실제 발송 내용이 요청 미반영·가독성·개성 부족. 원칙 #231/#233/#252. 상태: 진단 완료·Code 개선 대기.

## 0. 실물 진단 (스크린샷 5장)
| # | 알림 | 상태 | 문제 |
|---|---|---|---|
| 4 | 마진 경고 | ✅ 양호 | ADHD 포맷·우선순위·꼬띠 사투리·딥링크 — 정답 톤(유지) |
| 5 | 부활 다이제스트 | ✅ 양호 | 동일(유지) |
| 2 | 꼬띠 오늘의 추천 | ❌ 핵심 미반영 | 황금/니치/시즌 태그 안 뜸·"3."빈액션·외계어(모드/블루오션+1/datalab/소싱보관함)·뭉침 |
| 1 | 주간 리포트 | ❌ 데이터 부정확 | 판매중0(상품4개인데)·꿀통 0점(미계산)·"5개"(4개인데)·일반적 액션 |
| 3 | 품절(26.6.9) | ⚪ 무시 | 옛 테스트·봇명 꼬뛲(수정전) |

## 1. 황금/니치/시즌 태그 미노출 (최우선·핵심 요청)
- 원인: recoType 분류가 카테고리 삼중항(d1/d2/d3)+트렌드 필요한데, 추천 아이템 category가 'general'/D1명/미해소 → classifyRecommendationType null → 태그 없음(#231 정직처리가 역효과).
- 개선: 추천 아이템에 **카테고리 삼중항 해소를 강제**(상품의 naver 카테고리 코드→triple, 없으면 상품명·키워드로 추정) → 실제로 🏆황금/💎니치/🗓️시즌 붙게. 분류 조건도 4개 상품 현실에서 최소 1개는 유형이 붙도록 검토(단 허위분류 금지 유지·근거 있을 때만).
- 표시: 아이템명 앞 이모지+라벨 + 헤더 "황금 N·니치 N·시즌 N".

## 2. 외계어/구용어 제거 (전 알림·#233)
- "4가지 모드별"·"모드" → 삭제("오늘의 추천"으로 단순화).
- "검색량 블루오션 +1" → "경쟁 낮은 틈새" 등 셀러 언어 or 니치 태그로 통합.
- "(datalab)" → 노출 제거(내부 출처 숨김).
- "소싱보관함" → "꿀통 창고"(신 IA 용어 정합).
- "꿀통 68점" 유지 가능하나 "꿀통 점수"가 뭔지 첫 등장 시 1회 힌트.

## 3. 데이터 정확도 (주간 리포트·#231)
- "판매중 N개": 실제 상태 반영(미발행이면 "발행 대기 N"·판매중 실수치). 0 오표기 수정.
- "평균 꿀통 0점": 0=미계산이면 계산하거나 "집계 중"으로(0점 오인 금지).
- "5개 골라 튜닝": 실제 튜닝 대상 수로 동적(상품 4개면 최대 4). 하드코딩 금지.
- 액션: 데이터 기반("꿀통 50점 미만 N개")·N=실제 수.

## 4. 버그·가독성
- 추천 액션 "3." 빈 줄: 액션 수만큼만 렌더(빈 항목 제거).
- 현황 숫자 뭉침: 시각 청킹(핵심 3개 굵게·나머지 접기)·라인 정렬.

## 5. 개성·유머 일관화 (꽃틔움 톤)
- 이미지 4/5의 사투리 톤("~예유/~줘유"·"깨울 시간이에유")이 매력 → **추천·주간리포트에도 꼬띠 개성 일관 적용**(과하지 않게 1~2문장).
- 각 알림 유형별 꼬띠 보이스 변주: 추천="이 상품 놓치면 아까워유"·리포트="이번 주도 수고했어유, 다음 주는 더 피워봐유" 등. discord-strings.ko.json에 톤 통일.

## 6. 검증 절차
- Code가 위 반영 → **테스트 발송 라우트(84f09ce·/api/admin/test-daily-discord confirm:true)로 실발송** → 운영자 채널에서 실물 재확인 → 잔여 갭 반복 개선. (실물 확인 필수·코드만으로 단정 금지·#231)

## 작업 유의사항/원칙 (신규)
- **#258** 알림/콘텐츠 개선은 코드 반영 여부가 아니라 **실제 발송 실물로 검증**(스크린샷/테스트발송) — 코드에 로직이 있어도 데이터 미해소로 침묵할 수 있음(예: 카테고리 미해소→유형태그 null). 데이터 정확도(집계 0/하드코딩 수치) 실물 대조 필수. 외계어(모드·datalab·구IA용어) 전 알림 전수 제거·셀러 언어(#233). 개성/유머는 전 알림 유형에 일관 적용(꽃틔움 톤 SoT=discord-strings.ko.json). 허위 분류는 금지하되 실제 데이터로 유형이 실제 노출되도록 해소 로직 보강(#231 정직처리가 기능 침묵으로 이어지지 않게).
