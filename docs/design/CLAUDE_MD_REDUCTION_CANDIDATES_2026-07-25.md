# CLAUDE.md 축소 — 삭제 후보 목록 (1단계, 실행 전)

> SUPERSEDED BY: `docs/design/CLAUDE_MD_REDUCTION_CANDIDATES_2026-07-28.md` (v2 — DOCS_STANDARD v3 §13-17 기준 재평가 + 후보#3 수정 + 신규 3건)
> 원본 전문 보존: `docs/archive/CLAUDE_MD_FULL_2026-07-24.md` (346줄, 원본과 diff 0 확인).
> 이 문서는 **후보 제시만** — 실 삭제는 운영자 승인 후 2단계에서 진행(작업원칙 #314).
> 목표: 346줄 → ≤200줄. 아래 표 전체 적용 시 예상 **약 196줄**.

## 후보 표

| # | 무엇을 (현재 위치·줄수) | 왜 (이관 사유) | 어디로 |
|---|---|---|---|
| 1 | §2 "현재 단계 (스냅샷)" (L100-113, 14줄) | 본문이 스스로 "항상 PROGRESS.md로 재확인"이라 명시 — 이미 단일 출처가 아님. 내용도 2026-07-11~14 시점에 멈춰 있어 이후 수십 건의 handoff로 stale. 단일 소스 원칙(SoT) 위반. | 삭제(내용 손실 없음 — PROGRESS.md가 이미 최신 버전 보유). 1줄 포인터만 남김: "현재 단계는 항상 PROGRESS.md 확인." |
| 2 | §3-6 이미지/발행 자산 규칙 (L151-160, 10줄) | 이미지/합성/발행 파이프라인 작업할 때만 관련. 주문 동기화·SEO 등 무관한 세션에도 매번 로드되어 컨텍스트 낭비. | `.claude/rules/image-assets.md` — paths: `src/lib/design/**`, `src/app/api/**/composite/**`, `docs/design/ADAPTIVE_COMPOSITE_ENGINE.md` 관련 작업 시에만 자동 로드. |
| 3 | §3-7 네이버 v2 전체 페이로드 규칙 (L163-168, 6줄) | 네이버 상품 PUT 코드를 건드릴 때만 결정적. 나머지 세션엔 무관. | `.claude/rules/naver-api.md` — paths: `src/lib/naver/**`, `src/app/api/products/**/naver*/**`. |
| 4 | §5 핵심 파일 경로 전체 (L215-246, 32줄) | `docs/plan/REFERENCES.md`가 이미 "핵심 파일 경로" 전거로 지정돼 있음(§1 STEP1 목록에 있음) — 중복 보관. | 삭제, REFERENCES.md로 통합(경로 목록이 REFERENCES.md에 이미 없다면 이관 후 삭제). 포인터 1줄: "핵심 파일 경로는 REFERENCES.md 참조." |
| 5 | §6-1 도구 매핑 표 (L251-261, 11줄) | Claude Code 자체가 이미 아는 자기 도구 이름 매핑 — 프로젝트 고유 정보가 아님. | 삭제. |
| 6 | §6-2 권장 MCP 추가 명령 (L263-274, 12줄) | 최초 1회 설치 안내이지 매 세션 필요한 상시 규칙이 아님. | `docs/README.md`(신규 도입 파일)로 이관 — "환경 설정" 섹션. |
| 7 | §6-3 슬래시 커맨드 목록 (L276-283, 8줄) | Claude Code 내장 기능 — 프로젝트 지식이 아님. | 삭제. |
| 8 | §7 작업원칙 빠른 인덱스 전체 (L291-332, 42줄) | 최대 단일 블록. 본문 자체가 "#149~#203은 이미 PRINCIPLES_LEARNED.md 정식 이관완료"라 명시 — 상당 부분 이미 중복 보관 중. 결번(#165/#217~220/#225/#231) 안내까지 CLAUDE.md에 있을 필요 없음. | 전체를 `docs/plan/PRINCIPLES_CODE.md`(#1-25)·`PRINCIPLES_LEARNED.md`(#26+)로 통합 — 두 파일이 이미 정본. CLAUDE.md엔 3줄 포인터만: "작업원칙 전체 인덱스는 PRINCIPLES_CODE.md(#1-25)·PRINCIPLES_LEARNED.md(#26+) 참조. AI 인프라 변경 시 #42~#45 필독." |
| 9 | §8 새 세션 첫 메시지 템플릿 (L336-346, 10줄) | §1 STEP 0~3이 이미 "매 세션 첫 turn 자동 수행"을 강제 — 이 템플릿은 그 자동화가 실패했을 때의 수동 백업일 뿐인데 별도 대분류 섹션으로 존재. | 2줄로 축약해 §1 말미에 각주로 흡수(별도 섹션 제거). |
| 10 | §4-3 grep 명령 코드 블록만 (L196-198, 3줄) | 규칙 문장(1줄)은 남기고, 실행 커맨드 자체는 `.claude/rules/md-hangul-check.md`로. 매 세션 읽을 필요 없이 MD 갱신 작업 시에만 참조. | `.claude/rules/md-hangul-check.md` — paths: `docs/plan/**`, `docs/research/**`. 본문엔 규칙 존재만 1줄 유지. |

## 적용 안 하는 항목 (유지 사유)

| 섹션 | 유지 이유 |
|---|---|
| §0 최우선 상시 원칙(#262) | 짧고(9줄) 모든 화면 문구에 항상 적용되는 always-on 규칙 — 축소 대상 아님. |
| §1 세션 시작 절차 STEP 0~3 | 프로젝트 운영의 핵심 게이트. 짧게 만들 여지는 있으나 내용 삭제는 위험(#21 사전점검 의무 직결). |
| §3-1~3-5 (한글/Prisma/카테고리/env/검증) | 매 코드 작업마다 상시 적용되는 절대 규칙 — 좁은 paths로 못 좁힘(전 코드베이스 해당). |
| §4-1/4-2/4-4/4-5 (승인게이트/닉네임/git/의심파일) | 세션 전반에 항상 적용되는 행동 규칙. 짧고 이관해도 절약폭 작음. |
| §6-4 Auto-accept 주의 | 안전 관련 경고 — 항상 눈에 띄어야 함. |

## 예상 결과

- 원본 346줄 → 이관/삭제 적용 시 약 **196줄** (후보 1~10 라인수 합계 약 148줄 절감 + 포인터 대체분 반영).
- 손실 정보 0건: 모든 내용은 삭제되는 게 아니라 (a) 원본 전문이 `docs/archive/CLAUDE_MD_FULL_2026-07-24.md`에 이미 보존됨, (b) 각 항목이 더 적절한 기존/신규 정본 파일로 이관됨.
- 2단계 실행 시 필수: 이관 후 `grep -c` 또는 `grep -n`으로 옮긴 규칙이 실제로 새 위치에 살아있는지 검증 + `.claude/rules/*.md`가 실제로 paths 매칭 시 로드되는지 확인.
