# SF-4 — 860px 상세페이지 내보내기 스펙 (2026-07-03)

Authoring: DESKTOP. 권위: SLOT_FUNNEL_BOARD_SPEC(v1.1) §3 SF-4 상세화 + DETAIL_PAGE_PLAYBOOK(860px 규격) + buildDetailContent(기존 배관). 구현=Code. 원칙 #55/#181/#46/#184.
전제: SF-1(읽기)·SF-2(이미지 배정+detail_images)·SF-3a(카피 제안) DONE·prod검증. SF-3b(카피 영속)는 본 SF-4와 묶여 결정.

================================================================
## 0. 목적 (왜)
================================================================
조립된 보드(SF-2 이미지 순서 + SF-3 섹션 카피)를 **네이버 규격 상세페이지로 산출** → detailContent/detail_images 반영 → 발행 준비 완료. 조립 엔진의 최종 출력 단계.

================================================================
## 1. verify-first (#181 — Code 착수 전 실측)
================================================================
★Code 필수 실측:
- **buildDetailContent** 현 로직 — detail_images(SF-2 평면 순서) → detailContent HTML을 이미 생성. **어떤 HTML 구조·이미지 폭·순서**로 emit하는지 정확히 확인.
- **네이버 상세페이지 방식** — detailContent는 HTML(에디터) 렌더. 이미지 폭은 CSS/속성으로 제어(네이버 권장 가로 ~860px).
- **발행 경로** — detailContent가 Naver PUT에 실리는 필드/변환 확인(#46 발행은 GO 게이트).

================================================================
## 2. 핵심 아키텍처 분기 (★결정 필요)
================================================================
네이버 상세페이지 = **HTML(detailContent)** 방식. 두 경로:

| 경로 | 방식 | 장단 |
|---|---|---|
| **(A) HTML 조립 (MVP 권고)** | detail_images(SF-2) + 섹션 카피(SF-3)를 **860px 반응형 detailContent HTML로 조립**(이미지+텍스트 블록). 기존 buildDetailContent 확장. | 신규 렌더 0·빠름·수정 용이·SEO 텍스트 인덱싱↑. 디자인은 HTML 수준 |
| **(B) 이미지 합성 (프리미엄·후속)** | 섹션별 이미지+카피를 **하나의 860px 합성 이미지**로(canvas/Firefly). | 디자인 완성도↑·픽셀 완벽. 무겁고 텍스트 SEO 손실·크레딧 |

→ **권고: (A) HTML 조립 MVP** — 이미 detailContent 배관 존재, 텍스트가 HTML에 남아 **네이버 쇼핑 SEO 유리**(상세 텍스트 인덱싱). (B)는 고급 옵션으로 후속(SF-4b).

================================================================
## 3. SF-3b 결합 (카피 영속 = 이 분기로 결정)
================================================================
- 경로 (A) 채택 시: **섹션 카피는 detailContent HTML에 텍스트로 합성** → SF-3b 카피 영속 = detailContent(신규 필드 0). SF-3a 제안 → 편집 → detailContent 조립 시 반영.
- → SF-3b는 SF-4(A)와 **동시 구현**이 자연스러움: "보드 → detailContent 조립(이미지+카피)" 한 흐름.

================================================================
## 4. 단계 증분
================================================================
- **SF-4a (HTML 조립·미리보기)**: 보드 → 860px detailContent HTML 조립(detail_images 순서 + 섹션 카피) + **라이브 미리보기**(860 데스크톱/360 모바일 토글). 규격 인디케이터(가로 860·이미지 폭·용량). 영속: detailContent에 반영(가역·발행 무관 #46).
- **SF-4b (프리미엄 이미지 합성)**: 후속 옵션 — 섹션 합성 이미지(canvas/Firefly).
- 검증: 조립 미리보기 렌더·860px 규격·detailContent 갱신(GET 확인)·씨앗심기 상세 미리보기와 정합·발행은 GO 게이트 유지.

================================================================
## 5. 원칙·규격
================================================================
- 전상품 범용(#55)·발행 무관/GO 게이트(#46)·detail_images 평면계약 유지(#184).
- 네이버 규격: 가로 860px 권장·세로/용량 제한 준수·JPEG 최적화·로딩 3초·CTA 48px(DETAIL_PAGE_PLAYBOOK).
- 카피 톤 금칙(과장·의료·최상급) — 네이버 검수 리스크 회피.

================================================================
## 작업 유의사항/원칙
================================================================
- #189: 네이버 상세는 HTML(detailContent) 방식 — 이미지 합성보다 **HTML 조립이 SEO(텍스트 인덱싱)·유지보수 유리**. 프리미엄 이미지 합성은 옵션으로 분리.
- #181: buildDetailContent 현 emit 구조를 먼저 실측 후 확장(재구축 금지).
- SF-3b는 SF-4(A)와 결합 — 카피 영속처 = detailContent(신규 필드 0).
