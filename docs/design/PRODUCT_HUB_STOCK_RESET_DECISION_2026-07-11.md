# 상품 허브 — 재고/리셋 결정 대기 + 권장안 (2026-07-11)

Authoring: DESKTOP. 계기: Phase 2b에서 재고/리셋이 정의 필요로 defer됨. 원칙 #46/#55/#247. 상태: 운영자 결정 대기·Desktop 권장 제시.

## 0. 검증 (Phase 2b 후 net-zero)
- 3상품 전부 baseline DRAFT 복원 확인(아이스 테스트 후 DRAFT 복귀·달항아리 무변경·명화 SUSPENSION 정합). origin_kind 정합(명화 IMPORTED·나머지 APP_CREATED). 운영자 데이터 손상 0.

## 1. 결정 #1 — "재고 조정" (Product에 편집 stock 스칼라 없음)
- 사실(Code): Product에 수동 편집 가능한 stock 필드 없음(inventory=CrawlLog 소속). 위탁판매(도매매 소싱) 모델 = 재고 원천은 공급사.
- **Desktop 권장**: 자유 입력 재고칸 신설 대신 —
  - ⓐ "공급사 재고 동기화"(기존 crawler/stock-check 재사용)
  - ⓑ "품절 표시" 수동 토글(강제 품절·재입고 대기)
  - 이유: 위탁모델에서 자유 재고 입력은 실재고 괴리 위험. 동기화+품절토글이 실무 정합.
- 운영자 OK 시 → Code가 confirm 게이트로 구현.

## 2. 결정 #2 — "리셋" 정의
- **Desktop 권장**: "앱에서 튜닝한 내용(상품명·이미지·가격 등)을 **연동 원본 상태로 되돌림**"(undo app-tuning). 좀비부활 워크플로우 안전장치.
  - 연동(IMPORTED/HYBRID) 상품에만 활성·앱생성(APP_CREATED)엔 비활성(원본 없음).
  - 비가역이므로 confirm 게이트(#46)+원본 스냅샷 존재 확인.
- 운영자 정의 확정 시 → Code 구현.

## 3. 결정 무관 독립 진행 (Phase 3)
- link/import → 허브 진입점 재배치(Sidebar.tsx IA SoT #26). 위 결정과 무관·즉시 가능.

## 작업 유의사항/원칙 (재확인)
- **#247** 의미 모호/비가역 가능 액션(리셋·재고)은 구현 전 운영자 정의 확인·임의 구현 금지. Desktop은 파워셀러 관점 권장안을 함께 제시해 운영자 결정 부하를 낮춤(단순 질문이 아니라 근거 있는 옵션+추천).
- 위탁판매 모델 특성(재고=공급사 원천)을 기능 설계에 반영 — 자유 재고 입력보다 동기화+품절토글.
