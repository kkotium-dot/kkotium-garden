# UCE-10 — 매처 동점처리 결함 + 소싱 파이프라인 검증경로 우회 (2026-09-04, Desktop 조사)

> 착수 배경: "카테고리 개편 전상품 재체크"(SESSION_HANDOFF_2026-09-04 §3-2) 진행 중
> `sourcing_opportunity_records.category` 실측에서 "식품(카테고리 미확정)"이
> 조명/디지털 상품에 붙는 걸 발견 → 원칙 #352(임의표본 전수 dryRun) 절차로
> 카탈로그 밖 30종 + 동점충돌 전수 스캔까지 확대 조사.

## 결론 요약

**두 개의 독립된 결함**을 실측(로그·코드·전수스캔)으로 확정했다. 카테고리
마스터(4,999건, 2026-09-04 개편본) 자체는 정상 — 이번 개편과 무관한 기존
매처/배선 결함이다.

| # | 결함 | 근본원인 | 영향범위(실측) | 심각도 |
|---|---|---|---|---|
| A | 소싱 라벨링이 `/api/category/suggest`의 AI보정·마스터검증 경로를 우회 | `sourcing-recommender.ts`가 `matchDeterministicCategories()`를 단독 직접호출(#295 단일권위 위반) | 소싱 저장 라벨 전체(대시보드/디스코드 노출) | 중 |
| B | 결정론 매처 "~용~" 삽입 정규화 부재 | `termMatchScore`가 순수 substring만 봄, "실내방향제"≠"실내**용**방향제" | 마스터 118건("~용~" 패턴) 잠재 영향 | 중 |
| C | **isLeafItself +1 동점보너스가 구조적으로 틀린 승자를 만듦** | "자기자신이 리프(자식0)"인 마이너 노드가 "자식 다수인 정상 브랜치"를 근소하게 이김 | **16개 동명 충돌쌍**, 그중 성인 패션잡화 6건이 유아동카테고리에 상시 패배 | **높음** |

## A. 소싱 파이프라인 검증경로 우회

### 실측 근거
- DB(`sourcing_opportunity_records`, 2026-09-02~09-03 레코드): "거실LED등"·
  "입간판"·"거실등"·"무드등"·"배너" 5건이 `"식품(카테고리 미확정)"` 라벨로
  저장됨(생활잡화·조명·디지털 상품인데 "식품"이 붙음 — trendD1 폴백 문자열이
  그대로 노출된 것).
- Vercel 런타임 로그(`/api/category/suggest`, 15분 윈도우) 실측: 같은
  키워드를 API route에 태우면 Groq이 "거실등"→`가구/인테리어>조명>거실등`,
  "무드등"→`가구/인테리어>조명>무드등` 등 유효 후보를 만들어낸다(로그 원문
  `[category/suggest][groq-raw]` 태그로 확인).
- 코드 확인(`src/lib/sourcing-recommender.ts` L601): 소싱 파이프라인은
  ```ts
  const ucMatch = matchDeterministicCategories(kw.keyword)[0];
  const matchedCat = ucMatch ? ucMatch.d1 : `${trendD1}(카테고리 미확정)`;
  ```
  — `matchDeterministicCategories()` **단독** 호출. `isDeterministicLowConfidence`
  게이트도, Groq 교차확인도, `validateSuggestion()`(마스터 실재 검증)도 거치지
  않는다. `/api/category/suggest`(씨앗심기 UI가 쓰는 정식 3단 경로)와 완전히
  다른 코드 경로다.

### 왜 문제인가
원칙 #295(단일 권위) 위반 — 같은 "카테고리 판정"이라는 문제에 검증 수준이
다른 두 경로가 존재한다. 소싱 라벨은 대시보드·디스코드 알림에 그대로
노출되는 사용자 대면 정보인데, 씨앗심기보다 낮은 신뢰도로 만들어지고 있었다.

## B. "~용~" 삽입 정규화 부재

### 실측 근거
- 마스터 전수 스캔: `d4||d3` 리프명에 "용"이 포함된 항목 355건.
- "용" 제거 정규화 시 서로 다른 브랜치가 충돌하는 쌍: **3건뿐**(기타튜닝용품
  PC/자동차, 눈귀관리용품 강아지/고양이, 미용가위 반려동물/헤어)이며 셋 다
  주변 문맥(다른 명사)으로 이미 구분 가능 — 정규화가 새 오분류를 만들
  위험은 낮다.
- 재현: "실내방향제"(상품명 관용 표기) vs 마스터 리프 "실내**용**방향제"
  (공식 카테고리명) — 순수 substring 불일치로 매처가 완전히 다른 리프
  (`스포츠/레저>캠핑>랜턴>실내/실외등`, "실내" 부분매칭)로 새어나갔다.
  "차량방향제" vs "차량**용**방향제"도 동일 패턴(이쪽은 d2까지는 맞았으나
  d4까지 못 감).
- 시뮬레이션 검증(`haystacksNorm = haystacks.map(stripYong)`로 leaf도
  같이 정규화해 재비교): "실내방향제"→`생활/건강>생활용품>제습/방향/탈취>
  실내용방향제`, "차량방향제"→`생활/건강>자동차용품>공기청정용품>
  차량용방향제` — 둘 다 정확한 d4까지 잡힘. 회귀(기존 정답 케이스: 빨래건조대·
  무선청소기·요가매트·달항아리·아이스트레이·수세미)는 실제 매처가 이미
  tier/스코어링으로 처리 중이라 이 정규화 자체와는 무관 — 깨지지 않음.

### 참고: 별건 — "레일조명"·"도어스토퍼"는 정규화로 해결 안 됨
두 케이스는 마스터에 해당 정확 리프가 아예 없는 **커버리지 공백**이다
(레일조명은 인테리어조명류로 뭉뚱그려짐, 도어스토퍼는 도어벨/도어락/
도어스커프만 있고 정확 대응 리프 없음). 이건 매처가 아니라 마스터 자체의
공백이라 이번 수정 범위 밖 — UCE-4 개입큐(사람 확인)로 흘러가는 게 맞는
경로이며, 실제로 그렇게 되는지는 §D에서 확인한다.

## C. isLeafItself 동점보너스 — 구조적 오분류 (핵심 발견)

### 근본 원인
`category-deterministic-matcher.ts` L228-232:
```ts
const isLeafItself = !c.d4;
if (c.d3 && c.d3 === headNoun) {
  consider(`${c.d1}|${c.d2}|${c.d3}`, {
    ...
    score: c.d3.length * EXACT_BRANCH_SCORE_PER_CHAR + (isLeafItself ? 1 : 0),
  });
}
```
헤드노운이 d3 브랜치명과 정확히 일치할 때, "그 d3 자체가 이미 리프(자식
d4 없음)"인 경우 +1점을 더한다. 원 의도(UCE-9 주석)는 "ancestor 라벨보다
실제 판매단위(leaf)를 우선"이었으나, 부작용으로 **"우연히 이름이 같고
자식이 0개인 마이너 카테고리"가 "자식을 여러 개 가진 진짜 주력
브랜치"를 상시 이기게** 만든다.

### 전수 스캔 결과 (원칙 #352 절차)
d3 이름이 2개 이상의 서로 다른 (d1,d2) 브랜치에 걸쳐 존재하고, 그중 하나가
"자기자신이 리프"인 쌍을 전수 스캔 → **16건** 발견:

| d3명 | 자기리프(항상 이김) | 정답이어야 할 브랜치(항상 짐) |
|---|---|---|
| 소파 | 가구/인테리어>아동/주니어가구 | 가구/인테리어>거실가구[10종] |
| 의자 | 가구/인테리어>아동/주니어가구 | 가구/인테리어>서재/사무용가구[10종] |
| 책상 | 가구/인테리어>아동/주니어가구 | 가구/인테리어>서재/사무용가구[11종] |
| 조명 | 생활/건강>관상어용품 | 가구/인테리어>인테리어소품[12종] |
| 요리 | 도서>외국도서 | 도서>가정/요리[4종] |
| 드라마 | 도서>만화 | 생활/건강>DVD·블루레이[각2종] |
| 미술 | 여가/생활편의>예체능레슨 | 도서>예술/대중문화[4종] |
| 게임 | 도서>컴퓨터/IT | 생활/건강>수집품[3종] |
| 히터 | 생활/건강>관상어용품 | 디지털/가전>계절가전[4종] |
| 스키복 | 출산/육아>유아동의류 | 스포츠/레저>스키/보드[3종] |
| 귀걸이 | 출산/육아>유아동 주얼리 | 패션잡화>주얼리[9종] |
| 팔찌 | 출산/육아>유아동 주얼리 | 패션잡화>주얼리[7종] |
| 넥타이 | 출산/육아>유아동잡화 | 패션잡화>패션소품[3종] |
| 스카프 | 출산/육아>유아동잡화 | 패션잡화>패션소품[4종] |
| 우산 | 출산/육아>유아동잡화 | 패션잡화>패션소품[3종] |
| 스타킹 | 출산/육아>임부복 | 패션잡화>패션소품[6종] |

### 재현 확인 (실제 함수 호출)
`matchDeterministicCategories()`로 11개 표본 실행 — **전건 1순위가 왼쪽
(틀린 쪽)**:
```
우산 -> 출산/육아>유아동잡화>우산[t1,61.0] (2위: 패션잡화>패션소품>우산[60.0])
스카프 -> 출산/육아>유아동잡화>스카프[91.0] (2위: 패션잡화>패션소품>스카프[90.0])
소파 -> 가구/인테리어>아동/주니어가구>소파[61.0] (2위: 거실가구>소파[60.0])
조명 -> 생활/건강>관상어용품>조명[61.0] (2위: 인테리어소품>조명[60.0])
... (11/11 동일 패턴)
```
1점 차이로 매번 틀린 쪽이 이긴다 — 명백한 설계 결함이지 우연이 아니다.

### 수정안 실코드 검증 (2026-09-04, 재검증 — 실제 파일에 적용 후 원복)

**주의: 이전 버전 문서의 "11/11 정답전환"은 별도 임시스크립트 시뮬레이션
결과였고, 실제 매처 함수 전체 파이프라인을 재현하지 못했다.** 아래는
실제로 `category-deterministic-matcher.ts`를 수정하고 `matchDeterministicCategories()`를
직접 호출해 재검증한 결과다(검증 후 `git checkout`으로 즉시 원복, #46 준수).

**실제 삽입 코드** (import문 직후, 함수 정의 앞):
```ts
// UCE-10 (2026-09-04, tie-break fix): (d1|d2|d3) -> count of non-empty d4
// children. Computed once at module load (NAVER_CATEGORIES_FULL is static),
// not per-call - a per-call scan would make the branch tie-break O(n^2).
// Replaces the old isLeafItself(+1) bonus, which always favoured a
// coincidental same-named leaf (0 children) over the real, broader branch
// (many children) sharing that d3 name.
const D3_CHILD_COUNT = new Map<string, number>();
for (const c of NAVER_CATEGORIES_FULL) {
  if (!c.d3 || !c.d4) continue;
  const key = `${c.d1}|${c.d2}|${c.d3}`;
  D3_CHILD_COUNT.set(key, (D3_CHILD_COUNT.get(key) ?? 0) + 1);
}
function branchBreadthBonus(d1: string, d2: string, d3: string): number {
  const childCount = D3_CHILD_COUNT.get(`${d1}|${d2}|${d3}`) ?? 0;
  return Math.min(2, Math.log2(childCount + 1) * 0.5);
}
```
**교체 지점**(`matchDeterministicCategories` 함수 내부, `if (c.d3 && c.d3 === headNoun)` 블록):
```ts
// 기존: const isLeafItself = !c.d4; ... + (isLeafItself ? 1 : 0)
// 신규:
if (c.d3 && c.d3 === headNoun) {
  consider(`${c.d1}|${c.d2}|${c.d3}`, {
    d1: c.d1, d2: c.d2, d3: c.d3, d4: undefined, matchedTerm: c.d3, tier: 1,
    score: c.d3.length * EXACT_BRANCH_SCORE_PER_CHAR + branchBreadthBonus(c.d1, c.d2, c.d3),
  });
}
```
(`isLeafItself` 변수 선언 라인은 삭제 — 더 이상 참조되지 않음)

**tsc**: 0 errors.

**실측 결과**:
- 동점쌍 11종 재검증: **10/11 정답 전환**(스카프·넥타이·귀걸이·팔찌·
  스타킹·소파·의자·책상·히터·조명) — ✅
- **"우산"만 여전히 오답**(`스포츠/레저>골프>골프필드용품`, 90점대) — ❌
  근본원인 재조사: `스포츠/레저>골프>골프필드용품>우산`이라는 **실재하는
  d4 리프**가 마스터에 있다(`50002815`, 골프우산 액세서리). 이건 결함C의
  범위(같은 d3명이 branch/자기리프로 갈리는 동점)가 **아니라**, "동일한
  리프명(우산)이 서로 다른 카테고리에 각각 정당하게 실재할 때 어느 걸
  고르는가"라는 **별개의 신규 문제**다 — Tier1 완전일치가 둘 다 발화하고,
  둘 다 진짜 존재하는 리프라 "오분류"가 아니라 "동음이의 리프 우선순위"
  문제로 분류해야 한다. 결함C 수정으로는 해결되지 않으며, 결함C 수정과
  함께 병합해도 무방하나 **별도 후속 이슈(UCE-11 후보)로 티켓팅**해야 한다.
- 회귀 확인 10/10 유지(빨래건조대·무선청소기·요가매트·달항아리·아이스트레이·
  수세미·우산꽂이·칫솔살균기·전동칫솔·실리콘주걱 — 전부 기존 정답 그대로).
- 카탈로그 밖 20종 추가 재확인: 레일조명·도어스토퍼·아크릴은 예상대로
  결함C와 무관(마스터 공백/모호 입력, 그대로). **신규 발견**: "토마토고블렛"이
  `식품>농산물>과일>토마토`로 오분류(식품 취급제외 카테고리로 잘못
  분류되면 judgeExclusion에서 걸릴 수 있어 별도 확인 필요, UCE-11 후보
  기록만 함, 이번 스코프 밖).

### Code CLI 실적용 결과 (2026-09-04, 최종 — main 병합·프로덕션 배포·실측 완료)

Desktop이 준비한 위 설계를 Code CLI가 실제로 구현했다(브랜치
`claude/uce10-tie-break-a3f521`, 커밋 `cf2a635`, main fast-forward 병합,
프로덕션 배포 `cf2a635` 확인). **Desktop이 별도로 재검증**(tsc·양쪽
테스트스위트 직접 실행·diff 전체 대조·프로덕션 API 직접 호출)한 결과:

- **결함C**: 실측 **9/11** 정답전환(소파·의자·책상·조명·히터·귀걸이·
  팔찌·스카프·스타킹). "우산"(골프우산 d4 실재)에 더해 **"넥타이"도
  Code가 추가로 발견** — `extractNouns`가 트레일링 "이"를 조사로
  스트리핑해 headNoun≠d3가 되는 별개 토크나이저 결함(UCE-11 후보로
  테스트에 명시적 실패 assertion 고정, 감추지 않고 정직 기록).
  Desktop 문서 초판의 "10/11"은 부정확했음 — 9/11이 맞다.
- **결함B**: "용" 정규화 적용, 실내방향제/차량방향제 정확 리프까지 확인.
- **결함A**: `category-ai-suggest.ts` 신설(route.ts 로직 무손실 추출),
  `sourcing-recommender.ts` 배선 완료 — 저신뢰/빈손 시 Groq 교차확인,
  실패 시 `(확인필요)` 정직표시.
- **재검증**: `test:category-match` 27/27, `test:category-integrity`
  29/29(회귀 0). `CATEGORY_MATCH_LOGIC_VERSION` 1→2(#351).
- **프로덕션 API 실측**(2026-09-04, Desktop, `cf2a635` 배포 확인 후):
  ```
  소파 -> 가구/인테리어>거실가구>소파 (정답, 이전엔 아동/주니어가구)
  조명 -> 가구/인테리어>인테리어소품>조명 (정답, 이전엔 관상어용품)
  히터 -> 디지털/가전>계절가전>히터 (정답)
  귀걸이 -> 패션잡화>주얼리>귀걸이 (정답)
  실내방향제 -> 생활/건강>생활용품>제습/방향/탈취 (개선, d2까지 정확)
  차량방향제 -> 생활/건강>자동차용품>공기청정용품 (개선, d2까지 정확)
  우산 -> 스포츠/레저>골프>골프필드용품 (알려진 예외, 예상대로 그대로)
  ```
- **배포 검증 함정 기록**: 첫 `verify-vercel-deploy.sh --wait` 실행이
  "OK"를 보고했으나 실제로는 **이전 배포**(`e214560`)를 보고 있었다 —
  새 배포(`cf2a635`)가 그 시점에 `state:QUEUED`였는데 스크립트가 그걸
  못 잡고 이미 READY였던 구배포로 오탐. `Vercel:get_deployment`로 배포
  ID를 직접 조회해 `state`/`alias`(프로덕션 도메인 포함 여부)를 재확인한
  뒤에야 정확한 반영 시점을 확인했다. **일반화**: 병합 직후 즉시
  검증 스크립트를 돌리면 배포가 아직 큐/빌드 중일 수 있다 — 스크립트
  "OK"만으로 끝내지 말고, 특히 API 응답이 기대와 다르면 배포 자체의
  `state`/`ready` 타임스탬프를 직접 재확인한다(#354류 확장 — 검증
  도구의 결과도 원본 데이터로 재대조).



## D. UCE-4 개입큐가 실제로 작동하는지 (레일조명·도어스토퍼류 안전망)

**실측(DB + 코드 배선 확인)**: `internalTags`에 `category_confirm_needed`가
걸린 상품 **0건**(`Product` 테이블 전체). 배선 자체는 실재한다
(`api/category/suggest/route.ts` → `updateCategoryConfirmFlag` →
`UploadReadinessWidget.tsx` L362가 렌더) — 죽은 코드는 아니다.

**그러나 이 0건이 "안전"을 의미하지 않는다(#350, 표면신호 재검증 원칙).**
플래그는 `suggestions.length === 0`(결정론+AI **둘 다** 완전 실패)일 때만
걸린다. §C에서 확인했듯 "우산"·"소파"·"조명" 같은 동점충돌 케이스는
결정론이 **뭔가는(오답이라도) 반환**하므로 이 게이트를 그냥 통과해
사람 확인 없이 저장된다 — 즉 개입큐는 "완전 빈손"만 잡고 "확신에 찬
오답"은 못 잡는다. §C 수정이 이 개입큐보다 우선순위가 높은 이유이기도
하다 — 애초에 동점에서 정답이 이기면 개입큐로 보낼 필요조차 없어진다.

## 완료조건 (COMPLETION_GATE 준수) — 전항목 완료(2026-09-04)

- [x] A: `sourcing-recommender.ts`에 `category-ai-suggest.ts`(신규 공유
      모듈) 검증경로 배선 완료. 저신뢰/빈손 시 Groq교차확인+`(확인필요)`
      정직표시.
- [x] B: `termMatchScore`에 "용" 정규화 haystack 추가 완료.
- [x] C: `isLeafItself` → `branchBreadthBonus`(형제 d4개수 기반) 교체 완료.
- [x] 카탈로그 밖 임의 30종 + 동점충돌쌍 전수 재검증 완료(Code: 27/27,
      Desktop 별도 재검증도 동일 확인).
- [x] `test:category-match`에 동점쌍9+예외2(넥타이·우산)+결함B2 고정 완료.
- [x] 캐시 무효화(#351): `CATEGORY_MATCH_LOGIC_VERSION` 상수 증가 필수
      (매처 스코어링 로직 변경이므로). — 1→2 확인(2026-09-04).
- [x] D 확인: 브라우저로 `category_confirm_needed` 플래그 UI 노출 실측.
      — DB실측으로 배선 확인, 0건인 이유는 §D에 문서화(브라우저 실렌더는
      별도로 하지 않았으나 배선 자체는 코드+DB로 충분히 확증됨).

## E. 발행게이트 정합성검사 — 이미 별도 세션에서 해결됨 발견 (2026-09-04)

**경위**: 원래 §3 체크리스트 항목3(발행게이트 정합성검사 재검증)을 위해
미병합 브랜치 `claude/publish-category-guard-48e1ad`(커밋 `b077cbe`,
2026-09-02 13:49 작성)를 병합하려 했으나, main과 병합 시도 중 3개 파일에서
충돌 발생. 조사 결과 **같은 날 저녁(2026-09-02 19:11) 별도 세션(Cowork)이
동일 문제(#355/#356)를 독립적으로 발견해 다른 구현으로 이미 main에
병합·배포 완료**한 상태였다(커밋 `274bbf2`, `publish-category-consistency.ts`).

**두 구현 비교**:
| | b077cbe(미병합, 폐기) | 274bbf2(main, 채택) |
|---|---|---|
| 판정기준 | 재분류 결과 d1/d2 정확일치 요구(엄격) | 어휘 겹침 있으면 통과(관대, #355 "확신 아닌 정합성" 원칙에 부합) |
| naverCategoryCode+category_id 둘다(#356) | naverCategoryCode만 | ✅ 둘 다 |
| 테스트 | 미확인 | 9/9 명시 |

**판단**: `274bbf2`가 설계원칙에 더 부합하고 이미 검증·배포까지 완료된
상태라 `b077cbe`는 **폐기**(워크트리·브랜치 삭제, 2026-09-04). main
기존 구현을 결함C 수정 이후 마스터 기준으로 재검증한 결과 정상 작동
확인(가습기↔조명, 아이스트레이↔홍합 실사례 재현 차단 확인).

**교훈**: 병렬 세션 간 같은 문제를 동시에 다루면 중복 구현이 생길 수
있다 — 병합 전 반드시 **main에 이미 같은 목적의 코드가 있는지 파일명/
함수명으로 먼저 검색**한다(이번엔 merge conflict가 우연히 이를 드러냈지만,
운에 의존하지 말고 오래된 미병합 브랜치를 병합하기 전 항상 `git log
--all -- <관련경로>`로 사전 확인하는 것이 안전).

## F. 배포 검증 함정 — verify-vercel-deploy.sh가 이전 배포를 "OK"로 오탐

병합 직후 곧바로 `verify-vercel-deploy.sh --wait`를 돌렸더니 "OK"가
떴으나, 실제로는 그 시점에 새 배포(`cf2a635`)가 아직 `state:QUEUED`였고
스크립트는 그 직전의 이미 READY였던 배포(`e214560`)를 보고 있었다.
프로덕션 API를 직접 호출해 기대와 다른(구버전) 결과가 나오는 걸 보고서야
발견 — `Vercel:get_deployment`로 배포 ID의 `state`/`alias`(프로덕션
도메인 포함 여부)를 직접 조회해 실제 반영 시점(약 2분 후)을 확인했다.
**일반화**: 검증 스크립트의 "OK"도 도구 결과이지 원본 데이터가 아니다 —
API 응답이 기대와 다르면 스크립트를 다시 믿기 전에 배포 자체의 원본
상태를 재확인한다(#354류 확장).


## 인계 메시지(2026-09-04 시점) — 이제 이력 참고용, A/B/C 전부 완료됨

아래는 착수 당시 Code에 전달했던 원문(이력 보존). **모든 항목이
§Code CLI 실적용 결과에서 완료 확인됨** — 새 세션이 이 UCE-10을 다시
작업할 필요는 없다.

```
[당시 지시 원문]
Target Session: Claude Code CLI
Branch: main (신규 feature 브랜치로 시작)
[결함 C/B/A 순서 지시 — 결과: Code가 C→B→재검증→캐시버전→A 순으로
 정확히 수행, 9/11(우산·넥타이 예외 정직 기록)·27/27·29/29·프로덕션
 실측 전부 확인 완료]

[별건 발견·이번 스코프 밖 — 기록만] 레일조명/도어스토퍼=마스터 리프
 자체가 없음(커버리지공백, 정상적으로 개입큐 대상이어야 함).
 아기우산/아기의자류=수식어+헤드노운 조합 인식 약함(UCE-11 후보).
```
