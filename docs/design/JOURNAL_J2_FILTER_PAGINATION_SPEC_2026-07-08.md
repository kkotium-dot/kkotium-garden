# JOURNAL J2-2/J2-3 — 활동기록 필터 + 더보기 (2026-07-08)

Authoring: DESKTOP. 구현=Code. 권위: 본 문서 + JOURNAL2_ACTIVITY_LOG_SPEC_2026-07-03 + activity API(?limit). 원칙 #55/#181/#3-1. 비게이트·읽기전용.
전제: JOURNAL 2축(JobLifecyclePanel + ActivityTimeline·J2-1) DONE. activity API = 4소스 집계(crawl_logs/asset_jobs/asset_registry/status)·time-desc·limit. 실측: 명화 44건 → 필터·페이지네이션 없어 실사용 불편.

================================================================
## 0. 범위
================================================================
- **J2-2 필터**: 활동기록을 kind별로 필터(전체/크롤/이미지/자산/상태). 클라이언트 사이드(기존 데이터 재사용·추가 fetch 0).
- **J2-3 더보기**: 초기 N건(예 20) 표시 → [더보기]로 증분 로드(limit 확장 또는 클라 슬라이스). 44건+ 대비.

================================================================
## 1. 구현
================================================================
1. **J2-2**: ActivityTimeline에 kind 필터칩(전체·크롤·이미지·자산·상태). activity item의 kind로 클라 필터. 각 칩에 건수. 이모지0·Lucide.
   - kind 매핑(#3-1 i18n): crawl→크롤, asset_job→이미지, asset_registry→자산, status→상태.
2. **J2-3**: 기본 20건 표시 + [더보기](남은 건수). 클릭 시 +20 또는 전체. 필터와 조합(필터 후 더보기). 
   - activity API가 limit=50 반환 중이면 클라 슬라이스로 충분(추가 fetch 0). 50 초과 이력 필요 시 API limit 파라미터 확장(선택).
3. 빈 상태·단일 kind만 있을 때 필터칩 자연 처리(0건 kind는 비활성 또는 미표시).

================================================================
## 2. 검증
================================================================
- Code 로컬: 명화 활동 44건 → 필터칩별 건수 정확·필터 동작·더보기 증분·tsc0/build0.
- Desktop prod: 명화 일지 탭 → 필터(크롤/이미지/자산/상태) 전환·더보기 로드·건수 정확.

================================================================
## 작업 유의사항/원칙
================================================================
- 기존 activity 데이터 재사용(추가 fetch/로깅 테이블 0·#191 유지).
- 필터·페이지네이션은 클라 우선(서버 부하 0).
