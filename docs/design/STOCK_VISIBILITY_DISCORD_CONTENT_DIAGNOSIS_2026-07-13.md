# 재고 파악 + 디스코드 알림 — 진단 및 개선안 (2026-07-13)

Authoring: DESKTOP(Supabase+코드 실측). 계기: 운영자 "재고 알림 의미 없음"·"디스코드 개선 체감 안 됨". 원칙 #56/#231/#233/#252/#255. 상태: 진단 완료·개선 스펙·Code 대기.

## 1. 재고 파악 — 진단 (실측)
- ★도매매 재고 조회 **작동 중**: dome-inventory-poller가 Domeggook OpenAPI `getItemView`(multiple·최대100)로 조회 → inventory_snapshots **110행**·마지막 **2026-07-13 00:03(오늘 9시 KST)**. inventory-sync 크론 `0 0 * * *` 정상 가동.
- low_stock_alerts=0 → 고장 아님. **재고가 넉넉해 임계 미달이 없어서** 조용한 것(정상).
- supplier_stock_profiles=3 (코드 있는 상품만).
- 상품별 도매매 코드: 아이스36904429·달항아리63860451·명화65322245 = 있음 / **플라티코 화분=NULL**(네이버 연동됐으나 도매매 코드 없음→추적 불가).

### 진단 결론
- 기능은 되는데 **① 안 보임(건강하면 무알림)** + **② 커버리지 갭(코드 없는 연동상품 미추적)** → "의미 없다"는 체감의 원인.

### 개선안 (Code)
- **A. 현재 재고 가시화(꽃밭 돌보기 정보 화면)**: 각 상품에 "현재 도매매 재고 152개·양호·마지막 확인 오늘 9시" 표시(inventory_snapshots 최신값). 무알림이어도 "모니터링 중"이 보이면 신뢰 확보(#56).
- **B. 연동 상품 도매매 코드 자동 매핑**: import/link 시 supplier_product_code 자동 채움(공급사 상품 매칭) or 운영자 입력 UI. 플라티코 갭 해소·이후 전 상품 추적. (product-agnostic·#62)
- **C. "재고 모니터링 중" 상태 배지**: 마지막 확인 시각·다음 확인 예정 표시. 조용해도 살아있음을 가시화.
- ※실시간 아님(하루 1회)=적정 간격. 필요 시 크론 빈도 조정(도매매 API rate 고려).

## 2. 디스코드 알림 — 진단 (실측)
- 개선 코드 **반영됨**: 일일 크론(daily/route.ts)이 buildPublishReadyAlert→KKOTTI_RECOMMEND·buildRevivalAlert/buildZombieAlert/buildZombieDetectedAlert→KKOTTI_SCORE·buildMarginWarnAlert→PRICE_CHANGE·buildRecommendEmbed(top5·시즌·황금/니치/시즌 태깅 line499) 발송.
- buildRecommendEmbed(discord-builder.ts:221): 유형 태그(240)+유형 요약(243)+4단 구성(상황/영향/행동/꼬띠·267)+시즌 필드. buildAdhdAlert(72): [앱에서 열기]+우선순위 티어.

### 진단 결론 (정직·미확정)
- 코드엔 개선이 있으나 운영자 체감 없음 → 추정: **① 상품/소싱 데이터가 얇아** 추천이 비거나 빈약(4개 상품·Slot A 점수≥70 필요) **② 운영자가 본 메시지가 과거 것** 가능. **실제 발송 내용을 봐야 정밀 개선 가능**(코드만으론 "무엇이 비었나" 확정 불가).

### 개선안 (Code)
- **A. 라이브 발송 1회로 실제 내용 확인**: /api/cron/daily 수동 트리거(운영자 or Code 테스트 스크립트)→실제 KKOTTI_RECOMMEND/SCORE 메시지 육안 확인→갭 특정 후 개선. (실 발송=외부 발신·Desktop 임의 실행 안 함·#82)
- **B. 얇은 데이터 대비 폴백**: 추천 부족 시 "오늘은 추천할 신상품이 적어요·소싱을 늘려보세요" 등 빈 알림 대신 안내(정직·행동 유도).
- **C. 발송 확인 후 buildRecommendEmbed 실물 다듬기**: 실제 렌더에서 태그/딥링크/셀러 언어 최종 점검.

## 작업 유의사항/원칙 (신규)
- **#257** 기능이 "작동하지만 조용/안 보임"과 "고장"을 구분: 재고처럼 임계 무돌파 시 무알림이 정상일 수 있음 → 상태/현재값 가시화로 신뢰 확보(#56). 자동화 신호는 커버리지 갭(코드 없는 연동상품) 전수 점검·product-agnostic 매핑(#62). 알림 개선은 코드 반영 여부와 별개로 **실제 발송 내용 육안 확인**이 있어야 정밀 개선 가능(코드만으로 단정 금지·#231/#82); 실 발송은 외부 발신이라 운영자/테스트 경유.
