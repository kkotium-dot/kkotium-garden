# CLAUDE.md 축소 — 삭제 후보 목록 v2 (감사 모드)

> 상태: 시점(2026-07-28)
> 한 줄 요약 — DOCS_STANDARD v3(§13~§17) 기준으로 v1(07-25) 재평가 + 신규 후보 3건. **감사 모드(§15) — 이 문서 자체 외 어떤 파일도 수정하지 않음. CLAUDE.md 무변경.**
> 원본 전문 보존: `docs/archive/CLAUDE_MD_FULL_2026-07-24.md` (346줄, 현재 CLAUDE.md와 동일 — 07-25 이후 CLAUDE.md 무변경 확인됨).
> 근거: `docs/DOCS_STANDARD.md` v3 §13~17, `docs/research/CLAUDE_MD_KOREAN_PRACTICES_2026-07-25.md`.

## 0. v1 대비 변경 요약

1. **v1 10건 후보 전부 유효** — CLAUDE.md는 07-25 이후 한 줄도 바뀌지 않아 라인 번호가 그대로 맞음(재검증 완료).
2. **v1 후보#3 수정 필요** — §16 절대삭제금지 목록("네이버 PUT/POST 비가역 쓰기 금지 #46")과 정면 충돌 발견. 전체 이관이 아니라 **포인터 1줄 잔류 + 상세만 이관**으로 수정(§2 참조).
3. **v2 신규 후보 3건 추가** — §13-1(린터 위임 원칙)을 §3-1 한글처리 세부 항목에 처음으로 적용, §17 행동가이드라인 블록 삽입안.
4. **★ 감사 중 발견한 구조적 결함** — §16이 "절대삭제금지"로 지정한 5개 항목 중 3개(디스코드 실발송 금지·테스트 데이터 원복·3레인 경계)가 **현재 CLAUDE.md 본문에 아예 존재하지 않음**. 삭제 위험이 아니라 애초에 없는 상태 — 운영자 판단 필요(§4).

---

## 1. 후보표 (v1 유지 10건 + v2 신규 3건)

| # | 무엇을 (위치·줄수) | 왜 | 어디로 | 상태 |
|---|---|---|---|---|
| 1 | §2 "현재 단계(스냅샷)" (L100-113, 14줄) | 본문 스스로 "항상 PROGRESS.md로 재확인" 명시 — SoT 위반. 07-11~14 시점 고정이라 이미 stale. | 삭제, 1줄 포인터만: "현재 단계는 PROGRESS.md 확인." | v1 유지 |
| 2 | §3-6 이미지/발행 자산 규칙 (L151-160, 10줄) | 이미지·합성 작업 세션에만 관련. 주문·SEO 세션엔 매번 무관하게 로드. | `.claude/rules/image-assets.md` (paths: `src/lib/design/**`, `src/app/api/**/composite/**`) | v1 유지 |
| 3 | §3-7 네이버 v2 전체 페이로드 규칙 (L163-168, 6줄) | 네이버 PUT 코드 건드릴 때만 결정적. | **[수정]** 전체 이관 금지 — §16 "네이버 PUT/POST 비가역 쓰기 금지(#46)"과 충돌. **2줄만 CLAUDE.md 잔류**("네이버 v2 PUT=전체 payload 필수, 부분 PUT 절대 금지. 실 PUT은 confirm:true && !dryRun에서만.") + GET-merge 구현 상세만 `.claude/rules/naver-api.md`로 이관(paths: `src/lib/naver/**`, `src/app/api/products/**/naver*/**`). | **v1 수정** |
| 4 | §5 핵심 파일 경로 전체 (L215-246, 32줄) | `docs/plan/REFERENCES.md`가 이미 "핵심 파일 경로" 정본으로 지정됨(확인: REFERENCES.md에 동일 섹션 존재) — 중복. | 삭제, 포인터 1줄: "핵심 파일 경로는 REFERENCES.md 참조." | v1 유지 |
| 5 | §6-1 도구 매핑 표 (L251-261, 11줄) | Claude Code 자체 도구 이름 매핑 — 프로젝트 고유 정보 아님. | 삭제 | v1 유지 |
| 6 | §6-2 권장 MCP 추가 명령 (L263-274, 12줄) | 최초 1회 설치 안내, 상시 규칙 아님. | `docs/README.md` "환경 설정" 섹션 신설(현재 README.md에 해당 섹션 없음 — 이관 시 신설 필요) | v1 유지 |
| 7 | §6-3 슬래시 커맨드 목록 (L276-283, 8줄) | Claude Code 내장 기능. | 삭제 | v1 유지 |
| 8 | §7 작업원칙 빠른 인덱스 전체 (L291-332, 42줄) | 최대 단일 블록. 본문 자체가 "#149~#203 이미 이관완료" 명시 — 상당 부분 중복 보관 중. | `PRINCIPLES_CODE.md`(#1-25)·`PRINCIPLES_LEARNED.md`(#26+)로 통합. CLAUDE.md엔 3줄: "작업원칙 전체 인덱스는 PRINCIPLES_CODE.md(#1-25)·PRINCIPLES_LEARNED.md(#26+) 참조. AI 인프라 변경 시 #42~#45 필독." | v1 유지 |
| 9 | §8 새 세션 첫 메시지 템플릿 (L336-346, 10줄) | §1 STEP 0~3이 이미 "매 세션 첫 turn 자동 수행" 강제 — 이 템플릿은 그 자동화 실패 시의 수동 백업일 뿐. | 2줄로 축약해 §1 말미 각주로 흡수, 별도 섹션 제거. | v1 유지 |
| 10 | §4-3 grep 명령 코드 블록만 (L196-198, 3줄) | 규칙 문장(1줄)은 남기고 실행 커맨드는 MD 갱신 작업 시에만 필요. | `.claude/rules/md-hangul-check.md` (paths: `docs/plan/**`, `docs/research/**`). 본문엔 규칙 존재 1줄만 유지. | v1 유지 |
| 11 | §3-1 한글처리 중 "JSX 이모지 금지" + "코드 주석 영어 작성" (L123-125, 2줄) | DOCS_STANDARD §13-1 "Claude를 린터로 쓰지 마라" — 포매팅류 규칙은 결정적 도구(ESLint)에 위임. | **보류(즉시 삭제 금지)** — 현재 이 2개 규칙을 강제하는 ESLint 커스텀 룰이 저장소에 없음(미확인, grep 필요). 룰 부재 상태에서 CLAUDE.md 규칙까지 지우면 무방비. `docs/plan/TECH_DEBT.md`에 "ESLint 커스텀 룰 도입(no-jsx-emoji, comment-lang-en) 후 CLAUDE.md에서 제거" 항목으로 등록 제안 → 룰 도입 완료 후에만 실제 삭제. | **v2 신규(조건부)** |
| 12 | §3-1 "타입 리터럴 한글 금지" (L126, 1줄) | 위와 동일 원칙이나, 이건 tsc가 이미 부분적으로 캐치 가능한지 미검증. | 위와 동일 사유로 **보류**. 별도 검증 없이 후보#11에 합쳐 처리 권장(별항 유지는 과분할). | **v2 신규(조건부)** |
| 13 | §17 "행동 가이드라인" 12줄 블록 신설 | 첨부 리서치가 "운영자가 매 세션 반복 요구해온 내용과 거의 그대로 일치" — CLAUDE.md에 고정하면 반복 프롬프트 비용 소멸. | **신규 삽입**(삭제 아님, 순증) — §4-1 승인 게이트 뒤에 삽입 제안. DOCS_STANDARD §17 원문 그대로 사용 권장. | **v2 신규(삽입)** |

---

## 2. 후보#3 충돌 상세 (§16 절대삭제금지 검증)

- §16 원문: "네이버 PUT/POST 비가역 쓰기 금지(#46)" = 절대삭제금지.
- v1 후보#3은 §3-7 전체(비가역 가드 문장 포함)를 `.claude/rules/naver-api.md`로 통째 이관 — 이대로 실행하면 naver 관련 파일을 안 건드리는 세션에서는 이 규칙이 전혀 로드되지 않아, 사실상 "삭제"와 동일한 효과.
- **수정안**: CLAUDE.md에 아래 2줄만 상시 잔류(항상 로드), 나머지 GET-merge 구현 절차는 이관.
  ```
  네이버 v2 PUT은 항상 전체 payload(부분 PUT 절대 금지). 실 쓰기는 confirm:true && !dryRun에서만.
  구현 절차(GET-merge): .claude/rules/naver-api.md 참조.
  ```

## 3. WHAT/WHY/HOW 재구조화 매핑 (참고용 — 실행은 별도 승인 필요)

DOCS_STANDARD §13-4 구조 기준으로 현재 CLAUDE.md 섹션을 매핑하면:

| 목표 구조 | 현재 CLAUDE.md 대응 섹션 |
|---|---|
| **WHAT**(기술·구조) | §2 스택, §5 핵심 파일 경로(→REFERENCES.md 참조로 대체) |
| **WHY**(목적·맥락) | §2 본질, §0(#262 화면문구 원칙) |
| **HOW**(명령·워크플로) | §1 세션시작 절차, §3(코드 작성 규칙), §4(작업 흐름 규칙) |
| **절대금지** | §3-7 비가역 가드, §4-2 닉네임 금지, §4-5 의심파일, (§16 누락분 — §4 참조) |
| **행동 가이드라인**(신규) | 없음 → 후보#13 |
| **참조** | §7(→포인터 3줄), REFERENCES.md, PRINCIPLES_*.md |

전면 재배치는 이번 감사 범위 밖(파일 수정 금지 원칙 §15) — **구조 제안만** 기록, 실행은 `update` 모드 승인 후 별도 작업으로.

---

## 4. ★ 감사 중 발견 — §16 항목 중 CLAUDE.md에 부재한 3건

`grep -n "디스코드\|3레인.*경계\|테스트 데이터 원복" CLAUDE.md` 결과 0건. 즉:

| §16 절대삭제금지 항목 | CLAUDE.md 내 실재 위치 | 실태 |
|---|---|---|
| 디스코드 실발송 금지 | 없음. `TASK_BRIDGE.md`에 세션별 산발 언급만(예: L149, L386) | **CLAUDE.md엔 상시 규칙으로 존재한 적이 없음** |
| 테스트 데이터 원복 | 검색 결과 없음 | **CLAUDE.md엔 상시 규칙으로 존재한 적이 없음** |
| 3레인 능력 경계 및 인계 규칙 | 없음(§7 인덱스에 "#41 두 환경 핑퐁 프로토콜" 참조만 있고 본문 규칙 없음) | **포인터만 있고 본문 규칙 부재** |
| 네이버 PUT/POST 비가역(#46) | §3-7 (L163-168) | 존재 — §2 수정안대로 처리 |
| 상품 생애 흐름 1줄 + 발행전 검수(#307) | 없음 | **CLAUDE.md엔 상시 규칙으로 존재한 적이 없음** |

**의미**: "삭제 금지"는 이미 있는 걸 지키라는 뜻인데, 5개 중 3개는 애초에 CLAUDE.md에 넣은 적이 없다. 두 가지 해석이 가능하고 운영자 결정이 필요하다:

- (a) §16 목록은 "앞으로 CLAUDE.md 축소 작업 시 실수로 빠뜨리면 안 되는 것" = 지금 **추가해야** 하는 항목이다 → 축소(200줄 목표) 작업과 별개로 이 3건을 CLAUDE.md에 새로 넣는 작업이 선행돼야 함.
- (b) 이 3건은 의도적으로 TASK_BRIDGE.md/PRINCIPLES_LEARNED.md 등에 위임돼 있고 CLAUDE.md엔 필요 없다 → §16 문서 자체를 정정(해당 3건 제거 또는 "위치: TASK_BRIDGE.md" 명시)해야 함.

**본 감사는 판단을 내리지 않음** — 다음 세션 착수 전 운영자 확인 필요.

---

## 5. 예상 결과 (v2)

- v1 예상 196줄에서 후보#3 수정(순증 +2줄 잔류)·후보#13 삽입(+12줄) 반영 시 **약 208~210줄** — §16 목표(≤200) 근접이나 약간 초과 가능. 후보#9(§8 축약)·후보#5/#7(순삭제)로 여유 확보 가능해 최종은 실행 시점에 재계산 필요.
- 후보#11·#12는 즉시 반영分에서 제외(조건부 보류) — 200줄 계산에 포함하지 않음(현행 유지 가정).
- 손실 정보 0건 원칙 유지: 전문 보존(`docs/archive/CLAUDE_MD_FULL_2026-07-24.md`) + 이관 대상 전부 새 위치 명시.
- **실행 순서(2단계, #314)**: 이 후보표 승인 → §4 판단(3건 추가 여부) 확정 → `update` 모드로 CLAUDE.md 실제 수정 → 이관 검증(`grep -c`로 새 위치 확인 + `.claude/rules/*.md` paths 매칭 확인).
