# CORE_WORKING_PRINCIPLES — 앱 개발 핵심 작업 방법론

> 레인 공통 · 최초 2026-08-27 · 매 작업 착수·검증·완료 시 적용하는 상위 방법론.
> 관계: PRINCIPLES_LEARNED(번호별 세부원칙)의 **상위 3대 기둥**을 압축. 세부는 그쪽 참조.
> 이 문서는 "왜/어떻게 사고하는가"(방법론). `docs/CORE_PRINCIPLES.md`는 "무엇을 금지·사람결정하는가"(규칙 관문). 둘은 상호보완 — 방법론(이 문서) + 금지선(CORE_PRINCIPLES) + 세부(#PRINCIPLES_LEARNED).
> 이 문서는 "왜 이렇게 일하는가"의 헌법. 세부 #번호는 그 적용 사례.

---

## 3대 기둥 (모든 작업의 헌법)

### 기둥 1 — 출처로 교차검증 (환각 제거)
**어떤 판단도 추측/보고/문서를 그대로 믿지 않고, 1차 출처로 실측한다.**
- 코드 판단 → git·실제 소스 파일. 상태 판단 → 프로덕션 API·DB. 시장/SEO 판단 → 웹 출처(날짜 확인).
- 인계/보고는 "검증 대상"이지 "사실"이 아니다. Code가 "완료"라 해도 Desktop이 실측으로 재확인.
- **핵심 구분**: 구조 검증 ≠ 내용 검증.
  - 예(2026-08-27): naver_categories FK 무결성 0 orphan(구조 OK)인데, 연결된 값이 아이스트레이→홍합·디퓨저→교자상(내용 오류). "FK 연결됨 ≠ 올바른 카테고리".
  - **하이브리드 관문(2026-09-01, 위 사고의 재발방지)**: 내용 검증을 사람 기억에만 의존하지 않는다.
    **A=사람관문**(운영자 GO 없이 `--apply` 금지, 작업원칙 #41) + **B=기계관문**(tsx 정합성 테스트가 값을 assert,
    `npm run test:category-integrity` — `scripts/verify-category-integrity.ts`). A만으로는 "이번엔 맞겠지"라는
    확인 피로가 재발을 부르고, B만으로는 애매한 케이스의 최종 판단(운영자 GO)이 빠진다. 둘 다 있어야 완료
    (`docs/playbook/COMPLETION_GATE.md` 카테고리 작업 특칙).
- 내가 틀릴 수도 있다: 내 검증 쿼리가 틀린 컬럼을 봐서 오탐 낸 적 있음(같은 날 category_code vs id). 실측도 재검증한다.
- 없는 걸 지어내지 않는 것도 정확도다: 마스터에 없는 상품은 빈손+정직표시가 정답(무드등). 억지 매칭이 오류.
- **표면 결과 ≠ 사실(2026-09-06 disposition)**: 명화가 "품절"이라는 표면을 실측하니 DB에 상품 자체가 부재(도매매 삭제). "유리받침"이 프로덕션에서 오답처럼 보였으나 로컬매처+캐시DB 실측으로 결함F→AI경로 정상작동 확인. 표면만 보고 결함/정상 단정 금지 — 로그·DB·로컬재현 3중 대조.

### 기둥 2 — 단건 수습 금지 (근본·시스템 승격)
**증상 하나를 고치지 말고, 뿌리를 찾아 시스템 레벨로 승격한다.**
- 오류 발견 시 "이 상품만"이 아니라 "이 로직을 쓰는 모든 경로"를 점검(#62).
- 반복 위험이 있으면 원칙/개입점으로 박제:
  - 예: 캐시 포이즌(수동 DELETE) → logic_version 자동무효화 + 원칙 #351.
  - 예: 워크트리 방치 → 원칙 #347 + COMPLETION_GATE C6.
- "같은 뿌리"는 한 작업으로 병합: z-score 편중 + 소싱 카테고리 오라벨 = 둘 다 렌즈 d1 오용 → 병합 수정.
- 하나의 발견이 여러 문제의 뿌리일 수 있다: naver_categories 0행 = 미발행 category_id 공백 + 발행넛지 막힘 + UCE DB 부채의 공통 뿌리.
- **grep으로 동일 패턴 전수(2026-09-06 disposition 결함1)**: disposition.ts의 `qty ?? 0` 오판(스냅샷부재→품절)을 고치기 전, 같은 패턴을 전 프로젝트 grep → publish-gate.ts에도 동일 버그(발행 오차단) 발견. 한 파일만 고쳤으면 발행워크플로 봉쇄가 남았을 것. "이 로직을 쓰는 모든 경로" = grep이 그 실행 도구. 결함1을 2파일 통합 수정.
- **판정 함수 안의 형제 분기(if/else if)는 대칭 안전장치를 가져야 한다(IDIOM-6, 2026-09-21 #45)**: "같은 위험을 막으려는" 분기가 여러 개면, 하나를 고칠 때 형제 분기가 같은 안전장치를 갖고 있는지 반드시 grep 대조. headNounWeight의 weakHeadMatch만 partialMatch를 체크하고 strongHeadMatch는 안 하던 비대칭이 "휴대폰 스트랩"→카메라 오분류를 냈다(마스터에 같은 클래스 리스크 70건 확인). 새 안전장치 추가 시, 그 분기가 원래 정당하게 커버하던 케이스(완전일치 slash 그룹)까지 덮지 않는지 즉시 반대 사례로 재검증 — discount류 수정은 반드시 "이 discount가 걸리면 안 되는 반대 케이스"를 함께 실측한다.

### 기둥 3 — 전 상품 공통 시스템 (범용·개입점 내재화)
**특정 상품이 아니라 "어떤 상품이 올라와도" 작동하는 엔진으로 설계한다.**
- 상품별 하드코딩 금지. 상품데이터 → 구조 자동생성(UCE 매처·AEO 프롬프트·발행넛지).
- 검증은 카탈로그 밖 임의 상품으로: 12개가 아니라 "무드등·스텐빨대·수세미" 등 임의 20종으로 정확도 실측.
- 개입점을 자연스럽게 녹인다: 실패/모호 상품은 "확인 필요" 큐로 surface(강제 순서 아님, #56).
- 완료조건 = 존재가 아니라 정확도: "suggestions 비어있지 않음"이 아니라 "맞는 카테고리인가".
- **테스트 green ≠ 전수 안전(#352)**: 테스트/정합성검사가 통과해도 그건 "그 케이스만" 안전한 것. 비가역 쓰기(DB backfill·일괄 UPDATE)는 반드시 (1)카탈로그 밖 임의 20종+ 전수 dryRun 오분류0 + (2)실 카탈로그 전수 dryRun 개악0 + (3)회귀테스트 고정 후에만 --apply. dryRun 없는 비가역 쓰기 금지.
- **개악 > 미개선**: 원래 맞던 값을 틀리게 바꾸는 건 빈손 유지보다 나쁘다. 확신 없으면 NULL(정직)이 개악보다 낫다.
- **"데이터 없음" ≠ "값 0"(2026-09-06 disposition/publish-gate)**: `qty ?? 0`이 null/undefined(스냅샷부재=폴링 전)를 0(실품절)으로 뭉개, 재고 멀쩡한 발행상품을 품절 오판·발행 오차단. 판정 엔진은 "모름(신호부재)"과 "0(측정된 값)"을 반드시 구분 — 모르면 판정 보류(NONE/PASS), 측정된 0만 조치. `?? 0` fallback은 판정 로직에서 위험 관용구.

---

## 착수·검증·완료 체크 (3기둥의 실행형)

**착수 전**: DOMAIN_FACTS·CLAUDE·TRACKER·PRINCIPLES 읽음? write set 충돌 없나(병렬 안전)? 운영자 GO 필요한가?
**검증 시**: 실측했나(문서/보고 아님)? 구조뿐 아니라 내용도? 카탈로그 밖 임의 표본으로? 회귀 0?
**완료 시**: COMPLETION_GATE C1~C6(해시·병합·연결·데이터·실행·워크트리위생). 프로덕션 실효까지? 브라우저 실측?

---

## 학습 축적 규약 (이 문서의 목적 = 계속 자라게)

매 세션의 핵심 통찰은 아래 규칙으로 축적한다:
1. **재사용 방법론** → 이 문서(CORE_WORKING_PRINCIPLES) 또는 docs/playbook/*.
2. **번호형 세부 원칙** → PRINCIPLES_LEARNED.md(#다음번호). 근거(날짜·사건) 명시.
3. **반복 실패 패턴** → REPEATED_FAILURE_PATTERNS.md.
4. **진단 절차** → docs/playbook/*_DIAGNOSIS.md (예: AI_DEPENDENCY_DIAGNOSIS).
5. 세션 종료 시 #319대로 문서 3종(CURRENT·TRACKER·PRINCIPLES) 갱신 = 완료조건.

> 원칙: 통찰은 "말"이 아니라 "커밋된 문서"로 남을 때만 학습이다. 다음 세션이 이 문서를 읽고 같은 실수를 안 하면 성공.

---

## 학습화 개선안 (2026-09-06 검토 — 실효성 점검)

현재 학습 시스템은 잘 작동 중(3대기둥 문서 + PRINCIPLES_LEARNED #362까지 + archive 분할 + 클러스터 인덱스). 다만 "축적은 되는데 재사용이 보장 안 되는" 구조적 약점이 있어 아래를 단계적으로 개선한다:

1. **위험 관용구 사전(grep 대상) 신설** — 반복 결함이 특정 코드 패턴에서 나온다.
   지금까지 나온 것: `qty ?? 0`(데이터없음↔0 혼동, 결함1), `?? 0` 판정로직,
   substring 부분매칭(UCE-9~F), slash-synonym 파편. 이들을 `docs/playbook/
   RISKY_IDIOMS.md`로 모아, 매 코드 작업 착수 시 "이 관용구 쓰나?" grep 체크.
   → 단건이 아니라 "패턴 자체"를 박제(기둥2의 도구화).

2. **회귀표(regression table) 표준화** — 결함1·UCE 수정마다 Desktop이 만든
   "입력→기대출력" 표가 매번 재작성됨. 수정 PR/커밋에 회귀표를 필수 첨부하고,
   가능하면 그 표를 그대로 tsx 테스트로 고정(#352 3단계의 자동화). 표=명세=테스트 일치.

3. **원칙 트리거 자동화(제안, 미구현)** — PRINCIPLES_LEARNED가 523줄로 커지면
   "읽었지만 적용 안 함"이 생긴다. 클러스터 인덱스는 있으나, 작업 유형별로
   "이 작업엔 이 원칙 필독" 매핑(예: 카테고리매처 작업→#346,#352,#351 /
   disposition·게이트 작업→#260,#271,#272,결함1 / 배포검증→#361)을 CLAUDE.md
   착수 체크에 넣어 유형 진입 시 강제 상기.

4. **"표면≠사실" 3중대조 체크리스트** — 이번 명화·유리받침처럼 표면 결과로
   오판할 뻔한 사례가 반복. 검증 시 "① 프로덕션 응답 ② DB/캐시 실측 ③ 로컬
   재현" 3중 중 최소 2개 교차 없이는 "결함/정상" 단정 금지를 검증 체크에 추가.

**적용 우선순위**: 1(위험관용구 사전)이 즉효·저비용 — 다음 세션에서 신설 권장.
2~4는 기존 문서 보강으로 점진 적용.

---

## 신규 핵심 통찰 (2026-09-17, rev183 — 헤어핀 오분류 최종근본원인)

**기둥1(출처 교차검증) 보강 — "안전장치를 고쳤다" ≠ "그 안전장치에 도달한다":**
확신도 게이트(예: isDeterministicLowConfidence)가 있는 다단 파이프라인에서, 이전 단계
(AI크로스체크 계층)의 결함을 고쳐도 그보다 앞선 단계(결정론적 매처의 headNoun 선정)가
게이트를 "고신뢰"로 판정하면 뒤 단계는 아예 호출되지 않는다. 실측(Vercel 로그 usedAI
플래그)으로 "그 계층에 실제로 도달하는가"까지 확인하지 않으면, 고친 코드가 무의미하게
남아있는 걸 놓친다. 같은 증상(오분류)의 재발이 반드시 같은 원인이라는 보장이 없다 —
겉보기 계층 구분은 코드 리뷰로 안 보이고 실측 플래그로만 구분된다.

**기둥2(단건 수습 금지) 보강 — 병렬 판정 단계는 같은 사전을 공유해야 한다:**
leaf 매칭 단계(termMatchScore/isGenericTerm)와 headNoun 선정 단계(HEAD_NOUN_EXCLUDE)가
"후행 범용 동격어를 식별자로 안 씀"이라는 같은 목적의 로직인데 서로 다른 사전(STOP_NOUNS_SET
vs 1단어 로컬 Set)을 썼다. 한쪽만 고치면 다른 쪽에서 같은 결함이 재발한다 — #295 단일권위
원칙의 구체 사례. 수정: HEAD_NOUN_EXCLUDE가 STOP_NOUNS_SET을 스프레드로 흡수, 향후 STOP_NOUNS
추가가 자동으로 양쪽에 반영되게 통합.

**실증 방법**: CATDBG4~7 단계적 진단 로그(매번 임시 추가 → tsc 0에러 → 배포 → curl 트리거 →
Vercel 로그 확인 → 원인 확정 → 근본수정 → 재배포 → 5개 상이 상품군 재검증 → 로그 원복)를
반복. 마스터 데이터(naver-categories-full.ts) 직접 grep으로 "bare 리프로 존재하는 범용 단어"
(액세서리 2건, 세트 1건, 용품 0건)를 전수 확인 — 코드 추론이 아니라 실제 데이터 대조.

**정직한 잔여 이슈 기록 규칙**: 근본수정 하나를 완료 처리해도, 재검증 중 발견된 무관한 별건
결함(이번엔 헤어핀 동음이의 1위 선정, 스트랩→카메라 오분류)을 완료 표시에 끼워 넣지 않고
별도 신규 항목으로 등재한다. "이번 수정 범위 밖"과 "이번 수정이 미완료"를 혼동하지 않는다.

---

## 신규 핵심 통찰 (2026-09-21, rev185 — 스트랩 오분류 근본원인 및 배포검증 함정)

**기둥2(단건 수습 금지) 보강 — 대칭이어야 할 두 분기 중 하나만 안전장치가 있으면 반드시 재발한다:**
"휴대폰 스트랩"이 카메라 액세서리로 오분류된 근본원인은 headNounWeight 함수의
strongHeadMatch(완전일치)와 weakHeadMatch(부분일치) 두 분기가 "슬래시 합성어 일부만
매칭됐을 때 신뢰를 낮춘다"는 같은 목적인데, weakHeadMatch만 partialMatch를 체크하고
strongHeadMatch는 그 체크가 통째로 빠져있던 비대칭. #382(HEAD_NOUN_EXCLUDE/STOP_NOUNS
이원화)·#44(homonymUnconfirmed의 rival 조건이 자식0개 branch를 배제)와 정확히 같은
클래스 — 판정 함수 안에 "같은 위험을 막으려는" if/else 분기가 여러 개 있으면, 하나를
고치거나 새로 만들 때 형제 분기가 같은 안전장치를 갖는지 항상 grep으로 대조한다.
`docs/playbook/RISKY_IDIOMS.md`에 IDIOM-6으로 이 패턴 자체를 신설.

**기둥1(출처 교차검증) 보강 — 배포 직후 재검증에서 "안 고쳐진 것처럼" 보이면 전파 지연을 먼저 의심:**
2차 수정 배포 후 즉시 curl 재검증했을 때 여전히 이전 값(20점)이 나와 "로직이 틀렸다"고
오판할 뻔했으나, 내부 계산값을 직접 로그로 노출(진단로그)해 실제로는 코드가 정확히
계산되고 있음을 확인 — 몇 초 후 재요청하니 정상 값(60점)이 나왔다. 배포 상태(dep=커밋
해시)가 최신이어도 실행 중인 lambda 인스턴스가 이전 코드를 서빙할 수 있다. "고쳤는데
안 됨"이 보이면 (a) 배포가 진짜 전파됐는지 (b) 내부 계산값 자체가 틀렸는지를 분리해서
확인 — 둘을 섞으면 잘못된 원인으로 잘못된 재수정을 할 위험이 있다.

**부작용 즉시 재검증의 가치 재확인**: discount 안전장치를 추가한 직후, 그 안전장치가
정당하게 걸려야 할 반대 케이스("아로마 디퓨저" — 완전일치가 아니지만 modifier가 문맥으로
뒷받침하는 진짜 슬래시 동의어)를 즉시 실측해 회귀를 그 자리에서 잡아냈다. 원칙#379(전
상품 확장검증)를 "수정 완료 후 한 번"이 아니라 "각 수정 단계마다" 적용한 것이 회귀를
프로덕션에 남기지 않은 핵심.

- 클러스터 인덱스 "시스템 승격(단건 금지)"에 원칙#383, RISKY_IDIOMS IDIOM-6 추가.
