node_modules
.pnp
.pnp.js

coverage
.next/
out/
build
dist
.vercel

npm-debug.log*
yarn-debug.log*
yarn-error.log*
# All env files except example (prevents accidental key commits → Groq auto-revokes leaked keys)
.env
.env.*
!.env.example
*.db
*.db-journal
prisma/migrations
.vscode
.idea
*.swp
*.swo
*~
.DS_Store
Thumbs.db
*.pem
.env.local

# Belt-and-suspenders: explicit env backup patterns (covered by .env.* above too)
*.env.backup
.env.local.backup*
vercel_env_upload.env
vercel_env_upload*.env
patch_*.py
fix_*.py
test_*.js
write_*.py
write-*.js
write-sku.js
write-parser.js

# Local working assets (personal Excel templates, originals)
작업용 엑셀 기본포멧/

# Local image staging — canon is Supabase Storage product-assets/{pid}/{stage}/
# (docs/playbook/IMAGE_SEO_PIPELINE_PLAYBOOK.md §2). Raw downloads stay local
# only; the myeonghwa hero reference file is kept on disk, untracked.
assets/generated/

# TypeScript build cache (regenerated on every build)
tsconfig.tsbuildinfo

# Temporary commit message files
.commit-msg-*.txt
.env*.local

# Temporary work files (added 2026-05-06 — work principle #29 (b) safety pattern)
.tmp_*

.commit-msg.tmp
.tmp_*.py

# Claude Code worktrees (added 2026-05-11 — keep CLAUDE.md tracked, ignore worktree dirs only)
.claude/worktrees/

# Backup file patterns (added 2026-05-19 — work principle #43 정합)
# Reason: src/lib/gemini.ts.bak 잔존이 GEMINI_API_KEY revoke 사고 원인.
# 2026-05-19 PM 추가: .backup 패턴 60건 대량 잔존 발견 (#43 메타-단정) → 영구 ignore.
*.bak
*.backup
*.backup[0-9]*
*.backup.[0-9]*
*.BROKEN.*
*.old
*.tmp.[a-z]*
*.v[0-9]*

# Tesseract.js OCR runtime artifacts (Sprint 8-PF) — downloaded on first call
*.traineddata

# Claude Code preview dev config (local-only helper)
.claude/launch.json
